# Debug-Lite v3 · 换行符规范化（P7-7 报告 R-1/文档检查假红根因）
#
# 背景：本仓库 `core.autocrlf=true`，此前**没有** .gitattributes，于是同一份文本文件在不同机器/
#   不同 checkout 下会是 CRLF 或 LF。历史故障：CRLF 的 docs/frontend-spec.md 让 fe-spec-check 的
#   围栏标签行正则（`/^[ \t]*$/` 不容忍 `\r`）报"缺少围栏"→ C1 FAIL 并连带 8 条 FE-SPEC 测试红
#   （实测 CRLF→0 PASS / LF→9 PASS）；docs 一致性检查同样出现过 CRLF 假红。
#   检查器侧已改为**容忍** CRLF（`scripts/fe-spec-check.js` 的 fenced/tableRows），但那只治标：
#   根因是仓库没有声明换行符策略。本文件即为该策略。
#
# 策略：
#   · 所有文本文件（含未显式列出的新文件）一律在**仓库内**存为 LF；检出到工作区也统一 LF。
#     这正是"跨平台一致"最省心的选择：检查器/测试/脚本拿到的字节永远相同。
#   · 二进制文件显式标记 `-text`，防止 Git 误判后做行尾转换而损坏内容。
#
# ⚠️ 本文件**只声明规则，不做全仓 renormalize**（不执行 `git add --renormalize .`）：
#   那会产生巨量纯行尾 diff，淹没真实改动。规则自本文件起对后续 checkout/commit 生效。

* text=auto eol=lf

# ---------- 二进制（禁止任何行尾/编码转换） ----------
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.ico  binary
*.woff  binary
*.woff2 binary
*.ttf  binary
*.eot  binary
*.pdf  binary
*.zip  binary
*.gz   binary
*.7z   binary
*.sqlite binary
*.sqlite3 binary
*.db   binary
*.node binary

# ---------- 由工具生成的快照/审计产物：同样按文本 LF，但 diff 噪音大 ----------
# （仍参与 diff —— 这些文件是审计证据，不得隐藏）
.audit/*.json text eol=lf
