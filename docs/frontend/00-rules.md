# 前端实现文档 · 总体规则（P6 总纲）

> 版本：v1　创建：2026-09-20　状态：**规则已定；实现细节全部待逐项商定**
> 定位：本文件只规定**「我们如何写前端实现文档」**与**任何前端设计必须满足的边界**。它不含屏幕清单、不含技术选型、不含任何具体实现。
> 权威链：`docs/decisions.md` > `docs/systems/*` > `docs/v3-design.md` > `docs/interfaces.md` > `docs/tasks.md` > 本目录。
> 与既有文档的关系：`docs/tasks.md` §7 列出的「仍生效约束」（D-124 无框架等）继续有效，本文件是它们的**执行程序**；旧 v3 前端设计（`frontend-spec.md` / `screens.md` / 文档自检器 / 真实响应样本）已于 2026-09-20 全量作废并删除，**不得作为任何依据**。

---

## 0. 适用范围与非目标

- **适用对象**：P6 前端（分层 L7，`public/**`）的实现文档撰写、评审与批次出口。
- **本文件不决定**：技术选型、模块划分、屏幕/流程清单、批次划分、绘制原语的具体字段、无头测试的具体做法、AI 编辑器形态。以上一律由 §2 协议逐项商定后，另立分册（§3.1）或增补本文件。

---

## 1. 绘制范围（用户给定边界，不可协商）

1.1 本轮设计**只考虑前后端交互**与**按钮功能的完整性**；不考虑样式、动画、布局与视觉呈现。
1.2 绘制手段**封闭**为基本控件：**按钮、文字、输入框**（及其同类基本控件）。除此之外不引入任何图形绘制。
1.3 任何「本该画出来」的运行时内容（元素移动、命中、状态变化等动画语义）**一律转为文字表述**，
    例如 `元素p1：位置224`。不得引入画布、图形、精灵或动画。
1.4 文本化产物必须**自足**：把结果直接交给绘制方法即可绘制；
    **绘制方法内不得再查状态、再算公式、再拼字符串**（「绘制方法零逻辑」）。
1.5 由 §1.2–§1.4 直接推出的两条硬规则（违反即视为设计不完整）：
    - **投影单一真源**：后端数据 → 文字描述 的转换只能有一处实现，禁止前端与其他调用方（如 CLI）各写一份。
    - **禁止前端复制战斗公式**：伤害 / 命中 / 移动 / 胜负一律消费服务端结果，前端只做展示。

---

## 2. 协作协议（每部分设计的标准流程）

2.1 **输入单元**：用户每轮给出一部分设计（一屏 / 一条流程 / 一个子系统）。
2.2 **先查后问**：提问前必须先按权威链检索既有结论，**查得到的不问**；
    查到设计与之冲突 → **立即停止**，把冲突作为第一号问题提出，并附 `文件:行号` 证据。
2.3 **提问规格**：每条问题必须包含「编号 + 结论式标题 + 推荐答案 + 备选 + 各自后果 + 证据来源」；
    按「阻塞实现的程度」排序；只问影响**结构 / 接口 / 验收**的问题，不问措辞与审美偏好。
2.4 **轮次**：一轮内可提多条问题；用户答复后进入下一轮。用户可随时行使「一票否决」或「跳过讨论直接定」。
2.5 **定稿判定（四项全过才允许写入文档）**：
    ① 符合行业主流做法（须点名所采用的模式，以及被否决的替代方案与理由）；
    ② 在既有约束下合理（不与权威链冲突，不与门禁冲突）；
    ③ 功能完整（覆盖该部分应有的**全部用户动作与全部失败路径**）；
    ④ 已明确到可逐行实现（字段名、边界条件、错误分支均无待定项）。
    任一项未过 → 继续提问；**不得「先写文档再补」**。
2.6 **结论落账**：每条确认结论登记进 §5，格式 `编号 | 日期 | 结论 | 证据`。
2.7 **后端契约不可前端私改**：凡需改动 `/api/v1` 的路径、字段、错误码、鉴权的诉求，
    一律升级为决策记录（`docs/decisions.md` 追加 D 编号）+ 同步 `docs/interfaces.md`，
    走 `docs/tasks.md` §10 变更流程；前端**不得**自行兜底或绕行。
2.8 **不批量默认同意**：用户一次给出多屏 / 多流程时，按单元分批处理，**一批一轮**。

---

## 3. 文档体系与机器纪律

3.1 **位置与命名**：总纲 = `docs/frontend/00-rules.md`；后续分册 = `docs/frontend/<两位序号>-<主题>.md`。
3.2 **批次命名**：P6 批次必须**避开** `P\d+-\d+` 与 `B\d+` 两种形态——
    `scripts/check-docs.js` 的批次行正则会把它们并入「批次计数」，与现状「共 41 批」直接冲突（check-docs FAIL）。
    最终命名形态与其审查记录要求见开放项 O-6。
3.3 **引用即承诺**：本目录文档若出现 npm 脚本引用、或引用 `scripts/`、`assets/`、`server/data/` 下的具体文件，
    即构成「该文件存在」的承诺（对应 check-docs 的 D1/D2/D3 检查）；尚未落地的一律显式标注「计划中 / 未实现」。
3.4 **文档不得与实现漂移**：任何设计改动须在**同一提交**内更新相应分册与 `docs/progress.md`。

---

## 4. 验收机制（只规定「必须存在什么」，不规定技术做法）

> 背景：前两轮前端「机器测试全绿但不可玩」（见 `docs/acceptance.md` §8）。以下为**防复发条款**。

4.1 **按钮永不无声**：每个可点元素必须命中一个已实现的动作分支；必须存在**机器可判定**的全覆盖检查
    （既不存在「注册了但没实现」，也不存在「实现了但无入口」）。
4.2 **字段名不得来自散文**：前端读取的每个响应字段必须可追溯到**真实 HTTP 响应**；
    必须存在**机器可判定**的「文档 ↔ 真实响应」核对手段。
4.3 **端点可达**：`docs/interfaces.md` §2 的每个端点必须有明确 UI 路径，或被显式登记为「本轮不做」。
4.4 **可玩性出口**：每批必须能在真实浏览器按「人工走查剧本」（点哪里 / 看什么 / 期望文本）走完该批流程；
    只有机器断言不算完成。每批的实际走查结论写入 `docs/reviews/`。

---

## 5. 已确认细节

| 编号 | 日期 | 结论 | 证据 |
|---|---|---|---|
| FR-1 | 2026-09-20 | 总纲落地位置 = `docs/frontend/00-rules.md`（新建目录，后续分册同目录） | 用户本轮决定 |
| FR-2 | 2026-09-20 | 本轮**只定规则**，不定任何实现（技术选型 / 屏幕清单 / 绘制原语字段 / 测试做法 / AI 编辑器形态全部待定） | 用户本轮决定 |
| FR-3 | 2026-09-20 | 绘制边界与文本化要求 = §1 全部条款 | 用户原始指令 |
| FR-4 | 2026-09-20 | **静态托管 = 同源托管**（`server/index.js` 增加 `public/` 只读静态分支：仅 `GET`、扩展名白名单、穿越防护、插在最终 404 之前；不新增 `/api/v1` 端点、不新增日志事件） | 用户本轮 Q1 结论 A；分册 `01-auth.md` §9 |
| FR-5 | 2026-09-20 | **「设置密码」= 仅登录后改密**（`POST /api/v1/auth/password`，旧密码 + 新密码）；「忘记密码 / 免登录重置」显式登记为**本轮不做**（后端无该端点，且无邮件设施） | 用户本轮 Q2 结论 A；分册 §1 非目标 1 |
| FR-6 | 2026-09-20 | **P6 批次命名采用 `F1`**（登录与注册），审查记录 `docs/reviews/F1.md`；`F1` 不匹配 `check-docs` 的 `P\d+-\d+`/`B\d+`，故 D4/D5 的「共 41 批」不受影响 | 用户本轮 Q3 结论 A；`scripts/check-docs.js:107` |
| FR-7 | 2026-09-20 | **会话 token 存放 = `localStorage`**（键 `dl.token` / `dl.session`，`dl.` 前缀统一命名空间）；`localStorage` 不可用时降级为内存会话并提示 | 用户本轮 Q4 结论 A；分册 §7.3 |
| FR-8 | 2026-09-20 | **前端模块形态 = 零依赖双模模块**（浏览器 globals + Node `require`），配「单一网络出口 / 单一 DOM 写入点 / 纯 `render(state)`」，使 §4.1「按钮永不无声」与 §4.2「字段名可追溯」成为机器可判定断言 | 分册 §2 / §10；D-124 |

---

## 6. 开放项登记（登记 ≠ 决定；每项在对应轮次按 §2 协议商定）

| 编号 | 开放问题 | 相关证据 | 最晚决定时点 | 状态（2026-09-20） |
|---|---|---|---|---|
| O-1 | Blockly 是否在 P6 范围内？ | `docs/tasks.md` 既写「前端依赖（blockly）P6 才安装」，又写（D-124）「不引入 Blockly」；本机 `node_modules` 已物理存在 `blockly` / `jsdom`（未入库、未在 package.json 声明） | 排期前 | **仍开放**（本批未引入任何第三方依赖，与 D-124 一致） |
| O-2 | 无样式文本界面下，「能玩 / 功能完整」的判定标准 | `docs/acceptance.md` §8 历史失败根因 | F0 之前 | **本批已结**（判定标准 = 分册 `01-auth.md` §11 的人工走查剧本逐条通过 + §10 机器断言全绿；后续批次沿用「走查剧本 + 机器断言」双出口） |
| O-3 | 绘制原语的具体字段集（§1.2 的「等」字边界、是否需要语义标记、是否需要多行输入） | §1.2 措辞天然不封闭 | 首个分册之前 | **本批已结（限本批）**：控件 = 单行文本输入 + 密码输入 + 按钮 + 文本行；无多行输入（AI 编辑器分册再定） |
| O-4 | 帧 → 文字 投影的真源落在哪一层（前端内 / 跨层共享模块 / 后端新增） | §1.5 第一条；`docs/interfaces.md` 为接口唯一权威 | 回放分册之前 | **仍开放**（本批确立「投影 = 前端 `format.js` 单文件、只经 `pick()` 读字段、契约可机器核对」的同构做法，回放分册据此裁定跨层与否） |
| O-5 | 无头测试与 CI 的关系 | `.github/workflows/gate.yml` 无 `npm install` 步骤 | F0 之前 | **本批已结**：前端测试零依赖（`node:test` + Node 内建 `fetch`），随 `npm test` 进入 gate 项 7，无需新增 CI 安装步骤 |
| O-6 | P6 批次命名形态，及其审查记录是否纳入机器核对 | `scripts/check-docs.js` 的 D5/D6 | F0 之前 | **已决（FR-6）**：命名 `F<n>`；审查记录落 `docs/reviews/F<n>.md`。机器的 D5/D6 仍只核对 `P\d+-\d+`/`B\d+`（即 P6 不并入「共 41 批」），F 系列审查记录由本目录规则约束 |
| O-7 | 静态托管的落地方式；架构检查是否扩展到 L7 | `docs/tasks.md` §7 要求托管 `public/`；`server/index.js` 现无静态路由；`scripts/check-arch.js` 未含 L7 | F0 之前 | **落地方式已决（FR-4）**；**L7 分层登记仍未扩展**（`public/**` 暂不在 `check-arch.js` 扫描范围，前端依赖方向由 `tests/frontend/*` 的单一出口/单一写入点断言代偿） |
| O-8 | 本目录文档是否纳入 check-docs 的文档清单（即是否受 D1–D6 机器核对） | `scripts/check-docs.js` 的 `DOC_FILES` 为固定清单 | 本文件首次提交前 | **已核实（保持不纳入）**：`DOC_FILES` 为固定 9 项，`docs/frontend/*` 不在其中（2026-09-20 复核）；本目录自我约束见 §3.3/§3.4 |
