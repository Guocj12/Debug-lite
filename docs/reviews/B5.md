# B5 审查记录

> 批次：B5（P1：core/roles.js 角色系统）
> 审查方式：独立上下文 agent（R-1..R-8 数值手算复算 + FY 洗牌轨迹验算 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS（修复 1 个 P1 后）**

## 审查范围

新增 `server/core/roles.js`（instantiateRole/applyTypeModifier/equipPlugins/getFinalStats + withLogger）、`tests/unit/roles.test.js`（RO-1..10）；修改 `server/data/role-templates.json`（typeModifiers L9 入表）、`server/data/schema.js`（typeModifiers 冻结校验）、`tests/integration/data-schema.test.js`（DS-12）。

## 核对结果

| 项目 | 结果 |
|---|---|
| R-1..R-8 数值独立复算 8 项全过（R-1 112/11/8/72/41、R-2a 14 独立推导、R-2b 94、R-3a 121/16/9/57/28、R-4b/c、R-5b 19、R-6d 1）；**FY[1,2,1] 轨迹验算与 R-3a 修饰分配完全一致** | ✅ PASS |
| 语义：修饰-品质-取整顺序（R-3 冻结）、specialized 恰 1 低、expert 四修饰互斥（80 种子）、equipPlugins 校验集与 R-7 全分支、失败原子 | ✅ PASS |
| getFinalStats：applyAffixes 复用 + 幂等（间接证伪"装配不改 stats"）+ 概率封顶 + max* 映射 | ✅ PASS |
| 日志矩阵：role.instantiate(debug)/role.panel(debug) 与 §4.6 一致 | ✅ PASS |
| 测试：RO-* 过 §3.3 黑名单（定点复算非镜像、失败路径、边界通过+超限） | ✅ PASS |
| 铁律：L2 / **L9（typeModifiers 已入表，DS-12 漂移拦截）** / L11 / L17 | ✅ PASS |
| 实测：npm test 210/210；cov roles.js 100/90+/100；gate 8 PASS/0 FAIL/1 PEND | ✅ PASS |

## 已修复的问题（当批）

1. **P1（L9）修饰系数硬编码**：1.15/0.85/1.30/spread 移入 `role-templates.json.typeModifiers`；schema T-DC-1 冻结核对（漂移即 fail，DS-12 正反用例）；roles.js 改读表。
2. **P2-① 分支缺口**：kind_mismatch 与 hp_regen 叠加补 RO-10（真实插件 rp_regen 语义：每 tick hp +1）。
3. **P2-② 下限注释**：R-8"极低系数触发下限"在数据下不可达（最小基础 8×0.8=6.4）——断言保留为防御，注释已诚实。

## 登记取向

- instantiateRole 随机消耗顺序（测试 stubSeq 依赖）：修饰 ints → 5×品质 float → slotCount int → 每槽 float。
- equipPlugins = "校验+登记"（不改 stats）；getFinalStats 幂等重算；regen 词条直接叠加 role.regen。
- P3 备忘：examples 头部 `.audit/verify-rest.js` 引用已删文件——登记后续批次补复算脚本；模板 pluginPoints=3 与品质值冗余（B3 同款，B21 评估）。

## 遗留

- 项 8（日志冒烟）PEND 待 B11；`.audit` 复算脚本随 B11 黄金用例一并补齐。