# B28 身份与会话（P7-2）—— 审查记录

> **本文件是 `docs/tasks.md` §6 批次 B28 的审查记录**（`scripts/check-docs.js` D6）。

- **批次**：B28（P7-2 身份与会话；`docs/plan-p7-playable.md` §P7-2）
- **记录日期**：2026-09-19（**汇编/登记日期**）
- **审查人类型**：**独立审查**（阶段级「代码级审查」，执行者未参与该阶段实现；结论见 `docs/reviews/P7-7-test-audit.md`）；本次「中央文档同步」只做要点汇编与状态回填，**不新增独立审查结论**。
- **证据来源**
  - `server/auth.js`（`module.exports` 见 `auth.js:722-734`）；`server/account.js`（`account.js:636-648`）；`server/store/session-table.js`、`server/store/archive.js`。
  - HTTP 接线：`server/index.js` 的 `authenticate()`（`index.js:581-594`）与 `P74_POST`/`P74_GET` 路由（`index.js:780-895`）。
  - `docs/progress.md` 末尾自动同步条目（2026-09-19「P7-2 身份与会话」）。

## 交付物摘要

- `server/auth.js`：注册/登录/登出/改密、scrypt 凭据、Bearer 会话（只存 `sha256(token)`）、失败锁定与限速、`authenticate(token)` → `ctx.player`。
- **P0 并发注册闭合**：`withRegisterLock` 使 8 个并发同名注册 → **1×200 + 7×409 `username_taken`**（不再出现双写）。
- `session_expired` **可达**：`index.js:584-588` 用 `store.sessions.peek()` 只读探针区分"不存在"与"刚过期"，过期 → `401 session_expired`。
- 会话清理与 §3.4 一致：**启动一次 prune + 读时懒清理**（**无定时器**）。
- `service-config.auth.usernameMin/usernameMax/nicknameMax` 已被消费（`nicknameMax` 夹到 ≤16）。
- 死码清理：`rate_limited` / `warehouse_invalid` **已删**（前者属 P7-4 限流中间件，登记为 `docs/security-backlog.md` SEC-02）。
- `server/account.js`：档案摘要/配置槽/仓库镜像/战绩/未读/防守汇总/改昵称 + 注册事务 `createPlayerArchive`；两文件**无 `node:fs`**，写路径一律经 `server/store`（A 类原子写 / B 类 journal）。

## 独立审查结论摘要

- `T-AU-1~3` 对应的注册/登录/会话/越权用例已落地；并发注册、会话过期、改密撤销其他会话三条边界有专门用例。
- 残留审查未发现"文档写了但代码没有"的鉴权缺口；`store.auth.*` 事件名落在既有 `store` 通道，无需改通道注册表。

## 未决项

1. 全局限流/并发闸门**未实现**（`rate_limited` 429 属该中间件）→ `docs/security-backlog.md` **SEC-02 仍未处置**。
2. 登录设备列表（IP/UserAgent 的 sha256 前 16 hex）仅落档，无对外端点（设计口径见 `docs/systems/11-account-store.md` §12.3）。
