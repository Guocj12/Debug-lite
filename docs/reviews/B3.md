# B3 审查记录

> 批次：B3（P1：core/items.js 数值层）
> 审查方式：独立上下文 agent（I-1..I-12 数值手算复算 + 门禁豁免机制实测 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS（修复 1 个 P1 后）**

## 审查范围

新增 `server/core/items.js`（rollQuality/rollSlotCount/tierOf/generateRoleItem/generateSkillItem/generatePlugin/openBox/applyAffixes/validateUnlock + withLogger）、`tests/unit/items.test.js`（IT-1..14）；修改 `scripts/gate.js`（`// cl:` 行级豁免 + token 边界 + 接收者 `L.`）、`tests/integration/gate-extra.test.js`（GX-5d）、`scripts/README.md`（项 6 契约）。

## 核对结果

| 项目 | 结果 |
|---|---|
| I-1..I-12 数值独立复算全对（I-2a 112、I-3c 下限 1、I-6a 0.096、I-8a 20、S-1a 0.99、S-1c 7 等 12 项） | ✅ PASS |
| 语义：applyAffixes 聚合顺序（D-45/46）、openBox 门控池（I-9）、技能随机参数清单（D-115）、T-RO-7 regen 直透 | ✅ PASS |
| `// cl:` 豁免机制：按文件收集、单处使用（round2 精度 100）、GX-5d 正反用例 | ✅ PASS |
| 日志矩阵：items.roll.quality/generate/affix.apply 与 §4.6 一致 | ✅ PASS |
| 测试：IT-* 过 §3.3 黑名单（种子化大样本 2% 容差、定点复算、失败/防御路径） | ✅ PASS |
| 铁律：L2 / L9（字面量仅 cl 豁免的 100）/ L11 / L17 | ✅ PASS |
| 实测：npm test 186/186；cov items.js 100/90+/100；gate 8 PASS/0 FAIL/1 PEND | ✅ PASS |

## 已修复的问题（当批）

1. **P1-1 入包取整分歧**：doc I-6b 规定 flat 词条入包即取整（4.48→+4）；实现先前保留原值致 R-5b 聚合差 1（20 vs 19）→ `generatePlugin` 对 `_flat` 词条 Math.round、百分比保留 2 位；IT-4 断言改 4 + 新增 IT-14（R-5b → 19）锁定。
2. **P2-1 rollSlotCount v=1 越界** → 上限钳位 + IT-3c 断言。
3. **P2-2 costDeltaByTier 品质缩放**：登记 B20 做（items-data 已注 B21 校准）。
4. **P2-3 gate 项 6① 不认 `L.debug` 接收者**（items/effects/field 惯用短名）→ LOG_CALL_RE/LOG_GENERIC_RE 补 `L`；GX 用例已有实际覆盖（IT-10 补位）。

## 门禁机制演进（本批登记）

- checkNumericHardcode 增加**行级豁免标记** `// cl:<值>`（通用精度常量与机制数值撞值时使用，审查可见）；token 边界 lookbehind/lookahead（标识符误报，B1）。
- 加权循环"尾兜底"分支模式：防御路径要求可触达（stub 越界输入测试锁定，IT-3c/IT-3b）。

## 遗留

- 项 8（日志冒烟）PEND 待 B11；P2-2（costDelta 品质缩放）随 B20。