# P0-3 审查记录

> 批次：P0-3（测试基建：runner 固化 + gen.js + 覆盖率阈值）
> 审查方式：独立上下文 agent（只读，对抗式核对；含两条独立计算路径的机器复算）
> 日期：2026-09-12　结论：**PASS**（无 P0/P1；P2 建议 5 条已全部落实）

## 审查范围

新增 `tests/helpers/gen.js` + `tests/helpers/gen.test.js`（G-1..G-12）；修改 `tests/README.md`（runner 契约 + gen.js 契约两节）。

## 核对结果

| 项目 | 结果 |
|---|---|
| 契约一致性：gen.js 与 README 冻结契约逐项一致（含 D-91 派生语义对齐） | ✅ PASS |
| 机器复算：mulberry32(42) 冻结常量经 **BigInt 独立实现**（非 `|0`/`imul` 路径）复算 5/5 Object.is 相等；6 种子 × 8 值 & 20 万采样值域全部吻合 | ✅ PASS |
| 测试质量：12 用例过 §3.3 黑名单——无 truthy-only/无 skip/无镜像/无凑覆盖；边界、失败路径、≥10000 大样本统计齐备 | ✅ PASS |
| 先红后绿：红态（无 gen.js → MODULE_NOT_FOUND + exit 1）可信；冻结值非从实现抄取 | ✅ PASS |
| 铁律：L2（改动面 3 文件）/ L4（归属标注）/ L17（UTF-8 无 BOM 抽查） | ✅ PASS |
| 实测复核：`npm test` 12 pass exit 0；`npm run cov` gen.js 100/100/100 exit 0；失败用例 exit 1；目录参数 ERR_UNSUPPORTED_DIR_IMPORT exit 1；**阈值逐文件判定**（evil.js 75% < 90 → exit 1） | ✅ PASS |

## 说明与已落实的 P2

1. **阈值尖刺修正**：`--test-coverage-lines=100` 尖刺在 gen.js 恰 100% 时不触发（严格 `<`），阈值强制已改用"带未覆盖代码的临时文件"方式证明（evil.js → exit 1）——README 契约表述不变。
2. **P2-1**：tasks.md §6 P0-3 行记法修正（`tests/helpers.js` → `tests/helpers/`（gen.js、log.js））+ 标 `[x]`；`log.js` 录制器注明随 P0-4。
3. **P2-2**：progress.md §4.2 的 `.audit/verify-*.js` 过时表述 → 改为"独立可复跑的机器计算验证（脚本或测试内）"。
4. **P2-3**：G-10 探针 `hash32('abc')` 两次（替换平凡真）。
5. **P2-4**：G-6/G-7 统计口径统一为 10000 样本 / ±2%（与 G-12 一致；固定种子下 ≥7σ）。
6. **P2-5**：tasks.md §3.2 新增 P0 基建批测试点归属行（G-* / T-LG / T-DC-* / T-CLI-2）。

## 固化到文档的实测事实（P0-3 节拍 4 的产出）

- runner 契约表 + 覆盖率阈值语义（每文件、严格 `<`、非零退出）已写入 `tests/README.md`
- 失败退出码 1、无 spawn、空匹配静默陷阱（gate 第 7 项兜底，P0-5）——复查均已复跑确认
- §1.3 命令分批落地：test/cov 本批实测；start/gate/demo/demo:log/cli 随 P0-8/P0-5/B11（tasks.md P0-3 行已注明）