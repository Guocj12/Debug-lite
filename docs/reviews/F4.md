# F4 审查记录

> 批次：F4（P6 前端第四批：battle 配置屏 + /battle 起战链 + candidatesFor slot 预过滤）
> 审查方式：独立对抗性上下文（9 项核查清单逐条 + 缺口自查 5 项 + 探针 `.review-f4/probe-http.js`（真后端 listen(0) HTTP——OPPONENTS 逐模板经 /battle、missing_warehouse、seed 域/回带）+ `.review-f4/probe-chain.js`（battle/run 全链：真 fetch → battle/loaded → 帧契约/复现性/错误 toast）+ verifyLayout 手动复算（shell+battle real-state 组合）+ 实测六件套）
> 日期：2026-09-17　结论：**CONDITIONAL PASS→需修（P1×2 必须当批修复；已全部修复并复测绿）**：①对手模板第 3 技能 `skill_displacement_bash` 为**数据表不存在 id**（技能模板 common 档实际仅 2 个；displacement 技能全为 mythic）→ **三模板全部被真后端 /battle 拒绝 409 loadout_invalid**——起战链必然失败；②battle/run 请求体**不带 warehouse** → 含装配引用的真实 loadout 起战 → 后端 409 missing_warehouse（子码）。P2×13 登记；修复后复测基线 **510/510、gate 9 PASS/0 FAIL/0 PEND、cov 聚合 97.82/87.82/96.57、check-arch 40 文件 0 违规、golden 18 tick 一致**；HTTP 探针 4 段 + 全链探针 3+2+1 段全 PASS（复跑稳定）。

## 审查范围

交付物（git status 实证）：`public/js/views/battle.js`（新增：OPPONENTS 三模板/opponentOf/loadoutSummary/battleLayout）；`public/js/store/effects.js`（改：battle/run + seed/random + battle/opp）；`public/js/store/reducer.js`（改：battle/opp/set）；`public/js/views/warehouse.js`（改：candidatesFor 第 4 参 slotType 预过滤——F3 P2★ 落地）；`public/js/views/index.js`（改：battle 屏注册）；测试 `tests/frontend/battle.test.js`（新增）×2 + warehouse.test.js（改：slot 预过滤断言）+ views.test.js（改）。契约依据：frontend-spec.md §6.5（config 16,80,640,400 / preview 680,80,584,400 / start y500 / B22 后 POST /battle {p1,p2,seed} / 开始前 loadout/validate / seed 空→后端生成回带 / 随机 = Date.now()%1e9）；interfaces.md §2（/battle 409 信封）；server/battle.js（runBattle 真裁决：buildPlayer → validateLoadout 引用完整性/门控）、server/loadout.js、server/ranked.js（B24 bot loadout 先例）；B22/B24 沿革（players{p1,p2} 帧形状 / common 技能循环取满 3 槽）；F3 沿革（P1 几何 / P2★ slot 预过滤）。

## 核对结果

| # | 核查项 | 结果 | 依据 |
|---|---|---|---|
| 1 | **battleLayout vs §6.5 逐条 + payload 形状** | ✅ PASS（P1-1 修复附注） | config(16,80,640,400)/preview(680,80,584,400)/start y500 ✓（测试断言 + verifyLayout）；radio×3 步距 42（h32 余 10px 无交）载荷 `{id}` → battle/opp → activeTab.battle ✓；seed 行（null → 「后端生成回带」文案）+ 随机按钮 action=seed/random ✓；看面板 panel/show ✓（effect 已存在）；错误行读 aiDraft.errors 首条（code/message/不合法 兜底链断言 ✓）；preview 三态（hint/stats+skills/空技能摘要）✓；开始按钮 payload `{opponent: pair.loadout}`——**对手 loadout 完整形状（role + skills×3 + ai）**，B22 端点可消费（真后端探针 ① 三模板全 200 实证）。verifyLayout real-state 组合（shell+panel / 无 ui 层 / errors 态）手动复算全绿。 |
| 2 | **对手模板真实性（后端可消费性）** | ❌ FAIL→P1-1（已修） | **探针实证（真 HTTP /battle）**：修复前 kiter/charger/cautious 三模板逐一 POST → **409 loadout_invalid：「未知技能模板 skill_displacement_bash」×3**——模板第 3 技能为数据表不存在 id（skill-templates.json 10 个 id 无此名；displacement 实为 mythic 档 skill_dash_bash/skill_dash_shadow）。根因：common 档技能仅 2 个（skill_melee_whirl/skill_straight_precise），模板虚构了第 3 个 id。**修复**：与 B24 后端 bot 同构（ranked.js buildBotLoadout「common 技能模板实际仅 2 个——循环取满 3 槽，validateLoadout 不查 templateId 唯一」）——common 技能循环取满 3 槽；修复后探针三模板全 200（62/28/62 帧）。AI action 名：move_right/move_left/wait ∈ 引擎 ACTIONS 白名单（engine.js:19）+ runtime 无 action 名白名单（ast 只校验字段类型）+ ast.validate(tier=common) 通过（seq/action 为基础节点）——全部合法 ✓（探针实战跑通）。门控：模板 unlockTier=common 与数据表一致 ✓。 |
| 3 | **battle/run effect 请求体/回带/toast 链** | ⚠️ FAIL→P1-2（已修） | 请求体 `{p1: st.loadout, p2: opponent, seed, tier}` **缺 warehouse**。探针实证：p1 带 slot 装配引用（role.slots[0].pluginUid）且无 warehouse → 后端 409 loadout_invalid 子码 **missing_warehouse**（引用完整性校验 T-PB-9 需要仓库）；同一请求带 warehouse → 200 28 帧——**真实玩家装配后起战必失败**。**修复**：effect body 增 `warehouse: st.warehouse`（server/index.js 已透传 body.warehouse 到 runBattle，契约本就支持）。seed=null → 后端 `crypto.randomInt(1,0x7fffffff)` 生成回带 → api onSeed → seed/set（+ 落盘，SAVE_ON_ACTIONS 已含 seed/set——F2 P1-8）✓ 探针实证；err → toast `battle/run: <code>` 不跳转 ✓（测试+探针双实证）。 |
| 4 | **seed/random 注入缝** | ✅ PASS（P2-1 登记） | `ctx.randomInt` 注入缝：测试注入 12345 → seed/set 精确透传 ✓；缺省 `((Date.now()>>>0) % 0x7fffffff) + 1` 域 [1, 0x7fffffff] 整数 ✓（测试断言）。与 spec「随机 = Date.now()%1e9」差异登记 P2-1：%1e9 域 [0,999999999] **含 0 → 后端 bad_seed**（探针实证 seed0→400）；1e9 < 0x7fffffff 故两式域兼容，但实装 +1 避 0 且与后端 seed 校验域 [1,0x7fffffff] 完全一致——**实现优于 spec**；随机性属客户端选择域，同 seed 复现语义不受影响（探针：seed=42 ×2 帧全等 deepEqual）。注：store/index ctx 未透传 randomInt——注入缝当前仅测试面可达，浏览器 crypto 注入需一行透传（P2-8）。 |
| 5 | **candidatesFor slot 预过滤** | ✅ PASS | `slotType !== undefined && p.slot !== undefined && p.slot !== slotType → 排除`；三态断言：slotType undefined → 不过滤（回归语义）/ p.slot undefined → 放行后端 / 槽型匹配通过 + equipped 排除 ✓（battle.test 末用例 + warehouse.test 布局断言 [p1,p3]/[p3] 更新）。与后端关系：已知槽型错配面（F3 实证 ~86%）前端剔除，无 slot 字段者交后端权威裁决（slot_type_mismatch toast 兜底链路 F3 已实证）；词汇一致性实证：插件 slot ∈ {atk,def,hp,sp,mp,special,basic} = 物品槽型词汇（role 槽 mp/hp/sp/def/atk 实测）✓。 |
| 6 | **布局合规（verifyLayout/z 层级/文本语义）** | ✅ PASS（P2-4/5 登记） | verifyLayout real-state 三组合全绿（shell+battle、无 ui 层回退、errors 态）；start 按钮 z2 vs shell header z5 无几何相交、无 zconflict；child z1 > parent z0 全合规；无 clip/overlap（手动复算 + 测试断言）。loadoutSummary 以「；」拼单文本盒（w580 h48）——CSS `.dl-t` nowrap+ellipsis → 单行省略不换行（P2-4 视觉登记，与 shell seed w28 同型兜底）。开始按钮水平居中偏差（P2-5）。 |
| 7 | **架构/铁律** | ✅ PASS | check-arch **40 文件 0 违规**（39+F4 新增 battle.js=40，L7 依赖图完整）；public 全域 console 零命中（仅 app.js 注释提及）✓；无裸导入/无 server import ✓；views 纯函数无 window ✓；项 7 逐文件：battle.js **100/91.84/100**、warehouse.js 100/90.54/100、views/index 100/87.50/100 三阈全达标 ✓。 |
| 8 | **覆盖与门禁** | ✅ PASS | `npm test` **510/510**（509 + 审查新增 1：模板↔数据表同步锁）；`npm run gate` **9 PASS/0 FAIL/0 PEND**；`npm run cov` 聚合 **97.82/87.82/96.57**（与声明值精确一致）；check-arch 40 ✓；golden 18 tick ✓。 |
| 9 | **死代码/死分支** | ✅ PASS（P2-9 附注） | battleLayout 全分支（分支锤用例：空技能/无 stats/无 params/错误无 code/无 ui 层）✓；effects 四新 action 双态 ✓（battle/run ok/err、seed/random 默认/注入、battle/opp、battle/opp/set reducer 双向断言）；loadoutSummary 兜底链（name→templateId→uid、quality?、空→提示行）✓；`battle/opp` effect 的 `const st=ctx.store(); void st;` 死读取（P2-9）；battle.js 未用导入 SPACES（P2-9 并）。 |

## 缺口自查（五项按任务逐条）

① **/battle 请求缺 warehouse 的 409 missing_warehouse 风险——判定 P1 并已修**：探针实证（p1 含 slot 引用 + 无 warehouse → 409 missing_warehouse；带 warehouse → 200 28 帧）。修复方向采用「battle/run effect 带 st.warehouse」：后端契约本就支持 body.warehouse（server/index.js:354 透传），前端一行补齐，无需独立预校验提示；引用完整性校验仍由后端权威完成。修复后回归断言 + 探针全绿。
② **开始按钮无 loadout/validate 前置（§6.5「开始前 loadout/validate 失败 → 提示先修装配」）——判定 P2（P2-2）**：现实现为被动 409 toast（`battle/run: loadout_invalid`，探针实证不跳转、状态不变）+ `battle_ld_errors` 行仅读 aiDraft.errors（当前无任何 battle 屏动作可触发 loadout/validate——行为 F5 预留，非死分支但有 UI 惰性）。差异论证：后端权威 + toast 兜底与 F3 拒绝码链路同构（无损坏无静默失败）；但 409 的 details（各条具体错误）未上屏（toast 仅主码），玩家看不到「缺角色/技能必须恰 3 个」等具体原因——建议 F5 接线 loadout/validate 前置并把 details 展开进 battle_ld_errors（★）。
③ **对手模板 3 技能 unlockTier common 门控——实证并已修（P1-1）**：skill-templates 仅 2 个 common（whirl/precise）；模板第 3 技能 `skill_displacement_bash` 为不存在 id（displacement 实为 mythic）——比「超门控」更严重：**id 不存在 → 三模板全 409**。修复为 common 循环取满 3 槽（B24 同构）；新测试「模板↔数据表同步锁」防止回归（技能 id 存在性 + common 门控可达，require 数据表实证）。
④ **seed/random %1e9 vs 0x7fffffff 域——登记（P2-1）**：见核查项 4——1e9 < 0x7fffffff 域兼容，但 %1e9 含 0（bad_seed 域外）；实装 +1 且与后端校验域全同，为有意改进；spec 口径待改。
⑤ **loadoutSummary 单行 vs 多行视觉——登记（P2-4）**：`；` 拼单文本盒 + `.dl-t` nowrap ellipsis → 超长摘要单行省略（第 3 技能可能不可见）；与 shell seed w28 同型视觉策略，可接受；如需完整展示建议 F7 改多行盒/分段盒。

## 发现的问题

### P0

无。

### P1（必须当批修复——已全部修复并复测）

1. **P1-1 对手模板第 3 技能 `skill_displacement_bash` 为数据表不存在 templateId → 三模板全部 409，起战链必然失败**。真后端 HTTP 探针实证：`POST /api/v1/battle {p1: 合法, p2: <模板>}` — kiter/charger/cautious 三模板逐一 → `409 loadout_invalid :: 未知技能模板 skill_displacement_bash`。根因：skill-templates.json 无此 id（displacement 技能为 mythic 档 skill_dash_bash/skill_dash_shadow）；common 档技能实际仅 2 个（skill_melee_whirl/skill_straight_precise），模板虚构第 3 个 id 且声称 common 门控。**修复**：`battle.js` OPPONENTS 技能改为 common 档循环取满 3 槽（whirl/precise/whirl——`validateLoadout` 不查 templateId 唯一，与 B24 后端 bot `buildBotLoadout` 同构）；注释登记沿革。**回归锁**：battle.test.js 新增「模板 ↔ 后端数据表同步锁」用例（require skill-templates/role-templates 实证：三模板角色/技能 id 存在性 + common 门控可达；同步锁直接防「数据表 ≠ 前端模板」回归——本次缺陷即此类）；`.review-f4/probe-http.js` ① 段 P0 前红后绿。
2. **P1-2 battle/run 请求体缺 warehouse → 含装配引用的真实 loadout 起战必 409 missing_warehouse**。探针实证：`p1.role.slots=[{type:'atk',pluginUid:'x'}]`（装配中玩家的真实形态）+ 无 warehouse → `409 loadout_invalid` 子码 `missing_warehouse`（validateLoadout：「出战配置含装配引用，需要 warehouse 校验引用完整性（T-PB-9）」）；同一请求带 warehouse（引用可解析）→ 200 28 帧。**修复**：`effects.js` battle/run body 增 `warehouse: st.warehouse`（server/index.js 本已透传 body.warehouse → runBattle，契约支持且 B24 同构——ranked/run 同样接收 warehouse）。**回归锁**：battle.test.js battle/run 用例捕获请求体断言 `sent[1].warehouse === warehouse`；探针 ②③ 段实证。

### P2 登记（不阻塞；建议随批落实项以 ★ 标注）

1. **seed/random 公式 vs spec §6.5「随机 = Date.now()%1e9」差异登记论证（缺口④）**：%1e9 域 [0,999999999] 含 0 → 后端 bad_seed（探针实证 seed0→400）；实装 `(Date.now()>>>0) % 0x7fffffff + 1` 域 [1,0x7fffffff] 与后端 seed 校验域（server/battle.js:54）完全一致且避 0。同 seed 复现语义不受影响（探针：seed=42 ×2 帧 deepEqual 全等）。建议 spec 改口径为「Date.now()%0x7fffffff+1（或等价避 0 式）」。★
2. **§6.5「开始前 loadout/validate」未前置（缺口②）**：被动 409 toast + 不跳转（探针实证）；409 details（具体错误逐条）未上屏（toast 仅主码 loadout_invalid）；battle_ld_errors 行当前无 battle 屏动作可触发（读 aiDraft.errors，F5 编辑器接入 loadout/validate 后启用）。后端权威 + toast 兜底链路完整（与 F3 拒绝码同构），判定 P2；建议 F5 接线前置校验并展开 details 上屏。★
3. **对手模板 AI 为单 action（move_right/move_left/wait）**：实战双方均不放技能 → 比赛多为平局收场（探针：三对手场次 winner=draw，ticks 28/21/25，62 帧超时局亦有）；与交付物口径「单 action AI」一致，但游戏性偏弱——对手强度/技能释放属 F5/F7 打磨面。
4. **loadoutSummary 单行省略（缺口⑤）**：`；` 拼单文本盒 + `.dl-t` nowrap+ellipsis → 长摘要（角色+3 技能）第 3 技能可能被截断；h48 盒实际只用一行。视觉可接受（与 shell seed w28 同型兜底）；F7 可改多行盒或分段盒。
5. **开始按钮水平居中偏差**：`center(240,48)` 为装饰性调用（button() 固定 w160）——实际 x400..560（中心 480）≠ 视口中心 640，左偏 160px；y500..540 亦非「center 底部」垂直居中。无越界无重叠（verifyLayout 绿），spec 散文「center 底部 y500」与实装偏差登记；建议 x=560（w160 居中）或 spec 澄清。
6. **对手 radio 文本 w240 含「label（note）」长文本**：nowrap ellipsis 兜底；现文案 14 字内可容纳，note 加长即省略——防御登记。
7. **battle/run 无 busy/disabled 防连点**：双击开始 → 双 POST → 双 battle/loaded 后者胜（REPLAYS 注册表无害累积）；reducer battle.running 标志存在但 battleLayout 未使用——F5 编辑器接入时补 loading/disabled。
8. **randomInt 注入缝仅测试面可达**：effect 读 `ctx.randomInt`，但 store/index ctx 未透传（opts.randomInt 未纳入 ctx）——app 层浏览器 crypto 注入需一行透传；当前浏览器走 Date.now 缺省（spec 口径），无功能影响。
9. **死读取/死导入净化**：`battle/opp` effect 的 `const st = ctx.store(); void st;`（无副作用凑形）；battle.js 导入 SPACES 未用（F3 warehouse.js 同型遗留）——import 层净化登记。
10. **spec §4.3 帧散文 `diff{players[]...}` 与 B22 实装不符**：真帧 `players{p1,p2} 对象 / bases{p1,p2} / bullets[]/events[]/aiTrace[] / tick 1-based 连续（f.tick=i+1）`（probe-chain 实证 + replay-audit 同口径）；B22 已冻结对象形状，spec 散文待改——F6/7 渲染层应按对象形状实现。★
11. **模板 params 为前端声明并透传**：无插件时 buildPanel 聚合 params 直透（`Object.assign(inst, agg)` 覆盖实例化值）——模板 params（multiplier1/mp10/cd3）成为 p2 实际战斗参数，与模板表 base 值可不同；属模板设定口径，非校验面。登记以免误读。
12. **cov 用例数迁移**：509 → 510（审查新增同步锁 1 例）；聚合 97.82/87.82/96.57 与声明精确一致（分支/行/函数三值零漂移）。
13. **同秒连发 seed 观察**：探针三连发后端生成 seed 出现重复值（1682828175×3）——运行环境 CSPRNG 熵源受限所致，非产品缺陷（seed 回带机制/复现语义不受影响）；登记观察记录。

## 实测

| 命令 | 期望 | 实际 | 结果 |
|---|---|---|---|
| `npm test` | 509/509 全绿 | **510 pass / 0 fail / 0 skip**（509 + 审查新增 1：模板↔数据表同步锁；全量回归） | ✅ |
| `npm run gate` | 9 PASS/0 FAIL/0 PEND | **9 PASS / 0 FAIL / 0 PEND**（项 7=510 用例 + 五目录阈值；项 3=check-arch 40 文件；项 8=黄金 18 tick；修复后复跑） | ✅ |
| `npm run cov` | 聚合与声明一致 | 聚合 **97.82 / 87.82 / 96.57**（与声明精确一致）；battle.js 100/91.84/100、warehouse.js 100/90.54/100、views/index 100/87.50/100 | ✅ |
| `node scripts/check-arch.js` | 40 文件 0 违规 | `[PASS] 40 个文件无依赖违规` | ✅ |
| `node .audit/golden-battle.js` | 18 tick 一致 | **黄金战斗复算通过（18 tick, winner=p2, phase=role）** | ✅ |
| 探针 `.review-f4/probe-http.js` | 真 HTTP 后端裁决 | **4 段 PASS**：①三模板（修复前全 409「未知技能模板 skill_displacement_bash」→ 修复后全 200：kiter 62 帧/charger 28 帧/cautious 62 帧）②引用+无 warehouse → 409 missing_warehouse ③带 warehouse → 200 ④seed 域（0/超界 → 400 bad_seed；null → 生成回带 [1,0x7fffffff]） | ✅（P1 定案 + 修复验证） |
| 探针 `.review-f4/probe-chain.js` | battle/run 全链 | **全 PASS**：三对手真 HTTP 起战（frames 非空/tick 1-based 连续/players{p1,p2}/seed 回带/playing 复位）；同 seed ×2 帧 deepEqual 全等（确定性复现）；空 loadout → toast「battle/run: loadout_invalid」不跳转 | ✅ |
| 手动 node -e verifyLayout | real-state 组合 | shell+battle、无 ui 层回退、errors 态三组合全绿（无 overlap/clip/zconflict——修复前后均无几何问题） | ✅ |

## 独立实证（非测试代码路径，可复跑 `.review-f4/probe-http.js` / `.review-f4/probe-chain.js`）

① **P1-1 定案（真 HTTP）**：`server/index.js start(listen(0))` + 全局 fetch → `POST /api/v1/battle` 对每个 `OPPONENTS` 模板：修复前 3×`409 loadout_invalid: 未知技能模板 skill_displacement_bash`（details 逐条 message 落列）；修复后 3×200（帧数 62/28/62，winner=draw——单 action AI 对战语义）。同步锁测试（tests/frontend/battle.test.js）以 `require('../../server/data/skill-templates.json')` 反查模板 id 存在性与 common 门控，从测试面锁死同型回归。② **P1-2 定案**：p1 带 `role.slots[0].pluginUid` 引用 → 无 warehouse：`409 loadout_invalid :: 出战配置含装配引用，需要 warehouse 校验引用完整性（T-PB-9）`；带 warehouse（ghost 插件 equipped=true/unlockTier common/affixes[]）：200 28 帧。③ **帧契约实证**：真帧 `diff.players{p1,p2}{fromX,toX,facing,hp,mp,sp}` 为**对象非数组**、bases{p1,p2}、bullets/events/aiTrace 数组、tick 1-based（f.tick=i+1）——与 replay-audit「tick 连续」口径一致，spec §4.3 散文 `players[]` 不符（P2-10）。④ **seed 域实证**：0 → 400 bad_seed；0x7fffffff+1 → 400 bad_seed；null → 生成回带（域内整数经 onSeed 进 state + 落盘）。⑤ **verifyLayout 复算**：`renderScreen`（shell+battle 三组合）零问题；start 按钮 z2 vs shell z5 无交；radio 步距 42 无交。

## 结论

**CONDITIONAL PASS（P1×2 必须当批修复后转 PASS；本批已全部修复并复测）**。交付物覆盖面与测度声明达成：510/510（509+审查同步锁 1 例）、gate 9 PASS/0 FAIL/0 PEND、cov 聚合 97.82/87.82/96.57（声明值精确一致）、check-arch 40 文件 0 违规、golden 18 tick 一致、public/js 全域无 console；两项 P1 均由**真后端 HTTP 探针**定案且均为「前端模板/请求体 vs 后端裁决」断裂：对手模板第 3 技能为不存在 templateId（三模板全 409——起战链必败；B24 bot 同构循环修复 + 数据表同步锁防回归）、battle/run 缺 warehouse（装配后玩家起战必 409 missing_warehouse；effect 补带 + 请求体断言）。P2 登记 13 项中 3 项带 ★（seed/random 公式 spec 改口径、loadout/validate 前置 F5 接线、§4.3 帧形状散文改写）；五项缺口自查全部闭环（①missing_warehouse 判定 P1 已修、②前置校验判定 P2、③模板技能门控实证为 id 不存在（P1 已修）、④%1e9 域差异登记论证（实现更安全）、⑤单行省略视觉登记）。复跑基线：`npm test && npm run gate`（串行）＋ `.review-f4/probe-http.js` ＋ `.review-f4/probe-chain.js` 全绿即为回归通过。

## 修复落实（审查侧，2026-09-17；P1×2 当批）

1. **P1-1 对手模板技能修正**：`public/js/views/battle.js` OPPONENTS 技能自 `['skill_straight_precise','skill_melee_whirl','skill_displacement_bash']` 改为 common 档循环取满 3 槽 `[skill_melee_whirl, skill_straight_precise, skill_melee_whirl]`（`COMMON_SKILL_IDS[i % len]`，B24 后端 bot 同构；`validateLoadout` 不查 templateId 唯一）；注释登记沿革与新测试说明。
2. **P1-2 battle/run 补带 warehouse**：`public/js/store/effects.js` battle/run 请求体增 `warehouse: st.warehouse`（引用完整性校验 T-PB-9 所需；后端契约本就支持 body.warehouse）。
3. **回归锁**：`tests/frontend/battle.test.js` 新增「模板↔后端数据表同步锁」用例（技能/角色 id 存在性 + common 门控，require 真数据表）+ battle/run 用例断言请求体携带 warehouse（`sent[1].warehouse === warehouse`）。

复测：`npm test` 510/510 ✅；`npm run gate` 9 PASS/0 FAIL/0 PEND ✅；`npm run cov` 聚合 97.82/87.82/96.57 ✅（battle.js 100/91.84/100）；`node scripts/check-arch.js` 40 文件 0 违规 ✅；`node .audit/golden-battle.js` 18 tick 一致 ✅；`.review-f4/probe-http.js`（4 段）+ `.review-f4/probe-chain.js`（7 段）全 PASS 复跑稳定 ✅。