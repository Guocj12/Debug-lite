# P0-9 审查记录

> 批次：P0-9（assets 占位表：sprites/animations 经 API 提供）—— **P0 阶段收口批**
> 审查方式：独立上下文 agent（items-data §1 逐条对照 + 编码逐字节核验 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS**（无 P0/P1；P2×4 已当批落实）

## 审查范围

新增 `assets/sprites.json`、`assets/animations.json`；修改 `server/data/schema.js`（assets 纳入 T-DC-1）、`server/index.js`（tableNames/loadTable 合并 assets）、`tests/integration/data-schema.test.js`（DS-11 + assetsDir 参数）、`tests/api/api.test.js`（TABLES 清单）。

## 核对结果

| 项目 | 结果 |
|---|---|
| items-data §1 一致性：16×16 方块 + 黑边 + 品质描边色（五色与 qualities.json 逐值相同且 schema 交叉校验）；技能 8×8 四形状与类型语义对应；插件几何形枚举全命中 | ✅ PASS |
| schema 校验：id 交叉（多余/缺失/条数/重复全覆盖）、描边色交叉、形状枚举、帧规格、六基础动画必齐 | ✅ PASS |
| API 集成：9 表全 200（AP-2 实测）、unknown_table 语义不变、404 detail 可用表清单含 assets | ✅ PASS |
| 铁律：L2（改动面 6 文件）/ L9 / **L17（data-schema.test.js 曾遭 PS Set-Content 引入 BOM，node 祛除后逐字节核验无 BOM、无 U+FFFD）** / T-DC-1 覆盖 | ✅ PASS |
| 实测：npm test 133/133；gate **8 PASS/0 FAIL/1 PEND**（项 8 待 B11）；cov 97.23/85.29/96.30；真实 CLI `data sprites`/`data animations` 可达 | ✅ PASS |

## 已落实的 P2（当批）

1. **P2-a BASE_ANIMS 死常量** → 接线为"animations.role 六基础动画（idle/move/dodge/hit/cast/dead）必齐"校验。
2. **P2-b 形状↔类型映射**：占位阶段登记为 B21 前建议（技能形状↔type、插件形状↔槽位），不强行校验。
3. **P2-c 陈旧文案**：AP-2 标题改"9 张表"；cli data 用法提示补 sprites/animations。
4. **P2-d**：interfaces.md §1 schema.js 行补 `assetsDir?` 可选参数说明。

## 登记取向

- assets 形状枚举为 P0-9 占位定义（items-data §1"见下表"缺失，几何形清单由此定义，B21 前可扩展）。
- `validateStructure(dataDir, assetsDir?)`：gate 走默认推导 `<repo>/assets`；fixture 显式传入。

---

# P0 阶段收口确认（9/9 批）

| 批次 | 状态 | 关键出口 |
|---|---|---|
| P0-1..P0-8 | ✅ | 见各批记录 |
| P0-9 | ✅ 本批 | assets 经 /api/v1/data/:table 提供 |

**P0 出口**（tasks.md §6，已达成）：
- `npm run gate` 全绿 = **8 PASS / 0 FAIL / 1 PEND**（唯一 PEND 项 8 属 P1 范围，B11 激活）
- `cli health`、`cli data battle-config`（及全部 9 表，含 sprites/animations）可用 ✅（真实进程验证）
- trace 可见模块边界与 `api.req/res` ✅（api 测试断言）
- 门禁 9 项中 8 项实时生效；项 8（日志冒烟）接线完成待 B11

**后续**：P1 内核 11 批（B1 rng/field → B11 黄金战斗）从 B1 开始，`interfaces.md` 已冻结全部签名。