# F7 审查记录

> 批次：F7（P6 前端收尾批：①存档导出/导入 ②AI 轨迹可视化 ③自定义 Blockly 块注册 ④设置屏新控件）
> 审查方式：独立对抗性上下文（7 项核查清单逐条 + 探针 inline 实证：bridge 块型全集枚举（node 直接 import bridge.toBlocks 全类型程序递归收集 = 16 型）、BLOCK_TYPES↔bridge 差集矩阵、planTrail 边界矩阵、settings verifyLayout 带 15 条记录实跑、get/arch 复跑）
> 日期：2026-09-18　结论：**CONDITIONAL PASS→修复（P0×1 + P1×2 必须当批修复；已全部修复并复测绿）**：
> ①**P0 块词汇断裂**——blocks.js 原 20 键（statement_seq/move_right/var_declare/var_set/if_statement/break_loop/while_true/get_var/random_int/arith_expr/cmp_expr/logic_expr…）与 bridge.js 识别块型全集（16 型）几乎全异：14 个 bridge 类型（var/set/if/loop_count/random/action/break/function/call/get/bullets/arith/cmp/logic）未注册、18 个键 bridge 不认识 → 工具箱与序列化 round-trip 无法实例化（Block type not registered）；且 `registerBlockTypes` 全仓**无应用内调用点**（仅测试引用）→ F6 P2-1（根循环不落座）未闭环；②**P1 扁平仓库 import 归一缺失**——normWh 仅存在于 createStore **loadPersist 合并路径**，reducer 'save/import' case 直存 p.warehouse → 扁平仓库 import 后 state.warehouse 无 buckets（warehouse 屏读 buckets 断裂）；③**P1 loadout skills 非三槽**——`skills || [null,null,null]` 对长度 ≠3 数组透传。修复后复测基线 **533/533、gate 9 PASS/0 FAIL/0 PEND、cov 聚合 98.02/88.44/96.60、check-arch 49 文件 0 违规、golden 18 tick 一致**。

## 审查范围

交付物（git status 实证）：`public/js/util/archive.js`（新）、`public/js/render/trail.js`（新）、`public/js/editor/blocks.js`（新）、`tests/frontend/archive.test.js`（新）、`public/js/store/effects.js`（改：save/export + save/import + seed/random）、`public/js/mount/index.js`（改：paint 并入 planTrail + 装配点注册块）、`public/js/mount/canvas.js`（改：trail 折线绘制）、`public/js/views/settings.js`（改：seed 行/随机/导出/关于/导入提示）。契约依据：frontend-spec §8（存档导出/导入：schemaVersion 校验/export 全量/import 形状/缺字段→bad_save 语义按 F7 修正为缺省归一）、§6.6（回放渲染层只读 frames）、§4.2（effects 签名/action 冻结词汇 save/export|save/import）、§3.5/§6.0（verifyLayout/mount 唯一 DOM 写入）、§6.2（Blockly 装配）+ interfaces §1（AST 契约）+ bridge.js（16 块型词汇，F6 冻结）+ store/index.js normWh（loadPersist 合并）实测。

## 核对结果

| # | 核查项 | 结果 | 依据 |
|---|---|---|---|
| 1 | **archive：exportState/parseImport ↔ reducer save/import ↔ loadPersist 合并语义；schemaVersion/类型/形状码；tier/seed/loadout 缺省** | ❌ FAIL→P1-1/2（已修） | ①exportState=persistFields 全量（schemaVersion/tier/warehouse/loadout/gachaLastResult/seed/logPrefs）✓；parseImport 校验链（非字符串→bad_json、解析失败→bad_json、数组/标量→bad_json、schemaVersion≠1→bad_version）+ tier 缺省 'common' + seed 缺省 null ✓；reducer 'save/import' case 与之合并语义一致 ✓。**②缺口：扁平仓库归一不存在于 import 路径**——normWh 只在 store/index.js createStore **loadPersist**（初始合并）执行；reducer 'save/import' case `warehouse: p.warehouse || state.warehouse` **直存不归一** → 扁平 {role:[...]} import 后 state.warehouse 无 buckets（warehouse 屏/效果全断）。修复：parseImport 内 normWarehouse（与 normWh 同语义，buckets 分层）。**③loadout.skills 非三槽**：原 `skills || [null,null,null]` 对数组透传不归一 → normLoadout 补齐三槽/超长截断。测试如实断言两修复。 |
| 2 | **effects：save/export doc 守卫 + Blob try/catch；save/import dispatch 落盘 + toast；无 text noop；§4.2 词汇** | ✅ PASS | 'save/export'：`ctx.doc && typeof ctx.doc.createElement === 'function'` 守卫（无 doc 静默返回）+ `new Blob/URL.createObjectURL` 全在 try/catch（node 测试环境安全）✓；'save/import'：text payload 守卫（无 → noop 返回）→ parseImport → ok → dispatch 'save/import'（reducer 合并）→ `ctx.save(ctx.store())` 落盘 → toast「存档已导入」；fail → toast「导入失败：{message}」✓；action 名 'save/export'/'save/import' ∈ §4.2 冻结词汇 ✓；'seed/random' 同 EFFECTS 既有键 ✓。测试双 toast 分支 + noop + 无 doc 分支全断言。 |
| 3 | **planTrail：limit 32、tick 越界/undefined、缺字段/未知 owner/toX 非整数跳过、图元形状；mount paint 顺序；canvas trail 分支（beginPath 守卫/globalAlpha 复位/PALETTE）** | ✅ PASS（P2-1/2 登记） | 边界矩阵实测：40 帧 tick39 → 32 点 ✓、tick999 → clamp 40 帧 slice(-32)=32 点 ✓、tick undefined → 0 ✓、未知 owner p3 → 0 ✓、缺 players → 0 ✓、toX float（224.5）→ 跳过 ✓、输出 {kind:'trail',owner,points} ✓；mount paint：`planFrame(frame.diff,tick).concat(planTrail(p1)).concat(planTrail(p2))` —— player 图元之后并入 ✓；canvas.js trail 分支：`typeof ctx.beginPath === 'function'` 守卫、globalAlpha 0.4 → stroke → 复位 1、PALETTE.p1/p2 正确 ✓（测试断言 stroke 触发 + 单点不抛）。P2：trail y=96 硬编码（planFrame PLAYER_Y 同值，可改导入常量）；beginPath 守卫 false 臂不可达（地面线已无条件 beginPath，防御性）——测试注释如实。 |
| 4 | **blocks.js ↔ bridge：BLOCK_TYPES 键集一一对应（数量+命名）；幂等；null 安全；min init；tasks.md 打磨登记** | ❌ FAIL→P0-1（已修） | **探针实证 bridge 块型全集 = 16 型**（toBlocks 全类型程序递归收集：var/set/if/loop_forever/loop_count/random/action/break/function/call/num/get/bullets/arith/cmp/logic）；**差集矩阵**：原 20 键仅 loop_forever/num 重名，14 个 bridge 类型缺失、18 个键 bridge 不识别 → 注册后工具箱/序列化 round-trip 无法实例化（"Block type not registered"）+ 桥不认的块 toAst 直接丢弃。**并确认 `registerBlockTypes` 无应用内调用点**（mount/main/app/index.html 均未引）→ F6 P2-1（根循环不落座）未闭环。修复：blocks.js 重写为 16 键逐一对应 bridge 词汇（fields 元数据同步）+ mount 装配点 `registerBlockTypes(B)`（幂等/null 安全）。修复后测试断言键集 = BRIDGE_TYPES 16 值 deepEqual + 注册 16 + 幂等 0 + null 两臂 + init 四步。tasks.md §6 F7 行已登记视觉 init 最小化打磨（setColour/prev/next/appendField，无 setOutput/形状细分）。 |
| 5 | **settings 新控件：5 盒无 overlap/clip/zconflict；seed null 文案；action 拼写；verify 全绿** | ✅ PASS | 实跑 settingsLayout + verifyLayout（注入 15 条日志记录 → 记录列表满载）：`settings_seed`(32,112,300,24)/`settings_seed_rand`(340,108 ghost)/`settings_about`(32,152)/`settings_export`(32,184 主按钮)/`settings_import_hint`(204,196) 均在左面板(16,80,560,560)内、与既有 3 档 radio/日志列表互不相交、z=1 > 面板 z=0 无 zconflict → **verify ok=true**。seed null →「（未设）」✓、seed=7 →「seed：7」✓；action：'seed/random'/'save/export' 与 EFFECTS 键一致 ✓。 |
| 6 | **提交面：仅 F7 8 文件 + docs 登记；无 package/lock/index.html；archive.test.js 用例数与完整性** | ✅ PASS（P2-3 登记） | git status 实证：8 个 F7 文件（4 M + 4 ??）+ 本审查 docs 登记（tasks/progress 仅登记字段）；**无 package.json/package-lock.json（F6 已提交不再动）**、无 index.html（F7 未动——块注册经 ESM 装配点接线，无需 HTML 脚本标签）；`.audit/` 无 F7 残留调试脚本（verify-rest.js 为设计期遗留，progress L-4 已登记）；archive.test.js 6 用例（archive 往返+码、effects 双 toast+noop、planTrail 矩阵、blocks 词汇+幂等、settings verify、mount canvas trail）——无重复 test id、无未定义变量（`void prog` 显式）。口述"5 用例"为批次起始估算，实为 6（超覆盖，非缺）。 |
| 7 | **门禁复跑（修复后）** | ✅ PASS | `node --test --test-isolation=none` **533/533（0 fail）**；`node scripts/gate.js` **9 PASS/0 FAIL/0 PEND**（阈值未放宽）；`node scripts/check-arch.js` **49 文件 0 违规**（46+3 新文件，public ESM 裸导入白名单仍仅 blockly）；`node .audit/golden-battle.js` **18 tick winner=p2 一致**；cov 聚合 **98.02/88.44/96.60**（行/分支/函数）。 |

## 缺口自查（任务逐条对照）

① **块词汇对应关系（"20 型与 bridge AST 类型一一对应"）——判定 P0 并已修**：任务宣称 20 型，bridge 识别块型全集实为 16（探针枚举实证）；原 20 键与 bridge 命名几乎全异（仅 loop_forever/num 重名）→ 数量与命名双重不符，且注册无调用点 → 编辑器块定义实际不存在。修复：16 键逐一对应 + 装配点接线 + 测试 deepEqual 锁词汇。
② **扁平仓库 import 归一（"由 normWh 兜底是否真的存在"）——判定 P1 并已修**：normWh 仅 loadPersist 路径；reducer save/import 直存 → 兜底不存在 → parseImport 层内建 normWarehouse（同语义）。
③ **loadout skills 三槽——判定 P1 并已修**：`|| [null,null,null]` 对非三槽数组透传 → normLoadout 补齐/截断。
④ **trail 渲染顺序/绘制：实证 PASS**（probe + mount.test 现有 + 新 canvas 测试）。
⑤ **F6 P2 遗留归属**：P2-1（自定义块定义）**本批闭环**（注册接线+视觉 init 登记）；P2-2（/ai/battle 无 bullets）登记留档；P2-3/4/5/6/7/8/9 与 F7 无关，维持登记。

## 发现的问题

### P0（必须修复才能提交——已修复并复测）

1. **P0-1 自定义块词汇断裂 + 零调用点（F6 P2-1 未闭环）**：blocks.js 提交版 `BLOCK_TYPES` 为 20 键自创词汇（statement_seq/move_right/move_left/wait/defend/dodge_left/dodge_right/use_skill/var_declare/var_set/if_statement/break_loop/while_true/get_var/random_int/arith_expr/cmp_expr/logic_expr），与 bridge.js 识别块型全集（16 型：var/set/if/loop_forever/loop_count/random/action/break/function/call/num/get/bullets/arith/cmp/logic）**仅 loop_forever/num 重名**——差集：14 个 bridge 类型无注册（工具箱 action/var/… 拖取 = "Block type not registered" 抛错，bridge 序列化产物无法实例化）、18 个已注册键 bridge.toAst 不识别（块被静默丢弃）。且 `registerBlockTypes` 全仓唯一引用在测试 → 应用内从未注册 → presetLoop `newBlock('loop_forever')`（未注册类型）抛错被吞 → D-100 根循环不落座（F6 P2-1 原样未闭环）。**修复**：`public/js/editor/blocks.js` 重写 BLOCK_TYPES 为 16 键逐一对应 bridge（fields 元数据：var.name/set.name/action.name/function.name/call.name/num.value/get.path|name/arith|cmp|logic.op 等）；`public/js/mount/index.js` editor 屏装配点调用 `registerBlockTypes(B)`（幂等、null/无 Blocks 安全）。**回归锁**：archive.test.js blocks test 断言 `keys` deepEqual 16 型全集 + 注册数 16 + 幂等 0 + null 两臂 + init 四步；mount 既有 editor 装配测试（fakeBlockly 无 Blocks → {registered:0} 安全分支）继续绿。

### P1（应当修复——已修复并复测）

1. **P1-1 扁平仓库 import 归一缺失（normWh 兜底假象）**：清单 1 明问"扁平仓库 import 后由 normWh 兜底是否真的存在"——实测 **不存在**：normWh 仅 createStore 初始 loadPersist 合并路径；`reducer 'save/import'` case 直存 `p.warehouse`。测试提交版断言"normWh 在 createStore 层兜底"为**错误假设**（该路径不经过 loadPersist）。扁平 {role:[...]} import → state.warehouse 无 buckets → warehouse 屏读 buckets 断裂。**修复**：archive.js parseImport 内 `normWarehouse`（扁平→buckets 与 normWh 同语义；null→null 保留）；测试改为断言 buckets.role 命中 + skill/rolePlugin/skillPlugin 空桶。
2. **P1-2 loadout.skills 非三槽归一**：原 `skills: data.loadout.skills || [null,null,null]` 对数组原样透传（0/1/2/4 槽均可），违反 §4.1 三槽形状。**修复**：archive.js `normLoadout` 三槽补齐（空位 null）/超长截断（slice 3）；测试补缺槽/超长/缺失三例。

### P2 登记（不阻塞；tasks/progress 同步留档）

1. **trail 图元 y 硬编码 96**：planTrail 固定 `y:96`（与 planFrame PLAYER_Y=96 同值，视觉一致）；可改为导入 PLAYER_Y 常量消除魔法数（打磨）。
2. **canvas trail beginPath 守卫 false 臂不可达**：paintCanvas 地面线已无条件 beginPath，trail 分支守卫为防御性（不抛即可）；测试如实注释，不计为漏测。
3. **存档导入 UI 缺失**：设置屏仅"导入提示"占位（`导入：打磨期文件选择`）；真实文件选择控件（input[type=file]→FileReader→text payload）属打磨面——F7 范围仅要求提示位（progress F7 行已登记"导入文件选择器"打磨项）。
4. **F6 P2 遗留维持登记**：P2-2（/ai/battle 帧无 bullets 投影）等与 F7 无关项不重复处理。

## 修复落实（审查侧，2026-09-18）

1. **P0-1**：`public/js/editor/blocks.js` 重写（16 键 = bridge 块型全集，命名逐字一致；fields 元数据按 bridge blockToStmt/exprFromBlock 契约）+ `public/js/mount/index.js` 装配点 `registerBlockTypes(B)`（幂等/null 安全；关闭 F6 P2-1 根循环不落座）。
2. **P1-1**：`public/js/util/archive.js` 新增 `normWarehouse`（扁平→buckets；与 store/index.js normWh 同语义）并接入 parseImport。
3. **P1-2**：`public/js/util/archive.js` 新增 `normLoadout`（skills 三槽补齐/截断；role||null；ai 透传）并接入 parseImport。
4. **回归锁**：`tests/frontend/archive.test.js` 更新（words deepEqual 16 型、注册/幂等/null/init、扁平仓库 buckets 断言、skills 缺槽/截断/缺失三例、trail 单点不抛守卫修正）；mount editor 既有测试继续绿。
5. **文档登记**：`docs/tasks.md` §6 F7 行 + `docs/progress.md` F7 行补登记（16 型↔bridge、视觉 init 最小化、导入文件选择器打磨）。

## 实测

| 命令 | 期望 | 实际 | 结果 |
|---|---|---|---|
| `node --test --test-isolation=none tests/**/*.test.js` | 全绿 | **533 pass / 0 fail / 0 skip**（F6 527 + F7 archive.test.js 6 新用例；修复后复跑） | ✅ |
| `node scripts/gate.js` | 9 PASS/0 FAIL/0 PEND | **9 PASS / 0 FAIL / 0 PEND**（修复后复跑；阈值未放宽） | ✅ |
| `npm run cov` 等效（--experimental-test-coverage） | 聚合达标 | 聚合 **98.02 / 88.44 / 96.60**（行/分支/函数）；五目录逐文件达 90/85/90（gate 项 7 判定 PASS） | ✅ |
| `node scripts/check-arch.js` | 49 文件 0 违规 | **[PASS] 49 个文件无依赖违规**（含 3 个 F7 新文件；blockly 白名单生效） | ✅ |
| `node .audit/golden-battle.js` | 18 tick 一致 | 黄金战斗复算通过（18 tick, winner=p2, phase=role） | ✅ |
| 探针（inline node ESM） | 实证 | ①bridge 块型全集枚举 = 16 型；②BLOCK_TYPES↔bridge 差集矩阵（修复后 0 缺失 0 多余）；③planTrail 边界矩阵 8 例全符合；④settings verifyLayout 带 15 条记录 ok=true | ✅ |

## 结论

**CONDITIONAL PASS（P0×1 + P1×2 必须当批修复；已全部修复并复测绿）**。F7 三件套主体（存档 export/import 效果链、AI 轨迹可视化、设置屏新控件）契约/质量俱佳：§8 校验链与码、effects doc 守卫与落盘-toast 链、planTrail 限长/越界/缺字段全边界、settings 5 盒 verify 实测全绿、mount 并入顺序与 canvas trail 绘制（alpha 0.4 复位/PALETTE）均符合 frontend-spec。**核心缺陷在③自定义块注册**：提交版"20 型"与 bridge 16 块型词汇断裂（仅 2/16 重名）且应用内零调用点 → 该交付项实质未生效（F6 P2-1 未闭环的编辑器块定义继续悬空），判定 P0 当批修复——blocks.js 重写为 16 键逐一对应 + 装配点接线；另修 2 项 P1（扁平仓库 import 的 normWh 兜底实际不存在 → parseImport 内建归一；loadout skills 三槽）。修复后全链复测：**533/533、gate 9 PASS/0 FAIL/0 PEND、cov 聚合 98.02/88.44/96.60、check-arch 49 文件 0 违规、golden 18 tick 一致**。P2×4 登记（约束内允许的打磨面：trail y 常量、import 文件选择器 UI、F6 P2 遗留维持）。**无 NEEDS_DECISION 项**（"20 型"与 bridge 16 型的差异由契约（bridge.js 词汇，F6 冻结）+ 清单 4"数量与命名一一对应"明确裁定，非需父决策的歧义）。

复跑基线：`node --test --test-isolation=none tests/**/*.test.js && node scripts/gate.js && node scripts/check-arch.js && node .audit/golden-battle.js` 全绿即为回归通过。