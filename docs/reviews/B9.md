# B9 审查记录

> 批次：B9（P1：完整伤害链路 + 背击拍板）
> 审查方式：独立上下文 agent（数值独立复算 + 背击语义核对 + 回归验证 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS**（P2×3 已当批落实）

## 审查范围

扩展 `server/core/engine.js`（dealDamage 八步完整链路 §4.4 + isBackstab §4.5 + dodging 标记 + 步骤 9 重写 + damage.* 事件）、`server/core/bullets.js`（hit 事件补 dir/srcType）、`server/core/skills.js`（弹幕 spec 补 srcType）、`docs/systems/07-engine.md` §4.5 修订（拍板记录）、新增 `tests/unit/damage.test.js`（14 测试）。

## 用户拍板（2026-09-12）

**背击"追尾语义"**：§4.5 原字面（平射方向与受击方朝向**相反**=背击）与 examples/04-bullets P1（迎面相向却 9 无背击）冲突 → 拍板：平射与位移路径弹幕 = 方向与朝向**相同**（追尾）才背击；垂直永不；近战 = 攻方本体在受击方朝向反方向。示例数值（P1 9、M4/M5 12）全部保持。

## 核对结果

| 项目 | 结果 |
|---|---|
| 数值独立复算全过：DM-1 9、背击 14、T-BT-6 22（独立推导 12×40/49×2.25）、真实 15、吸血 4/封顶、Q1 12/10/7、防御单调 100/50/16 | ✅ PASS |
| 语义：闪避前置、dodging 步骤 1/6 时序（控制复写后置位不错加 bonus）、crit 流每 tick 共享消费序固定（D-91）、srcType 链路一致、碰撞完整机制、基地无暴击/背击/吸血 | ✅ PASS |
| 日志矩阵：damage.calc(debug 中间值全字段)/damage.dodge(debug)/damage.lifesteal(trace) 与 §4.6 B9 行一致 | ✅ PASS |
| 测试：DM-* 过 §3.3（定点复算+陷阱提示、失败路径、无镜像——DM-11 与 rng 解耦） | ✅ PASS |
| 铁律：L2 / L9 / L11 / L17；回归 273 全过（B8 EN 系列未被背击语义破坏） | ✅ PASS |
| 实测：npm test 273/273；cov 聚合 98.28/87.76/97.60；gate 8 PASS/0 FAIL/1 PEND | ✅ PASS |

## 已落实的 P2（当批）

1. **P2-a true_dmg 无日志**：附加真实伤害补 damage.calc（trueDamage 标记）。
2. **P2-b pull/true_dmg 无断言**：DM-13 补全（pull → control −1 格、真实直扣 5 + 日志断言）。
3. **P2-c dodging 初始化**：createBattle 显式 `dodging:false`（与 defending/fullDodgeDuring 一致）。

## 登记取向

- 附加效果（§4.4 步骤 9 后添加）：stun(control 0, r1)/knockback(±dir×v)/pull(−dir×v)/dot(continuous hp −v, remaining 3)/true_dmg（数值直扣）——B21 校准候选。
- 引擎 crit 流每 tick deriveStream(tick,'crit') 共享（命中→碰撞固定消费序）；dodge 判定同流。

## 遗留

- 项 8（日志冒烟）PEND 待 B11；质回放链（events cid 串联）随 B10 帧输出；测试名与 §3.2 编号错位登记（DM 专属编号，注释标明）。