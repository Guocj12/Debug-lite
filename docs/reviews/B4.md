# B4 审查记录

> 批次：B4（P1：core/unlock.js 解锁系统 + /api/v1/unlock 端点）
> 审查方式：独立上下文 agent（U-1..U-6 全分支逐条核对 + 数据链三处一致核验 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS（P2×3 已当批落实）**

## 审查范围

新增 `server/core/unlock.js`（tierIndex/isUnlocked/filterByTier/validateAi/validateLoadout/availableNodes + withLogger）、`tests/unit/unlock.test.js`（UL-1..10）；修改 `server/index.js`（GET /api/v1/unlock?tier= 路由，L14）、`tests/api/api.test.js`（AP-11）。

## 核对结果

| 项目 | 结果 |
|---|---|
| U-1..U-6 全分支逐条核对：availableNodes 19 全集、isUnlocked 继承/保守拒绝、filterByTier 五分支、validateAi 七分支（path/node 细节）、validateLoadout 定位、tierIndex 未知 | ✅ PASS |
| 数据链三处一致：unlock.json 增量 ∩ schema.js AI_NODES_BY_TIER ∩ unlock.js 节点集 = 累计 11/14/17/17/19 | ✅ PASS |
| 日志矩阵：unlock.check(debug 查询即记)/unlock.reject(warn 拒绝即记，含 ai_invalid/unknown_node/node_locked/tier_locked 全部拒绝路径) | ✅ PASS |
| 测试：UL-1..10 过 §3.3 黑名单（全集硬编码属数据契约核对非镜像；UL-7 与 items.validateUnlock 口径协同） | ✅ PASS |
| 铁律：L2 / L9 / L11 / L17；**L14 落实**（/api/v1/unlock 端点 + AP-11：common 11 节点/不良用法 400 bad_tier） | ✅ PASS |
| 实测：npm test 197/197；cov unlock.js 97.93/85.51/100；gate 8 PASS/0 FAIL/1 PEND | ✅ PASS |

## 已落实的 P2（当批）

1. **P2-a 拒绝日志缺口**：validateLoadout tier_locked 与 validateAi unknown_node/ai_invalid 补 `unlock.reject`(warn)（UL-9 断言）。
2. **P2-b validateLoadout(null) 抛错** → `ld = loadout || {}`（UL-9 锁定）。
3. **P2-c L14 缺口**：补齐 `GET /api/v1/unlock?tier=`（availableNodes + filterByTier 三表，未知段位 400 bad_tier）→ AP-11 测试；interfaces.md §2 批次列落定（B4）。

## 登记取向

- validateAi 只做未知节点 + 段位门控（完整结构校验 B12 ast.js 接管）；collectUsedNodes 路径为 `body.statements[i]` 形态（B12 与 08-ai 的 `body.s[i]` 记法统一）。
- 插件当前全部无 unlockTier（数据事实）→ 高段位插件 tier_locked 路径随 B20（T-PB-7）。
- validateLoadout 未知 templateId/pluginId 静默放行（引用完整性 T-PB-9 属 B18/B19）。

## 遗留

- 项 8（日志冒烟）PEND 待 B11；B20 引入带 unlockTier 插件后补 U-5d 真分支。