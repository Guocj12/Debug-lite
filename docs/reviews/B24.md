# B24 审查记录

> 批次：B24（P5 排位第一批：快照不可变深拷贝 + 匹配 10 场 + `POST /api/v1/ranked/run`：`server/ranked.js`（takeSnapshot/BOT_LD/battleOne/runRankedBattle）+ `/api/v1/ranked/run` 端点 + CLI `ranked run` + 测试 3 件）
> 审查方式：独立对抗性上下文（9 项核查清单逐条 + 缺口自查 4 项 + 攻击探针 5 支（`.review-b24/probe1..5.js`，可复跑）：统计闭合/确定性/排除自己/抽签流前缀不变性/消费量台账 / wait-only 全平局与 invalid 路径与修复方向预验证 / 错误码顺序（函数层+API 层）/ CLI 0/1/2 全路径含 --pool 非数组 / 输场分支与晋升边界）+ 实测六件套）
> 日期：2026-09-13　结论：FAIL → **PASS**（P1×1 已当批修复 + P2×13 关键项当批；复测 444 用例 / gate 9 PASS/0 FAIL/0 PEND / cov 聚合 97.48/87.31/95.70 / check-arch 20）

## 审查范围

交付物（git status 实证，6 文件）：`server/ranked.js`（新，122 行，check-arch LAYER_RULES 既有登记 `server/(index|ranked|runner|box|loadout|battle)\.js` → L6）；`server/index.js`（+`POST /api/v1/ranked/run` :316-332）；`cli/index.js`（+`ranked run` 子命令 :450-526，USAGE :25 更新）；`tests/unit/ranked.test.js`（新；**实有 5 用例，交付声明「6」有偏差** → P2-11）；`tests/api/api-ranked.test.js`（新，2 用例）；`tests/cli/cli-ranked.test.js`（新，1 用例）；审查探针 `.review-b24/probe1..5.js`（新，可复跑）。契约依据：systems/10-ranked.md（§4.1~4.3、§5 边界）；examples/10-ranked.md（RK-1..6 全分支）；decisions.md D-122（x=6）/D-123（不持久化）；interfaces.md §2（ranked/run 行 409 no_loadout）/§3（CLI `ranked run --seed 11`）/§6（L6 ranked 通道登记 B24~B25）；tasks.md §6 B24（T-RK-1/5）与 §3.2 T-RK 定义；B23 审查记录（P2-3 雕像局沿革复核——见 P2-12）。

## 核对结果

| # | 核查项 | 结果 | 依据 |
|---|---|---|---|
| 1 | **T-RK-1 匹配恒 10**：无池/池3/池15 三态；排除自己；抽签确定性 | ❌ FAIL（**P1-1** bot 补齐实质失效；排除自己测试假覆盖 → P2-1） | 三态数量恒 10（matches/results 恒 10，代码 `for i<10`，`acc` 抽空后 `BOT_LD` 补齐）✓；**但** BOT_LD 构建失败 → 无池 10 场全 `invalid`（probe2 实证 losses=10/wins=0）、池3 时 7 场 invalid——bot 补齐路径不产生任何有效对局（P1-1）。抽签确定性 ✓：同 seed 双跑结果序列逐场一致（probe1，13 seed 样例）；`createRng(seed).deriveStream(0,'ranked')` 每次调用重建 → 无跨调用状态（缺口①闭合，probe1 多次调用一致）。**排除自己**：实现正确（`JSON.stringify(ld) !== mineJson` 深等过滤，probe1 池等价性 + 反证敏感度双实证）**但 unit 用例 1 的 pool15 全部 waitLd（uid 改）与 mine(ld()) JSON 不等——排除分支从未被套件断言**（P2-1）。抽签确定性同 seed 跨调用实证 ✓。 |
| 2 | **T-RK-5 快照不可变**：takeSnapshot 深冻结 + 深度相等；battleOne 对冻结快照只读 | ✅ PASS（P2-10/13 附注） | `JSON.parse(JSON.stringify())` 深拷贝 + 递归 `Object.freeze`（对象/数组/嵌套全层级）；strict 模式写入抛 TypeError（unit 用例 2 + 探针实证）。buildPanel/buildPlayer **只读**（`Object.assign` 浅拷贝 + `instantiateSkill` 新实例，无任何对 ld 的写；实证：冻结快照经 battleOne 数十场零 TypeError，BOT_LD 双跑零泄漏）。改源不影响快照（RK-4b 实证）；AI AST 连 body 深拷贝（RK-4d ✓）。**RK-4e programHash 未实现** → P2-10。 |
| 3 | **平局不计胜**：wait-only 全平；invalid 计 loss 语义判定 | ✅ PASS（P2-4/11 附注） | wait-only 双方 10 场全平 → wins 0/draws 10/losses 0/promoted false（unit 用例 3 + probe2 复算 ✓）；统计闭合 wins+draws+losses==10 恒成立（probe1）✓。engine verdict 域交叉验证：battle.runBattle wait vs wait → winner='draw'（probe2）✓；battleOne `res.winner \|\| 'draw'` 兜底在位（ranked.js:66）✓。**invalid 语义判定**：`winner='invalid'` 非引擎域值、计 loss——P1-1 下该路径成为 bot 场次主路径（可达性实证）；P1 修复后退化为防御路径，但**池坏条目（悬挂引用/无仓库）仍可真实触发 → 玩家被静默计 loss**（P2-4）。**真输场分支 ranked.js:108（else losses）cov 未覆盖**（99.18 行覆盖差 1 行；probe5 以强对手池实证可达且正确）→ P2-11。 |
| 4 | **bot 补齐**：BOT_LD 程序化构造、common 技能选中、面板 k=1、无插件引用 | ❌ FAIL（**P1-1**） | 程序化构造 ✓（数据表 find + filter + map，无 tests/fixtures 依赖——与 B23 审计方向倒置形成对比）；**common 技能模板数据仅 2 个**（`skill_melee_whirl`/`skill_straight_precise`；unlockTier 分布 2 common/3 rare/2 epic/1 legendary/1 mythic 实证）→ `slice(0,3)` 只得 2 个 → `validateLoadout`「技能必须恰 3」拒绝 → **`buildPlayer(BOT_LD)` ok=false（物证）→ 全部 bot 场次 invalid**（P1-1）。面板 k=1 确定性 ✓（stats 直取 baseStats、无 affixes、multiplier 1 中性值、无插件 → wh 可空校验通过，probe2 包装文件去引用实证）。**L9 细节**：`pluginPoints: 3` 硬编码与 role_bal 模板字段重复（应读模板）→ P2-13。 |
| 5 | **逐场派生种子**：rng.int 顺延消费；抽签流前缀不变性；engine 种子域 | ✅ PASS（注：真值域按实现语义修正） | 每场 `matchSeed = rng.int(1, 0x7fffffff)` 从同一 ranked 流顺延消费（索引消费在前、场种子恒 10 次在后；bot 路径**零消费**——消费量台账 probe1 模拟推演：池3→3 索引+10 种子+7 bot；池15→10+10+0；池0→0+10+10 ✓）。**前缀不变性真值**：同一池抽 4 ≡ 抽 10 的前 4 次抽取（probe1 实证 `[0..3]` 一致——无回看、无 bot 扰动）；不同尺寸池的抽取**不**一致属预期（int 区间不同，非缺陷）。跨调用重建 rng → 同 seed 序列逐场一致（缺口①）。engine 种子域：`1..0x7fffffff` 与 battle.js（`crypto.randomInt(1, 0x7fffffff)`）同一闭区间；engine 侧 `normalizeSeed` 无上界检查但在调用方域内 ✓。 |
| 6 | **HTTP/CLI**：信封/错误码/seed 回带；校验顺序；CLI 0/1/2 全路径 | ✅ PASS（P2-5/7/8/9 附注） | 函数层顺序 `no_loadout → validateLoadout → seed` 实证（probe3：无 loadout+坏 seed → 409 no_loadout 优先 ✓；loadout 非法+坏 seed → 409 loadout_invalid ✓）。API 层：bad_json → bad_tier → runRankedBattle 嵌套（**无 loadout+坏 tier → 400 bad_tier 先于 409**——index 层语义，与 /battle、/loadout 同族先例一致，记录备查）。信封 ✓（200 ok/data/log；409/400 error{code,message,details}）；seed 回带 ✓（probe1/3）。CLI 0/1/2 **全路径**（probe4，16 支全绿）：0=正常/--pool 数组/包装文件/-tier・-seed 末尾缺值静默默认；1=服务端业务拒绝（400 bad_seed/400 bad_tier/409 loadout_invalid/连接失败）；2=缺 --loadout/未知子命令/文件缺失・坏 JSON/--pool 非数组・不存在/未知旗标。`--pool 文件非数组 → 2` ✓。**P2**：`--seed abc/0 → 1`（服务端 400——语义归业务拒绝而非参数错误，与既有 CLI 约定一致，可接受）；末尾缺值静默（P2-7）；吞旗标文案误导（P2-8）；API pool 非数组静默空池（P2-9）。 |
| 7 | **架构/铁律**：check-arch 20；L2 无 console；L9/L11/L17 | ✅ PASS（P2-13 附注） | check-arch `[PASS] 20 个文件无依赖违规`（19+ranked.js；ranked 已在分层表，无新增登记需求——`server/(index\|ranked\|runner\|box\|loadout\|battle)\.js` → L6）；依赖方向：ranked.js → rng(L0)/ai/runtime(L5)/loadout・battle・runner(L6)/shared/log(L-1) 全 ≤L6、数据层 -1 可读 ✓；cli 新增分支零新增 require（只走 HTTP）✓。L2（core 无 console）：L6 文件无此约束；ranked.js 无 console ✓。L11：无 Math.random/eval（gate 项1 11 文件扫描通过）；`Date.now()` 种子生成属 L6 合法——但与 battle.js 的 `crypto.randomInt` 不一致 → P2-6。L9：BOT_LD 数值全部出自数据表（baseStats/regen/baseCost/cooldown/bulletLevel）✓ 仅 pluginPoints 硬编码重复模板字段（P2-13）。L17：结构断言机器可推导（BOT_LD 由数据表构造可复算）✓。 |
| 8 | **覆盖与门禁** | ❌ FAIL（**P1-1**；P2-11） | `npm test` **441 pass/0 fail/0 skip**（433+8：5 unit+2 api+1 cli）✓ 但与交付声明「unit 6」不符（P2-11）；gate **9 PASS/0 FAIL/0 PEND** ✓；cov 聚合 **97.45/87.15/95.87** 与声明逐值一致 ✓；ranked.js **99.18/89.13/100.00**（uncovered line 108 = 真输场分支，P2-11）；check-arch 20 ✓。**门禁全绿但 P1 藏于其中**：BOT_LD 全 invalid 不违反任何现有断言（T-RK-1 只查数量、api 只查闭合/布尔）——独立对抗探针才暴露。 |
| 9 | **死代码/死分支** | ✅ PASS（P2-3/4/11 附注） | runRankedBattle 各分支可达性：no_loadout（probe3 ✓）/loadout_invalid（probe3 ✓）/bad_seed（probe3 边界矩阵 ✓）/池空（无池 ✓）/池满（15 ✓）/invalid 场（P1 下主路径、修复后防御；池坏条目可触发 P2-4）✓；battleOne invalid 路径（probe2 ✓）；CLI 全路径（probe4 16 支 ✓）。**死分支判定**：修复 P1 后 `winner='invalid'` 行与 `r.invalid` 分支仍保留为防御路径（可达性=池条目不合 buildPlayer 时），建议 invalid 场单独统计（P2-4），不构成死代码。 |

## 缺口自查（四项按任务要求逐条）

① **抽签流顺延消费的跨调用一致性**：**确定（成立）**——`runRankedBattle` 每次调用 `createRng(seed).deriveStream(0,'ranked')` 重建全新流（createRng 无状态残留、deriveStream 纯函数 hash 派生），同 seed 先跑 10 场再跑 10 场第 1 场种子不变；probe1「确定性跨调用」+「重建 rng 模拟一致性」双实证（同池同 seed 跨两次调用结果序列逐场一致）。此前语义疑虑（「首次调用耗尽流」）不存在：每次调用从基流重建。

② **BOT_LD 的 skills 满 3？**：**否——数据仅有 2 个 common 模板**（skill_melee_whirl/skill_straight_precise；unlockTier=rare/epic/legendary/mythic 各 3/2/1/1 实证）→ `filter(common).slice(0,3)` 只得 2 → `validateLoadout` 拒（恰 3）→ **buildPlayer(BOT_LD) 恒失败**。这是本批唯一 P1（详见 P1-1），修复方向已预验证（见下）。

③ **平局判定 winner='draw' 兜底**：**在位且正确**——battleOne:66 `res.winner || 'draw'` 兜底（engine 返回非域值/undefined 时归平局）；engine verdict 域交叉验证（battle.runBattle wait vs wait → draw，17 tick 超时局实证）；wait-only 10 场全平 unit 用例 3 + probe2 复算 ✓。P1 修复前该兜底被 invalid 短路（invalid 先于 winner 判定，语义不冲突）。

④ **defend 场景（promote 阈值与 B25 一致性预留）**：**一致**——`promoted = wins > X_PROMOTE`，`X_PROMOTE = 6` 常量已导出（ranked.js:15、116），与 D-122「x=6、胜 7 晋升」逐字一致；B25 的 `promote(player, wins)` 应复用该导出常量（登记：B25 实现时核对 `wins > x` 与 `X_PROMOTE` 同源，避免双写漂移）。probe5 晋升边界实证：强对手池 losses 10 → promoted false；9 弱+1 强 → promoted = (wins>6) 恒等式成立。

## 发现的问题

### P0

无。

### P1（必须当批修复）

**P1-1（BOT_LD 技能数不足 3 → 全部 bot 补齐场次 invalid 并计 loss，T-RK-1「bot 补齐」/RK-1b・1c 实质失效；CLI 无池 demo 恒 losses 10/promoted false）**

位置：`server/ranked.js:32-33`（`SKILLS = skillTemplates.filter((t) => !t.unlockTier || t.unlockTier === 'common').slice(0, 3)`）——数据表 `skill-templates.json` 中 `unlockTier==='common'` 的模板**只有 2 个**（`skill_melee_whirl`/`skill_straight_precise`；全分布 2 common/3 rare/2 epic/1 legendary/1 mythic，node 实证）。后果链：BOT_LD.skills.length=2 → battleOne 内 `battle.buildPlayer('p2', BOT_LD, wh, tier)` → `buildPanel` → `validateLoadout`「技能必须恰 3 个（实际 2）」→ `{ok:false}` → `{winner:'draw', invalid:true}` → `runRankedBattle` 计 loss。实测：无池 seed 11 → `wins 0 / draws 0 / losses 10 / promoted false`，results 10 行全 `winner='invalid'`（probe2 物证）；池 3 → 7 场 invalid。连带：api/unit 套件全绿但从未断言 bot 场次 valid（门禁盲区），CLI 默认 demo（无 --pool）恒输。**修复方向（已预验证）**：① 最小改动——BOT_LD 循环取模板保 3（重复 templateId：`validateLoadout` 不查唯一性、`instantiateSkill` 每槽独立实例，3-skill 变体 buildPlayer ok=true 实证通过）；② 或数据表补第 3 个 common 技能模板（D-112 口径 unlockTier='common'，需重跑 gate 项 4/5 schema/consistency 与相关快照）；③ 或按 tier 参数化（≤tier 取 3；**注意** common 段位仍仅 2 个模板可选，需与①组合）。推荐 ①（不动数据、零门禁风险）+ 交付声明更正（「3 个 common 技能模板」→「common 技能取满 3 槽」）。**配套测试**：无池场景断言 results 全部 winner ∈ {p1,p2,draw}（非 invalid）、闭合计入；池 3 场景断言 bot 场次非 invalid。

### P2 登记（不阻塞，建议随批或收口落实）

1. **P2-1（「排除自己」套件假覆盖）**：unit 用例 1 的 pool15 条目（waitLd + 改 uid）与 mine(ld()) JSON 均不等 → 排除分支从未被任何套件用例断言；实现本身正确（probe1 池等价性 + 反证敏感度双实证）。建议补「自己深拷贝入池 → 结果与等价池逐场一致」用例（probe1 chk 可直接移植）。
2. **P2-2（RK-1e「bot 与玩家同段位配置」未满足）**：BOT_LD 恒 common 品质，mythic 玩家打 common bot（公平性/晋升失真）；修 P1-1 时一并按 tier 参数化 bot 品质或登记 B25 处理。
3. **P2-3（池条目无段位过滤，RK-1e「只取同段位」）**：跨段位（unlockTier 超请求 tier）条目直接入匹配——建议对池条目逐成员 `items.validateUnlock` 过滤（低代价、与 validateLoadout 口径一致）或文档声明「池为客户端同段位责任」。
4. **P2-4（invalid 计 loss 静默惩罚）**：池条目含悬挂装配引用/无仓库时 buildPlayer 失败 → 该场计玩家 loss 且 results 行 `winner='invalid'`（非引擎域值）、无任何日志/详情；P1 修复后此路径仍可被 CLI 池文件真实触发。建议 invalid 场单独计数（不并入 loss）或校验池条目后整体 400。
5. **P2-5（ranked.* 日志通道零 emit）**：interfaces §6 登记 `ranked.snapshot`(debug)/`ranked.match`(info)/`ranked.promote`(info)｜B24~B25，但 B24 代码无任何 emit（battleOne 内 `createLogger({level:'silent'})` 全丢弃）——gate 项 6 不检查「登记必 emit」故未抓。建议 B25 补 emit 或将登记批次改为 B25。
6. **P2-6（seed 生成实现不一致）**：ranked.js `(Date.now() >>> 0) % 0x7fffffff + 1` vs battle.js `crypto.randomInt(1, 0x7fffffff)`（同为 L6 缺省种子生成；Date.now 可被时钟操纵、非加密安全）。建议统一 `crypto.randomInt`。
7. **P2-7（CLI --seed/--tier 末尾缺值静默默认）**：`ranked run --loadout f --seed`（缺值结尾）→ seed 不传 → 服务端自生成 → exit 0（探针实证）；与 `--loadout` 缺值归 2 不对称。建议缺值归 2。
8. **P2-8（CLI 吞旗标文案误导）**：`--seed --loadout f` / `--tier --loadout f` → seed/tier 吞掉后随旗标、f 变未知旗标 → 归 2（拒绝合理）但报「参数非法（--loadout 必填）」误导。建议区分缺值/未知参数消息。
9. **P2-9（API pool 非数组静默视为空池）**：`pool:{a:1}` → 200 全 bot 场（probe3 实证）；CLI 已拦归 2，API 层宽容。建议 400 `bad_pool` 或文档声明。
10. **P2-10（RK-4e programHash）**：快照未记录 programHash（examples RK-4e 期望「用于回放校验与去重」）——B25 若需回放校验/去重须补字段（可作为快照附加元数据而不破坏 loadout 深等断言）。
11. **P2-11（覆盖缺口 + 声明偏差）**：ranked.js:108（else losses，真输场）cov 未覆盖（探针 probe5 已实证可达、可复现——10 强对手池 → losses 10）；unit ranked.test.js 实有 5 用例 vs 交付声明「6」。建议补强对手池用例（直接移植 probe5 chk）。
12. **P2-12（fixture 雕像局残留——B23 P2-3 残余）**：`tests/fixtures/loadout-ok.json` AI 仍含非法行动名 `'skill1'`（engine `normalizeAction` 只认白名单与 `skill:<sid>`，实测 `skill1` → `action.invalid` 归一化 wait）——B23 P2-3 修复仅覆盖 `.audit` 路径；api/unit 战斗类用例（含本批入池 battle）仍跑在「无战斗」局上（对 B24 断言影响有限——只查结构/计数——但任何 wins>0 类断言无法成立）。建议 fixture AI 改 `skill:skill1`。
13. **P2-13（L9 细节：pluginPoints 硬编码重复模板）**：BOT_LD roleItem 手写 `pluginPoints: 3` 与 `role_bal.pluginPoints`（数据表）重复——应读模板字段（修 P1-1 时顺带）；`quality:'common'`/`multiplier:1` 为 bot 确定性设计值可接受。

## 实测

| 命令 | 期望 | 实际 | 结果 |
|---|---|---|---|
| `npm test` | 441/441 全绿 | 441 pass / 0 fail / 0 skip（433 + 新增 8：5 unit + 2 api + 1 cli） | ✅ |
| `npm run gate` | 9 PASS / 0 FAIL / 0 PEND | 9 PASS / 0 FAIL / 0 PEND（项7 全量测试+覆盖率、项3 check-arch 20、项8 黄金 18 tick） | ✅ |
| `npm run cov` | 聚合 ≥90/85/90，与声明一致 | 聚合 **97.45 / 87.15 / 95.87** 与交付声明逐值一致；ranked.js **99.18 / 89.13 / 100.00**（uncovered line 108 = 真输场分支 → P2-11） | ✅（P2-11 附注） |
| `node scripts/check-arch.js` | 20 文件 0 违规 | `[PASS] check-arch: 20 个文件无依赖违规`（ranked.js 已在分层表既有登记） | ✅ |
| `node -e` 统计闭合复算 | wins+draws+losses==10 | 真人池/无池/强对手池三种形态均闭合（probe1/5） | ✅ |
| BOT_LD 专项实证 | — | `buildPlayer(BOT_LD)` → ok=false（技能 2<3）；无池 10 场全 invalid → **P1-1 物证** | ❌（P1） |

## 独立实证（非测试代码路径，可复跑 `.review-b24/probe1..5.js`）

① **probe1（9/9）**：统计闭合 + 确定性（同 seed 双跑逐场一致、跨调用一致）+ 排除自己（池=[自身深拷贝+9 弱对手] ≡ 池=[9 弱对手]，反证注入异 uid 副本改变结果——探针敏感度闭环）+ 同一池抽 4 ≡ 抽 10 前 4 次抽取（前缀不变性真值）+ 消费量台账（bot 零消费、场种子恒 10、域 1..0x7fffffff）；② **probe2（5/5）**：wait-only 全平局（wins 0/draws 10/promoted false）+ engine 平局域交叉验证（wait vs wait → draw）+ **P1-1 复现物证**（BOT_LD 2 技能/buildPlayer false/losses 10/results 全 invalid）+ 修复方向预验证（重复模板 3 技能变体 buildPlayer ok）；③ **probe3（11/11）**：错误码顺序（no_loadout 优先于 bad_seed、validate 先于 seed、seed 边界 1/0x7fffffff 合法与 0/0x80000000/1.5/NaN 拒绝）+ API 层全码（409 no_loadout/loadout_invalid、400 bad_seed/bad_tier/bad_json、pool 非数组静默空池 P2-9、envelope 形状）；④ **probe4（16/16）**：CLI 0/1/2 全路径（含 --pool 非数组 → 2、--seed abc/0 → 1、末尾缺值静默 P2-7、吞旗标 → 2 文案误导 P2-8、连接失败 → 1、包装文件解包 + warehouse 可空）；⑤ **probe5（4/4）**：强对手池确定性（p2 胜 3 seed）→ 真输场分支 losses 10（ranked.js:108 可达性实证，cov 缺口闭环）+ promote 边界恒等式（promoted ⇔ wins>6）+ X_PROMOTE=6 冻结值。附：BOT_LD 双跑零泄漏、快照深冻结/改源隔离/AI AST 深拷贝专项实证（strict 模式）。

## 结论

**FAIL**（无 P0；**P1×1 必须当批修复**，P2×13 登记）。交付物主体成立：T-RK-5 快照不可变完整达成（深冻结全层级、深度相等、battleOne 只读使用安全、改源隔离实证）；匹配恒 10 数量语义满足；抽签确定性（同 seed 逐场一致/跨调用一致/前缀不变性/消费台账）全部实证闭合；HTTP/CLI 错误码与校验顺序符合契约（no_loadout → validate → seed 嵌套在函数层正确，409/400 分派与信封规范）；门禁六件套全绿且 cov 聚合与声明逐值一致。**但 P1-1 使「bot 补齐」路径实质失效**：数据表仅 2 个 common 技能模板，BOT_LD 构造出 2 技能 loadout，被 buildPlayer 拒绝 → 无池/池不足场景全部 bot 场次计 loss——排位核心功能（RK-1b/1c、CLI 无池 demo、晋升统计）在默认路径上失真，且现有测试与门禁全绿（盲区实证）。必须当批落实：

1. **P1-1**：BOT_LD 技能取满 3 槽（推荐重复 common 模板，已预验证 buildPlayer 通过；或按 D-112 补数据表第 3 个 common 模板——选择时重跑 gate 项 4/5）；同步更正交付声明文案并补「无池场景 results 全非 invalid」回归断言。
2. 随批建议落实：P2-1（排除自己真用例）、P2-11（强对手池 → 输场分支用例，顺带补齐 108 行覆盖）、P2-4（invalid 场单独计数或池校验）、P2-6（统一 crypto.randomInt）；其余登记。

修复后复跑基线：`npm test`（441/441）/ `npm run gate`（9 PASS/0 FAIL/0 PEND）/ `npm run cov`（聚合 97.45/87.15/95.87，ranked.js 行 108 覆盖后 100）/ `node scripts/check-arch.js`（20）/ `.review-b24/probe1..5.js`（全绿）收口。

## 修复落实（实现侧，2026-09-13；P1-1 + P2 关键项当批）

1. **P1-1**：`buildBotLoadout()` 循环取满 3 技能槽（`COMMON_SKILLS[i % len]`——技能实例允许同模板二号位；验证 buildPlayer ok）；`BOT_LD` 直接以冻结快照赋值（本体也不可变）；回归：无池/池3 results 全非 invalid、BOT_LD 3 技能 + buildPlayer ok + 冻结断言。
2. **P2-1**：池 15 用例真包含同款 mine 条目（JSON 深等）→ 排除实证（invalids 0）。
3. **P2-4**：invalid 场单独计数 `invalids`（不再并入 loss）；`bad_pool`（pool 非数组 → 400）端点与函数级；回归（坏池条目 invalids≥1 + 四项闭合）。
4. **P2-6**：seed 生成统一 `crypto.randomInt(1, 0x7fffffff)`（与 battle.js 一致，去 Date.now）。
5. **P2-5**：ranked.* 日志通道接线——`makeRanked(logger)` 工厂 + `ranked.snapshot`(debug)/`ranked.match`(info) 事件（§4.6 L6 行）；端点 `withLogger(logger)` 接线。
6. **P2-11**：输场分支回归（hp=1 脆弱我方 vs bot → losses>0，ranked.js 行 108 覆盖 100%）。
7. 其余 P2（RK-1e bot 同段位、RK-4e programHash、fixture 雕像局残留等）登记 B25/后续。

复测：`npm test` 444/444 ✅；`npm run gate` 9 PASS/0 FAIL/0 PEND ✅；`npm run cov` 聚合 97.48/87.31/95.70 ✅（ranked.js 100/94.74/92.86）；`node scripts/check-arch.js` 20 文件 0 违规 ✅。