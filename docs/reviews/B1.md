# B1 审查记录

> 批次：B1（P1 首批：core/rng.js + core/field.js）
> 审查方式：独立上下文 agent（mulberry32 独立 Python 实现复算 + cellRange 独立复算 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS**（无 P0/P1；P2×3 已当批落实）

## 审查范围

新增 `server/core/rng.js`、`server/core/field.js`、`tests/unit/rng.test.js`（R-1..10）、`tests/unit/field.test.js`（F-1..9）；修改 `scripts/gate.js`（checkNumericHardcode token 边界）、`tests/integration/gate-extra.test.js`（GX-5c）。

## 核对结果

| 项目 | 结果 |
|---|---|
| rng：mulberry32 独立复算逐位一致（seed 0/1/42/7/999 前 5 值，锚点与 gen.test.js G-1 同源）；deriveStream 纯派生不消耗父流；state/restore 往返序列一致；purpose 尾参符合 §2.2 | ✅ PASS |
| field：clampX 不量化（F-08）；cellOf 半开+贴边；cellRange 独立复算 F-20/21/23/25/26 全命中（含贴边截断）；touchesBase 的 D-61 语义（F-30 vs F-31 关键区分） | ✅ PASS |
| L9：core 源码无战斗数值字面量（grep 实测仅注释）；gate 项 6② token 边界修复正确（mulberry32 不误报、真字面量仍抓） | ✅ PASS |
| 日志矩阵：rng.create/draw/stream、field.clamp/base 与 §4.6 一致；tick 提升契约利用正确 | ✅ PASS |
| 测试：R-1..10/F-1..9 过 §3.3 黑名单（固定种子 + 统计容差 2% + 失败路径 + 无镜像） | ✅ PASS |
| 铁律：L2（改动面 6 文件）/ L11（grep 验证无 IO/console/Math.random）/ L17 | ✅ PASS |
| 实测：npm test 153/153；cov rng 100/87.5/100、field 100/100/100；gate 8 PASS/0 FAIL/1 PEND | ✅ PASS |

## 已落实的 P2（当批）

1. **P2-a 签名文档漂移**：`deriveStream` 三处文档（interfaces.md/tasks.md/07-engine.md）改为 `(tick, purpose)`（seed 于 createRng 绑定；D-91 概念式 hash(seed,tick,purpose) 保持）。
2. **P2-b 锚点缺失**：rng.test.js 补 R-1b 固定值锚点（mulberry32(42) 前 5 值，与独立复算一致——算法被改坏即红）。
3. **P2-c 自伤基地**：touchesBase 翻转朝向后会命中自家基地——已在 field.js 注释登记，B8 起由 engine 保证行动集语义。

## 过程中发现的门禁缺陷（本批修复）

- checkNumericHardcode 曾把标识符 `mulberry32/uint32/hash32` 里的 "32" 误判为战斗数值（配置恰有 actorHalfPx=32）→ 增加 `(?<![\w.])…(?![\w.])` token 边界；GX-5c 正反用例锁定。
- rng 方法签名首参/尾参之争：§2.2 的 `(...,purpose)` 判定为**尾参**，实现与测试对齐。

## 遗留

- 项 8（日志冒烟）仍 PEND，B11 激活。
- P3 备忘：`0x40` 十六进制写法可逃逸数值硬编码检查（启发式可接受）；touchesBase owner 推导为 facing 语义，engine 层复核。