# B27 存储层（P7-1）—— 审查记录

> **本文件是 `docs/tasks.md` §6 批次 B27 的审查记录**（`scripts/check-docs.js` D6：每个已勾选批次必须有 `docs/reviews/<批次>.md`）。

- **批次**：B27（P7-1 存储层；`docs/plan-p7-playable.md` §P7-1）
- **记录日期**：2026-09-19（**汇编/登记日期**；各交付物的落地日期与阶段报告见「证据来源」）
- **审查人类型**：**独立审查**——阶段级「代码级审查」由未参与该阶段实现者执行，结论见 `docs/reviews/P7-7-test-audit.md` 与 `docs/reviews/P7-7-wave2-code-review-residual.md`；**本次「中央文档同步」只做要点汇编与状态回填，不新增独立审查结论**（避免把文档同步冒充为代码审查）。
- **证据来源**
  - `server/store/` 15 文件：`adapter-json.js`、`adapter-sqlite.js`、`archive.js`、`canonical.js`、`config.js`、`errors.js`、`fsatomic.js`、`index.js`、`index-file.js`、`journal.js`、`ledger.js`、`lock.js`、`recovery.js`、`session-table.js`、`snapshot-store.js`（`server/store/index.js:1-113` 的适配器契约注释为唯一接口清单）。
  - 存储层用例实测：`node --test --test-isolation=none tests/unit/store-*.test.js tests/contract/store-contract.test.js` → **128 用例 / 128 通过 / 0 失败**（2026-09-19 本轮实测）。
  - `docs/progress.md` §3.4/末尾自动同步条目（2026-09-19「存储层后续项 A+B 完成」）。

## 交付物摘要

- JSON 适配器 + 原子写（`tmp → fsync → rename`，Windows `EPERM/EACCES/EBUSY` 退避重试）、append-only journal（分段 + group commit + 末行半写截断 + 按月 checkpoint）、物化档案、内容寻址快照库、内存索引、单进程锁、崩溃恢复五步、版本迁移钩子。
- `server/data/service-config.json` 与 `server/data/rating-config.json` 两张参数表：**表为数值单一来源**、代码端 `server/store/config.js` 提供同值默认兜底、`server/data/schema.js` 做**schema 冻结值 + 跨字段不变量**校验、**缺表必 FAIL**。
- journal 新增记录类型 `player.removed`（墓碑删除；**全量重放不复活**已删玩家），日志事件 `store.player.removed`(info，通道 `store`)。
- `scripts/check-arch.js` 已登记 `/^server\/store\//` 层归属；`server/data/README.md` 已补两张参数表（见该 README「数据表清单」）。

## 独立审查结论摘要

- 崩溃恢复/幂等/半写截断/单进程锁/版本拒绝等 8 项 `T-ST-*` 与契约 `T-CN-1`（json 侧）有实测用例；恢复流程"journal 已写、档案未写"补放路径有覆盖。
- 残留审查（`P7-7-wave2-code-review-residual.md`）未发现"只有单测可用、经 HTTP/CLI 失效"的存储层缺口；`store` 通道事件名与 `scripts/gate.js` 通道注册表一致。

## 未决项（**不阻塞 B27 勾选，但必须显式登记**）

1. `DL_STORE=sqlite` **未实现**：`server/store/adapter-sqlite.js` 的 `open()/close()` 与全部方法**抛** `store_adapter_unavailable`（`adapter-sqlite.js:53-57`）——有意不静默退回 json。对应 `docs/systems/11-account-store.md` §11.4 的迁移判据。
2. `scripts/bench-store.js`（`npm run bench:store`）**未实现**：`T-ST-8` 容量哨兵因此无脚本承载（见 `docs/reviews/B33.md` 未决项）。
3. journal 月度 checkpoint 的**自动触发策略**（`journal.compactAfterDays`）无定时器，只有显式调用路径；`service-config.pool.ttlDays` 参数已留、未启用。
