# B6 审查记录

> 批次：B6（P1：core/skills.js 技能系统）
> 审查方式：独立上下文 agent（S-1..S-9 数值手算复算 + 路径弹幕 M 系列交叉核对 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS（修复 1 个 P1 后）**

## 审查范围

新增 `server/core/skills.js`（instantiateSkill/applySkillPlugins/canCast/buildSkillAction/coveredCellRanges + withLogger）、`tests/unit/skills.test.js`（SK-1..12 共 14 测试）。

## 核对结果

| 项目 | 结果 |
|---|---|
| S-1..S-9 数值独立复算全过（S-1a 0.99、S-2b/e 1.035/mp9+sp12、S-3 ceil 8/10、S-6 三枚、S-7 v512、S-8 672/736/800 + 贴边、S-9 路径 5 枚含终点与 M 系列一致） | ✅ PASS |
| 语义：路径弹幕与 07-movement M4/M5/M8 路径定义表一致（逐格 0 速、含端点、与截停无关）；canCast 纯函数 + ≥ 语义；falloff/distCells 进 payload；affixes 特殊类 8 个 id 与插件数据逐一对上 | ✅ PASS |
| 日志矩阵：skill.instantiate/plugin.apply/area/cast/reject 五事件与 §4.6 一致 | ✅ PASS |
| 测试：SK-* 过 §3.3 黑名单（定点复算、失败路径、矩阵分支、仅注入 rng/logger） | ✅ PASS |
| 铁律：L2 / L9 / L11 / L17（UTF-8 无 BOM 校验） | ✅ PASS |
| 实测：npm test 224/224；cov skills.js 100/93.59/100；gate 8 PASS/0 FAIL/1 PEND | ✅ PASS |

## 已修复的问题（当批）

1. **P1（L9）`costBaseOf[p.quality] || 2`**：兜底字面量 2 复制表值 → `?? costBaseOf.common`（读表兜底，SK-10 行为不变）。
2. **P2-1 skill.area 缺失**：coveredCellRanges 内补 `skill.area`(trace)（含 cells/impactX），SK-7 断言。
3. **P2-2 payload 共享引用**：straight/displacement 弹幕改逐枚 `{...payload}` 拷贝（防 B9 打标记串染）。
4. **P2-3 位移路径弹幕无 distCells**：登记——falloff 距离基准由 B9 命中结算定义（当前位移技能 falloff 恒 0）。

## 登记取向

- sid = templateId；instantiateSkill 复用 items.generateSkillItem 参数随机（同构去重，B3/B5 双实现教训）。
- 消耗补偿 = costDeltaBase[quality]×tier（D-113）；减耗类 = ceil(cost×(1−v))（S-3）。
- rangeUp：straight/vertical.range 与 displacement.distance（melee 不可增强，B21 校准）。
- canCast 成功返回扣资源/写 CD 的克隆（纯函数）；buildSkillAction 产出弹幕规格 + move 意图（落位/碰撞 B8、伤害 B9 消费）。

## 遗留

- 项 8（日志冒烟）PEND 待 B11；B9 需定义位移路径弹幕 falloff 基准（P2-3）；`.audit` 复算脚本随 B11。