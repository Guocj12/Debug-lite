# B13 审查记录

> 批次：B13（P2：合法性检测 + 段位门控 + unlock.validateAi 退役整合）
> 审查方式：独立上下文 agent（A-6 全案逐条 + 表达式位逃逸实证 + 退役完整性核对 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS**（P2×4 已当批落实）

## 审查范围

`server/ai/ast.js` 扩展（checkLegality：分支 action 规则/break 位置/call hoisting + validate 三段合一 + ai.validate.reject 事件）；`server/core/unlock.js`（validateAi/collectUsedNodes/tierOfNode/ALL_NODES 退役）；`tests/unit/unlock.test.js` 迁移；`tests/fixtures/ai-programs.json`（coverageProgram break 移入循环）；新增 `tests/unit/ai-validate.test.js`（6 测试）；`docs/interfaces.md`（unlock 行/ai 行同步）。

## 核对结果

| 项目 | 结果 |
|---|---|
| A-6 全案 a..h 逐条相符（合法 4/拒绝 4；A-6b 双报 loop body + 具体分支；A-6e 隐式空 else；A-6g 仅 break；A-6h 函数体内循环） | ✅ PASS |
| 规则边界：顶层 if 无 else 合法；break 跨函数拒（function 重置 loopDepth）；call hoisting 合法 | ✅ PASS |
| 门控：与 unlock 原语一致（rare→loop/epic→random/mythic→function）；unknown tier 保守；node_locked 带 path+node | ✅ PASS |
| 退役完整性：unlock.js 零残留；server/index.js /api/v1/unlock 只用保留原语；check-arch（ai→core L5→L1 合法）；T-UL-1..4 原语测试保留 | ✅ PASS |
| 测试：AV-* 过 §3.3 黑名单 | ✅ PASS |
| 铁律：L2 / L9 / L11 / L17 | ✅ PASS |
| 实测：npm test 302/302；cov 聚合 97.60/87.81/96.57；gate 9 PASS/0 FAIL/0 PEND | ✅ PASS |

## 已落实的 P2（当批）

1. **P2-1 表达式位逃逸**：if/loop case 补 exprChildren scan——if.cond/loop.cond/times 位的 call/break 不再逃逸（T-AI-12b 锁定）。
2. **P2-2 文档同步**：interfaces.md §1（unlock 行删 validateAi 并注退役；ai/ast 行补 validate）；tasks.md §2.2 与 systems/09-unlock.md §4.4/§6 同步（L1 冻结）。
3. **P2-3 死代码**：validate() 内未使用的 pathOf 构建删除。
4. **P2-4 fixture 归因**：a1Countdown 实际含 else——注释修正 + 顶层 if 无 else 显式用例补入 T-AI-12b。

## 登记取向

- 分支行动规则仅约束 loop 体内 if（顶层 if 无 else 合法，A-1）；call 视为行动产出点（静态保守，B14 运行时兜底）。
- break 同函数作用域内 loop；跨函数 break 拒绝（runtime 语义未定义，保守）。
- analyzeDepth=16 静态分析防爆（cl 豁免）；纯 random 循环体由 loop 级检查保守拦截；reject warn 覆盖合法性/门控四类 code（结构拒绝静默于 validate 汇总）。

## 遗留

- B14（ai/runtime.js 状态机：续执行/作用域/循环/函数/随机流/只读快照）——P2 核心。