# P0-4 审查记录

> 批次：P0-4（日志子系统：shared/log.js + tests/helpers/log.js + T-LG-1/2/3/6/7）
> 审查方式：独立上下文 agent（对抗式核对；环计数独立模型复算 + 静态检索 + vm 双环境验证）
> 日期：2026-09-12　结论：**PASS**（无 P0/P1；P2 共 6 项，4 项当批修复，1 项写入契约，1 项记录）

## 审查范围

新增 `shared/log.js`（UMD 零依赖）、`tests/helpers/log.js`、8 个测试文件（51 用例，本批 39）；修改 `shared/README.md`（log 契约冻结）。

## 核对结果

| 项目 | 结果 |
|---|---|
| ICD（tasks §2.2）与 README 契约逐项一致；新导出为纯增量（L8 允许） | ✅ PASS |
| §4 规范：三底线（可观测/零副作用/不改结果）、8 级级别、21 通道逐字节一致、记录结构 10 字段、on() 门控、UMD 双入口、无 IO 静态检索（唯一命中为注释） | ✅ PASS |
| 环形缓冲精确计数（dropped/suppressed 边界 100/200、环内 seq 区间）经**独立模型复算** 6 场景全部吻合 | ✅ PASS |
| 测试质量：过 §3.3 黑名单；边界/失败路径/无随机/无镜像 | ✅ PASS |
| 先红后绿：红态确定性（HEAD 无实现文件 → MODULE_NOT_FOUND）| ✅ PASS |
| 铁律：L2（改动面 9 文件）/ L4（T-LG-* 标注，编号重复已修）/ L8 / L17 字节抽查 | ✅ PASS |

## 已修复的 P2（当批）

1. **env-config 测试编号重复**（两个 T-LG-1h）→ 顺延为 1i/1j/1k。
2. **死条件 `channel !== 'log'`**（'log' 已在注册表）→ 删除。
3. **vm 默认级别缺口**：未覆盖行 66（process.env 缺失分支）从未被执行 → umd.test.js 补 `createLogger().getLevel() === 'debug'`。
4. **§3.2 归属登记**：补 H-1..H-4、T-LG-2g/2h 的登记行。

## 契约化/记录的决定

5. **sink 异常策略**：契约明示「sink 抛出的异常原样穿透 log() 调用方，server/cli 层自行捕获」（shared/README.md 已写入；本模块不吞异常）。
6. **suppressed 节拍取向**：§4.7 原文「每 100 tick」→ 实现为「每累计 100 次丢弃」。审查评估无实质问题（丢弃节拍信息量更优、稳态重合、无丢弃即无需汇报）；README 已登记偏差并注明 B11 可调。**此为 P0-4 实现取向，如用户要求按字面 tick 节拍可再调整。**

## 固化事实

- 51/51 绿；cov：shared/log.js 98.68/95.16/97.62（未覆盖 13-14 浏览器分支、66 env 缺失分支——均有 vm/行为验证），helpers 100%
- 实现过程自发现并修复 2 bug：createLogger 无参默认级别（RangeError→env/默认）、normalizeLevel 死代码分支