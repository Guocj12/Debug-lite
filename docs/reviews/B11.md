# B11 审查记录

> 批次：B11（P1 收尾：黄金战斗 + 走查复算 + 门禁项 8 激活）
> 审查方式：独立上下文 agent（黄金帧逐帧复核 + 项 8 实现核对 + verify-rest 独立性 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS（修复 1 个 P1 后）**

## 审查范围

新增 `.audit/golden-battle.js` + `golden-battle.json`（18 tick 快照，p2/role）+ `.audit/verify-rest.js`（22 项机器复算）；`tests/unit/golden.test.js`（3 测试）；`scripts/gate.js` 项 8 checkLogSmoke 激活；`server/core/engine.js`（stepLog 补 data.tick）；`server/core/bullets.js`（uid seq 实例级修复）。

## 核对结果

| 项目 | 结果 |
|---|---|
| 黄金战斗逐帧复核全自洽（t4 @442 t=0.3、t6 −9、t7 −20、t9 碰撞 256、t15 bash+碰撞、t17 D-33 后推、t18 背击碰撞 −19/−11、sp 轨迹）；18 帧全帧锚定 | ✅ PASS |
| 项 8：事件齐备集 = §4.6 L4 行子集；cid 流序校验；trace/silent 一致验证（含 bulletHits uid，修复前必红——真实有效）；GX-13 正反用例；lazy require 与项 7 会话无冲突 | ✅ PASS |
| verify-rest：E-1/E-2a/E-5b/c/M2/M5/N2/N3/伤害 12·7·12/基地 3/背击×暴击 22/超时 7 全对；纯公式独立于引擎 | ✅ PASS |
| bullets uid 实例级彻底（engine 每 battle withLogger 新实例；模块默认实例无消费方）；T-EN-1 成真回归守卫 | ✅ PASS |
| 测试：golden.test.js 过 §3.3（逐值 deepEqual、边界、无 skip） | ✅ PASS |
| 铁律：L2 / L9 / L11 / L17 | ✅ PASS |
| 实测：npm test 285/285；**gate 9 PASS/0 FAIL/0 PEND（九项门禁全部实时）**；cov 97.72/87.86/96.39 | ✅ PASS |

## 已修复的问题（当批）

1. **P1 T-EN 编号错位**：T-EN-9/13 在 tasks §3.2 不存在 → 修正归属：确定性 = **T-EN-1/T-BT-5**、快照锚定 = **T-BT-13/14**（golden.test.js/.audit 注释同步）。
2. **P2 快照缺命中锚 → summary 补 hits**（uid/target/atX 每帧）+ `--write` 重锚。
3. **P2 seed 惰性**（全 0 概率无随机消费）→ p1 critChance 0.5 走真实 crit 流后重锚（确定性由种子保证）。
4. **P2 verify-rest 计数**：24→22 实际（补 N3 相向同格 + E-5c clampX）+ 动态计数。

## 过程中发现并修复（登记）

- **bullets uid seq 模块级共享**：跨 battle uid 顺延（b_5 vs b_28）致 trace/silent 逐帧不一致 → 移入 makeBullets 实例级。
- **rng.chance 参数序**（B9 遗留未登记）：真实 rng 路径 `chance('dodge', p)` RangeError → 修正为 `chance(p, purpose)`（B1 冻结尾参签名）；dodging bonus>0 时才暴露（B9 测试 stub 屏蔽）。

## 登记取向

- 项 8 cid 校验为事件流序（首 cast 后依次首现）；事件 data 带 tick 的统一重构登记后续（T-LG-4 语义）。
- 黄金战斗 = 固定面板 + 固定技能实例 + 40 tick AI 序列 + seed 20260912 + critChance 0.5；快照改动须独立复核后 --write。

---
# P1 阶段收口确认（11/11 批）

| 批次 | 关键出口 |
|---|---|
| B1 rng/field → B10 结束判定 | 逐批审查记录（docs/reviews/B1..B10.md） |
| **B11 本批** | 黄金战斗锚定 + 项 8 激活 → **gate 9 PASS/0 FAIL/0 PEND** |

**P1 出口**（tasks.md §6，全部达成）：
- 确定性内核：rng 每用途派生流（D-91）、field px 坐标、effects 控制/持续、items 数值层、unlock 段位门控、roles 面板、skills 四类型释放、bullets 当 tick 全解算（连续方程/等级矩阵/递归）、engine 14 步管线（统一落位 M/N/O/P 全矩阵）、完整伤害链路（背击追尾语义拍板）、结束判定/回放一致
- 293→285 用例体系（11 批净增 190+）；**门禁九项全实时**
- `.audit/` 复算脚本补齐（golden-battle + verify-rest，examples 引用落地）

**用户拍板记录**：背击追尾语义（2026-09-12，straight-backstab 决策）。

**后续**：P2 AI 阶段（B12 ast → B16 指令集），从 `docs/tasks.md` §6 继续。