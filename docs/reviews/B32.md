# B32 快速对战与积分（P7-3）—— 审查记录

> **本文件是 `docs/tasks.md` §6 批次 B32 的审查记录**（`scripts/check-docs.js` D6）。

- **批次**：B32（`server/quickmatch.js` + `rating-config.json` + 非对称 Elo + 排行榜联动）
- **记录日期**：2026-09-19（**汇编/登记日期**）
- **审查人类型**：**独立审查**（阶段级「代码级审查」，执行者未参与该阶段实现；结论见 `docs/reviews/P7-7-wave2-code-review-residual.md`）；本次「中央文档同步」只做要点汇编与状态回填，**不新增独立审查结论**。
- **证据来源**
  - `server/quickmatch.js`（`findMatch`、`matchCandidates`、`splitByCooldown`、`settle`、`expectedScore`、`ratingDelta`、`maxSingleMatchDelta`；导出见 `quickmatch.js:375-392`）。
  - `server/data/rating-config.json`（`base:0`、`cap:3000`、`scale:400`、`kBase:32`、`kMin:8`、`kMax:64`、`drawFactor:0.5`、窗口 100→600）。
  - HTTP：`server/index.js:861-869`（`POST /quick/run`）。
  - `docs/systems/11-account-store.md` §8.3（公式与性质）。

## 交付物摘要

- 非对称 Elo：0 起、cap 3000、`E = 1/(1+10^((Ropp−Rself)/scale))`、`K_gain/K_loss` 裁剪（`clamp`）、平局项 `drawFactor`；参数**全部**来自 `rating-config.json`（代码零字面量）。
- 匹配窗口递进 **100 → 600**（`matchWindowStart/Step/Max`）；无候选 → `409 no_opponent`（**不注入 bot**）。
- 去重：与 B31 同一裁定语义（strict ≥72h / relaxed 24–72h / <24h 硬拒）。
- 结算返回 `zeroSum`（实测值 = `Δself + Δopp === 0` 的布尔）与 **`nonZeroSumByDesign: true`**（D-133 有意非零和"汇"）；日志 `ranked` 通道 `quick.match`(info) / `quick.settle`(info) 已生效（`PREFIX_MAP.ranked` 已含 `'quick'`）。
- 双向结算写 journal（`battle.recorded`, `mode:'quick'`）+ 双方档案；`battleId` 内容寻址幂等，重复结算响应 Δ 取**档案落盘值之差**（此时为 0）。
- `POST /quick/run` 的响应 `seed` = **对局种子**（由匹配种子派生或注入 `battleSeed`）；**入参 `seed` 只影响匹配抽选**。

## 独立审查结论摘要

- `T-QM-1~4`（窗口递进/公式表驱动/双向结算/boundary 0 与 cap）有实测；`maxSingleMatchDelta` 提供"单场最大变动"上界供测试断言。
- 残留审查确认 `settle` 为纯函数、可被测试独立复算（无 store 依赖），公式无隐藏状态。

## 未决项

1. `service-config.pool.dailyBattleLimit` / `rating-config.dailyBattleLimit` **参数已留、默认 0（不限制）**——反刷仅靠对手去重。
2. 高分段"分数汇"导致的**匹配面收缩**（积分高 → 赢加得少、输扣得多）属设计取舍（D-133），无额外补偿机制。
