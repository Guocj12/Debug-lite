# F6 审查记录

> 批次：F6（P6 前端第六批：AI 编辑器——bridge JSON↔AST + blockly 静态路由 + editorLayout + main 装配 + ai/edit 实时校验）
> 审查方式：独立对抗性上下文（8 项核查清单逐条 + 缺口自查 5 项 + 探针 `.review-f6/probe-f6.js`（真后端 listen(0)：① vendor 穿越矩阵 10 变体（原始套接字防客户端归一化）+ 类型 + GET-only；② **bridge 往返 ↔ 后端 ast.validate 双向等价**——真实 `tests/fixtures/ai-programs.json` 全 6 程序 toBlocks→toAst 后与后端 validate 判等（深等/ok+errors/迁移后 programHash 三方一致）+ 二次幂等 + 编辑器侧构造验证；③ findBlockByPath × 真实后端 details.path 病态程序逐一命中 + HTTP /ai/validate 实拍）+ 实测六件套）
> 日期：2026-09-17　结论：**CONDITIONAL PASS→需修（P1×6 必须当批修复；已全部修复并复测绿）**：①**根因级**——bridge 为自创"编辑器方言"（11 型 num/get/loop_forever/`{type:'seq'}`），与后端契约（interfaces §1 / ast.js 白名单 16 节点 / D-100..103）形状、类型、字段名全不符 → `ai/edit`(实时校验)/`ai/compile`/`ai/run` 三链恒被后端拒（`not_program`）→ **编辑器全线不可用**；②`ai/edit` 不带 tier（后端缺省 common → 高段位节点误报 node_locked）；③`ai/compile` 读 `data.hash` 而契约是 `data.programHash` → 编译后 hash 永显「未编译」；④`ai/run` 帧为扁平形状（无 diff/bullets/bases）→ 试运行回放屏空白；⑤应用内零接线——`createEditor/presetLoop/高亮` 全仓无调用点、index.html 无 Blockly、workspace 盒 id 非 blocklyDiv、错误行点击无高亮；⑥toolbox 门控键错位（19 availableNodes vs 11 方言键：num/loop_forever 永被剔除、8 个后端节点无积木）。修复后复测基线 **527/527、gate 9 PASS/0 FAIL/0 PEND、cov 聚合 97.97/88.36/96.54、check-arch 46 文件 0 违规、golden 18 tick 一致**；探针 3 段全 PASS（复跑 2 次稳定）。

## 审查范围

交付物（git status 实证）：`public/js/editor/bridge.js`（新）、`public/js/editor/main.js`（新）、`public/js/views/editor.js`（新）、`public/js/store/effects.js`（改：ai/edit 实时校验）、`server/index.js`（改：/vendor/blockly/* 静态路由）、`scripts/check-arch.js`（改：public 裸导入白名单 'blockly'）、`package.json`+`package-lock.json`（改：blockly 依赖）、`public/js/views/index.js`（改：7 屏注册齐）、`tests/api/api-static.test.js`（改：/vendor 可达）、`tests/frontend/views.test.js`（改：editor 注册断言）、`tests/frontend/bridge.test.js`（新）。契约依据：frontend-spec §6.2（toolbox 0,64,120,592 / workspace 120,64,1024,432 / panel 1144,64,136,592 / errors 120,496,1024,160；Blockly.inject 选项 grid:24/zoom;change→debounce 300→ai/edit→/ai/validate→errors 带 path；编译 hash 前 8；试运行 opponent:kiter；errors 行点击→highlight（找不到→toast「程序已变化」）；Blockly 区 DOM 由 Blockly 管理——mount 不触碰）+ §1.1（/vendor/blockly/* 静态路由）+ interfaces §1/§2（/api/v1/ai/validate|compile|battle 信封与程序形状）+ decisions D-100（编辑器显式可见不可删隐式主循环）/D-101..103（AST 结构）+ server/ai/ast.js 白名单（16 节点）与 childList 路径命名（seq→s[i]、if/random→then/else、loop/function→body、表达式→.expr——**逐字形核对实证**）+ unlock.json（availableNodes 19 键）。

## 核对结果

| # | 核查项 | 结果 | 依据 |
|---|---|---|---|
| 1 | **bridge 往返：全类型矩阵 vs ast 白名单（漏类型检测）；findBlockByPath vs 后端 details.path；畸形安全** | ❌ FAIL→P1-1（已修） | **t-a 矩阵逐类型核对**：后端 NODE_TYPES 16 节点 `literal/get/bullets/var/set/getVar/arith/cmp/logic/random/if/loop/break/function/call/action/seq`；原桥只覆盖 11 方言型且**类型名/字段名全异**（num↔literal、get{.name}↔getVar/get{.path}、loop_forever↔loop{kind}、var{init}↔var{value}、if.then[]↔then{node}、random{min,max}↔random{prob,then,else}）——**漏 7 类型（literal/getVar/bullets/loop(count)/break/function/call）+ 程序形状漏 program/version/body**。修复后桥为后端契约直译（18 块型 16 节点，seq 隐式），`_seq` 标记保真单语句 seq（pDeepRec else=seq[1]），get 按 path/name 字段判别 get/getVar 双语义，while(true) cond 省略输入/合成节点注册路径映射。**真实 fixture 全 6 程序 toBlocks→toAst 深等（version 归一）+ 后端 validate ok/errors 判等 + 迁移后 programHash 一致（探针②）**；findBlockByPath 与后端 getNodeAtPath **同语义逐字形核对**（'body.s[i].then/.else/.body/.expr' 全段命中；'.expr' 按 value/left/right/cond/prob/times 取首对象；**旧样例 'body.s[2].then[0]'/'body.s[1].cond.a' 非当前 ast.js childList 产出形态——实证后按未知段安全 miss**；空分支容器（缺 else）回退高亮容器块）。畸形安全：null/缺字段/未知块/防御 seq 包装全臂不抛。 |
| 2 | **toolbox 门控：19 availableNodes 键 vs 11 方言积木（差集）+ rare 无 random 与 B6 一致** | ❌ FAIL→P1-6（已修） | **19 vs 11 差异核对**：unlock 累计 mythic 19 键 `seq,literal,get,bullets,var,set,getVar,arith,cmp,action,if,loop,while,break,random,logic,arith_ext,function,call`；原 buildToolbox 按方言键过滤 → **num/loop_forever 不在 19 键 → 任一段位永不可建（核心循环/数字缺失）**，且 getVar/bullets/break/function/call/literal/loop_count 无积木（**8 类型门控漏项**）。修复：TOOLBOX 16 条按**后端节点键**门控（seq 隐式；arith 兼 arith_ext；get 兼 getVar/get 双语义）→ common 9 / rare 12 / epic 14 / mythic 16（逐段位测试断言）；rare 无 random（epic 解锁）与 B6 一致 ✓（测试+buildToolbox(['random'])[0].label='随机分支'）。根循环（D-100 隐式主循环）由 presetLoop 预置、不入 toolbox 门控（恒有）。 |
| 3 | **main/createEditor：inject 选项与 §6.2；debounce；dispose；无 Blockly/容器/document 回退；presetLoop 预置判定；试运行 payload kiter** | ❌ FAIL→P1-5（已修） | inject 选项 grid{spacing:24}/zoom{controls:true} 与 §6.2 逐值一致（测试断言）；debounce 300ms 重置/单向触发（clearTimeout+覆盖）；dispose 清定时器+透传 widget.dispose；no-blockly/no-container/inject-failed（catch）/document 回退/无 dispatch noop 全臂安全；**presetLoop 原为纯桩（B&&widget truthy 恒 true）→ 真预置**：newBlock('loop_forever')+setMovable(false)+setDeletable(false)+initSvg+render+moveBy(24,0)（D-100「显式可见不可删」），无 newBlock/建块抛错 → false。**试运行 payload {opponent:'kiter'}**（按钮 payload + effect 缺省兜底双位）。**★应用内零接线（P1-5）**：原 createEditor 全仓无调用点、index.html 无 Blockly、workspace 盒 id='editor_workspace'（≠ blocklyDiv）→ 浏览器编辑器为静态壳——现 mount 装配点（editor 屏 #blocklyDiv body 级骨架显隐/定位 + 一次装配 + 离屏 dispose + 高亮消费/toast）+ index.html 脚本/容器骨架 + 盒 id 修正。 |
| 4 | **静态路由：/vendor/blockly/* 前缀限定（穿越测试）；content-type；check-arch 白名单收口；package.json 锁版本；许可** | ✅ PASS（P2-3/4 登记） | **穿越矩阵探针①（原始套接字 10 变体）**：`/vendor/blockly/../package.json`、`..%2f..%2fpackage.json|server%2findex.js`、`..%5c..%5c…`、`%2e%2e/%2e%2e/…`、双编码 `%252f` → 全部 400 bad_static 信封（无 200 泄漏、无仓库文件内容）；`/vendor/%2e%2e/…`、`/vendor/other/…`、`/node_modules/blockly/…` → 404（**node_modules 仅经 /vendor/blockly 前缀暴露**）；blockly 主文件 200+content-type text/javascript；GET-only（POST→404）。check-arch public 裸导入白名单收口为仅 'blockly'（ELSE 仍拒绝）。package.json+lock **锁 13.3.0**（去 ^）。首次外部依赖许可链：D-124 无框架不阻止 vendor 静态库（桥与主装配均不 import blockly——浏览器全局注入，L7 无 server 依赖）；**blockly LICENSE 实证 Apache-2.0**（node_modules/blockly/LICENSE 头部）。 |
| 5 | **ai/edit 实时校验链：change→toAst→ai/edit→/ai/validate→errors(带 path)→editorLayout 渲染 + findBlockByPath 高亮闭环** | ❌ FAIL→P1-1/2/4/5（已修） | 原链三段断：①toAst 产方言 → 恒 `not_program`（**P1-1**）；②不带 tier → 高段位节点 node_locked 误报（**P1-2**）；③**前端高亮调用点缺失**——错误行无任何点击标注、reducer/mount/主控均无消费路径（**P1-5**）。修复后闭环实证：change → debounce 300 → toAst（后端形状）→ effects ai/edit（带 tier）→ /ai/validate → errors（真实 details.path）→ editorLayout 行（action:'editor/highlight'+payload.path）→ reducer aiDraft.highlightPath → mount 消费 → highlightByPath → widget.highlightBlock(id)；未找到 → toast「程序已变化」；Blockly 未装配 → 清空瞬时态防陈旧。探针③ + mount 测试 + store 测试三重回归锁。 |
| 6 | **架构/铁律：check-arch 46；public 无 console/无其它裸导入；项 7 逐文件；首次外部依赖许可链** | ✅ PASS（P2-3 登记） | check-arch **46 文件 0 违规**（项3 复实测）；public 全域 console 零命中（新文件零 console）；无其它裸导入（仅 'blockly' 白名单，当前无文件 import——注入式）；项 7 逐文件：bridge.js **99.68/92.96/100**、main.js **100/98.00/100**、views/editor.js **100/100/100**、effects.js **100/85.57/93.55**、mount/index.js **100/95.71/100**、reducer.js **99.31/87.34/100** —— 全达阈值（行90/分支85/函数90）；mount→editor/main→bridge 层内（L7）依赖无环（check-arch DFS）；blockly 不进层图（node_modules 白名单注释登记）。 |
| 7 | **覆盖与门禁** | ✅ PASS | `npm test` **527/527**（前批 523 + 审查新增 4：store F6 ai 链、store highlight reducer、mount editor 装配、bridge 回归锁增量；0 fail/0 skip）；`npm run gate` **9 PASS/0 FAIL/0 PEND**；`npm run cov` 聚合 **97.97 / 88.36 / 96.54**（声明 97.89/88.15/96.33 之上）；check-arch 46 ✓；golden 18 tick ✓（快照未改写）。 |
| 8 | **死代码/死分支：bridge 默认臂矩阵；main 三态；editorLayout 分支锤；mount 高亮清空臂** | ✅ PASS | bridge：stmtToBlock/exprToBlock default 臂、表达式语句双向往返、`_seq` 单/多块链、合成 while(true) cond 路径注册、loop 无 kind→while、arith 缺 op→undefined（交后端 bad_field 不造默认）、get/getVar 字段判别、防御 seq 包装（畸形多链不丢语句）；main：no-blockly/no-container/inject-failed/转换异常 catch/dispose 无 widget.dispose 臂/presetLoop 真建+抛错+null 返回臂/highlight 无 API 臂；editorLayout：aiDraft 缺层/裸错误/无 path 行不挂 action/错误计数/hash 未编译态；mount：无 Blockly 高亮清空臂/骨架显隐两态/装配仅一次。 |

## 缺口自查（五项按任务逐条）

① **19 节点 vs 11 积木差集——判定 P1-6 并已修**：19 键 vs 方言 11 的差值 8 键（getVar/bullets/literal/loop/while/count/break/function/call/arith_ext 形态）+ 方言 2 键不在 19（num/loop_forever）——**门控键错位使核心循环/数字永不可建**。修复：TOOLBOX 16 条按后端键门控（16 节点全覆盖、seq 隐式、arith_ext 并入 arith op 扩展、get 块兼 getVar/get 双语义——登记）。
② **presetLoop 未做「不可删外层循环」注入——判定 P1-5 并已修**：真建根 loop_forever（movable/deletable false；Blockly 语义逐项断言）；无 newBlock/fake → false（与 no-blockly 同语义）；成败由调用方（mount）记录。
③ **错误行点击高亮调用缺失——判定 P1-5 并已修**：行 action editor/highlight + payload.path → reducer highlightPath → mount 消费（highlightByPath → widget.highlightBlock）→ miss toast「程序已变化」；Blockly 未装配清空臂。链路测试：bridge 路径矩阵 + store reducer 瞬时态 + mount 消费/toast + 探针③真路径命中。
④ **ast.validate 对 bridge 产物的接受性（往返后验证等价）——探针②实证 PASS**：6 fixture 全部 `toBlocks→toAst` 后端 validate **结果等价**（ok+errors 深等）、**programHash 与迁移后原程序逐字节一致**、二次往返幂等、编辑器侧构造程序 validate ok —— 双向等价关键实证（修复前恒 not_program）。
⑤ **ai/run（/ai/battle）vs battle/run（/battle）双路径并存——判定 P2-2 登记**：§6.2 试运行位即 /ai/battle（单玩家 AI + 内置对手 kiter —— 快速试 AI）；正式对战走 battle 屏 /battle（完整双配置+warehouse）。语义差异合理并存；**注意 /ai/battle 帧无 bullets（runner 未投影）→ 前端包装 bullets:[] → 试运行回放无弹幕几何（碰撞/命中火花仍可视）**——登记 P2-2。

## 发现的问题

### P0

无。

### P1（必须当批修复——已全部修复并复测）

1. **P1-1（根因级）bridge 方言 vs 后端契约断裂**：桥输出 `{type:'seq',statements}` + 11 方言型（num/get/loop_forever…），与后端（interfaces §1/ast.js 16 节点/D-100..103）不符 → `ai/edit`（/ai/validate）/`ai/compile`/`ai/run`（/ai/battle）**三链恒被后端拒**（`not_program`）——实时校验、编译、试运行全线不可用；且 6 fixture 程序无法过桥（literal/getVar/bullets/loop/break/function/call 漏类型）。**修复**：bridge 重写为后端契约直译（toBlocks 根=显式主循环 loop_forever 包装 D-100；toAst 解包产 program/version/body；18 块型 16 节点全映射；`_seq` 保真单语句 seq；get 字段判别 getVar/get 双语义；while(true) cond 合成+路径注册；表达式语句双向往返），**并修复 f1-extra/store 测试的旧契约假设**（data.hash→programHash；空程序跳过臂）。**回归锁**：bridge.test.js 重写（fixtures 全 6 深等往返 + 二次幂等 + 表达式语句 + 畸形矩阵）+ store F6 ai 链测试 + 探针②（validate/programHash 双向等价）。
2. **P1-2 ai/edit 不带 tier**：/ai/validate 缺省 tier='common' → 高段位节点（loop/random/logic/function…）在 mythic 玩家下误报 `node_locked`（F1 P1-4「带 tier 防门控放宽」同型，F6 漏带）。**修复**：`{ ai, tier: st.tier }`（ai/edit 与 ai/validate 共用 validateEditorProgram）。
3. **P1-3 ai/compile 读错 hash 字段**：后端契约 `data.programHash`（interfaces §2/runner.compileAi），原读 `data.hash` 恒 undefined → 编译后 hash 永显「未编译」。**修复**：`hash: r.data.programHash`；空程序不设守卫（手动编译 null → 400 bad_ai → errors 上屏，用户可见反馈）。
4. **P1-4 ai/run 帧形状与回放屏不兼容**：/ai/battle 帧为扁平 `{tick,players,collision,bulletHits,verdict,aiTrace}`（无 diff 包装/bullets/bases）→ 试运行 goto replay 后 `frame.diff` undefined → 画布/HUD 空白。**修复**：effect 将帧转换为 B22 diff 形状（players/bullets[]/bases{}/events[]/aiTrace[]/collision/bulletHits/verdict）再 battle/loaded（与 F4 battle/run 产物同构）；失败仍 toast。
5. **P1-5 应用内零接线（createEditor 无调用点 / 无 Blockly / 无 blocklyDiv / presetLoop 桩 / 高亮调用点缺失）**：全仓唯一引用方是测试——浏览器编辑器为静态壳（校验/编译/试运行/高亮全部不可达）。**修复**：①`index.html` 增 body 级 `<div id="blocklyDiv">` 骨架（同 #battle 画布模式，mount innerHTML 重建不触碰 Blockly 内部，§6.2「mount 不触碰 workspace 内部」）+ `/vendor/blockly/blockly_compressed.js` + `msg/en.js` 脚本（vendor 路由 §1.1）；②editorLayout workspace 盒 id `editor_workspace`→`blocklyDiv`；③mount paint：editor 屏骨架显隐/定位（坐标来自 blocklyDiv 盒），globalThis.Blockly 存在则装配一次（createEditor+presetLoop），消费 aiDraft.highlightPath（highlightByPath → toast miss），离屏 dispose+隐藏；渲染剔除 blocklyDiv 重复盒（防双重 id + innerHTML 摧毁工作区）；④main.js presetLoop 真预置（movable/deletable false）+ 新增 highlightByPath；⑤错误行 action editor/highlight + reducer 瞬时态（highlight/done）。**回归锁**：mount editor 装配测试（装配一次/change→ai/edit/高亮命中+miss toast/dispose/再进重配）+ bridge 高亮链测试 + 探针③。
6. **P1-6 toolbox 门控键错位（19 vs 11）**：num/loop_forever 不在 availableNodes 19 键 → 真实 mythic 只放行 9/11（核心循环与数字永不可建）；8 个后端节点（getVar/bullets/literal/loop(count+while)/break/function/call）无积木映射。**修复**：buildToolbox 16 条按**后端节点键**门控（mythic 16/legendary 16/epic 14/rare 12/common 9——与 unlock 语义逐段位断言）；根循环恒有（D-100）。

### P2 登记（不阻塞；建议随批落实项以 ★ 标注）

1. **自定义积木定义缺失（Blockly.Blocks 注册 + toolbox 分类 + 序列化/高亮样式）**：浏览器可装配 Blockly 工作区、监听/校验/编译/试运行/高亮链全部就绪，但无自定义块定义 → 工作区空（presetLoop newBlock 未注册类型时抛错→false→根循环不落座）。属 F7（编辑体验打磨）——登记论证：本批已把装配点、契约、测试、探针做实，块定义是纯展示层增量。★
2. **试运行回放弹幕几何缺失（/ai/battle 帧无 bullets）**：runner.js 帧映射未投影 bullets → 前端包装 bullets:[] → 试运行回放无弹幕线（碰撞/命中火花仍可视）。后续可让 runner 补投影（后端改动）或接受现状。
3. **while/arith_ext 幻影解锁键**：unlock.json 有键（loop/while/arith_ext）而 ast NODE_TYPES 无 while/arith_ext 类型（loop 的 kind / arith 的 op 扩展）——门控以 loop/arith 措辞；建议 unlock 表后续清理或文档注明（★）。
4. **get 块单块双语义（getVar|get）**：按 path/name 字段判别——往返保真、门控挂 getVar；正式积木定义（F7）可拆两型。
5. **旧方言路径段兼容性**：'body.s[2].then[0]'/'body.s[1].cond.a' 等历史样例非当前后端产出（probe ②③ 实证 ast.js childList 实装）→ findBlockByPath 按未知段安全 miss（不误命、不改形状）。
6. **合成 while(true) cond 的高亮指向**：'…loop.expr' 命中循环块本身（块上无 num 输入）——探针/测试锁定行为边界。
7. **seq 语句位（AST 合法、编辑器词汇不产块）**：载入编辑会丢该语句（极罕见结构）——桥安全跳过不抛。
8. **'.value'/'.times' 等非标准路径段 miss**：与后端 getNodeAtPath 同口径一致（两端都 null）——一致行为登记。
9. **试运行双路径语义（缺口⑤）**：/ai/battle（单玩家+内置对手）vs /battle（完整双配置）按屏分工为设计语义；差异（loadout 面板/双 AI/回放注册）登记。

## 实测

| 命令 | 期望 | 实际 | 结果 |
|---|---|---|---|
| `npm test` | 523/523 全绿 | **527 pass / 0 fail**（前批 523 + 审查新增 4：store F6 ai 链、store highlight reducer、mount editor 装配、bridge 回归锁增量；0 skip） | ✅ |
| `npm run gate` | 9 PASS/0 FAIL/0 PEND | **9 PASS / 0 FAIL / 0 PEND**（修复后复跑；阈值未放宽） | ✅ |
| `npm run cov` | 聚合与声明一致 | 聚合 **97.97 / 88.36 / 96.54**；逐文件 bridge 99.68/92.96/100、main 100/98.00/100、views/editor 100/100/100、effects 100/85.57/93.55、mount/index 100/95.71/100、reducer 99.31/87.34/100 | ✅ |
| `node scripts/check-arch.js` | 46 文件 0 违规 | `[PASS] 46 个文件无依赖违规` | ✅ |
| `node .audit/golden-battle.js` | 18 tick 一致 | 黄金战斗复算通过（18 tick, winner=p2, phase=role）；快照未改写 | ✅ |
| 探针 `.review-f6/probe-f6.js` | 3 段全 PASS | **3 段全 PASS（复跑 2 次稳定）**：①vendor 穿越矩阵 10 变体（原始套接字）+ 类型 + GET-only 无泄漏；②6 fixture 桥往返↔后端 validate 双向等价（深等+ok/errors+programHash 三方一致）+ 幂等 + 编辑器侧构造 valid；③病态程序 details.path 逐一命中（branch_without_action/break_outside_loop/unknown_call/node_locked/unknown_node/缺 else 回退）+ HTTP /ai/validate 实拍 | ✅（P1 定案 + 修复验证） |

## 独立实证（非测试代码路径，可复跑 `.review-f6/probe-f6.js`）

① **P1-1 定案（t-a 矩阵逐类型）**：backend NODE_TYPES 16 节点 vs 原桥 11 方言型——差异 7 类型 + 程序形状（program/version/body）三处全符；实证原链 `/ai/validate` 对 `{type:'seq'}` 恒 400 `not_program`。修复后探针②：**6 fixture 全部 `toBlocks→toAst` 与后端 validate 判等（ok/errors 深等）且 `programHash` 与迁移后原程序逐字节一致**——双向等价闭环。② **P1-2 定案**：/ai/validate 缺省 tier='common'（server/index.js），mythic-only 节点（random/logic/function/call）在 epics 以下玩家必然 node_locked；修复后 effect 请求体含 `tier`（store 测试断言）。③ **P1-5 定案（代码路径逐行）**：createEditor/presetLoop/highlight 全仓 grep 唯一引用 = 测试；index.html 无 blockly 脚本/容器；editorLayout 原 id 'editor_workspace' ≠ createEditor document 回退的 'blocklyDiv' → 零装配可达。修复后 mount 测试断言装配一次/显隐/定位/dispose/再进重配 + index.html 骨架实体。④ **P1-6 定案**：availableNodes('mythic') 19 键（unlock.json 累计）不含 'num'/'loop_forever' → 原门控换真列表后仅 9 条；修复后 16 条（测试断言）+ rare 无 random（B6 一致）。⑤ **路径段逐字形核对**：当前 ast.js childList 实装 seq→`s[i]`、if/random→`then/else`、loop/function→`body`、表达式→`.expr`（多表达式字段共享段）；前端 findBlockByPath 同口径（batches 契约的 'a/b/init' 段由统一 AST 映射覆盖）；旧样例 `.then[0]`/`.cond.a` 非产出形态——按未知段安全 miss（不改形状不误命）。⑥ **穿越矩阵原始套接字**：undici fetch 会在客户端归一化 `..` 段（首次探针因此误判）——改 raw http.request 以服务器视角验证：10 变体全 400/404、无仓库文件泄漏；`/node_modules/blockly/*` 直取 404（node_modules 仅经 /vendor/blockly 前缀暴露）。

## 结论

**CONDITIONAL PASS（P1×6 必须当批修复后转 PASS；本批已全部修复并复测）**。F6 核心交付（编辑器）原处于**契约级断裂**：bridge 自创方言与后端 AST 契约（16 节点/形状/字段）不符 → 实时校验/编译/试运行三链恒被拒；且装配零接线、门控键错位、hash 字段错读、帧形状不兼容等连环断裂。本批修复：①bridge 重写为后端契约直译（含 7 漏类型补齐、D-100 根循环解包、_seq 保真、getVar/get 双语义、表达式语句、合成 cond 路径注册）——**探针②实证 6 fixture 双向等价（validate 判等 + programHash 一致）**；②ai/edit 带 tier；③compile programHash；④/ai/battle 帧转换；⑤应用内装配闭环（index.html 骨架+脚本、mount 装配点、blocklyDiv 盒、presetLoop 真预置、错误行高亮链 + miss toast）；⑥toolbox 改为后端键门控（19 键 16 条全节点覆盖）。测试 527/527（+4 回归锁）、gate 9 PASS/0 FAIL/0 PEND（阈值未放宽）、cov 聚合 97.97/88.36/96.54、check-arch 46 文件 0 违规、golden 18 tick 一致、探针 3 段全 PASS。P2×9 登记；**遗留依赖（P2-1）**：自定义积木定义（Blockly.Blocks 注册/分类/序列化）属 F7——工作区/校验/编译/试运行/高亮链已就绪，块定义落位后编辑器即完整可用。复跑基线：`npm test && npm run gate`（串行）＋ `.review-f6/probe-f6.js` 全绿即为回归通过。

## 修复落实（审查侧，2026-09-17；P1×6 当批）

1. **P1-1**：`public/js/editor/bridge.js` 重写（后端契约：toBlocks 根=显式主循环；toAst 解包产 program/version/body；18 块型 16 节点；_seq 标记；get 字段判别 getVar/get；while(true) cond 合成+map 注册；表达式语句往返；findBlockByPath 与 getNodeAtPath 同语义 + 空分支容器回退 + 语义容器限定）。
2. **P1-2**：`public/js/store/effects.js` 提取 `validateEditorProgram(ctx)`（ai/edit 与新增 ai/validate 共用）——`{ai, tier: st.tier}` + 空程序跳过；注释登记沿革。
3. **P1-3**：`effects.js` ai/compile 读 `data.programHash`（interfaces §2 契约），空程序不守卫（手动编译失败有反馈）。
4. **P1-4**：`effects.js` ai/run 将 /ai/battle 扁平帧包装为 B22 diff 形状再 battle/loaded（players/bullets/bases/events/aiTrace/collision/bulletHits/verdict）。
5. **P1-5**：`public/index.html` 增 `#blocklyDiv` body 级骨架 + `/vendor/blockly/blockly_compressed.js`、`msg/en.js`；`views/editor.js` workspace 盒 id→blocklyDiv + 错误行 action/payload（editor/highlight）+ 补「校验」按钮（screens.md btn_validate）；`store/reducer.js` editor/highlight + done 瞬时态；`mount/index.js` 装配点（骨架显隐/定位、Blockly 一次装配、presetLoop、高亮消费+toast+未装配清空、离屏 dispose、渲染剔除重复盒）；`editor/main.js` presetLoop 真预置（movable/deletable false）+ 新增 highlightByPath。
6. **P1-6**：`views/editor.js` buildToolbox 16 条按后端节点键门控（mythic 16/rare 12/common 9/epic 14）。
7. **回归锁**：`tests/frontend/bridge.test.js` 重写（7 用例：fixtures 深等往返/真实路径命中/畸形矩阵/toolbox 门控/editorLayout/编辑流+presetLoop+高亮/兜底）；`tests/frontend/store.test.js` 增 F6 ai 链与 highlight reducer 用例；`tests/frontend/mount.test.js` 增 editor 屏装配用例；`tests/frontend/f1-extra.test.js` 契约假设修正（programHash）；探针 `.review-f6/probe-f6.js`（vendor 穿越矩阵 + 双向等价 + 病态路径命中/HTTP 实拍）。