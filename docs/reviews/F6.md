# F6 审查记录：快速对战屏 + 战斗查看器 + AI 逻辑查看器

> 批次：**`F6`**（P6 第四批；命名见 `docs/frontend/00-rules.md` FR-6，**不并入 §6 的「共 41 批」**）
> 日期：2026-09-25（实现提交 `6dfaaed`；本记录由**独立上下文子代理**在实现提交之后出具）
> 审查者：**独立上下文子代理**（对抗式、只读；**仓库零改动**——除本文件外未修改、未新增任何仓库文件；全部探针落在 `%TEMP%\probe-f6\`）
> 被审对象（唯一实现依据）：`docs/frontend/04-quickmatch.md`（全文 322 行，含 §0 取证 / §1 端点映射 / §2 采用模式与被否决替代 / §3 屏与交互 / §4 动作白名单 / §5 字段契约 / §6 失败路径 / §7 状态 / §8 边界 / §9 后端变更 / §10 机器核对 / §11 走查剧本 / §12 自署禁止 / §13 已知未覆盖 / §14 对账）
> 上级权威：`docs/frontend/00-rules.md`（§1 绘制边界 / §1.4 绘制零逻辑 / §1.5 投影单一真源与禁止前端复制战斗公式 / §3.3 引用即承诺 / §3.4 文档不得漂移 / §4.1 按钮永不无声 / §4.2 字段名不得来自散文 / §4.3 端点可达 / §4.4 可玩性出口 / §2.10 四项判定）；`docs/interfaces.md` §2（端点）/§4.3（帧契约）；`docs/decisions.md` §14.4–14.7（**D-164** 守方 AI 完整镜像 / **D-167** 帧契约 + 双方 aiTrace + 匹配内联帧 / **D-168** 软冷却 / **D-169** 胜负绝对口径）；`docs/systems/08-ai.md` §3（AI 节点与路径语法）
> 实现：`public/{store,format,actions,api,contract}.js`（F6 提交**未改** `public/app.js` / `public/render.js`：`git show --name-only 6dfaaed` 只含 store/format/actions/api/contract + 4 个登记测试 + quick-battle-flow 测试 + 6 份文档）
> 测试：`tests/frontend/quick-battle-flow.test.js`（QB-1…QB-10）+ 登记同步 `tests/frontend/{auth-ui-contract,admin-ui-contract,auth-field-contract,hub-warehouse-flow}.test.js`
> **本文件存在的理由**：`docs/progress.md:97` 已按 §3.3「引用即承诺」写明 F6 的审查与走查记录落 `docs/reviews/F6.md`。**`docs/reviews/` 无索引文件**（目录内无 `README.md`/`index.md`；批次登记散落在 `docs/tasks.md`/`docs/progress.md`/`docs/acceptance.md`，均不在本审查者的可写范围内）→ **无需追加索引行**；本次仅新建本文件。

---

## 1. 定稿判定（总纲 §2.10 四项）

> 口径：分册 §12 明写「本批不自署四项结论」；本节由独立上下文审查出具。**四项须全过方能定稿**；本节给出逐项 PASS/FAIL 与决定该结论的证据。

### ① 与行业主流做法的关系已说明（点名模式 + 被否决替代 + 本项目采/不采理由）—— **PASS**

| 判据 | 证据 |
|---|---|
| 点名的主流模式 | `04-quickmatch.md:59`：「主流是『用 canvas/WebGL 回放战斗 + 时间轴拖动』」 |
| 被否决的替代 | 分册 §2 反驳 1（自动播放/定时器逐帧推进）、反驳 6（帧列表按钮墙）；两条都给了**反例/后果**与**备选+代价**（`:52`、`:57`），符合总纲 §2.4「无具体反例不得提出」 |
| 本项目**不采**的理由 | `:59` ①零依赖/无框架/无图形（§1.2）；②本批合格线 = 前后端交互与按钮完整性，画面呈现不是合格线 |
| 本项目**采**的部分 | `:59`「采的部分：帧数据的组织方式（服务端权威、客户端只投影）与主流一致」 |
| 额外的自攻击清单 | §2 共 6 条被反驳提案，逐条含「反例/后果 + 本批处置」（`:52`–`:57`），其中反驳 4（§1.5 禁止前端复制战斗公式）、反驳 5（对手源码不外泄 SEC-33）直接锚到权威条款 |

结论：**PASS**。既点名了主流模式，也说明了采/不采的分界与理由；主流只作对照不作合格线（与总纲 §2.10① 一致）。

### ② 既有约束下合理（无权威链冲突、无门禁冲突）—— **PASS**（附 1 条登记风险）

| 判据 | 证据 |
|---|---|
| 零后端改动（不触 `/api/v1` 契约） | 提交 `6dfaaed` 的文件清单无 `server/**`；`04 §9` 明写「本批零后端改动」；F6 只消费 D-167 已落地的内联帧/`replay/:id`/`me/configs` |
| 与 D-169（胜负绝对口径）一致 | `format.js:1154-1160` 把 `p1/p2/draw/invalid` 翻成「你赢了/你输了/平局/无效对局」，`:1163-1169` 帧内保留绝对口径 `p1（我方）/p2（对手）`——与 `decisions.md:298`「前端把 `p1/p2` 翻成"你赢了/你输了"」逐条对应 |
| 与 D-164（守方镜像只作用于 AI 视角、帧保留真实坐标）一致 | `format.js:1611-1612` 的屏提示明写「帧里 p1=我方(进攻方)、p2=对手(防守方)；玩家 AI 一律按 p1 坐标系书写，守方位由服务端镜像（D-164）」；投影只按 side 呈现，不做任何坐标换算（`:1216-1225`） |
| 与 §1.5「投影单一真源 / 禁止前端复制战斗公式」一致 | F6 段（`format.js:1110-1618`）只读 `winner/verdict/damages[]/bullets[].outcome`，无任何命中/伤害/胜负推演（见 §3 的"未被证伪的攻击"第 1 条） |
| 与 §1.4「绘制零逻辑」一致 | `format.js` 全部为纯函数（无 DOM/请求/时钟）；`app.js`/`render.js` 未改；F6 未引入定时器或第二个副作用源（分册 §2 反驳 1 的处置） |
| 门禁/批次命名 | 未新增批次号（`F6` 不匹配 `scripts/check-docs.js` 的 `P\d+-\d+`/`B\d+`）；`04 §4` 的「不新增 `data-*` 寻址键」经提交核对为**真**（`git show HEAD:public/app.js` 的 `payloadOf` 在 F6 提交时仍是 9 键，F6 未改 `app.js`） |
| ⚠️ 唯一权威性缺口（**今日不可达，登记**） | D-169 明写「防守方会看到自己在 p2」（`decisions.md:294`），但共享切片 `state.viewer` 无"我是哪一侧"的表达，`format.js:1268-1269` 把 `我方/对手` 与 `p1/p2` 硬绑定。F6 的全部可达 UI 路径（`quick/run` 内联帧、按自己 `battleId` 读回放）里请求者恒为 p1（D-169③ 不变量成立），**故当前无冲突**；`/me/defense` 按 `04 §1` 属 F7 之后 → 记为风险 **F6-9**（见 §5） |

结论：**PASS**（无权威链冲突、无门禁冲突、零后端改动、未新增批次号）；例外项以风险登记而非冲突计入（F6-9）。

### ③ 功能完整（全部用户动作 + 全部失败路径）+「已知未覆盖/已知风险」清单不为空 —— **FAIL**

- **「已知未覆盖/已知风险」清单非空 → 该附加子项 PASS**：分册 §13 有 R-1…R-7 七条（`:303-311`）。
- **「全部用户动作」FAIL（不是缺动作，而是 1 条动作未按规格完成 + 2 处"可点却无可见结果"）**：

| 反证 | 证据 |
|---|---|
| AI 逻辑查看器**程序树未打印稳定路径**（分册 §3.3 第 2 条明文要求「行尾带**稳定路径**（与帧里 `aiTrace[].path` 同一语法：`body.s[i]` / `.then` / `.else` / `.body`）」） | 真实 62 帧对局的弹窗 19 行中，程序树带标记 3 行、**含 `body…` 路径 0 行**（`%TEMP%\probe-f6\probe-real.js` 输出）；`format.js:1472-1477` 只输出 `line.text`，`walkProgramNode` 收集的 `line.path` 从不落文本（`:1383-1462`）。分册 §11 步 8 的期望文本「程序树（`if`/`action` 逐行，含 `body.s[0]…` 路径）」因此无法由程序树满足 |
| 快速对战屏在「本屏没有对局、但 `state.viewer` 里有帧」时渲染**可点却无可见结果**的按钮 | `%TEMP%\probe-f6\probe-orphan-viewer.js` ②：`quick.envelope=null` + viewer 3 帧 → `viewer-next/last`、`viewer-trace-p1/p2` 全部 `disabled=false`；点击后只有按钮禁用态变化，**帧文本一行都不渲染**（`format.js:1583-1593` 只在 `loaded` 时渲染帧） |
| 同屏还能出现「成功文案与屏幕内容矛盾」 | 同探针 ④：`viewer-load-replay` 被渲染（`format.js:1605-1607` 的条件在 `loaded` 分支**之外**）→ 点击后提示 `已读取回放 b_ranked：3 帧…`，而屏幕仍是 `（尚未发起对局：点「开始快速对战」）`、帧文本 0 行 → 「按钮永不无声」的**实质**（有按钮但语义不完整）被判违反 |
| 分册 §6 的「本地拦截」行（`：201`）声明「无帧时点步进/轨迹按钮 → 按钮 `disabled`，不出现空文案按钮」 | 上述状态下按钮**不是** disabled（有帧，但帧不属于本屏），该行的承诺在"跨屏残留"这一可达形态下不成立 |

- **「全部失败路径」FAIL（1 条已实现的失败分支未登记）**：

| 反证 | 证据 |
|---|---|
| `viewer-ai-logic` 实际依赖 **两次**请求（`GET /me/configs` + `GET /me/ai`），而分册 §3.3（`:97`）/§4（`:123`）只登记了 `/me/configs`，§6（`:191-201`）**没有**「AI 库读取失败」这一行 | `actions.js:1441-1450`：`aiList` 失败即 `return failFrom(...)`，不落 `viewer.configs`、不开弹窗。`%TEMP%\probe-f6\probe-ailist-fail.js`：configs 200 + aiList 500/传输失败 → `modal=null`、`viewer.configs` 未落，只有一条错误提示；而仅取 configs 时弹窗首行就是 `我方 AI：ai_1`（`format.js:1496-1503` 的 `aiNameOf` 本可回落到 aiId）→ 名字这条**软**依赖被实现成**硬**依赖 |
| QB-5 反过来把这条未登记依赖固化了 | `quick-battle-flow.test.js:234` `assert.deepEqual(h.calls.slice(-2), ['configs','aiList'], '应静默取配置与 AI 库（各一次）')` |

结论：**FAIL**。动作表 9 条都已注册且双向闭合（QB-9 复跑通过），但其中 1 条（`viewer-ai-logic`）未达成分册 §3.3 的规格；且存在 2 处"按钮报了成功/可点，用户却看不到任何结果"的状态（**F6-1/F6-2/F6-4**）。失败路径表缺 1 行（**F6-3**）。

### ④ 已明确到可逐行实现（字段名/边界条件/错误分支无待定项）—— **FAIL**

| 反证 | 证据 |
|---|---|
| **边界条件**有未定项：`state.viewer` 被判为 F6/F7 共用切片并带「数据源」字段（§2 反驳 2 `:53`、§7 `:211`），但 §8 的 Q-1…Q-12（`:221-234`）**没有**覆盖「viewer 里有帧、而本屏没有对局」这一跨屏态，也没规定"数据源不匹配时该怎么办" | 该未定项直接产出了 F6-1/F6-2 两个用户可见缺陷。实测：`state.viewer.source` 在**全仓只有 1 处写入**（`store.js:295`），**没有任何投影或动作读它**（`grep '\.source' public/` 仅命中 `store.js:295`）→ 分册为它写明的用途（「数据源」）在实现中落空 |
| **边界条件**有未定项：0 帧时 AI 逻辑查看器打印什么 | 实现打印 `本帧：第 1/0 帧`（`format.js:1519` + `:1533-1534`，`viewerIndex(state,0)` 返回 0）；HTML 实测见 `probe-render-modal.js`：`<div class="line">出战槽：slot1　本帧：第 1/0 帧</div>`。无帧态是分册 §8 Q-1/Q-2 明文可达的；分册未规定该文案 → 实现自行取值且取值无意义（**F6-5**） |
| **字段名**有与实现不符的登记：`§5.2` 的 `effects[i]` 含 `uid`，§13 R-7 进一步声明「`effects` 的 `uid` 可能为 `null` → 打印为 `-`」 | `format.js:1194-1205` 的 `effectsText` **从不读也不打印** `uid`；实测合成 `{uid:null,kind:'burn',stat:'hp',delta:-3,remaining:2}` → 渲染 `burn（hp-3，剩2）`，既无 uid 也无 `-`（`f6rev-malformed.js` 用例 17）→ R-7 是一条与实现不符的陈述（**F6-6**） |
| **错误分支**未登记：§6 表缺 `/me/ai` 失败行 | 同 ③ 的 F6-3 |
| **对账数字**与实现不符：§14 `:317`「contract.js（**28 条端点路径** + **9 个帧字段数组**…）」、§14 `:321`「§5 字段 **28 条** ✅」、§10 QB-6 `:264`「contract 的 **9 个字段数组**」 | 实测：contract 中 F6 端点条目 = **26**（`quick/run` 21 + `replay/:id` 5，与 §5.1 表格由 FC-1/FC-2/FC-3 双向闭合）；`FRAME_*` 数组 = **15**；§5.2 分组成员 = **12**（`probe-counts.js`）→ §14/§10 的自述数字不可复核（**F6-7**） |

结论：**FAIL**。字段名、边界条件、错误分支三处均存在待定项或与实现不符的陈述；§14 的对账数字亦不成立。

### 四项汇总

| # | 项 | 判定 | 一句话依据 |
|---|---|---|---|
| ① | 与行业主流的关系已说明 | **PASS** | 点名 canvas/WebGL + 时间轴；6 条反驳均含反例与代价；采/不采分界写明（`:52-59`） |
| ② | 既有约束下合理（无权威链/门禁冲突） | **PASS** | 零后端改动、未新增批次号、未改 app.js/render.js、无公式复制；D-169 的"防守方 p2"缺口今日不可达 → 记风险 F6-9 |
| ③ | 功能完整（全部用户动作 + 失败路径） | **FAIL** | `viewer-ai-logic` 未按 §3.3 完成（程序树无路径）；2 处"可点却无可见结果"；§6 缺 1 条已实现的失败分支 |
| ④ | 已明确到可逐行实现 | **FAIL** | viewer 数据源跨屏态未定（F6-1/F6-2）、0 帧文案未定（F6-5）、`effects.uid`/R-7 与实现不符（F6-6）、§14 数字 28/9 与实测 26/15 不符（F6-7） |

> **总判定：四项**不**全过（③④ FAIL）⇒ 按总纲 §2.10 本批**尚不能判定定稿/交付**。**须先修 F6-1…F6-4（P1）并回填 §8/§6/§5.2/§14 的待定项与数字，再出具复评**；F6-5…F6-12 为应同批处理的 P2。机器核对全绿（§2）不改变本判定——本条即为"机器绿 ≠ 定稿"的实例。

---

## 2. 机器核对复跑

> 全部由本审查者在当前工作区（F6 提交 `6dfaaed` + 未提交的 F7 追加）**独立复跑**，非实现者自报。仓库零改动。

| # | 命令（原样） | 观察结果 |
|---|---|---|
| 1 | `node --test --test-isolation=none "tests/frontend/quick-battle-flow.test.js"` | ✅ **10 pass / 0 fail**（`tests 10 / pass 10 / fail 0 / duration_ms 979.78`）；逐条 `✔ QB-1…QB-10` |
| 2 | `node --test --test-isolation=none "tests/frontend/*.test.js"` | ✅ **109 pass / 0 fail**（两次复跑一致：`duration_ms 15441.71` / `14723.92`）；含登记类 `✔ AU-1`、`✔ UI-2`、`✔ FC-1/FC-2/FC-3`、`✔ WH-1…` |

命令 1 的尾部原文：

```
✔ QB-1 真实快速对战：结果行逐字由真实字段拼出；帧与 ticks 同源；viewer 与响应同源 (154.8271ms)
✔ QB-2 帧投影：全帧遍历无 undefined/null 泄漏；末帧含判决行 (113.0183ms)
✔ QB-3 步进动作：夹取边界、本地动作不发任何请求 (102.9822ms)
✔ QB-4 轨迹侧位切换：轨迹行与当前帧 aiTrace 同源（条数一致） (91.0987ms)
✔ QB-5 AI 逻辑查看器：真实配置 + AI 库 → 程序树 + 本帧执行标记 + 对手只有轨迹 (103.2128ms)
✔ QB-6 帧字段三方一致：contract == 04 §5.2 == format.js 实读，且真实帧可解析 (93.4961ms)
✔ QB-7 AI 节点类型与 server/ai/ast.js 的 NODE_TYPES 逐值相等；出厂程序无未知节点 (89.9906ms)
✔ QB-8 无帧与失败路径：步进空操作、AI 查看器失败不开弹窗、replay 文案 (0.6281ms)
✔ QB-9 快速对战屏动作双向闭合（渲染集合 == 注册表的 F6 动作） (88.2857ms)
✔ QB-10 busy 时全部按钮禁用；quick-run 不重入 (105.8505ms)
ℹ tests 10
ℹ suites 0
ℹ pass 10
ℹ fail 0
```

命令 2 的尾部原文：

```
✔ TB-10 busy 全禁用 + 不重入；data-tier 经真 DOM 事件变成动作 payload (203.3356ms)
ℹ tests 109
ℹ suites 0
ℹ pass 109
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 15441.7098
```

> 本审查者**只**复跑了上表两条命令（审查方法规定的范围）；**未**跑 `npm test` / `npm run gate` / `e2e` / `load-test`（属实现者收口口径，见分册 §14 与 `progress.md:97`，本记录不代其背书）。所有探针脚本落在 `%TEMP%\probe-f6\`：`probe-docparse / probe-cross-source / probe-real / f6rev-malformed / probe-ailogic / probe-counts / probe-groups / probe-orphan-viewer / probe-ailist-fail / probe-render-modal .js`。
> ⚠️ 该临时目录**同时**被其它批次（F7）的探针占用（`probe-f7-review.js`、`stage*.js` 等并非本审查产物）；我的畸形输入电池因重名风险改名为 **`f6rev-malformed.js`**（用例编号 01…31），其余脚本文件名见上。

---

## 3. 代码级审查（缺陷与风险）

> 格式：位置 `file:line` / 主张 / 反例或复现 / 严重度（**P0 必须修 / P1 应修 / P2 备案**）/ 处置建议。
> 复现脚本一律在 `%TEMP%\probe-f6\`（仓库内不放探针）。
> **P0：无。** 未发现崩溃、数据损坏、越权、契约破坏或静默丢数据；F6 是纯展示批次，最坏后果是"显示错/显示空"。

### F6-1（P1）快速对战屏的查看器区未按"数据源 / 本屏对局"门控 → 会把别屏的帧当作"本场"渲染

- **位置**：`public/format.js:1578-1607`（`quickViewModel` 只读 `state.viewer.frames`，**不读** `state.viewer.source`，也不与 `loaded`（本屏是否已跑过对局）做与运算）；`public/store.js:295`（`source` 只写不读）。
- **主张**：分册 §2 反驳 2（`:53`）把「**数据源**」列为共享切片存在的理由；§7（`:211`）也登记了 `source`。实现里它是**死字段**（全仓仅 `store.js:295` 一处写入，零处读取）→ 跨屏残留时查看器无法自证"这帧属于哪一场"。
- **反例（实测）**：`%TEMP%\probe-f6\probe-cross-source.js`
  1. 快速对战成功（`b_quick`，2 帧，`共 2 tick`）→ 屏幕 `第 1/2 帧`；
  2. 同一 `state.viewer` 被 F7 的「看这一场」覆盖（`b_ranked`，3 帧，`source='tournament'`）；
  3. `返回主界面 → 快速对战`（分册 §11 步 10 明文的"帧仍在、步进可用"流程）→ 屏幕**同时**显示 结果行 `共 2 tick` 与 查看器 `第 1/3 帧`，**自相矛盾**；`b_ranked` 在屏幕上完全不出现（用户无法察觉帧已换场）。
- **严重度**：**P1**（看错数据；且分册自己把"同一事实两个来源"列为必须避免的坏结果）。
- **处置建议**：
  1. `quickViewModel` 增加显式门控：`var viewerActive = loaded && (state.viewer.source === 'quick' || state.viewer.source === 'replay');`，非 active 时 `frames` 视为 0 帧（不渲染帧/轨迹、禁用四个步进+两个轨迹按钮），并在文本区写明「本屏当前无可查看的对局」之类的一句。
  2. `store.js` 给 `viewer.set` 的 `source` 加白名单（`VIEWER_SOURCES = ['quick','tournament','replay']`，与 `VIEWER_OWNERS` 同构），避免任意字符串进入状态。
  3. F7 的 `tournamentViewModel`（`format.js:1719-1724`）用**同一个**门控（`source === 'tournament'`），否则同一缺陷会在锦标赛屏反向重演。

### F6-2（P1）「读取本场回放」在本屏没有对局时仍渲染；点成功却不显示任何帧，且后续按钮变成"可点但无可见变化"

- **位置**：`public/format.js:1605-1607`（`if (noFrames && battleId !== null) buttons.push(viewer-load-replay)` —— 该判断在 `loaded` 分支**之外**，只看 viewer）；`public/actions.js:1458-1485`（`loadReplay` 成功后只 `viewer.set`，不校验本屏是否已加载对局）；渲染侧 `format.js:1583-1593`（帧只在 `loaded` 时进文本区）。
- **主张**：分册 §4（`:124`）规定该按钮「**仅当无内联帧**时出现」，且成功可见文本为 `已读取回放：<n> 帧`（语义前提是本屏正在看这一场）；实现在"本屏没有对局"时也给出该按钮，并且成功文案与屏幕内容互相矛盾。
- **反例（实测）**：`%TEMP%\probe-f6\probe-orphan-viewer.js` ③/④（`quick.envelope=null`，`state.viewer={frames:null,battleId:'b_ranked'}`）
  - ③ 屏幕渲染出 `viewer-load-replay`，`disabled=false`；
  - ④ 点击后（假 api 200/3 帧）提示 `已读取回放 b_ranked：3 帧（p1（我方），phase=role，共 3 tick）`，`viewer.frames.length=3`，但屏幕**仍然**是 `（尚未发起对局：点「开始快速对战」）`、**帧文本 0 行**；同时 `viewer-next/last`、`viewer-trace-p1/p2` 从 disabled 变为**可点**，点了只改 `state.viewer.index`，屏幕无任何内容变化 → 总纲 §4.1 的实质（每个可点元素必须产出可见结果）被违反。
- **严重度**：**P1**（"成功的按钮没有结果"是 00-rules §4 反复强调的防复发条款）。
- **处置建议**：与 F6-1 同处门控 —— `viewer-load-replay` 的出现条件加 `loaded`（`if (loaded && noFrames && battleId !== null)`）；非 active 的 viewer 不得渲染任何步进/轨迹按钮（或渲染为 disabled + 一句原因）。

### F6-3（P1）AI 逻辑查看器把"AI 名字"这条软依赖做成硬依赖：`GET /me/ai` 失败即整个弹窗打不开

- **位置**：`public/actions.js:1441-1450`（`aiList` 成功才 `viewer.configs.set` + `modal.set`）；分册 §1 端点映射（`:34`）§3.3（`:97`）§4（`:123`）只登记 `GET /me/configs`；§6（`:191-201`）无该失败行。
- **主张**：分册 §3.3 第 1 条自身的回落链是 `我方 AI：<name 或 aiId 或「（该配置没有 AI）」>`（`:99`）——`name` 只是**展示增强**；实现却把它变成开弹窗的前置条件。
- **反例（实测）**：`%TEMP%\probe-f6\probe-ailist-fail.js`
  - `configs 200` + `aiList 500` → `modal=null`、`viewer.configs` 仍为 `null`、提示 `服务端内部错误：服务端内部错误`；
  - `aiList` 传输失败 → `无法连接服务器：…`，同样不开弹窗；
  - 对照：只喂 `configs` 时弹窗首行 = `我方 AI：ai_1`（`aiNameOf` 的 aiId 回落本来可用）。
  - 另：`quick-battle-flow.test.js:234` 用 `assert.deepEqual(h.calls.slice(-2), ['configs','aiList'])` 把这条**未登记**的依赖固化成了契约。
- **严重度**：**P1**（功能被非必要依赖阻断；且属 ③「全部失败路径」/④「错误分支」的待补项）。
- **处置建议**：
  1. 实现：`configs` 成功即 `viewer.configs.set` + `modal.set`；`aiList` 的失败只 `noticeNotice(error, ...)`（弹窗内 `aiNameOf` 已回落到 aiId/`（未登记 aiId）`），不要 `return`。
  2. 分册：§1 行 3 / §3.3 / §4 登记 `GET /me/ai`（"仅用于把 aiId 显示成名字，失败不阻断"），§6 补一行「AI 库读取失败 → 服务端文案；弹窗仍打开，AI 名回落 aiId」。
  3. 测试：QB-5 的 `calls` 断言改为"成功路径两次请求 + 失败路径仍开弹窗且首行含 aiId"。

### F6-4（P1）AI 逻辑查看器的程序树**不打印稳定路径**（分册 §3.3 第 2 条的明文规格未实现）

- **位置**：`public/format.js:1472-1477`（`markedProgramLines` 只输出 `line.text`（+标记））；路径收集在 `:1383-1462`（`walkProgramNode` 把 `path` 放进行对象但从不落文本）；规格在分册 `:100` 与 §11 步 8（`:285`）。
- **主张**：分册要求程序树每行「行尾带**稳定路径**（与帧里 `aiTrace[].path` 同一语法）」，这正是"树节点 ↔ 轨迹条目"可对照的唯一手段；缺了它，`← 本帧执行` 标记无法与下方 `#seq … path …` 轨迹行对上。
- **反例（实测）**：`%TEMP%\probe-f6\probe-real.js`（真实 2 账号 + 真 `quick-run`，62 帧）
  - 弹窗共 19 行；程序树 3 行带 `← 本帧执行`；**含 `body…` 的程序树行 = 0**；
  - 路径只出现在轨迹行（`#0 p1 body.s[0] if` / `· #1 p1 body.s[0].then.s[0] action → move_right`）→ 分册 §11 步 8 的「程序树（…含 `body.s[0]…` 路径）」无法由程序树本身满足。
- **机器核对为何没抓住**：`quick-battle-flow.test.js:245` 的 `assert.ok(text.indexOf('body.s[0]') !== -1 || text.indexOf('body') !== -1)`——后一支被**下方轨迹行**满足，断言恒真（"或"把该腿打成了空断言）。
- **严重度**：**P1**（规格未实现 + 断言失效，两者都必须改）。
- **处置建议**：
  1. 代码：在 `programLines`（或 `markedProgramLines`）里对 `line.path !== null` 的行输出 `line.text + '  ' + line.path`，标记追加在其后（`… body.s[0].then.s[0]  ← 本帧执行`）；分支标题行（`then:`/`else:`，`path === null`）保持不带路径。
  2. 测试：QB-5 改为**区段断言**——取「程序（…）：」之后到「本帧执行轨迹」之前的行，断言其中至少一行含 `body`（并断言 `path` 语法与同帧 `aiTrace[].path` 的集合有交集），删掉 `|| text.indexOf('body')`。

### F6-5（P2）0 帧时 AI 逻辑查看器打印 `本帧：第 1/0 帧`

- **位置**：`public/format.js:1519`（`idx = viewerIndex(state, frames.length)`，total=0 时返回 0）、`:1533-1534`（`'　本帧：第 ' + (idx+1) + '/' + frames.length + ' 帧'`）。
- **主张**：无帧态（分册 §8 Q-1/Q-2，`:223-224`）下该文案无意义；且 `viewer-ai-logic` 在无帧时**可用**（`format.js:1603` 只按 busy 禁用），故这是可达的默认行为，不是防御分支。
- **反例（实测）**：`%TEMP%\probe-f6\probe-ailogic.js` ⑦/⑧/⑨（含 `frames=[]` 与 `frames` 缺失两种）→ 首行区出现 `本帧：第 1/0 帧`；`probe-render-modal.js` 的 HTML 里确有 `<div class="line">出战槽：slot1　本帧：第 1/0 帧</div>`。
- **严重度**：**P2**（无崩溃、无 `undefined` 泄漏；属边界文案缺陷 + §8 未覆盖）。
- **处置建议**：`frames.length === 0` 时写 `本帧：无帧（点「读取本场回放」）`（或省略该段）；分册 §8 增补一行边界（如 Q-13）与一条 QB 断言。

### F6-6（P2）`effects[].uid` 与 §13 R-7 是"幽灵字段"：投影从不读也不打印 uid；QB-6 的"format.js 实读"腿抓不到分组错配

- **位置**：`public/format.js:1194-1205`（`effectsText` 只读 `kind/stat/delta/displacement/remaining`）；分册 `:173`（§5.2 `effects[i]` 含 `uid`）、`:311`（R-7「`effects` 的 `uid` 可能为 `null` → **打印为 `-`**」）；`public/contract.js:229`（`FRAME_EFFECT_FIELDS` 含 `uid`）。
- **主张**：分册 §5.2 是"前端读哪些字段"的清单，R-7 是"已知风险"；两者都陈述了一个不存在的渲染行为。
- **反例（实测）**：`%TEMP%\probe-f6\f6rev-malformed.js` 用例 17 → 合成 `{uid:null, kind:'burn', stat:'hp', delta:-3, displacement:null, remaining:2}` 渲染为 `burn（hp-3，剩2）`，**无 uid、无 `-`**。
- **机器核对为何没抓住**：QB-6 第三腿是**全文件子串查找**（`quick-battle-flow.test.js:286` `src.indexOf("'" + f + "'") === -1`）——`'uid'` 在弹幕行/物品行出现即通过。我另用探针按组抽取 `pick(<var>, '<literal>')`：`FRAME_EFFECT_FIELDS` 的 `uid` **从未**作为 `pick()` 路径出现（`probe-docparse.js` 输出），即"声明在 effects 组、读在弹幕组"这类错配永远绿。
- **严重度**：**P2**（文档/契约与实际读法不符，无用户可见损失）。
- **处置建议**（二选一，必须**同时**改文档与 contract，因为 QB-6 的 `missingInDoc`/`extraInDoc` 是双向集合相等）：
  - **(a) 推荐**：从 §5.2 与 `FRAME_EFFECT_FIELDS` 同时删掉 `uid`，并删除/改写 R-7（改为"本批不显示 effect uid"）；
  - (b) 让 R-7 成真：`effectsText` 打印 uid（`<uid|->(…)`），并同步分册 §3.2 的 effects 文本规格。
  - 另建议把 QB-6 第三腿升级为**按组上下文**抽取（`walkProgramNode`/`effectsText` 等函数体内取 `pick(x,'lit')`），否则该腿只是"字面量出现过"的同义反复。

### F6-7（P2）分册 §10/§14 的自述数字与实现不符（"28 条端点路径" / "9 个字段数组"）

- **位置**：`docs/frontend/04-quickmatch.md:264`（QB-6「contract 的 **9 个**字段数组」）、`:317`（「contract.js（**28 条**端点路径 + **9 个**帧字段数组 + `AI_NODE_TYPES`）」）、`:321`（「§5 字段 **28 条** ✅」）；另 `progress.md:97` 与提交信息沿用「9 组」。
- **实测**（`%TEMP%\probe-f6\probe-counts.js`）：`AUTH_FIELD_CONTRACT` 中 `endpoint ∈ {quick/run, replay/:id}` 的条目 = **26**（21 + 5，与 §5.1 表格行数一致，且被 FC-1/FC-2/FC-3 双向闭合）；`FRAME_*` 数组 = **15**；§5.2 分组成员行 = **12**。
- **严重度**：**P2**（文档漂移，违反 §3.4；会误导后续复评与 F7 复用）。
- **处置建议**：把 §14 的 `28`/`9` 改为 `26`/`15`（或写"§5.2 的 12 组 + 3 组可选字段数组"），§10 QB-6 的"9 个"同改；提交信息无需回改。

### F6-8（P2）机器核对在**真实响应**上从未覆盖 `effects` / `baseHits` / `collision` 三组（QB-6 的"真实帧逐字段验证存在"对它们不成立）

- **位置**：`tests/frontend/quick-battle-flow.test.js:289-374`（真实响应分支只有 `withBullet` / `withDamage` / `withHit` / 末帧 `verdict` / `aiTrace` 五处，**且 `FRAME_ACTION_FIELDS` 被挂在 `if (withBullet)` 里面**，`:320`）；分册 §5.2 的承诺在 `:167`（「并由 QB-1 在**真实响应**上逐字段验证存在」）与 §10 QB-6（`:264`）。
- **反例（实测）**：`%TEMP%\probe-f6\probe-groups.js`（连打 6 场真实快速对战，**258 帧**）：含 `effects` 的帧 **0**、含 `baseHits` 的帧 **0**、含 `collision` 的帧 **0**（含 `bullets`/`damages`/`bulletHits` 各 150 帧、含 `aiTrace` 258 帧）。→ 我不得不用**合成**帧才能触发 effects/baseHits/collision 的投影分支（`f6rev-malformed.js`）。
- **严重度**：**P2**（覆盖缺口；这三组的真实字段名若漂移，测试与实现会一起静默）。
- **处置建议**：仿 `auth-field-contract.test.js:47` 的 `injectAssemblable` 手法注入确定性夹具（带控制/持续效果的技能或直接构造 `diff` 的合成帧并明确标注"合成"），至少对 `effects` 组补一条逐字段断言；把 `FRAME_ACTION_FIELDS` 的核对移出 `withBullet` 分支（行动字段每帧都有）。

### F6-9（P2）共享查看器无法表达"我在 p2"：`p1=我方` 是硬编码，而 D-169 明确"防守方会看到自己在 p2"

- **位置**：`public/format.js:1153`（注释「请求者恒为 p1」）、`:1268-1269`（`我方 p1` / `对手 p2` 硬编码）、`:1611-1612`（屏提示同口径）；`public/store.js:100-102`（`emptyViewer()` 无 `selfSide`，只有 `source`）。
- **主张**：`decisions.md:294`（D-169 用户口径）：「每场战斗结束后给双方玩家发送**同一个**回放（防守方会看到自己在 p2，进攻方看到自己在 p1）」；共享切片的侧位标签却是常量。F6 的可达路径（`quick/run` 内联、按自己 `battleId` 读回放）里请求者恒为 p1，故**当前不可达**；但 §1（`:37`）把 `/me/defense` 登记为 F7 之后，届时同一批帧会被标成"我方"。
- **反例**：本批**无**（这是登记风险，不是现存缺陷）——我尝试构造并证伪失败：F6 的 9 个动作里没有任何入口能载入"我作为 p2"的帧。
- **严重度**：**P2**（备案；跨批复用风险）。
- **处置建议**：`emptyViewer()` 增 `selfSide: 'p1'` 并在 `viewer.set` 由调用方给出（`quick`/`tournament`/自己 `battleId` → `'p1'`；防守回放 → `'p2'`），投影用 `labelOf(selfSide)` 生成"我方/对手"；或至少在分册 §13 显式登记"本批只支持进攻方视角（p1=我方）"。

### F6-10（P2）文案与分册 §3.2/§11 的逐字期望不一致（清单）

- **位置/实测**（真实帧渲染，`probe-counts.js` 与 `probe-real.js`）：

| 项 | 分册 | 实现 |
|---|---|---|
| 行动文本 | `:92` `move→左移/右移`、`dodge→左闪/右闪`、`forced_move→被推左/右` | `format.js:1183-1187` `移动右` / `闪避右` / `被推右（2 格）` |
| 朝向 | `:82` `朝向 +1` | `format.js:1218` `朝向 +1（向右）` |
| 基地行 | `:84` `基地：p1 100/100（def 64）` | `format.js:1228` `基地：p1 hp 100/100（def 64）` |
| 弹幕行 | `:85` `弹幕：b_43（p2，straight，dir -1，v 512，长 512）…` | `format.js:1242-1248` 追加 `，等级 <n>` |
| 弹幕命中行 | §3.2 样例**未列** | `format.js:1281-1284` 打印 `弹幕命中：…` |
| 判决行 | `:89` `判决：p2 胜（phase=role）`；§11 步 5 `判决：p1\|p2\|draw 胜（phase=…）` | `format.js:1288` `判决：p2（对手）（phase=role）`（**无"胜"字**） |

- **主张**：差异均为"更啰嗦/更细"，**没有信息丢失、没有 `undefined`/`null` 泄漏**；但 §3.4 要求文档与实现一致，且 §11 步 5 的期望文本与实际不符会让走查者误判。
- **严重度**：**P2**。
- **处置建议**：以实现为准更新分册 §3.2 的样例与 §11 步 5 的期望文本（反向改码成本更高、且现状信息更全）。

### F6-11（P2）"无配置槽 / `loadout=null`"时弹窗重复打印 `（该配置没有 AI）` 两行

- **位置**：`public/format.js:1528`（首行 `我方 AI：` + `AI_LOGIC_NO_AI`）与 `:1531`（`program === null` → 再 push 一次 `AI_LOGIC_NO_AI`）。
- **反例（实测）**：`probe-ailogic.js` ④（`slots: []`）与 ⑤（`loadout: null`）→ 弹窗第 0、1 行同文案。
- **可达性**：**低** —— 服务端 `/me/configs` 正常响应恒有 3 槽且 `loadout` 为对象（O-15 已按"正文随 journal 落盘"落地），故只在存档异常/未来契约变化时出现。
- **严重度**：**P2**。
- **处置建议**：`loadout` 为 null 时只保留首行（或首行写 `（无出战配置槽）`），第二条仅在"有槽但无 AI"时输出。

### F6-12（P2）`literal` 节点缺 `value`（或 `value=null`）时程序树泄漏 `undefined` / `null`

- **位置**：`public/format.js:1370`（`return JSON.stringify(pick(node,'value'))` —— `JSON.stringify(undefined)` 返回**非字符串** `undefined`）。
- **反例（实测）**：`f6rev-malformed.js` 用例 25 → `if undefined`；用例 26 → `if null`。
- **可达性**：经服务端校验的程序**不可达** —— `server/ai/ast.js:745`（`literal: [['value','any']]`）配合 `:330-334`（`null`/`undefined` 视为"缺必填字段"→ 校验期拒绝）；只对绕过校验的历史存档/migrate 遗留数据有效。
- **严重度**：**P2**（防御性）。
- **处置建议**：`var v = JSON.stringify(pick(node,'value')); return v === undefined ? '?' : v;`；并把 QB-7 的 `undefined` 断言扩到 `null`（`quick-battle-flow.test.js:394` 目前只查 `undefined`）。

### 非本批（F7）观察，仅登记不计入 F6

- `04 §4`（`:127`）「本批不新增 `data-*` 寻址键（`app.js` 的 `payloadOf` 白名单 **9 键**不变）」在 **F6** 提交上为**真**（`git show HEAD:public/app.js` 的 `keys` 恰为 9 键，且 `6dfaaed` 未改 `app.js`）。当前工作区已是 **10 键**（F7 追加 `tier`，`public/app.js:151`）→ 属 F7 的登记事项（05 分册），**不是 F6 缺陷**；此处记录以免复评时误判。
- `_meta` 说明：本审查期间工作区含**未提交的 F7 改动**（`docs/frontend/05-tournament.md`、`tests/frontend/tournament-board-flow.test.js`、`public/*` 的 F7 增量）。我已逐 hunk 核对 `git diff HEAD -- public/format.js`：F7 只改了 `EMPTY_PAGES`、把 `quickNoticeText` 抽成 `pvpNoticeText` 别名（语义不变）、并在 `:1617+` 追加 F7 段；**本记录的全部 F6 结论在 `6dfaaed` 的 `quickViewModel`/`frameLines`/`aiLogicLines`/`markProgramLines` 原文上复现过**（`git show HEAD:public/format.js` 对照）。

### 我尝试证伪但**未**成立的攻击（用于说明"零问题"不是漏检）

| # | 攻击 | 结果 |
|---|---|---|
| 1 | **前端复制战斗公式**（§1.5） | 未成立。F6 段只做 `num/or/signedText/percentText(Math.round(v*1000)/10)` 与 `p1/p2/draw/invalid → 你赢了/你输了/平局/无效对局` 的枚举映射（D-169② 明确要求前端做这一层翻译）；未见 hp/伤害/命中/胜负推演，未见读取 `data.seed`（`grep seed public/` 仅注释） |
| 2 | **帧投影泄漏 `undefined`/`null` 或抛错** | 未成立。真实 62 帧（`probe-real.js`，泄漏 0 行）+ 31 组畸形输入（`f6rev-malformed.js` 用例 01…31：`frame=null/undefined`、无 `diff`、`diff=null`、`players.p1=null`、`action=null/{}`、`kind='dash'`、`verdict={}`、`winner='p3'`、`collision={}`、`damages[{}]`、`atX=null`、`outcome=null`、`falloffFactor`、`baseHits[{}]`、`aiTrace=null`/条目缺字段/`owner=7`、`frames=[]`、`frames` 缺键、`index=999`）**零抛错**；唯二泄漏是 `literal` 缺 `value`/`value=null`（用例 25/26，即 F6-12，经服务端校验不可达） |
| 3 | **`pick()` 路径不存在于真实响应**（规则 4.2） | 信封级未成立：FC-1 逐条在真实 HTTP 上解析、FC-2 双向相等（我复跑通过）。帧级仅 F6-6（分组归属）+ F6-8（真实覆盖缺口），**无**"读了不存在的路径" |
| 4 | **D-164 镜像语义被误读**（把 p2 当 p1） | 未成立。轨迹按 `owner` 过滤（`format.js:1293-1298`）、按钮/标签与 `owner` 同源（`:1310-1316`、`:1601-1602`）；F6 全部可达路径请求者=p1（D-169③ 不变量），故未发现误标（长期风险 = F6-9） |
| 5 | **§6 失败路径静默** | 未成立（逐条调用 `quickNoticeText`/`sessionLost`/`networkText` 复核：`no_opponent`/`no_active_config`/`loadout_invalid`(+`details[0].path`)/`store_not_found`/`replay_expired`/`replay_forbidden`/401/传输失败 均有可见文案）。**唯二的"无声"是 F6-2 的"成功却无内容"与 F6-3 的未登记分支**，已单列 |
| 6 | **busy / 0 帧禁用不严** | 未成立。QB-10 复跑通过；0 帧时四个步进 + 两轨迹按钮均 disabled（`probe-orphan-viewer.js` ①） |
| 7 | **病态 AI 程序打爆屏幕/死循环** | 未成立。200 层嵌套 → 输出被 200 行上限截断并带 `…（超出显示深度）`；自引用环不递归死循环（`f6rev-malformed.js` 用例 30/31） |
| 8 | **动作注册表有死动作/不可达动作**（§4.1 双向） | 未成立。QB-9 复跑通过：注册表 F6 动作 = 渲染集合 = 9；`F6_ACTIONS` 在 `admin-ui-contract.test.js:45` 与 `quick-battle-flow.test.js:34` 一致 |

---

## 4. 人工走查（总纲 §4.4）

- **状态：待用户本人执行。** 本节由审查者留空 —— 本会话**无真实浏览器**，机器断言**不能**替代走查（总纲 §4.4：「只有机器断言不算完成」；`docs/acceptance.md` §8 的历史失败根因正是"机器全绿但不可玩"）。
- **剧本位置与步数**：`docs/frontend/04-quickmatch.md` **§11（`:272-291`）共 12 步**（步 1 静态文字/禁用 → 步 2 结果行 → 步 3 抽池行 → 步 4 `下一帧`×3 → 步 5 `最后一帧`+判决行 → 步 6 `第一帧` → 步 7 轨迹侧位切换 → 步 8 AI 逻辑查看器（含程序树路径与 `← 本帧执行`）→ 步 9 点背景关闭 → 步 10 返回主界面后再进（帧仍在）→ 步 11 `no_opponent` 文案 → 步 12 F5 刷新不落盘）。
- **前置**：`npm start`；至少两个池内账号（或 `DL_DEBUG_BOTS=1` + 管理员注入 bot）。
- **走查时请注意（本次审查已实测到的、会影响期望值的三点）**：
  1. **步 8 期望的"程序树含 `body.s[0]…` 路径"当前不成立**（F6-4）——程序树行只有缩进文本与 `← 本帧执行`，路径只出现在下方轨迹行；走查者若按字面核对会判 FAIL（这也正是 F6-4 要求修码的原因）。
  2. **步 10 的"再进快速对战"** 是本批 P1 缺陷的触发场景之一：若此前去过锦标赛屏点过「看这一场」，快速对战屏会把那一场的帧当作本场渲染（F6-1）。**建议 F6-1 修复后再做步 10**，否则会看到"结果行 `共 N tick` 与 `第 i/m 帧` 不一致"。
  3. 步 5 的期望文本「`判决：p1|p2|draw 胜（phase=…）`」与实现「`判决：p2（对手）（phase=role）`」措辞不同（F6-10）。
- **已知不覆盖**（分册 §11 自述 + 本审查确认）：本剧本不含「无内联帧 → 读取本场回放」（需构造幂等重放/重放批次，见 §13 R-1；机器侧由 QB-8 覆盖，**但 QB-8 用的是合成状态、不是真实重放响应**）。
- 走查结论请由用户本人回填本节（含日期、执行人、逐步实测文本或"以当次确认为准"的口径说明），做法可照 `docs/reviews/F3.md` §4。

---

## 5. 已知未覆盖 / 已知风险

### 5.1 分册 §13 原有条目（逐条复核意见）

| # | 分册原文要点 | 独立复核意见 |
|---|---|---|
| R-1 | 人工走查未覆盖「无内联帧 → 读取本场回放」；由 QB-8 机器覆盖，留待 F7 走查 | **成立**，但需补充：QB-8 用**合成** `{ok:true,data:{frames:null,battleId:'b_x'}}` 触发该分支，未用真实重放批次响应（`server/ranked.js:527-559` 的 `results[]` 确无 `frames` 键——我已核对该行，分册 §2 反驳 3 的引证准确）；另 F6-2 表明该分支在"本屏无对局"时行为错误，走查时应一并覆盖 |
| R-2 | 对手 AI 源码不可见（后端不提供，前端不伪造） | **成立**。弹窗末行固定写 SEC-33/D-167（`format.js:1127`、`:1542`）；轨迹来自服务端 `aiTrace`，无伪造 |
| R-3 | 无"跳到第 N 帧"输入；代价 = 长对局定位中间帧需连点 | **成立**（§2 反驳 6 已论证按钮墙被否决）。补充：翻到末帧需 n 次点击，但「最后一帧」按钮可用，代价可接受 |
| R-4 | 前端不做帧的本地缓存/预取 | **成立**（无 localStorage 新键；`grep` 未见 F6 写存储） |
| R-5 | 文本化信息量上限；本批不做折叠/过滤 | **成立**。实测 62 帧单帧 6–12 行；长对局（>64 tick）总信息量大但每帧有限 |
| R-6 | 未接 `/me/records` 战绩列表 | **成立**（§1 已登记为 F7 之后） |
| R-7 | `effects` 的 `uid` 可能为 `null` → 打印为 `-` | **不成立（与实现不符）** → 归入 **F6-6**：`effectsText` 从不打印 uid |

### 5.2 本审查新增的已知未覆盖 / 已知风险

| # | 项 | 说明 | 建议去处 |
|---|---|---|---|
| N-1 | **`state.viewer.source` 是死字段**，快速对战屏未做数据源/本屏对局门控 | F6-1/F6-2 的根因；分册 §2 反驳 2 为它写明的用途在实现中落空 | 修码（F6-1 处置）+ 分册 §7 写明"投影必须按 `source` 门控" |
| N-2 | **`viewer-ai-logic` 的第二依赖（`GET /me/ai`）未登记**，且失败即阻断 | F6-3；分册 §1/§3.3/§4/§6 四处需同步 | 修码 + 补文档 |
| N-3 | **程序树无稳定路径** + QB-5 的 `|| text.indexOf('body')` 空断言 | F6-4；修码后必须同时收紧断言，否则改了也无人守 | 修码 + 改测试 |
| N-4 | **真实响应未覆盖 `effects`/`baseHits`/`collision`**（258 帧实测为 0） | F6-8；这三组的"字段名来自真实响应"（规则 4.2）目前只有字面量级证据 | 补确定性夹具 |
| N-5 | **防守方视角（我在 p2）无表达** | F6-9；D-169 明文承认该情形存在，本批共享切片只支持 p1 视角 | 加 `selfSide` 或登记风险 |
| N-6 | 共享查看器跨屏残留（"帧属哪一场"）**在锦标赛屏同样**未门控 | 见 F6-1 处置 3；两屏必须用同一口径，否则修一处漏一处 | 与 F6-1 同批 |
| N-7 | 本审查**未**执行 `npm test` / `npm run gate` / `e2e` / `load-test`，**未**做浏览器走查，**未**测多进程/并发/性能 | 审查方法只要求两条前端命令；其余口径由实现者与用户走查负责，本记录不代背书 | 复评时按需补 |
| N-8 | 审查期间工作区含**未提交的 F7 改动** | 已逐 hunk 隔离（见 §3 末"非本批观察"）；若复评基线改为 F7 提交，需重跑 F6 相关探针 | 复评说明 |

---

## 附：本记录的证据索引（命令与文件）

| 证据 | 位置 |
|---|---|
| 机器核对 | `node --test --test-isolation=none "tests/frontend/quick-battle-flow.test.js"`（10/10）；`node --test --test-isolation=none "tests/frontend/*.test.js"`（109/109） |
| F6-1 | `%TEMP%\probe-f6\probe-cross-source.js` |
| F6-2 | `%TEMP%\probe-f6\probe-orphan-viewer.js` |
| F6-3 | `%TEMP%\probe-f6\probe-ailist-fail.js` |
| F6-4 | `%TEMP%\probe-f6\probe-real.js`（真实 62 帧 + 弹窗 19 行） |
| F6-5 | `%TEMP%\probe-f6\probe-ailogic.js` ⑦⑧⑨、`probe-render-modal.js` |
| F6-6 | `%TEMP%\probe-f6\f6rev-malformed.js` 用例 17、`probe-docparse.js` |
| F6-7 | `%TEMP%\probe-f6\probe-counts.js` |
| F6-8 | `%TEMP%\probe-f6\probe-groups.js`（6 场 / 258 帧） |
| F6-9 | `docs/decisions.md:294`（D-169）+ `public/format.js:1153/1268/1611` |
| F6-10 | `%TEMP%\probe-f6\probe-counts.js`（合成帧逐行打印） |
| F6-11 | `%TEMP%\probe-f6\probe-ailogic.js` ④⑤ |
| F6-12 | `%TEMP%\probe-f6\f6rev-malformed.js` 用例 25/26 + `server/ai/ast.js:745/330-334` |
| 畸形输入电池全表 | `%TEMP%\probe-f6\f6rev-malformed.js`（用例 01…31，2 泄漏 0 抛错）+ `probe-ailogic.js`（①…⑨） |
| 文档数字复核 | `%TEMP%\probe-f6\probe-docparse.js`、`probe-counts.js` |
| 提交基线 | `git show --name-only 6dfaaed`、`git show HEAD:public/format.js`、`git show HEAD:public/app.js`、`git diff HEAD -- public/format.js` |

> 探针脚本一律留在 `%TEMP%\probe-f6\`（未清理，便于复核）；**仓库内零探针、零改动** —— 本审查者唯一新增的仓库文件就是本记录（同目录的 `docs/reviews/F7.md` 由并行的 F7 审查者新建，与本记录无关）。


---

## 6. 修复回执（父代理，2026-09-25；本节的编号引用 §3 的缺陷编号）

> 审查出具后，父代理按"P1 必修 + P2 同批修"逐条处置。**本节只记录"审查发现 → 修复位置 → 回归证据"**，
> 不修改上文审查原文（判定与反例保持原样，便于复评对照）。

| 编号 | 处置 | 修复位置 | 回归证据 |
|---|---|---|---|
| F6-1 | ✅ 已修 | `format.js` 新增 `quickViewerActive`/`tournamentViewerActive`/`activeViewerFrames`（按 `viewer.source` + `viewer.battleId` 归属门控）；`quickViewModel`/`tournamentViewModel`/`aiLogicLines` 全部走该门控；`store.js` 新增 `VIEWER_SOURCES` 白名单并在 `viewer.set` 校验 | **QB-11**（跨屏门控：别场帧不渲染 + 步进/轨迹禁用 + 出现"别处战斗"提示）、**TB-12**（锦标赛屏同口径） |
| F6-2 | ✅ 已修 | `quickViewModel` 的「读取本场回放」条件改为 `loaded && noFrames && envBattleId !== null`（判据取**本屏对局**而非 viewer）；锦标赛屏同改为"查看器里的对局必须属于本批" | **QB-11** 第二段（本屏无对局 + viewer 持有别场 id → 不渲染该按钮）、**QB-8** 相应更新 |
| F6-3 | ✅ 已修 | `actions.js` 的 `openAiLogic`：`configs` 成功即开弹窗；`aiList` 失败只写 `AI_LOGIC_NAME_FAIL_TEXT`（名字回落 aiId），不再 `return` | **QB-12**（aiList 500 / 传输失败两种都开弹窗且首行含 aiId；configs 失败仍不开空弹窗） |
| F6-4 | ✅ 已修 | `format.js` 的 `markedProgramLines`：`path !== null` 的行输出 `text + '  ' + path` 再追加执行标记 | **QB-5** 改为区段断言（程序段内至少一行含 `body`，且路径集合与同帧 `aiTrace[].path` 有交集），删掉原恒真 `|| indexOf('body')` |
| F6-5 | ✅ 已修 | `aiLogicLines`：0 帧写 `本帧：无帧（本屏暂无可查看的对局）` | **QB-12**（无帧态）＋ 04 §8 Q-13 |
| F6-6 | ✅ 已修（按建议 a） | `contract.FRAME_EFFECT_FIELDS` 与 04 §5.2 同步**删掉 `uid`**；R-7 改写为"本批不显示 effect uid" | **QB-6** 的合成 effects 逐字段断言（按新清单） |
| F6-7 | ✅ 已修 | 04 §10/§14 的 `28 条`/`9 组` → **26 条**（quick 21 + replay 5）/ **15 个** `FRAME_*` 数组 | 数字按 `contract` 实测重算（`AUTH_FIELD_CONTRACT` 过滤 + `FRAME_*` 计数） |
| F6-8 | ✅ 已修 | `quick-battle-flow.test.js`：行动字段断言移出 `if (withBullet)`；新增**合成帧**覆盖 `effects`/`baseHits`/`collision`/`bulletHits` 四组的投影分支与逐字段存在性（并注明是合成数据） | **QB-6**（合成段：`碰撞：接触点 640`/`撞基地：…`/`弹幕命中：…`/`buff burn（hp-3，剩2）` 逐行断言） |
| F6-9 | ✅ 已登记（不实现） | 04 §13 新增 **R-8**：本批只支持进攻方视角（`p1 = 我方`）；防守回放（`/me/defense`）批次需给 `state.viewer` 增 `selfSide` | 无（本批不可达，登记为跨批复用风险） |
| F6-10 | ✅ 已修（以实现为准） | 04 §3.2/§11 的样例与期望文本逐条对齐实现（`移动右`/`朝向 +1（向右）`/`基地：… hp …`/弹幕含`等级`/弹幕命中行/`判决：p2（对手）`） | **QB-2**（全帧遍历无泄漏）＋ 走查剧本 §11 步 5/8 已更新 |
| F6-11 | ✅ 已修 | `aiLogicLines`：无槽/`loadout=null` 时只写一行 `（该配置没有 AI）` | **QB-12**（真实配置无槽路径由合成用例覆盖） |
| F6-12 | ✅ 已修 | `exprText` 改用 `literalText`：`value === undefined` → `?`，`JSON.stringify` 非字符串 → `?` | **QB-7** 增加"程序树不得打印 `null`"断言 |

**复评结论（父代理）**：§3 的 4 条 P1 与 8 条 P2 已全部处置；`docs/frontend/04-quickmatch.md` §10/§13/§14 已同步登记；
`node --test --test-isolation=none "tests/frontend/quick-battle-flow.test.js"` = **12 pass / 0 fail**；全量 `npm test` = **1112 pass / 0 fail**。
**人工走查仍待用户本人执行**（§4：12 步；F6-1/F6-4/F6-10 的走查阻塞项已消除）。
