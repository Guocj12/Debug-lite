# F3 审查记录

> 批次：F3（P6 前端第四批：gacha 开箱屏 + warehouse 仓库/装配屏）
> 审查方式：独立对抗性上下文（8 项核查清单逐条 + 缺口自查 6 项 + 探针 `.review-f3/probe-loop.js`（真后端 openBoxes/assemble/disassemble 经 effects→reducer→布局全链路闭环）+ verifyLayout 手动复算（详情区按钮堆叠几何、真实数据形状溢出、gacha 空态相交）+ 实测六件套）
> 日期：2026-09-17　结论：**CONDITIONAL PASS→需修（P1×3 必须当批修复；已全部修复并复测绿）**：①详情区候选/拆卸按钮 h32 在 30px 步距下**逐对重叠 2px**（verifyLayout 实测 11+ 项 overlap，与记录数无关）；②真实数据形状下详情内容**溢出面板/视口**（roleSlotRange 最高 [5,7] 槽；实测 4 槽角色+8 插件 → 槽行/按钮渲染到 y672，面板底 580 之下；mythic 7 槽 → 720 clip）；③gacha 空态提示盒与 z0 中面板**同层相交**（verifyLayout 每帧误报 overlap）。P2×15 登记；修复后复测基线 **503/503、gate 9 PASS/0 FAIL/0 PEND、cov 聚合 97.78/87.77/96.50、check-arch 39 文件 0 违规、golden 18 tick 一致**；闭环探针 7 段 PASS（含真后端 points_exceeded / slot_type_mismatch 拒绝码经 toast 呈现实证）。

## 审查范围

交付物（git status 实证）：`public/js/views/gacha.js`、`public/js/views/warehouse.js`（新增）；`public/js/views/index.js`（改：挂 gacha/warehouse，battle 仍 null fallback）；`public/js/store/reducer.js`（改：wh/tab + wh/select）；`public/js/store/effects.js`（改：wh/assemble 带 tier + wh/disassemble/wh/take 共用 doDisassemble）；测试 `tests/frontend/gacha.test.js`、`warehouse.test.js`（新增）+ `views.test.js`（改）。契约依据：frontend-spec.md §6.3/§6.4；interfaces.md §1（B18 物品实例形状）/§2（/box、/warehouse/assemble|disassemble 错误码）；server/box.js、server/core/items.js（真形真逻辑）；F1 沿革（buckets 形状）、F2 沿革（P1-3 z 层级/几何教训、P2-16 动态 import、P2-19 双落盘）。

## 核对结果

| # | 核查项 | 结果 | 依据 |
|---|---|---|---|
| 1 | **gacha 布局/载荷/忙态/结果/空态** | ✅ PASS（P1-3 修复附注） | 面板 center(480,300)=(400,210) ✓；radio 5 档 TIERS 载荷 `{tier}` 与 reducer tier/set 语义一致 ✓（settings 的 `{level}` 属 log/level，非同义）；次数快选 ×1/×5/×10 载荷 `{times:n}` 与 /box 契约一致 ✓；主按钮 payload{times:1}、busy→disabled+「开箱中…」双文案 ✓（reducer box/open 置 opening+busy 双标志，box/done 复位）；结果 grid(96,420,6,168,108,16) ✓ 实测 10 件两行（行2 y544..652）右缘 1184 不越 1280 ✓；品质色条 q-\*、名回退 templateId/kind、detail=kind ✓；**空态 gacha_empty 原 y420 与 z0 面板（210..510）同层相交（P1-3）**——修复后 y560；忙态无空态提示 ✓；§6.4「段位下拉/次数 Field」→ 实现 radio+快选（任务交付物口径，spec 散文待改，P2-1）。 |
| 2 | **warehouse Tab/过滤/详情/拆卸/高度** | ⚠️ FAIL→P1×2（①②，已修） | Tab 4 枚 x16 步 44 + wh/tab → activeTab.warehouse → itemsOf 网格重选 ✓；跨桶选中查找失败 → 详情「未选择」✓；candidatesFor = kind 匹配/equipped 排除/unlockTier 门控/自排除 ✓ 纯函数；**与后端 assemble 差异面：无 slot 类型与点数预算预过滤**（spec §6.3「本地按 slot/点数/门控过滤」未落）——真数据实证错配按钮 5/9 面，后端四码（slot_type_mismatch/points_exceeded/tier_locked/plugin_equipped）经 toast 链路**完整**（api/client 信封归一到 {ok,code} → toastCtx → ui/toast；探针实测 points_exceeded 与 slot_type_mismatch 两码落列且拒绝后状态不变）——P2-4 登记；详情点数/槽位与真形一致（skill 无 pluginPoints → 「点数 x/0」兜底，P2-6）；已装槽 wh_take → doDisassemble ✓（测试断言与 wh/disassemble 同一函数引用）；**按钮堆叠 30px 步距 vs h32 逐对 2px 重叠 + 真实数据溢出（P1-1/2）**；高度截断无提示——与任务口径一致登记 P2-5；**抽屉（Modal 480）简化为详情内嵌装配——偏差登记论证（P2-3）**：详情区一次性承载槽位清单/候选按钮/拆卸/点数，装配动作即时反馈（wh/replaced + toast），无需 Modal 开合状态机，功能面无损失；F2 modal 基建留给 F4/F5（排位/结算）复用。 |
| 3 | **reducer/effects** | ✅ PASS | wh/tab 只改 activeTab、wh/select 只改 selected（新增双向断言：tab 不清 select、select 不建 tab）✓；两 action 均无 effect（runEffect 白名单无 → resolve）✓；doDisassemble 共用（wh/disassemble ≡ wh/take）且 toast 文案按 action.type 区分 ✓；wh/assemble/wh/take 成功 → wh/replaced 整体替换（后端权威）+ store/save ✓；box/open 失败 → box/done(!ok) 复位 + toast；network 异常 → code 'network' 不抛 ✓；客户端落盘双路径（wh/replaced ∈ SAVE_ON_ACTIONS + 显式 store/save——F2 P2-19 同构延续，P2-14）。 |
| 4 | **布局合规（verifyLayout）** | ⚠️ FAIL→P1×3（已修） | 修复前实测：warehouse 已装槽态 `wh_take_0 overlap wh_slot_1`、长候选 11 连对 overlap、真实形状（4 槽+8 插件）12 项 overlap + 内容到 y672（面板底 580 之下，mythic 7 槽 → 720 clip）；gacha 空态 `gacha_panel∩gacha_empty`。修复后全绿：gacha 3 态 + warehouse 空态/已装/长候选/真实形状 5 夹具 verifyLayout 断言入测试锁死。**点数提示条 §6.3 y592..640 整条未实现——判定 P2（P2-2 论证）**：点数 X/Y 与槽位清单已内嵌详情面板（信息等价），条带为 spec 残留构件且与「满仓」网格无界增长（角色桶 21+ 件 → 行 6 y700+ clip）几何冲突；spec 应删/改写。另：gacha 结果卡行1 与中面板下部视交叠为 §6.4 坐标固有（grid y420 vs 面板 210..510，异 z 不被自检）——spec 坐标语义登记（P2-10 并）。 |
| 5 | **架构/铁律** | ✅ PASS | check-arch **39 文件 0 违规**（37+F3 新增 2=39，L7 依赖图完整）；public 全域 console 零命中（仅注释提及）✓；无裸导入/无 server import ✓；views 纯函数文件无 window ✓；L7 项 7 逐文件（新增 gacha 100/94.74/100、warehouse 100/91.55/100——行/分支/函数三阈达标；动态 import 两处仍不在依赖图——F2 P2-16 延续登记）。 |
| 6 | **覆盖与门禁** | ✅ PASS | `npm test` **503/503**（501+F3 新增 2：gacha verifyLayout 全绿组 + warehouse P1 回归夹具）；`npm run gate` **9 PASS/0 FAIL/0 PEND**（项 7=503 用例+五目录阈值；项 3=39 文件）；`npm run cov` 聚合 **97.78/87.77/96.50**（声明 97.78/87.67/96.50——分支 +0.1 系新增断言迁移，P2-15 登记）；check-arch 39 ✓；golden 18 tick ✓。 |
| 7 | **死代码/死分支** | ✅ PASS（两处死代码当批清除） | 原 `already` 三元臂（`s.pluginUid===p.uid` 在候选路径**恒假**——候选仅在空槽渲染且 candidatesFor 已排除 equipped；且其 `text:` opts 被 button() 忽略——双重死代码）已删简（后端 slot_occupied 为同病理兜底）；index.js 重复 `battle: null` 键（F2 遗留）已删；fallback 文案「（F2 进度）」批次引用已去。gacha 空态/忙态/结果 3 态全臂 ✓；warehouse 分支锤全臂 + 新守卫臂（行前断/按钮预检）均有断言 ✓；candidatesFor 全臂 ✓。 |
| 8 | **闭环真实性（探针）** | ✅ PASS | `.review-f3/probe-loop.js`：真 `server/box.js openBoxes(seed=157, common×5)` → effects box/open→box/done → 分桶（role:2+rolePlugin:3）→ gachaLayout 5 卡全绿（品质/名/kind 与真形一致）→ goto warehouse → 详情点数 0/3（真 pluginPoints）→ 候选 3（含 5 个错配按钮面）→ 真后端 assemble 落槽 + equipped=true 回带 → 再装 → **points_exceeded toast** → atk 槽装 special 插件 → **slot_type_mismatch toast** → wh/take 拆卸 → equipped=false 恢复候选 → 空态双屏全绿。7 段 PASS（复跑稳定）。 |

## 缺口自查（六项按任务逐条）

① **点数提示条（§6.3 y592..640 装配区职责）整条未实现——判定 P2（非 P1）**：信息等价物已内嵌详情（`点数 used/pluginPoints` + 槽位清单逐行）；条带若实现将与「满仓」无界网格（详见 P2-11）几何冲突；且 spec 自身冗余（条带内容=详情内容）。建议 spec 删条带或改写为「详情面板内含点数提示」（P2-2）。
② **tier_locked 段位置灰（§6.4）未实现——登记**：F3 无段位锁定感知面（radios 恒可选；tierInfo 未用于置灰且 boot 未接线 /unlock——F2 P2-7 承接）；现行为：后端 409 `tier_locked` → toast 呈现 ✓（链路完整）。F4 接线 unlock 后补置灰。
③ **gacha 结果动画 120ms 逐张 + render.box + toast「新物品 xN」缺失——登记 F7**（§6.4 场景行；现为一次性整列渲染）。
④ **候选按钮无「已用点数」感知——登记**（候选装 1 个后第 2 个仍显示；后端 `points_exceeded` 兜底，探针实证拒绝码 toast 链路与「拒绝后状态不变」——无损坏无静默失败）。
⑤ **gacha 结果卡点击无目标（无 wh/select 联动）——登记**（F4/F7：卡片点击 → wh/select + goto warehouse 并定位）。
⑥ **抽屉（Modal 480）简化为内嵌——登记论证（P2-3）**：与 F2 modal 基建对齐判断——内嵌完成装配交互全量且反馈即时，免状态机；Modal 基座留给 F4/F5。

## 发现的问题

### P0

无。

### P1（必须当批修复——已全部修复并复测）

1. **P1-1 详情区按钮堆叠几何：候选/拆卸按钮（h=32）在 30px 步距下逐对重叠 2px + 已装槽 take 与下一槽行相交**。verifyLayout 实测：≥2 候选的任意选中态 `wh_put_x0_i overlap wh_put_x1_i`（11 连对），已装槽+后随槽 `wh_take_0 overlap wh_slot_1`——mount 每次 paint 自检 → **正常游戏态下每帧 ui.layout warn 噪声**（F2 P1-3 同型教训）；且每个按钮底部 2px 命中区被下一按钮覆盖（不可点）。**修复**：详情区按钮步距 BTN_PITCH = SIZES.buttonGhost.h+2 = 34（行距 24 vs 行高 20 无交同理）；测试补 verifyLayout 全绿断言（已装态/长候选态）。
2. **P1-2 真实数据形状下详情内容溢出面板/视口**。后端 qualities.json roleSlotRange 最高 **[5,7]**（mythic），实测 4 槽角色+8 插件：槽行/候选按钮渲染到 y672（面板底 580 之下 92px），mythic 7 槽 + 大桶 → 行越出 720 → clip。原 `if (sy > 560) break` 为**后置**检查（先推后查）且**不防多槽接力**。**修复**：DETAIL_MAX_Y=560 常量 + 槽行前断（`sy+20>560` 停行）+ 候选**预检**（`sy+32>560` 先查后推）；回归测试用真实形状夹具（4 槽+8 插件）断言全盒底 ≤580 且 verifyLayout 全绿。
3. **P1-3 gacha 空态提示盒与中面板同层相交**。gacha_empty（z0, 96..696×420..444）∩ gacha_panel（z0, 400..880×210..510）——verifyLayout 每帧误报 + 文本压在面板下部。**修复**：空态盒移至结果区 y560（面板底 510 之下）；测试补 3 态 verifyLayout 全绿断言（10 件两行/忙态/空态）。

### P2 登记（不阻塞；建议随批落实项以 ★ 标注）

1. **spec §6.4 散文 vs 实现：段位「下拉」→ 5 radio、次数「Field 1..10」→ 快选 1/5/10**：任务书 F3 交付物口径（radio+快选）先于 spec 散文；spec 场景行待改。★
2. **点数提示条 y592..640 未实现（缺口①判定）**：信息已内嵌详情面板；条带冗余且与无界网格冲突；建议 spec 删条带或改写为详情内嵌口径。★
3. **装配抽屉 Modal 480 → 详情内嵌（缺口⑥判定）**：内嵌承载装配交互全量（槽/候选/拆卸/点数/toast 即时反馈），功能面无损失；Modal 基建留 F4/F5 复用；spec §6.3 wh/assemble 场景行待改写。★
4. **candidatesFor 无 slot 类型/点数预算预过滤（spec「本地按 slot/点数/门控过滤」未落）**：真数据实证——seed157 的 3 候选 × 3 槽产生 5 个错配按钮面（按槽错配率 ~86%：插件 7 槽型 vs 槽型 1）——装配点击多数被后端拒绝，toast 链路完整但提示面噪音大；建议 F4 补 `p.slot === slot.type` 预过滤（低风险高收益）。★
5. **详情候选截断无提示（DETAIL_MAX_Y=560 上限）**：与任务口径一致登记；建议截断处加「+N」徽标（F7 打磨）。
6. **skill 物品无 pluginPoints 字段（B18 契约面）→ 详情显示「点数 x/0」**：显示兜底可接受；建议按 kind 区分展示（技能行省略点数）。
7. **gacha 结果动画 120ms 逐张 / render.box 日志 / toast「新物品 xN」（缺口③）→ F7**。
8. **tier_locked 置灰（缺口②）**：需 tierInfo 感知（boot 未接线 /unlock，F2 P2-7 承接）；现 toast 兜底。
9. **gacha 结果卡无点击目标（缺口⑤）**：F4/F7 联动 wh/select + 跳转仓库定位。
10. **gacha 结果卡行1 与中面板下部视交叠（§6.4 坐标固有：grid y420 vs 面板 210..510）**：spec 坐标本身相交（异 z 不被自检器捕获）；视觉可接受（面板下部为空），建议 spec 澄清或 F7 调 grid y→540。★
11. **仓库网格无界增长**：角色桶 21+ 件 → 行 6（y700+）clip；spec「满仓仅提示『建议整理』」未实现——F7 分页/上限 + 提示条（与 2 的条带规划合并考虑）。★
12. **已装槽全满极端（如 mythic 7 槽全装）最后 take 按钮约 8px 探出面板底**：拆卸可达性优先（不截断）；F7 可改双列/详情滚动。
13. **gacha 结果 >12 件第三行 clip**：times≤10 契约内不可达（box/done 恒 ≤10 件），防御登记。
14. **双落盘延续（F2 P2-19 同构）**：wh/replaced ∈ SAVE_ON_ACTIONS + 显式 store/save 双写，无危害，可去一。
15. **cov 声明值迁移**：修复+新增断言后分支 87.77 vs 声明 87.67（行/函数不变）——登记。
16. **check-arch 动态 import 扫描缺失（F2 P2-16）延续**：effects→views/settings、app→mount/index 仍不在依赖图。

## 实测

| 命令 | 期望 | 实际 | 结果 |
|---|---|---|---|
| `npm test` | 501/501 全绿 | **503 pass / 0 fail / 0 skip**（501+F3 新增 2：gacha 全绿组、warehouse P1 回归夹具；全量回归） | ✅ |
| `npm run gate` | 9 PASS/0 FAIL/0 PEND | **9 PASS / 0 FAIL / 0 PEND**（项 7=503 用例 + 五目录阈值；项 3=check-arch 39 文件；项 8=黄金 18 tick；修复后复跑稳定） | ✅ |
| `npm run cov` | 聚合与声明一致 | 聚合 **97.78 / 87.77 / 96.50**（声明 97.78/87.67/96.50——分支 +0.1 系新增断言，P2-15）；新增文件逐文件达标：gacha 100/94.74/100、warehouse 100/91.55/100、views/index 100/87.50/100 | ✅ |
| `node scripts/check-arch.js` | 39 文件 0 违规 | `[PASS] 39 个文件无依赖违规`（37+F3 新增 2） | ✅ |
| `node .audit/golden-battle.js` | 18 tick 一致 | **黄金战斗复算通过（18 tick, winner=p2, phase=role）** | ✅ |
| 探针 `.review-f3/probe-loop.js` | 真后端闭环 | 7 段 PASS：box/open×5（真 openBoxes）→ 分桶 → gachaLayout 5 卡全绿 → 详情点数 0/3 → 真 assemble 落槽（equipped=true 回带）→ points_exceeded toast → slot_type_mismatch toast → wh/take 拆卸（equipped=false 恢复）→ 空态双屏全绿；错误码 toast 文本 `wh/assemble: <code>` 实证 | ✅（P1 触发 + 闭环达成） |
| 手动 node -e verifyLayout | 修复前后对比 | 修复前：长候选 11 连对 overlap + 已装态 take∩slot_1 + 4 槽/8 插件 12 项 overlap 且内容达 y672；gacha 空态 panel∩empty；修复后五夹具全绿 | ✅（P1-1/2/3 定案） |

## 独立实证（非测试代码路径，可复跑 `.review-f3/probe-loop.js` 与上述 node -e）

① **探针（P1+闭环定案）**：真实 `server/box.js + server/core/items.js` 参与——前端 effect 链路（box/open→box/done、wh/assemble、wh/take）与后端真逻辑（openBoxes/assemble/disassemble）闭环；拒绝码 `points_exceeded`（2+3>3）、`slot_type_mismatch`（special→atk）均经 `ui/toast` 落列且拒绝后 wh 状态零变化；组装成功回带 `equipped=true`、拆卸 `equipped=false`（candidatesFor 依赖的双向契约实测成立）。② **verifyLayout 复算**：候选按钮几何 `y+32` vs 30px 步距 → 逐对 2px 相交（11 连对实证）；`sy>560` 后置 break 不阻多槽接力（槽行在 sy≥562 继续渲染）——修复后预检+34px 步距全绿。③ 数据真形：qualities.json roleSlotRange common[1,3]→mythic[5,7]（P1-2 溢出面）；role 物品含 pluginPoints/slots、skill 无 pluginPoints（P2-6）、rolePlugin 含 slot/pointCost 且初始无 equipped 字段（candidatesFor 缺省真值即放行——契约实测一致）。④ 死代码实证：`already` 三元在候选路径恒假（候选仅空槽渲染 + candidatesFor 排除 equipped）；且 button() 忽略 opts.text（`✓ ` 前缀永不渲染）。⑤ 架构 grep：public/js console 零命中（仅注释）、无裸导入/无 server import、views 纯函数无 window。

## 结论

**CONDITIONAL PASS（P1×3 必须当批修复后转 PASS；本批已全部修复并复测）**。交付物覆盖面与测度声明达成：503/503、gate 9 PASS/0 FAIL/0 PEND、cov 聚合 97.78/87.77/96.50（新增文件三阈达标）、check-arch 39 文件 0 违规、golden 18 tick 一致、public/js 全域无 console；闭环探针以**真后端形状与真后端裁决**贯通 gacha→warehouse→装配/拆卸/拒绝 toast 全链。三项 P1 均为「几何契约 vs 实现」断裂且均有 verifyLayout 实测/探针实证：详情区按钮 30px 步距 vs h32 重叠（每帧自检噪声 + 2px 死命中区）、真实数据形状溢出面板/视口（roleSlotRange [5,7] 实证）、gacha 空态与面板同层相交。P2 登记 15 项中 3 项带 ★ 建议随批落实（spec §6.4 散文改写、点数提示条 spec 清理 + 详情内嵌口径、candidatesFor 补 slot 预过滤）；六项缺口自查全部闭环（①点数条 P2 判定、②tier_locked 登记、③动画 F7、④点数感知登记+toast 兜底实证、⑤结果卡无目标登记、⑥抽屉内嵌论证）。复跑基线：`npm test && npm run gate`（串行）＋ `.review-f3/probe-loop.js` 全绿即为回归通过。

## 修复落实（实现侧，2026-09-17；P1×3 当批）

1. **P1-1 详情按钮步距**：`warehouse.js` 新增 `BTN_PITCH = SIZES.buttonGhost.h + 2`（34）；候选循环与已装槽 take 步距 30→34；删除后置 `if (sy > 560) break`。回归：warehouse.test 已装态/长候选态补 verifyLayout 全绿断言（原「≤600」断言收紧为 ≤560）。
2. **P1-2 真实形状溢出**：`warehouse.js` 新增 `DETAIL_MAX_Y = 560`（面板底 580 余 20px）；槽行前断（`sy+20>DETAIL_MAX_Y` 停行）+ 候选按钮预检（`sy+h>DETAIL_MAX_Y` 先查后推）。回归：新增「F3 P1 回归：真实数据形状（4 槽+8 插件）不越面板且自检全绿」用例（断言全盒底 ≤580）。
3. **P1-3 gacha 空态相交**：`gacha.js` 空态盒 y420→560（结果区，面板底之下）。回归：gacha.test 新增 verifyLayout 全绿组（10 件两行含次行坐标断言/忙态/空态）+ reducer 双向断言（tab 不清 select、select 不建 tab）。
4. **死代码清除（随批）**：`warehouse.js` 删 `already` 三元（恒假 + opts.text 被 button() 忽略之双重死代码）与冗余 `text:` opts；`views/index.js` 删重复 `battle: null` 键、fallback 文案去「（F2 进度）」批次引用。

复测：`npm test` 503/503 ✅；`npm run gate` 9 PASS/0 FAIL/0 PEND ✅（项 7=503）；`npm run cov` 聚合 97.78/87.77/96.50 ✅（gacha 100/94.74/100、warehouse 100/91.55/100）；`node scripts/check-arch.js` 39 文件 0 违规 ✅；`node .audit/golden-battle.js` 18 tick 一致 ✅；`.review-f3/probe-loop.js` 7 段 PASS 复跑稳定 ✅。