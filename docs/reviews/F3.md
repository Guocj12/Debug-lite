# F3 审查与走查记录：主界面 / 用户 / 仓库 / 开箱 / 设置 / 出战配置

> 批次：**`F3`**（P6 第三批；命名见 `docs/frontend/00-rules.md` FR-6，**不并入 §6 的「共 41 批」**）
> 日期：2026-09-22（提交① 后端契约落地）/ 2026-09-24（提交② 前端主界面线）
> 设计依据：`docs/frontend/03-hub-warehouse-loadout.md`（冻结稿 + §15 实现对账）＋ `docs/decisions.md` **D-159…D-162**
> 结论：**提交① 机器核对 PASS；代码级审查（独立上下文）见 §3；浏览器人工走查 —— 待执行（§4）**

---

## 1. 交付物

| 提交 | 层 | 文件 | 状态 |
|---|---|---|---|
| ① 后端契约 | 决策/接口 | `docs/decisions.md`（§14.3 D-159…D-162）、`docs/interfaces.md`（§1/§2/§2.1/§4/§5/§6/§7） | ✅ 已提交（`0a2e27e`） |
| ① | 存储 | `server/store/{archive,adapter-json,ledger,journal,errors,config,index}.js`（仓库/AI 段、`ARCHIVE_VERSION=2`+迁移、5 类新记录、`NON_COMPACTABLE`、上限、新门面） | ✅ |
| ① | 生成 | **`server/starter.js`（新）** + `scripts/check-arch.js` 分层登记 | ✅ |
| ① | 门面/HTTP/编排 | `server/account.js`、`server/index.js`、`server/box.js`、`cli/index.js`、`server/data/{service-config.json,schema.js}` | ✅ |
| ① | 测试 | 新增 6 个文件 **34 条**（starter 7 / 仓库 7 / 崩溃恢复 3 / 开箱 5 / AI 库 6 / 配置完整性 6）+ 既有语料迁移（18 个测试文件 + 3 个 helper/脚本） | ✅ |
| ① | 设计稿 | `docs/frontend/00-rules.md`（FR-10…FR-13、O-13…O-16）、`docs/frontend/03-hub-warehouse-loadout.md`（新，含 §15 实现对账） | ✅ |
| ② 前端主界面线 | 前端 | `public/{index.html,store,format,render,actions,api,app,contract}.js`（**仍 9 个文件**）；hub/profile/warehouse/box/settings + 4 空页 + 弹窗机制；F1 契约变更（`home`→`profile`、登出移入设置） | 🔄 实现中（详见 §6 登记） |
| ② | 机器核对 | `tests/frontend/*`（UI-1/UI-2/AU-1/FC-* 口径更新 + 新屏断言） | 🔄 |
| ③ 出战配置编辑器 | 前端 | 出战配置弹窗（模板替换/装配/空位/仓库 `usage` 标记） | ⏳ 未开始 |

> **提交③ 不在提交② 范围内**：提交② 里「出战配置 1/2/3」只是**占位弹窗**（`尚未实现（计划批次 F3-③）` + 关闭），与用户口径一致（分册 §14）。

---

## 2. 机器证据（收口实跑）

| 命令 | 结果 |
|---|---|
| `npm test`（提交① 后） | ✅ **1024 通过 / 0 失败** |
| `npm run gate`（提交① 后） | ✅ **9 PASS / 0 FAIL / 0 PEND**（覆盖率逐文件达标） |
| `node scripts/check-docs.js` | ✅ PASS（批次计数仍 **41**，未新增批次号） |
| `node scripts/check-arch.js` | ✅ PASS（**40** 文件） |
| `node scripts/e2e.js`（`npm run e2e`） | ✅ **exit 0，22/22**（含新增 D-159-R1 回放回归；连跑 7 次稳定） |
| `node scripts/load-test.js --players 20 --deep` | ✅ **ok=true，7/7 完整性断言**（无 5xx、双方均为真实注册玩家） |
| 提交② 前端 | 🔄 见 §6（完成后在本节补实测行） |

---

## 3. 代码级审查（D-150：功能完整度 / 空实现 / 冲突重合 / 副作用回归）

### 3.1 提交①（后端契约）—— **由独立上下文审查者出具**

- 审查方式：**对抗式独立审查**（新上下文子代理，只读、不改文件），按 D-150 四维度逐条核对，并被要求**真的跑探针**（进程内真服务 + 真实 HTTP）覆盖：装配边界（双装/点数/越界/悬空/拆卸后重装/同槽并发）、幂等与重放（`rebuildArchive` 重复、装配→拆卸→装配三段重建、**开箱 >256 次后从 journal 重建不丢物品**）、上限原子性（500 / 101）、v1 档案迁移（老账号不得意外获得 starter）、不带镜像保存后的回放（含**进程重启**后）、归档一致性（`usage` ↔ `loads[]` 引用逐 uid 对账、`counts` ↔ 四桶长度）。
- **审查结论与发现**：见 §3.2（待独立审查者报告回填）。

### 3.2 独立审查者结论（**已出具**：新上下文子代理，只读、零仓库改动；探针在 `%TEMP%\f3review\`）

**结论：未发现高危真缺陷；2 条真缺陷（1 中 + 1 低）+ 4 条疑似/含糊；核心链路实测全部成立。**

| 编号 | 严重度 | 发现 | 处置 |
|---|---|---|---|
| **F-1** | 中 | `createPlayerArchive` 的"显式 loadout"路径用 `o.warehouse` 校验并冻结快照，却只把 `starter.warehouse` 落档 → **校验镜像 ≠ 落档镜像**，该配置的插件引用在档案侧永久悬空，之后连"重存同一份配置"都 409 | **已采纳并修复**（一行：`warehouse: starter ? starter.warehouse : (o.warehouse \|\| undefined)`）+ 回归 **WI-1** |
| **F-2** | 低 | `box.opened` 防御分支超限时**静默丢弃**却仍 `changed:true` 并推进水位 → journal 与档案永久漂移（正常路径由 `grantBox` 前置拒绝，实测 409 原子零写入；仅"档案与 journal 不一致"时可达） | **已采纳并修复**：逐件 `error` 记录（带 grantId/uid/桶）+ `dropped`/`droppedUids` 落进 `grantIds` 条目（差额可审计；不抛错以免 journal 重放永久失败）+ 回归 **WI-3** |
| 疑似 1 | 中 | 直接替换槽位引用时未复位旧插件的 `equipped`（HTTP 被 `slot_occupied` 拦死，不可达） | **已加固**（替换分支复位旧插件）+ 回归 **WI-2** |
| 疑似 2 | 低 | `applyCheckpoint` 硬编码 `starterIssued:true` | **已核实为口径一致**（与 `applyRecordToArchive` 的直接 apply 路径同源），不改 |
| 疑似 3 | 低 | v1 迁移后老账号"可玩但配置存不回"（409 悬挂引用） | **设计有意**（D-159② 老账号保持空仓）；details 已逐条给出 `悬挂引用 <uid>`（可判定）；登记为分册 **N-11** |
| 疑似 4 | 低 | `usage` 覆盖全部配置，而 D-160④ 措辞易读成"仅出战"；`/me/box` 满仓 409 会消耗一个 boxSeed 序号且不回带 seed | **已修文档口径**（`decisions.md` D-160④ / D-162 与 `interfaces.md` §7 各补一句澄清） |
| 疑似 5 | 低 | 无物品删除/分解端点 → **桶满 500 后玩家无法自行解封** | **登记为产品级待决事项**（分册 **N-10**）：需用户拍板是否新增删除/分解能力或把上限改软 |

**已核实无问题（审查者逐条实测，摘要）**：500 上限三条写路径全部生效且**原子**（498+3→409、seq 与计数不变；times=100 混批整批拒）；AI 第 101 条 409 `ai_limit`、删出战引用 409 `ai_in_use`；`usage` ↔ `/me/configs` 逐 uid 双推一致、`counts` = 四桶长度；**320 次开箱**溢出 `grantIds`(256) 后 rebuild 逐值一致、×3 不翻倍、320 件零丢失；装→拆→装三段记录 rebuild 逐值一致（含 `equipped`）；同槽并发装配 [200,409] 且自洽；插件当目标/越界槽/悬空拆卸的拒绝码与 core 口径一致；v1 迁移（含 v1 形态 journal 重放）可用、`starterIssued=false`、不误发 starter；不带 `warehouse` 保存后回放**重启前后均 200**；admin bot 建档仍可打且可被抽中；`DL_LEGACY_STATELESS=0` 下新端点不受影响；`start({boxSeed})` 第 n 次 = base+n−1；starter **300 身份**抽样零契约违反、**跨 3 进程内容级指纹一致**；`check-docs`/`check-arch` PASS。

**测试质量评价**：数值化程度高（逐桶计数、增量对账、重启后逐值、指纹比对）、失败路径与重启用例齐全。**该测而未测（已由本轮补齐 5 条）**：`grantIds` 窗口溢出（**WI-4**，300 次开箱）、同槽并发装配（**WI-5**）、插件装第二处 → `plugin_equipped`（含在 WI-5 的不变量里）、`grantBox` 混合批次上限、`createPlayerArchive` 显式 loadout+warehouse（**WI-1**）、替换槽位时的 `equipped` 自洽（**WI-2**）、`box.opened` 防御分支差额（**WI-3**）。新增回归网：`tests/unit/warehouse-invariants.test.js`（WI-1…WI-5）。

**盲区声明（审查者原文要点）**：未跑完整 `npm test`/`gate`/`e2e`；未测多进程并发、真实崩溃（SIGKILL／半写截断／磁盘满）、Windows 文件占用异常；未测容量与 1 万玩家性能；未测生产 scrypt 参数与限速的组合；未做前端联调与分册逐句对齐；sqlite 适配器新方法仍为 `notImpl` 占位（既有约定）；**当前无物品删除端点**（审查者探针均通过直接改档绕过）。

### 3.3 实现期自查已发现并修掉的真缺陷（5 条，见分册 §15.3）

1. 装配/拆卸未落档 `equipped` → 拆卸不可逆 + 新装插件无法进任何出战配置（**D-159 闭环曾完全走不通**）。
2. 归档回放重算只取快照子集 → 按 D-159「保存不传镜像」时该场回放**永久 410**。
3. `boxSeed` 注入缝差一（注释/文档 vs 实现）。
4. 显式非法 seed 被静默忽略（已收紧为 400 `bad_seed`；`null`/`undefined` 才算未提供）。
5. `PUT /me/warehouse` 回执字段语义分叉（补 `archiveUnverifiedLoadout`）。

### 3.4 独立审查后续处置汇总（**7 条已闭环，2 条登记待决**）

| 来源 | 条目 | 状态 |
|---|---|---|
| 自查 | equipped 未落档 / 回放仓库来源 / boxSeed 差一 / 非法 seed 静默 / 回执字段分叉 | ✅ 已修（5/5） |
| 独立审查 | F-1 显式 loadout 丢 warehouse | ✅ 已修 + WI-1 |
| 独立审查 | F-2 防御分支静默漂移 | ✅ 已修 + WI-3 |
| 独立审查 | 疑似1 替换未复位 equipped | ✅ 已加固 + WI-2 |
| 独立审查 | 疑似2 `applyCheckpoint` 硬编码 | ✅ 已核实为口径一致（不改） |
| 独立审查 | 疑似3 老账号配置存不回 | 📌 登记（分册 N-11；设计有意） |
| 独立审查 | 疑似4 文档措辞（usage / boxSeed 序号） | ✅ 已修文档 |
| 独立审查 | 疑似5 **桶满后无解封手段** | ⏳ **产品级待决**（分册 N-10，需用户拍板） |
| 独立审查 | N-9 "读→校验→写"非原子（last-write-wins） | 📌 登记（分册 N-9；终态自洽，单客户端无影响） |

### 3.4 登记未修（既有缺陷，超出本批范围）

- `server/core/effects.js` 的 effect uid 为**进程级自增** → 归档回放帧只做到「除 `eff_N` 外逐字节一致」（D-90/D-91 口径被弱化）。旧默认配置无持续效果词条掩盖了它，D-159 的 starter 带 `castEffect`/`hitEffect` 后暴露。建议修法见分册 §15.4 **N-7**（uid 改为按对局确定 + 重锚黄金战斗）。

---

## 4. 人工走查（总纲 §4.4）—— **状态：待执行**

**F1 + F2 + F3 一次收口**（用户 2026-09-22 裁定："与 F3 并行，F3 收口时一起走查"）。

前置：

```powershell
# 管理员/调试相关（F2 走查需要；F3 走查只需普通账号）
$env:DL_ADMIN_USERS='<你的用户名>'; $env:DL_ADMIN_TOKEN='<可选令牌>'; $env:DL_DEBUG_BOTS='1'
npm start          # 浏览器打开 http://127.0.0.1:3000/
```

| 批次 | 剧本 | 步数 | 状态 |
|---|---|---|---|
| `F1` | `docs/frontend/01-auth.md` §11 | 四屏（登录/注册/主页/改密） | ☐ 待执行 |
| `F2` | `docs/frontend/02-accounts.md` §11 | 17 步（管理面板/分页/删除/封禁/bots） | ☐ 待执行 |
| `F3` | `docs/frontend/03-hub-warehouse-loadout.md` §11 | 25 步（主界面→仓库→开箱→设置→空页→F2 回归） | ☐ 待执行 |

**未走查前 `F1`/`F2`/`F3` 均不得判定"可玩"**（两轮历史失败均发生在测试全绿时）。走查完成后在本节填写：执行人 / 日期 / 浏览器版本 / 逐步实测文本 / 结论。

> **走查注意（本次契约变更带来的现场差异）**：
> - 新号**注册即 3 个配置槽**（slot1 出战中 + slot2/slot3 空槽），且仓库里**已有 starter 物品**（不再是空仓库）——与 F1/F2 时代的界面预期不同。
> - 老账号（如早期 `test01`）**没有 starter**（D-159：迁移只补空仓）→ 走查仓库/开箱请用**新注册账号**，或先删号重注册（F2 管理面板可删）。
> - 「登出」不在用户详情页，**只在设置屏**（D-160 相关口径见分册 §9）。

---

## 5. 与设计稿的偏离

提交① 的 6 条偏离逐条记于分册 **§15.2**（以该节为准）；提交② 完成后在此追加"实现 vs 分册 §3/§4/§5"的对账（做法同 F2 分册 §13）。

---

## 6. 提交② 交付与登记项（2026-09-24）

### 6.1 交付物（前端主界面线）

| 项 | 结果 |
|---|---|
| 屏 | `hub` / `profile`（F1 `home` 降级）/ `warehouse` / `box` / `settings` + 4 空页（`quick`/`tournament`/`leaderboard`/`ai-editor`）+ F1 的登录/注册/改密 + F2 的 `admin`/`accounts`（共 12 屏）；**登录/注册/启动自检落点改为 `hub`**（FR-11）；`logout` **只在 `settings`**，`profile` 无登出 |
| 动作注册表 | **42** 个（F1 9 + F2 16 + 提交② 17）；非管理员态渲染 **26**（= 42 − 16 管理动作）；提交③ 的 9 个编辑器动作**未注册空壳**（保证"注册表 ↔ 渲染"双向断言仍有判别力） |
| 弹窗机制 | `state.modal`（屏内区块）；背景元素 `data-action="modal-close"` + 显式「关闭」；`view.go` 清弹窗；至多一个（分册 §3.8 / FR-10） |
| 空页规范 | `EMPTY_PAGES` + `emptyPageViewModel`：标题 + `尚未实现（计划批次 F5/F6/F7）` + 仅「返回主界面」；**零请求**（FR-12） |
| 出战配置 1/2/3 | 本批为**占位弹窗**（`config-open` 纯本地 → `config-open` 弹窗显示 `尚未实现（计划批次 F3-③）`）；编辑器属提交③ |
| 字段契约 | `contract.js` 新增信封路径 9 条（`me/nickname`、`me/warehouse`（四桶/`usage`/`caps`）、`me/box`（`items`/`times`）、`me/ai`（`items`）），**每条来自真起服务的实测抓取**；另登记「明确不读」4 条（`data.seed`/`maxSlots`/`caps.max`/`data.warehouse`）并纳入 FC-3/FC-4 双向核对；新增 `ITEM_DETAIL_FIELDS`（34 条）与「契约 == 分册 §5.1+§5.3 == `format.js` 实读」三方核对（WH-11） |

### 6.2 机器核对（提交② 收口实跑）

| 命令 | 结果 |
|---|---|
| `node --test --test-isolation=none "tests/frontend/*.test.js"` | ✅ **73/73**（原 60 + 新 `hub-warehouse-flow` 13） |
| `npm test` | ✅ **1042 通过 / 0 失败**（含并行会话的 WI 5 条） |
| `npm run gate` | ✅ **9 PASS / 0 FAIL / 0 PEND** |
| `check-docs` / `check-arch` | ✅ PASS（批次仍 41；40 文件） |

### 6.3 既有断言的改写（等价或更强，依据已写进测试注释）

`UI-2`（非管理员态 9 → **26**，双向 + 管理动作一个不出现，取样扩到 14 屏 + 弹窗子态）｜`UI-4`（3 → 5 个 `data-enter`，并新增"提交按钮动作 == 所属表单动作"）｜`UI-5`（禁用态扩到全部屏 + 弹窗子态）｜`AU-1`（白名单 25 → **42**）｜`AU-2/AU-3/AU-6`（`'已登录'`→`'Debug-Lite'`、`home`→`hub`、`返回主页`→`返回主界面`）｜`FC-1`（端点分组 +4，真实 HTTP）｜`FC-3`（两分册并集 → **三分册并集 == 契约 ∪ 不读清单（互斥）**）｜`FL-1/FL-2/FL-11`（落点与请求计数、`homeLines`→`detailLines`、profile 无登出）｜`AF-1/AF-2`（`home`→`hub`）。

### 6.4 登记项

| # | 事项 | 处置 |
|---|---|---|
| R-1 | **进入开箱屏时取一次 `GET /me/warehouse`**（否则"满仓禁用且不发请求"无法本地判定，分册 §3.4 的禁用语义落空） | ✅ 已按规格实现（仅进入时一次，不轮询；开箱成功后刷新本地仓库） |
| R-2 | **提交③ 未做**：三个配置按钮为占位弹窗；9 个编辑器动作未注册 | ⏳ 下一提交（本文件 §7 记录） |
| R-3 | `public/**` 仍未纳入 `check-arch.js` 的 L7 登记（总纲 O-7 剩余） | 以 `tests/frontend/*` 的"单一出口 / 单一写入点 / 投影单一真源"断言代偿 |
| R-4 | effect uid 进程级自增（D-90/D-91 口径弱化） | 📌 登记未修（§3.4 / 分册 N-7） |
| R-5 | **人工浏览器走查未执行**（本会话无浏览器；已做无头等价逐屏走查：摘要行/仓库 `[装配于配置1]`/详情弹窗/点背景关闭/开箱结果/空页/设置） | ⏳ 待有浏览器者按分册 §11 填写（§4） |
| R-6 | **既有 flaky（非本批引入）** `tests/api/api-ranked.test.js` P2-5「5 场全平局」 | ✅ **已确定性化**：D-159 后 starter 种子 = `sha256('starter\|publicId\|playerId')`，随机身份使"至少一场分出胜负"变成概率断言 → 改为**固定 publicId + playerId**（实测 3 次结果恒定 `["loss","loss","loss","win","win"]`、AI 程序 4 种），断言本意不变；`tests/helpers/http.js` 的 `register` 增加 `publicId`/`playerId` 透传 |
| R-7 | 后端口径核对 | ✅ 未发现契约不符（7 个端点与分册 §5.1/§5.2 逐字段一致，含 `usage`/`caps`/无 seed 入参） |

---

## 7. 提交③ 交付与登记项（2026-09-25）

> 设计与实现对账见 `docs/frontend/03-hub-warehouse-loadout.md` **§15.7**（本节只记交付/证据/登记，不重复设计论证）。

### 7.1 交付物（出战配置编辑器）

| 项 | 结果 |
|---|---|
| 两级弹窗 | 弹窗 A = 编辑器（角色模板 / 角色插槽**按实际数量与类型** / 技能1–3 及其插槽 / 战斗AI；**每个位置都是可点按钮，`空` 也可点**）；弹窗 B = `slot-pick`（同分类 + `空`；**出战中的配置不提供 `空`**）/ `plugin-pick`（**全部列出**、类型不符 → 置灰 + 行内写原因 + 另有「清空此槽」）/ `ai-pick`（AI 库条目）；仍是**至多一个** `state.modal` |
| 动作注册表 | **51** 个（F1 9 + F2 16 + 提交② 17 + 提交③ 9）；非管理员态渲染 **35**（= 51 − 16 管理动作）。UI-2/AU-1 均按**实际注册表**双向核对（数字不写死） |
| ⚠️ 两步顺序 | `plugin-set`/`plugin-clear` = ① `POST /me/warehouse/assemble\|disassemble`（改**仓库里那件物品**）→ ② 用响应回带的 `data.warehouse` 取回**更新后的那件物品**替换草稿 → ③ 点「保存」才 `PUT /me/configs/:slotId`；替换模板**没有**任何"先拆插件"的清理代码（B-7） |
| 草稿语义 | 所有编辑先落 `state.configs.draft`（`dirty` 标记）；「关闭 / 点背景」一律关弹窗 + 丢弃草稿；`dirty` 时点「设为出战」被本地前置拦下并提示先保存（超设计稿行为，登记 R-11） |
| 字段契约 | `contract.js` 新增 `me/configs` 两行 + `me/warehouse/assemble` 三行（`data.warehouse` 由「明确不读」**移入**正式契约）+ `CONFIG_SLOT_FIELDS`/`AI_ITEM_FIELDS`；`data.maxSlots`/`data.caps.max` 仍留「不读」并写明**为何不需要**（新建/删除槽不在本批；AI 满库由服务端 409 兜底） |
| 完整性判据 | 前端镜像 `format.missingOf` 与服务端 `archive.loadoutMissingOf` **逐规则一致**、文案与 `loadoutMissingDetails` **逐字一致**；镜像**只**服务状态行与前置提示，**不**决定是否发请求（是否 409 一律由服务端判定） |
| 转义不变量（补前批盲区） | 新增 `tests/frontend/render-escaping.test.js`（ESC-1…ESC-5）：逐插值点恶意载荷、属性逃逸、`esc()` 字符表、**真实 HTTP 证明服务端不消毒**（昵称只校验 1~16 字符、AI 名只校验 1~24，字符集均不限）→ 转义是唯一防线；结构断言 `render.js` 不碰 DOM（与 `public/app.js:109` 唯一 `innerHTML` 互为印证） |

### 7.2 机器核对（提交③ 收口实跑，2026-09-25；由**父代理独立复跑**，非实现者自报）

| 命令 | 结果 |
|---|---|
| `node --test --test-isolation=none "tests/frontend/*.test.js"` | ✅ **89/89**（HEAD 73 + 本批 CF 11 + ESC 5） |
| `npm test`（连跑两轮：一次显式 + 一次 gate 项 7） | ✅ **1058 通过 / 0 失败**（两轮一致） |
| `npm run gate` | ✅ **9 PASS / 0 FAIL / 0 PEND**（项 7 = 1058 用例，digest `a1c5b11c0092`；项 3 = **40** 文件无依赖违规） |
| `check-docs` / `check-arch` | ✅ PASS（批次仍 **41**，未新增批次号）/ ✅ PASS（**40** 文件） |
| `node scripts/e2e.js` | ✅ **exit 0，22/22 检查点** |
| `node scripts/load-test.js --players 20 --deep` | ✅ **ok=true，7/7 完整性断言**（220 场积分恒等、无 5xx；数据根在 `os.tmpdir()` 且跑完删除，用户 `runtime/` 不受影响） |
| 去 flaky 复核 | ✅ `config-editor-flow.test.js` **连跑 5 次 = 5×11/11**（CF 夹具在需要处注入确定性物品，不赌随机 starter 内容） |
| 动作注册表**独立复算**（父代理探针，非本测试自证） | ✅ 注册表 **51**（F1 9 + F2 16 + F3 26）；管理动作 **16**；非管理 **35**；我**自己**枚举 14 屏 + 5 类弹窗子态所得渲染集合与注册表**双向相等**、管理动作零泄漏、**无死动作/无不可达动作** |
| 「按钮永不无声」+ 畸形输入**独立探针**（12 种畸形/敌意 payload × 11 个动作 × 2 种会话态 = 264 次调用） | ✅ **抛异常 0**（收尾期修掉 R-8）；静默调用仅 `modal-close` 在**无弹窗**时（12×2 = 24 次）——该状态在 UI 里**不可达**（`modal-close` 只出现在弹窗内部），属探针假阳性 |
| 前端覆盖率（`public/**` **不在**门禁四目录内，故仅作诊断） | `format.js` 行 98.74 / `render.js` 99.33 / `contract.js` 99.43 / `store.js` 97.49 / `api.js` 97.30 / `actions.js` 93.44；**分支**偏低：`app.js` 53.01、`actions.js` 67.45（未覆盖集中在防御性早返回分支，登记 R-12） |

### 7.3 既有断言的改写（**全部为加强**，无删除、无 `assert.ok(true)`；依据写进测试注释）

`UI-2`（非管理员态渲染 26 → **35**；取样集合扩到 14 屏 + **5 类**弹窗子态：`config`/`slot-pick`/`plugin-pick`/`ai-pick`/`item-detail`——`slot-set`/`ai-set`/`plugin-set`/`plugin-clear` 只在二级弹窗出现，不纳入就会漏判）｜`AU-1`（`F3_ACTIONS` 17 → **26**，`adminRenderings()` 同步 +3 个弹窗子态）｜`FC-1`（新增 `GET /me/configs` 与一次**真实** `POST /me/warehouse/assemble`（注入确定性夹具 `injectAssemblable`）两个宿主响应分组）｜`WH-8`（占位弹窗断言 → **真编辑器**断言：状态行含 slotId + `CONFIG_DRAFT_HINT` + 草稿已灌入；关闭用例改为**先造脏草稿**再断言"关闭即丢弃 + `dirty` 归零"）｜`ESC-1`/`ESC-2`（补入弹窗候选行这一新插值点、按钮计数 5 → 6）。

### 7.4 登记项

| # | 事项 | 处置 |
|---|---|---|
| R-8 | **`plugin-pick` / `plugin-set`/`plugin-clear` 在 `payload === undefined` 时抛 TypeError**（代码写作 `payload === null ? null : payload.idx`，只挡了 `null`） | ✅ **已修**（收尾期）：两处（`warehouseChange` 与 `plugin-pick`）改为 `payload === null \|\| payload === undefined ? …`。**当前不可达**（`public/app.js:138` 的 `def.run(ctx, payload \|\| null)` 是唯一调用点，已把 `undefined` 归一为 `null`；测试夹具 `payload \|\| null` 同）。之所以仍修：加固点消失后第二个调用点会立刻复活它（属于"单点归一"依赖） |
| R-9 | `format.js` 三处**玩家可见** `hint` 混入 Markdown 强调符（`选中即替换**草稿**`） | ✅ **已修**（父代理自查）：本 UI 无 Markdown 渲染器（`00-rules.md` §1 只允许 按钮/文字/输入框），玩家会原样看到 `**草稿**` → 改为纯文本。**这条正是"机器测试 ≠ 能玩"的实例**：1057 条用例全绿时它依然存在 |
| R-10 | **二级弹窗的「关闭/点背景」按分册 §3.8 字面执行 = 关闭整个弹窗 + 丢弃整个草稿**（编辑中途误点背景会丢失全部未保存改动） | 📌 **登记待裁定**（分册 N-12）：另一种读法是"取消本次选择并回到编辑器"（可复用 `modal-close`、无需新动作，但需改 §3.8 与 WH-8/CF-7 的断言语义） |
| R-11 | **偏差**：`config-activate` 增加客户端前置"草稿 `dirty` 时先保存再设为出战"（设计稿 §4 无此条） | 📌 **登记待裁定**（分册 N-13）：不加会出现"看着是新的、出战的是旧的"（`activate` 作用在服务端那份上）；若要求严格照稿则在 CF-6 中删掉该前置断言 |
| R-12 | `public/**` 不在覆盖率门禁四目录（`server/core`/`server/ai`/`shared`/`cli`）内，故新前端代码**不受覆盖率门禁约束** | 📌 登记（分册 N-15）：行覆盖 93–99%，但分支最低 53%（`app.js`）——是否把 `public/**` 纳入门禁（或单列前端阈值）属工程口解决策 |
| R-13 | 完整性判据**两份实现**（服务端 + 前端镜像，含 5 条中文文案） | 📌 登记（分册 N-14）：当前逐规则/逐字一致（本次收口核对过），但服务端改文案时前端不报错；缓解 = 错误路径优先用服务端 `error.details[].message` |
| R-14 | 后端口径核对 | ✅ **未发现契约不符**：装配/拆卸体、`slot_type_mismatch`/`item_missing`/`slot_occupied`/`slot_empty`、非出战槽不完整 200、`activate` 409 `cannot_activate_incomplete`、清空插件 200 —— 逐条与分册 §5/§6 一致（证据 = CF-4/CF-6/CF-10 真实 HTTP） |
| R-15 | **人工浏览器走查**（分册 §11 步 10–21：编辑器逐位置、两级弹窗、保存/设为出战、仓库 `[装配于配置N]` 同步） | ⏳ **待有浏览器者执行**（本会话无浏览器）。走查提醒见分册 §15.7 末尾：先保存再设为出战、二级弹窗**别点外面**、出战中的配置无「空」、步 14/15 的 `[装配于配置N]` 时点 |

### 7.5 独立审查（2026-09-25，新上下文子代理；只读、零仓库改动，探针在 `%TEMP%\probe\`）

**审查者结论：有条件通过（有需修缺陷）** —— 编辑器核心四条（两步装配顺序 / 本地草稿 / B-5+B-8 门控 / 51+35 计数）经真实 HTTP 验证成立；7 项声称逐条核对（5 成立、1 部分成立、1 因父代理同时改 `docs/**` 而作废）。发现 **2 条需修 + 3 条疑似 + 3 条记录**；逐条处置已写进分册 **§15.7** 的"独立审查与收口修复"表，此处只留结论与索引。

| 编号 | 类型 | 一句话 | 处置 |
|---|---|---|---|
| **F-1** | **需修** | 取消弹窗会被 in-flight 响应撤销（弹窗复活 + 草稿已丢弃 ⇒ 界面/提示/仓库三方矛盾） | ✅ 新增条件 reducer `modal.setIfOpen` + 回归 **CF-11** |
| **F-2** | **需修** | `loadout.aiId` 未登记进契约，而"三方一致"断言是**单向**的（直接属性访问不进 `pick()` 集合）⇒ 字段漂移可让"删除被引用的 AI"静默放行 | ✅ 新增 `CONFIG_LOADOUT_FIELDS` + §5.1 登记 + **CF-8 源码级双向断言** |
| F-3 | 疑似 | 取消不回滚两步顺序已造成的仓库改动，且无提示 | ✅ 成功文案写明作用对象（`已写入仓库那件物品；点「保存」后才进这份配置`） |
| F-4 | 疑似 | `points_exceeded` 等文案缺"需要 x/剩余 y"（§6 表格要求），数字拿不到 | 📌 登记 **N-17**（需后端在 details 里补 need/left） |
| F-5 | 疑似（不可达） | `missingSummaryText` 对 `path===null` 会抛 | ✅ 已加固（防御写法） |
| F-6 | 疑似 | 「清空此槽」在出战中配置也渲染，与 D-160 措辞看似冲突 | ✅ 口径澄清：D-160 的"不能拆卸"指**模板位置**；插槽插件可清空（服务端 200） |
| F-7 | 记录 | §11 步 14 按字面**无法通过**（`usage` 只统计已保存配置） | ✅ 剧本改正：步 14 看编辑器、**步 15（保存后）**才看 `[装配于配置N]` |
| F-8 | 记录 | 常量重复、注释把 CF-8 误写成 CF-10、§7 状态形状与实现不符 | ✅ 已订正 |

**审查者独立复跑并确认**：`npm test` 1057/0、`gate` 9 PASS、`check-docs`/`check-arch` PASS（**父代理随后改过 `docs/**`，已按审查者要求重跑 `check-docs` = PASS**）；`missingOf` 与服务端 `loadoutMissingOf` 在 25 组输入上逐规则 + 逐文案一致；草稿全链路无就地改写；`dirty` 四个归零点正确；§6 的 7 个失败码全部翻成可读文案；转义面完整（含新增 `data-pos`/`data-idx`/`data-ai-id`）；`public/` 仍 9 文件、单一 `fetch`/`innerHTML`/`pick(` 出口成立；无填充式断言、无删除断言（唯一删除项被 3 条更强断言取代）。

**审查者的残余不确定性**：① ~~`check-docs` 需在 docs 收尾后重跑~~ → **已重跑 PASS**；② F-1 的真实浏览器触发窗口未实测（审查者用假 api 延迟做确定性复现；**父代理已补 CF-11 用 60ms 延迟 + 期间点关闭的确定性回归**）；③ 人工走查仍未执行（§4）；④ `points_exceeded` 的 x/y 是否本可由前端已知量算出未展开 → 已并入 N-17。

### 7.6 父代理独立复核（与审查者并行、互不依赖）

- **动作注册表独立复算**（自写探针）：注册表 51 = 9 + 16 + 26；管理动作 16；非管理 35；**自己**枚举 14 屏 + 5 类弹窗子态得到的渲染集合与注册表双向相等、管理动作零泄漏、无死动作/不可达动作。
- **「按钮永不无声」+ 畸形输入探针**：12 种畸形/敌意 payload × 11 个动作 × 2 种会话态 = **264 次调用 → 抛异常 0**（收尾期修掉 R-8）；唯一"静默"是 `modal-close` 在**无弹窗**时（UI 不可达：该动作只出现在弹窗内部）。
- **转义不变量**：ESC-1…ESC-5（含**真实 HTTP 证明服务端不消毒**昵称/AI 名 ⇒ 转义是唯一防线）。
- **CF 稳定性**：`config-editor-flow` 连跑 5 次 = 5×10/10（收尾新增 CF-11 后为 11 条）。
