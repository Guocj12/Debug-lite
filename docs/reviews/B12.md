# B12 审查记录

> 批次：B12（P2：ai/ast.js 结构校验 + 覆盖型 fixtures）
> 审查方式：独立上下文 agent（结构契约核对 + random 盲区实证 + 接口名对齐验证 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS（修复 2 个 P1 后）**

## 审查范围

新增 `server/ai/ast.js`（validateProgram/collectUsedNodeTypes/nodePathOf/limits/NODE_TYPES + withLogger；事件 ai.validate）、`tests/fixtures/ai-programs.json`（覆盖型 17 类型 + A-1/A-2/A-5）、`tests/unit/ast.test.js`（13 测试 AF-*）。

## 核对结果

| 项目 | 结果 |
|---|---|
| 结构契约：17 白名单（16 语义 + seq）、字段类型表、深度 30/33 边界、节点/字节上限、危险键（含根）、body 必须 seq（D-100）、version | ✅ PASS |
| 路径 id：body.s[i]/then/else/body/.expr 与 A-6h 复现一致；同引用两次一致；表达式段 .expr 稳定 | ✅ PASS |
| collectUsedNodeTypes 去重保序；B13 段位门控复用就绪 | ✅ PASS |
| fixtures：17 类型齐；A-1 逐 tick 与文档一致；A-2/A-5 直接可复用 | ✅ PASS |
| 日志矩阵：ai.validate(debug) 通道 ai.ast 与 §4.6 L5 行一致 | ✅ PASS |
| 测试：AF-* 过 §3.3 黑名单（边界 32/33、2001、256KB+1、失败路径、无镜像） | ✅ PASS |
| 铁律：L2 / L9（cl 豁免 LIMITS 撞值）/ L11 / L17；check-arch（ai 仅依赖 shared/log） | ✅ PASS |
| 实测：npm test 298/298；cov ast.js 94.87/91.00/100；gate 9 PASS/0 FAIL/0 PEND | ✅ PASS |

## 已修复的问题（当批）

1. **P1-1 random 分支校验盲区**：childList 未覆盖 random.then/else → 分支内未知节点/危险键/计数/深度/路径全可绕过（A-10g/h 失效）→ childList 按 if 同构纳入 random 分支；AF-13 锁定（eval/危险键/收集/路径/自引用环 ai_cycle）。
2. **P1-2 接口名偏离冻结接口**：validateStructure/collectNodeTypes/LIMITS → `validateProgram`/`collectUsedNodeTypes`/`limits`（interfaces §1 三方一致）；事件 `ai.ast.validate` → `ai.validate`（§4.6 L5 行）。
3. **P2-2 自引用环**：visit 加 visited Set → `ai_cycle` 错误（不栈溢出）。
4. **P3**：删除 makeAstFinish 占位；FIELD_CHECKS 未使用的 number/bool 检查分支删除。

## 登记取向

- 路径段规范：seq→s[i]、if/random→then/else、loop/function→body、表达式→.expr（文档 A-6h 的 .body 段仅在 then 为容器时出现）。
- 结构校验与合法性（B13 分支 action 规则）/段位门控分离；空 body 合法（运行时 wait 兜底）；B4 unlock 的旧路径方言（body.statements[i]）由 B13 整合替换。

## 遗留

- P2-1（事件名互斥）已随接口对齐消解；B13（合法性检测 + 段位门控 + ai.validate.reject）。