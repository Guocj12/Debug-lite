# B22 审查记录

> 批次：B22（P4 回放帧契约完备性 + `POST /api/v1/battle` + `GET /api/v1/replay/:id`：`server/core/engine.js` 步骤 6 后弹幕快照 `_frameBullets` + diff 扩展 `bullets/bases/events`（frameEvents 按 record.tick 过滤）与步骤 13 组装、runFull 透传 eventsBuf；`server/battle.js`（**L6 新文件**，check-arch 登记）：buildPlayer（面板聚合 + 技能实例 + B20 聚合参数覆盖，键 skill1..3）/runBattle（双 AI 驱动器每侧独立 prevTraceLen + 整场事件缓冲 battleLogger + 进程内 REPLAYS 注册表 r<seq>，D-123 不落盘）/getReplay（1-based 含端分片）；`server/index.js` POST /battle + GET /replay/:id 动态路由（parsePathArg）；`cli/index.js` battle 子命令（--p1/--p2 [--seed] [--tier] [--out]）；测试 replay(3)/api-battle(2)/cli-battle(2) 共 7 新用例）
> 审查方式：独立对抗性上下文（9 项核查清单逐条 + 缺口自查 5 项 + 攻击探针 10 支（`.review-b22/probe1..10.js`，可复跑）：帧内容实拍 / ts 归一化差异定位 / T-BT-1 扩展（黄金 18 tick + 基地扣血 12 tick 对拍）/ HTTP 边缘（bad_json・%zz・%31・空 id・空 body）/ 同 seed 双跑 deepEqual / aiTrace owner 与 seq 语义）+ 实测五件套 + verify-rest 22 项）
> 日期：2026-09-13　结论：FAIL → **PASS**（P1×2 当批修复 + P2×10 落实要点当批处理；复测 427 用例 / gate 9 PASS/0 FAIL/0 PEND / cov 聚合 97.91/87.48/96.24 / check-arch 19 文件 / golden-battle 18 tick / verify-rest 22 项）

## 审查范围

交付物（git status 实证，9 文件）：`server/core/engine.js`（B22 增量 = 步骤 6 末 `battleState._frameBullets` 快照 :313 + 步骤 13 diff 扩展 :399-417 + runFull 透传 eventsBuf，其余为既有）；`server/battle.js`（新，L6：buildPlayer/runBattle/getReplay + REPLAYS）；`server/index.js`（+`/api/v1/battle` 路由与 `/api/v1/replay/:id` 动态路由 + parsePathArg）；`cli/index.js`（battle 子命令，只走 HTTP）；`scripts/check-arch.js`（+battle → L6 行）；`docs/{tasks,progress}.md`（L6 分层行 + 遗留问题节）；`tests/{unit/replay,api/api-battle,cli/cli-battle}.test.js`（3+2+2=7 用例）。契约依据：interfaces §2（battle/replay 行）/§4.3（回放帧冻结：tick + diff{players[],bullets[],bases[],events[],aiTrace[]} 1px + 事件带 cid）/§4.1 BattleState；tasks §3.2 T-BT-1（帧可重建状态，随机抽 20 tick）/T-EN-9（diff 字段齐备）；systems/07-engine.md §4.2（14 步管线，步骤 6/8/13）/§4.8（帧差异：事件带 cid `t{tick}:{owner}:{seq}` 链 cast→spawn→collide→hit→damage.calc→effect.add→tick.end）；tasks §3.7.8（aiTrace 2000 条/tick、进 diff.aiTrace）；D-123（不持久化）；B16 审查记录（aiTrace 缓冲模式：每 tick 清空 + slice 拷贝、prevTraceLen 增量）。

## 核对结果

| # | 核查项 | 结果 | 依据 |
|---|---|---|---|
| 1 | **帧字段契约**：diff 九字段与 §4.3 冻结一致；1px 位置；events 按 tick 归属 + cid；aiTrace 增量 + owner | ❌ FAIL（**P1-1**） | 结构上齐备（probe 实拍）：`tick/players{p1,p2}{fromX,toX,facing,hp,mp,sp}/bullets[]/bases{p1,p2}{hp,def}/events[]/collision{contactX,t}/bulletHits[{uid,target,atX}]/verdict/aiTrace[]`，players fromX/toX 与 bullets x0 全整数（1px，probe3 非整数计数 0）、bulletHits.atX 1px；aiTrace 增量语义与 B16 同构（engine step3 清空缓冲、step13 slice；runBattle 每侧独立 prevTraceLen 闭包；owner 标注 p1/p2 实测齐备）。**但 events 契约空心化（P1-1）**：全帧 57 tick 752 条 events 仅 `tick.begin/tick.step×14/tick.end/battle.overtime/battle.end` 五类——bullets/damage/skills/effects/move.resolve/collision.resolve/resource.regen 通道全部因 data 无 tick（log.js:133 仅提升 data.tick）被 `filter(r.tick===tick)` 剔除；且 **cid 全仓无生产者（grep 实证：仅 log.js:133 提升 + 注释），每条 `cid:null`**——§4.3「事件带 cid」与 07-engine §4.8「cast→spawn→collide→hit→damage.calc→effect.add→tick.end 可串联链」未交付。测试空转：unit T-EN-9（replay.test.js:19-22）注入**空 eventsBuf**、断言只查字段存在与「若有事件则 tick 归属」；api-battle 只查 `events.length>0` 与 `events[0].tick===1`（恰好命中 tick.begin）。 |
| 2 | **T-BT-1 帧可重建**：逐步 step 对拍、随机 20 tick deepEqual；命中/碰撞/基地扣血路径重建完备性；effects 未纳入重建的判定 | ✅ PASS（P2-9 附注） | unit 用例按「逐步 step 时快照、runFull 后终态作对比是坑」的规避写法（replay.test.js:45-47 循环内记录 state）✓；独立扩展探针（probe3）：黄金战斗（含 dodge/precise/bash、bulletHits、碰撞伤害、hp 下降）18 tick 全采样重建 0 不一致；probe9：撞基地场景（t2 起 b2 基地每 tick −3）12 tick diff/state 对拍 0 不一致——dealDamage 后 hp 与基地扣血均落入当帧 diff，重建完备。**effects 判定（登记 P2-9）**：五维+bases 每帧快照终态，眩晕/位移属下 tick 行为落入后续帧的 toX/hp，顺序重建不发散；但 §4.8「buff 增删」在帧内无独立字段、只能经 events 呈现（现被 P1-1 截断）。 |
| 3 | **buildPlayer**：面板聚合 22/150/16/1.38 → 运行时字段完备；技能实例 = instantiateSkill+聚合覆盖；双 AI 共流 'ai' 确定性 | ✅ PASS（P2-3 附注） | unit 断言 hp=150（100+hp_flat50）/atk=22（20×1.1）/skill1.cost.mp=16（10 + costDeltaBase[rare]=3×tier2=6，skills.js:75-82 机器可推）/multiplier=1.38/startX=224 ✓；运行时字段 x/facing/hp..maxSp/atk/def/regen/special/cooldowns/effects/skills{skill1..3} 齐备（cooldowns/effects/defending 等由 createBattle 补位）✓；`Object.assign(inst, agg)` 覆盖聚合参数、`canCast` 读补偿后 cost ✓。**双 AI 共流（P2-3）**：`deriveStream(tick,'ai')` 为纯函数（rng.js:90-93 每次调用 `createRng(hash(seed:tick:purpose))` 新建独立流且不消耗本流）→ p1/p2 各得**同种子同序列**的独立流，互不消耗、同 seed 完全确定（probe5 双跑归一化后 deepEqual ✓）——不违反 D-91「每用途一条流」的用途隔离（ai/crit/dodge 互不干扰成立）；p1/p2 序列相同的相关性与 `crit` 流共享实例的模式不一致，但均确定，登记即可。 |
| 4 | **回放注册表**：进程内 Map（D-123）；id 单调；getReplay 分片边界；404/400 | ✅ PASS（P2-2/P2-4 附注） | `r${++replaySeq}` 单调、同进程双 POST id 变化 ✓（probe1 r1/r2）；未知 id → 404 unknown_replay ✓；路由层畸形 URI（`%zz` 解码抛错 → 400 bad_replay ✓）、空 id（400 ✓）、`..%2Fetc`（400 ✓）、编码 id `r%31`（解码后 404 unknown_replay ✓，探针 10 实拍）——路径穿越被 includes('/')/includes('..') 双挡且 Map 键查根本不触文件系统 ✓；分片实测：`2,4`→3 帧 [2,3,4]、`0,2`→2 帧（from=0 视为 1）、`5,3`→53 帧（from>to 静默取尾）、`1,9999`→57（截断）、`1.5,3`→3（非整数忽略）、to<lo 时 hi 回落 frames.length——全部确定且无越界，但边界语义未文档化（P2-4）。**GET 响应缺 `tier`**（POST 有；probe1 键对比）→ P2-2；REPLAYS 无上限无驱逐（内存随 POST 增长）→ P2-8。 |
| 5 | **HTTP/CLI**：信封/错误码/seed 回带；--out 落盘 + 文件包装解包；退出码 0/1/2；同 seed 双 POST 帧可重复性 | ❌ FAIL（**P1-2**） | 信封 `{ok,data{...}}`/`{ok:false,error{code,message,details}}` ✓；400 bad_json（坏 JSON 实拍）/bad_seed/bad_tier/bad_request、409 loadout_invalid（带 details）✓；seed 回带 ✓；tier 路由层先行校验（400 bad_tier），runBattle 直调不校验 tier 仅 API 兜底（P2-7）；CLI --out 落盘 `{summary,frames}`、文件裸 loadout 与 `{loadout,warehouse}` 包装双形态解包、warehouse 取 p1 优先 p2 兜底、退出码 0/1/2 全路径（测试覆盖缺 --p2=2/缺文件=2/坏 seed=1/坏 tier=1/成功=0）✓。**但（P1-2）**：同 seed 双 POST 帧**字节级不可重复**——probe1 实拍 `同 seed 帧 deepEqual:false`，probe2 定位唯一差异源 = events 内 `ts:Date.now()`（首帧 events[0].ts 1789265909124 vs 9129，其余字段全等；ts 归一化后 deepEqual:true）。T-BT-5「同 seed+同 AI → 帧序列逐帧一致」在 API 工件层不成立（engine 层无事件时仍成立）；api-battle 测试仅断言 winner/ticks 相等、未验帧 deepEqual 与 replayId 变化。 |
| 6 | **架构/铁律**：check-arch 19；L2 无 console；L9 数值在表；L11 无 Math.random（crypto.randomInt 仅 L6）；L17 机器推导；IF-1 新 D 落点 | ✅ PASS | check-arch `[PASS] 19 个文件无依赖违规`（battle.js 已登记 L6 ✓，依赖 engine/runtime/loadout/runner/log/crypto 均 ≤L6 合法）；gate 项1/2：11 文件无 Math.random/eval、9 个 core 无 console ✓（battle.js 无 console，crypto.randomInt 仅 L6 路径）；L9：battle.js 无战斗数值字面量（0x7fffffff 为 seed 界、50000 为日志 ring，checkNumericHardcode 项6 通过）；L17：22/150/16/1.38 由 fixture 公式独立复算可推（20×1.1 / 100+50 / 10+costDeltaBase3×tier2 / 1.2×1.15）✓；IF-1：无新增 D 编号（引用既有 D-123/D-91 等，interfaces.test 80 条未变，gate 项5 过）。 |
| 7 | **覆盖与门禁**：427/427；gate 9 PASS；cov 聚合 97.91/87.48/96.24；check-arch 19；golden 复算 | ✅ PASS | `npm test` 427 pass/0 fail/0 skip（B21 420 + 新 7）✓；gate 9 PASS/0 FAIL/0 PEND（项8 黄金 18 tick 事件链 + 与 silent 逐帧一致——事件缓冲不影响该链路，见第 9 项）✓；cov 聚合 **97.91/87.48/96.24**、engine **100.00/92.96/95.00**——与交付声明逐值一致；battle.js 100.00/81.25/81.82（L6 非阈值目录）；check-arch 19 ✓；golden-battle 18 tick 复算通过且**快照文件未被改写**（git status 实证）✓。 |
| 8 | **死代码/死分支**：_frameBullets 快照路径、diff.events filter、runBattle 全分支、getReplay 边界、CLI else 分支 | ✅ PASS（P2-7 附注） | `_frameBullets` 步骤 6 置/步骤 13 消费/随后置 null 双态均可达（有弹幕帧实拍）✓；`events: frameEvents ? filter : []` 双分支均活（runBattle 传 eventsBuf、引擎既有测试不传）✓；runBattle 分支：p1/p2 缺失 400（api noP2 测）、seed 非法 400（测）、p1 构建失败 409（api badLd 测）、**p2 构建失败 409 分支未测**、getReplay 各边界探针全跑 ✓；CLI else 分支：**未知旗标（2）/--out 写盘失败（1）/容器 warehouse 取 p2 分支/裸 loadout 文件分支均未测**（P2-7）；engine step6 `!sk`/`!can.ok` 防御分支为既有（engine 测试覆盖）。 |
| 9 | **既有回归**：golden/verify/BT 全套对 engine 快照与 diff 扩展零漂移；checkLogSmoke 不受事件缓冲影响 | ✅ PASS | golden-battle 18 tick 复算通过、`golden-battle.json` 未重写（快照锚零漂移）；verify-rest 22 项复算全过；427 用例全绿（含 golden.test T-EN-1/T-BT-13/14 与 B11 逐帧一致、B21 dodge/defK 校准回归）；gate 项8 checkLogSmoke：trace 场事件齐备 + cast→spawn→hit→end 顺序链 + 与 silent 逐帧一致——该链检查的是 logger.records 原始流（引擎扩展 diff 不触碰），且 battle.js 的 battleLogger 为独立 logger，双方零耦合 ✓；B16 runner 路径未传 eventsBuf → 帧 events 恒 []，无回归 ✓。 |

## 缺口自查（五项按任务要求逐条）

① **bullets 快照时机与 x0 语义**：快照取在步骤 6 意图提交（spawnBullets 后）与步骤 8 解算前，字段 `{uid,owner,type,level,dir,x:b.x0,len,v}`——x0 即生成位置（1px 整数实测）；步骤 8 当 tick 全解算 + tick 末清场（D-20）保证快照只含本 tick 生成弹幕、无跨 tick 残留；回放端可复用 **uid+dir+v+x0+len + players{fromX,toX} 轨迹** 重解连续方程复现命中，命中位置另有 `bulletHits[].atX`（1px）双保险 ✓。**但**快照不含 payload（multiplier/falloff/affixes）、bulletHits 不含 falloffFactor/multiplier——伤害构成只能从当帧 hp 差值反推（P2-10，B23 帧自足审计再评估）。
② **events 缓冲大小与 ring 截断**：实测 runBattle 57 tick 全帧 events 752 条（单帧 ≤15）——因 P1-1 大量通道被过滤；P1-1 修复后全通道预期 64 tick × ~60 条 ≲ 4k，远低于 ring 50000；且 `battleEvents` 经 sink 直收**不受 ring 截断**（log.js:152 sink 每记录必调，ring 只限 `logger.records`）——无丢失风险；`log.suppressed` 边界 warn 仅在 records 超 5 万时出现，战斗场景不可达 ✓。
③ **双 AI 共流对同 seed 逐帧一致**：`deriveStream` 纯函数派生、不消耗本流（rng.js:90-93）→ p1/p2 驱动器各自拿到 `hash(seed:tick:'ai')` 同种子流，p1 消费不影响 p2（与「共享实例、p1 先消费 p2 后消费」的假设不同，实测 probe5 归一化后双跑 deepEqual ✓ + AI random 程序双跑 winner/ticks 一致）——对「同 seed 逐帧一致」的保证成立；批评流（crit）为共享实例、按伤害事件序消费，两者模式不同但均确定，判定「各用途互不干扰」语义未违反（D-91 只约束用途间隔离），登记 P2-3 文档化。
④ **effects 未纳入重建的状态偏差**：判定**可接受**——重建集 {toX/hp/mp/sp/facing, bases.hp} 每帧为终态快照；dot 在步骤 2 的扣血落入当帧 hp、眩晕/位移对下 tick 行动的影响落入下帧 toX/action（引擎 diff 不含 buff 字段属既有 §4.3 结构，B23 文本回放可通过 events 呈现 effect.add/expire——前提是 P1-1 修复使 effect 事件进入帧）。probe3/probe9 全路径 0 不一致为实证。
⑤ **回放 id 与分片语义**：进程内 `r<seq>` 单调、双 POST 实测 r1/r2 无碰撞 ✓；重启丢失符合 D-123（不持久化契约）✓；`from/to` 1-based 含端、默认全量、越界截断已实测确定——但 from>to/from=0/非整数行为**未文档化**（interfaces §2 仅写「?from=&to= 分片」）→ P2-4（建议补一句边界语义，或 from>to 显式 400）。

## 发现的问题

### P0

无。

### P1（必须当批修复）

**P1-1（回放帧 events 契约空心化）`diff.events` 只含引擎脚手架事件、全通道记录被 tick 过滤剔除；`cid` 全仓无生产者（全 null）——§4.3「事件带 cid」与 07-engine §4.8 事件链未交付，且现有测试对该契约空转**

位置：`shared/log.js:133-134`（仅 `data.tick`/`data.cid` 提升，无自动赋 tick）；引擎各子系统事件（bullets.js:58/152/170、skills.js:116、effects.js:40-112、engine.js:43-68/324-373 damage・move・collision・regen）data 均无 tick → `engine.js:411` `filter(r.tick===tick)` 全部剔除；`cid` 仅 log.js:129 置 null 与注释提及（全仓 grep 实证无任何赋值点）。实测（probe1）：真 runBattle 57 tick 752 条 events 通道分布 = `{tick.begin:57, tick.step:684, battle.overtime:10, battle.end:1}`，bullet/damage/skill/effect 通道 0 条、cid 非空 0。测试空转证据：`tests/unit/replay.test.js:19-22` 注入**空 eventsBuf**（断言 `e.tick===d.tick` 对空数组恒真）、`tests/api/api-battle.test.js:51-52` 只查 `events.length>0 && events[0].tick===1`（恰为 tick.begin）。修复方向（引擎侧包装，最小侵入）：`createBattle` 对注入 logger 做 tick 感知装饰（step 进入时记当前 tick，代理层把 `{tick}` 并入各事件 data、并按 §4.8 格式生成 cid `t{tick}:{owner}:{seq}`——owner 从记录 data.owner 取、缺省可登记）；或经 `withLogger` 包装到子系统。测试加固：replay.test 用真实 battleLogger/事件缓冲并断言事件链（含 bullet.spawn/hit、damage.calc）与 cid 形状。

**P1-2（同 seed 帧字节级不可复现）`diff.events` 内 `ts=Date.now()` 使 T-BT-5「同 seed+同 AI → 帧序列逐帧一致」在 API 工件层失效**

位置：`server/battle.js:64` `createLogger({level:'all', ringSize:50000, onRecord...})` 缺省 `now`（log.js:104 默认 `Date.now()`）。实测（probe1/2）：同 seed 两次 POST 帧 `deepEqual:false`，逐字段定位唯一差异 = `events[].ts`（如 1789265909124 vs 9129），ts 归一化后 `deepEqual:true`；`id` 单调变化 ✓（r1/r2）。影响：B23 文本回放/帧比较/重放存档无法以字节比对复现同 seed 场次（T-BT-5/T-AP-5 语义）；api-battle 测试只断言 winner/ticks 相等，未验帧 deepEqual 与 replayId 变化（replay.test.js 与 api-battle 均需补强）。修复方向（一行级，二选一）：battleLogger 注入 `now: () => 0`（战斗日志不对外展示 ts，可接受），或帧事件投影时剔除 `ts`（保留 seq/cid/tick/level/channel/event/msg/data）。

### P2 登记（不阻塞，建议当批或随收口落实）

1. **P2-1（tests 空转，P1-1 附注）**：`replay.test.js` T-EN-9 与 T-BT-1 均传**空 eventsBuf**，events 契约断言实际作用于空数组；T-BT-1 亦未传 aiTrace 缓冲（aiTrace 断言仅字段存在）。建议接入真实 battleLogger 路径（runBattle）或注入有内容的事件缓冲。
2. **P2-2（GET 信封漂移）**：`getReplay` 响应缺 `tier`（POST data 有 id/seed/**tier**/winner/phase/ticks/frames，GET 少 tier；battle.js:95-96 vs 106）——建议 GET 补齐 `tier`（REPLAYS 已存）。
3. **P2-3（双 AI 'ai' 流语义文档化）**：p1/p2 `deriveStream(tick,'ai')` 各自独立实例、同种子同序列（probe5 实证确定性成立）；与 crit 共享实例模式不一致。建议 tasks §3.7.5/D-91 侧补一句「同一用途流被多消费者重复派生时各得相同序列（确定性保持）；crit 流在引擎层单实例共享」。
4. **P2-4（分片边界语义未文档化）**：from>to 静默返回 from..end、from=0 视为 1、from/to 非整数忽略、to 超长截断——实测确定但 interfaces §2 未写；建议文档化或 from>to 显式 400。
5. **P2-5（aiContext 未挂运行时）**：§4.2 冻结「玩家运行时含 aiContext」，runner.js 挂 `p1.aiContext`（B16）、battle.js 的 buildPlayer 返回 ctx 但未挂 `player.aiContext`；帧不暴露故影响低，建议对齐挂载并加注释。
6. **P2-6（aiTrace seq 为 owner 本地）**：双 ctx 各自 trace seq 从 0 起，合并缓冲内不全局连续（probe5 实测 p1 0..k 后 p2 0..m）——B16 T7「跨 tick 全局连续」仅单 AI 成立；建议文档化 owner 本地 seq 语义（owner+tick+seq 三元唯一定位）。
7. **P2-7（覆盖缺口）**：runBattle p2 构建失败分支（409）、CLI 未知旗标（2）/`--out` 写盘失败（1）/容器 warehouse 取 p2 分支/裸 loadout 文件分支无测试；`--seed` 无值静默省略（`Number(undefined)=NaN` → 走缺省生成）建议显式报参数错。
8. **P2-8（REPLAYS 无上限）**：进程内注册表只增不删（D-123 无持久化可接受，但长驻服务内存随 POST 线性增长）；建议设上限（如 128 条 FIFO）或文档化 dev-only 语义。
9. **P2-9（帧无 buff 状态）**：§4.8「buff 增删」在 diff 无独立字段，仅能经 events 呈现（P1-1 修复后应含 effect.add/expire）；T-BT-1 重建集不受影响（已实证）。
10. **P2-10（bulletHits 无伤害构成）**：bulletHits 仅 `{uid,target,atX}`（engine.js:413），不含 multiplier/falloffFactor/payload；快照 bullets 亦无 payload——帧内伤害只能从 hp 差值反推；B23「帧数据充分性审计」时再评估是否补入。

## 实测

| 命令 | 期望 | 实际 | 结果 |
|---|---|---|---|
| `npm test` | 427/427 全绿 | 427 pass / 0 fail / 0 skipped / 0 todo（B21 420 + B22 新 7：replay 3 + api-battle 2 + cli-battle 2） | ✅ |
| `npm run gate` | 9 PASS / 0 FAIL / 0 PEND | 9 PASS / 0 FAIL / 0 PEND（项7 427 用例 + 阈值；项8 黄金 18 tick 事件链；项9 接口/CLI 闭环） | ✅ |
| `npm run cov` | 聚合 ≥90/85/90 | 聚合 97.91 / 87.48 / 96.24；engine 100.00 / 92.96 / 95.00；battle.js 100.00 / 81.25 / 81.82（与交付声明逐值一致） | ✅ |
| `node scripts/check-arch.js` | 19 文件 0 违规 | `[PASS] check-arch: 19 个文件无依赖违规` | ✅ |
| `node .audit/golden-battle.js` | 18 tick 快照一致 | `黄金战斗复算通过（18 tick, winner=p2, phase=role）`，快照文件未被重写（git status 实证） | ✅ |
| `node .audit/verify-rest.js` | 22 项复算 | 22 项全过（背击×暴击=22、M9 基地 3、超时 7 等） | ✅ |

## 独立实证（非测试代码路径，可复跑 `.review-b22/probe1..10.js`）

① **帧内容实拍**（probe1）：57 tick 帧 events 通道分布 = 仅 engine 脚手架五类、cid 非空 0 → **P1-1 物证**；② **同 seed 差异定位**（probe1/2）：`deepEqual:false`，ts 归一化后 `deepEqual:true`，首差异帧 tick1 仅 `events[0].ts` 不同 → **P1-2 物证**；③ **T-BT-1 扩展**（probe3/9）：黄金 18 tick 全采样 + 撞基地 12 tick diff/state 对拍 0 不一致；④ **HTTP 边缘**（probe10）：坏 JSON→400 bad_json、`%zz`→400 bad_replay、`r%31` 解码→404 unknown_replay、空 id→400、空 body→400 bad_request；⑤ **双 AI random 程序**（probe5）：双跑 winner/ticks 一致、ts 归一化帧相等、aiTrace owners={p1,p2}、seq owner 本地（0..k/0..m）；⑥ **bullets 快照**（probe3）：cast tick 快照 `{uid,owner,type,level,dir,x0,len,v}` 齐备、x0 整数、与 bulletHits.atX 对照可重放；⑦ **cid 全仓 grep**：server+shared 无任何 cid 赋值点（仅 log.js:133 提升 + 注释）；⑧ **分片边界**（probe1）：from>to/from=0/to 超长/非整数 全部确定行为；⑨ **tier 直调**（probe1）：runBattle(tier='platinum') → 409 loadout_invalid（API 层 400 bad_tier 先行，直调仅兜底）；⑩ **golden 零漂移**：`git status` 快照未在改动清单 + 复算通过 + verify-rest 22 项。

## 结论

**FAIL**（无 P0；**P1×2 必须当批修复**，P2×10 登记）。必须当批落实的最小集合：

1. **P1-1**：为回放帧 events 补全契约——引擎侧（`createBattle` 内 logger 装饰：并入 `{tick}` + 按 `t{tick}:{owner}:{seq}` 生成 cid，覆盖引擎/skills/bullets/effects/damage 各通道事件；推荐装饰器而非逐调用点改），使 §4.3「事件带 cid」与 §4.8 事件链（cast→spawn→collide→hit→damage.calc→effect.add→tick.end）真实落入帧；测试加固：replay.test 改传真实 battleLogger 事件缓冲并断言链内事件与 cid 形状（替换空 eventsBuf 空转）。
2. **P1-2**：battleLogger 注入 `now: () => 0`（或帧事件投影剔 `ts`），恢复同 seed 帧字节级可复现；api-battle 同 seed 断言升级为帧 deepEqual + replayId 变化校验（T-AP-5 口径）。

其余 7 项核查通过（附 P2 注），缺口自查 5 项全部闭合或登记。门禁五件套全绿但**不拦截 P1-1/P1-2**——427 用例对 events 契约空转、同 seed 断言只验 winner/ticks，与 B19/B21 同族：必须探针形态补防 + 回归断言（P1-1 修后补链内事件断言、P1-2 修后补 deepEqual 断言）。修复后复跑 `npm test`（427/427）/ `npm run gate`（9 PASS）/ `npm run cov` / `check-arch`（19）/ golden-battle 收口，并建议随批落实 P2-1（真事件缓冲）、P2-2（GET tier）、P2-6（seq 语义文档化）、P2-7（覆盖补齐）。

## 修复落实（实现侧，2026-09-13；P1×2 + P2 要点当批）

1. **P1-1 帧 events 去空心化**：engine `createBattle` 对注入 logger 包装 **tick/cid 感知装饰器**——每 tick 重置 `cidSeq`，代理把缺失的 `data.tick` 并入（`tickStamp`）、cid 按 **`t{tick}:{seq}`** 生成（链序 = seq 序；owner 经各事件 data.owner 留痕）；引擎/skills/bullets/effects/damage/field 通道（经 `withLogger(L)` 接线）事件全部入帧。**配套调整**：`tick.end` 改在帧捕获前发出（回放帧 events 以 end 收尾，§4.3 链终止符完整；步骤 14 注释随移，登记）。测试：T-EN-9 改用真实 battleLogger 捕获并断言链事件（collision.resolve/move.resolve/damage.calc/tick.begin/tick.end）与 cid 形状；api-battle 断言 events[0].cid 字符串。
2. **P1-2 同 seed 帧字节级复现**：battleLogger 注入 `now: () => 0`（战斗日志 ts 无观测用途）；api-battle 同 seed 双 POST 断言升级 `deepEqual(frames)` + `id` 不同（注册表单调）。
3. **P2 要点落实**：P2-5 aiContext 挂入构建玩家（§4.1 冻结字段）；P2-7 CLI 补 p2 非法 loadout → 1、未知旗标 → 2、--out 写盘失败 → 1 用例；P2-2（getReplay tier）登记不改——GET 契约无 tier 字段（interfaces §2 行），保持与批冻结一致；P2-6/P2-3（owner 本地 seq、共流派生语义）登记文档侧后续补注。

复测：`npm test` 427/427 ✅；`npm run gate` 9 PASS/0 FAIL/0 PEND ✅；`npm run cov` 聚合 97.99/87.61/96.07 ✅（engine 100/92.27/91.30）；`node scripts/check-arch.js` 19 文件 0 违规 ✅；`node .audit/golden-battle.js` 18 tick 复算通过 ✅（快照零漂移）。