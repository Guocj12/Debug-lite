# F5 审查记录

> 批次：F5（P6 前端第五批：replay 画布 planFrame + 控制条 + HUD + 结算 + 播放状态机）
> 审查方式：独立对抗性上下文（9 项核查清单逐条 + 缺口自查 6 项 + 探针 `.review-f5/probe-replay.js`（真后端 listen(0) 双局起战 → planFrame 全帧图元快照（1px 域/字段映射/确定性）→ 播放状态机真实帧模拟推进（假 timers）→ 结算时刻/HUD 基地/aiTrace 实装字段 → mount 坐标注入+画布显隐（style 感知假 doc）→ 技能局子弹/直线局命中真帧投影 → paintCanvas stub ctx 全帧绘制）+ 实测六件套）
> 日期：2026-09-17　结论：**CONDITIONAL PASS→需修（P1×5 必须当批修复；已全部修复并复测绿）**：①播放中暂停按钮 action `replay/pause` 无 effect/reducer → **死按钮（播放无法暂停）**；②结算 Modal 时刻错误——result 存在即全屏遮罩 → battle/loaded 起 tick=0 控制条全程不可点，**回放不可用**；③`#battle` 画布骨架 `display:none` 且无任何坐标注入（.dl-box 坐标注入整体缺失——style.css 注释声明 JS 注入从未实现）→ **replay 画布与全部盒子在浏览器不可见**；④HUD 基地条缺失（bases 每帧可用）；⑤aiTrace 右栏按 t.name||t.type 渲染——实装字段为 {path,nodeType,result} → **行文本恒空转**。P2×11 登记；修复后复测基线 **516/516、gate 9 PASS/0 FAIL/0 PEND、cov 聚合 97.90/87.94/96.37、check-arch 43 文件 0 违规、golden 18 tick 一致**；探针 6 段全 PASS（复跑 2 次稳定）。

## 审查范围

交付物（git status 实证）：`public/js/render/planFrame.js`（新：FIELD_PX/CELL_PX/ACTOR_HALF + planFrame + clampTick）；`public/js/views/replay.js`（新）；`public/js/mount/canvas.js`（新：paintCanvas + PALETTE）；`public/js/store/effects.js`（改：replay/step|play|pause|speed + battle/pause）；`public/js/mount/index.js`（改：replay 屏 canvas 绘制 + render.frame 日志）；`public/js/ui/verify.js`（改：mask 递归后代豁免）；`public/js/views/index.js`（改：replay 注册）；测试 `tests/frontend/replay.test.js`（新）。契约依据：frontend-spec.md §6.6（canvas 16,80,1024,128 + HUD y84..112（含基地条）+ 控制条 16,220,1024,56（播放/暂停/步进/倍速 1/2/4/tick 滑块）+ aiTrace 右 1064,80,200,400 + 结算 Modal；播放状态机 1000/speed ms、末帧自动 pause + 结算；渲染层只读 frames 绝不重算）+ §7.1/7.2（s=1、planFrame 纯函数、z 层、绘制日志）+ §4.3（帧形状：players{p1,p2} 对象、tick 1-based——B22 冻结）；B22 沿革（帧 diff 字段实拍：players/bullets[{uid,owner,type,level,dir,x,len,v}]/bases{p1,p2}{hp,def}/collision{contactX,t}/bulletHits[{uid,target,atX}]/verdict{winner,phase}/aiTrace 每帧增量）；F4 沿革（§4.3 散文 players[] vs 实装对象偏差登记——**渲染层按实装**，本批即实装形状）。

## 核对结果

| # | 核查项 | 结果 | 依据 |
|---|---|---|---|
| 1 | **planFrame 与 B22 帧契约逐字段 + 1px 投影 + 无重算纪律** | ✅ PASS（P1-4/5 附注） | players{p1,p2}{fromX,toX,facing,hp,mp,sp}→player 图元 ✓（engine.js:422 实装核对）；bullets[]（实装 {owner,x, len,dir}）→bullet 图元 ✓；**bases{p1,p2}{hp,def} 每帧可用但未投影 → P1-4（HUD 补 base hp）**；bulletHits{target,atX}→hit ✓；collision{contactX,t}→collision ✓；verdict{winner,phase}→verdict 文本 ✓；aiTrace 每帧增量 → replayLayout 渲染（非 planFrame）✓。1px 投影实证：探针段1/5/6 全帧 INT 断言（toX/atX/contactX/bullet.x 全整数 0 违例）；无重算纪律：planFrame 仅常量投影+字段映射（FIELD_PX=1024/CELL_PX=64/ACTOR_HALF=32 与 battle-config.json fieldPx/cellPx/actorHalfPx **逐值一致**），零战斗公式（grep 核对）✓；clampTick 域封顶 0..n-1（null/undefined/0/delta 负分支锤）✓。 |
| 2 | **replayLayout 坐标/层级/verify 全绿 + 播放暂停语义 + 倍速样式 + aiTrace 尾 8 + 结算层级** | ❌ FAIL→P1-1/P1-2/P1-5（已修） | 坐标逐项核对：canvas(16,80,1024,128)✓、HUD y84 h24（84..108 ⊆ 84..112）✓、控制条(16,220,1024,56) 按钮 y228 h40（228..268 ⊆ 220..276）✓、倍速 416/520/624 步距 104 = ghost w96+8 防重叠 ✓（F5 实测修正确认）、aiTrace(1064,80,200,400) 行 w176 ⊆ 面板 ✓、Modal mask(0,0,1280,720) z90 fullscreen + 面板(400,260,480,200) z91 + 文本/按钮 z92（92>91>90 全合规）✓。verifyLayout 全绿（无结算/结算两态断言 + verify.js mask 递归后代豁免改进实证）✓。**P1-1：播放中按钮 action='replay/pause' 但 EFFECTS/reducer 均无此键 → 死按钮**；**P1-2：`b.result` 存在即弹遮罩 → battle/loaded 起 tick0 全屏覆盖，控制条不可点**（不符 §6.6「到最后一帧自动 pause + 结算」）；倍速样式 on/off ✓；aiTrace slice(-8) 尾 8 ✓（**P1-5：字段读 t.name||t.type 恒空——实装为 path/nodeType/result（runtime.js:133-134）**）；结算遮罩递归后代豁免 ✓。 |
| 3 | **播放状态机：防重入/末帧 pause/速度重启/speed 0/clamp/storedTimers** | ✅ PASS（P2-1/2 登记） | 防重入 `playbackTimer !== null → return` ✓；末帧自动 pause ✓（探针段2：28 帧真实帧 27 seeks 到达末帧后 playing=false）；速度播放中重启（clear+replay/play）✓；**speed=0：`(speed || 1)` 短路 → 1000ms 无除零**；负值 → max(50, -500)=50（20fps 兜底不崩）——reducer 未防 0/负 → **P2-1**；tick 越界 clampTick 0..n-1 ✓（reducer 直 set 不校验属 effects 层兜底模型，同 F4 口径）；storedTimers 兜底（缺省真 setInterval/clearInterval，node 真定时器不泄漏——battle/pause 在 null 时 clearInterval(null) no-op）✓；假 timers 全链注入 ✓（**审查加固：原 fake 不记录 id → clearInterval 断言空转——补 id 存根**）；跨 goto 泄漏判定 **P2-2**（见缺口③）。 |
| 4 | **paintCanvas：绘制序列与图元一一对应 + PALETTE 一致性 + 命中色 + s=1** | ✅ PASS（P2-3 登记） | 序列 clearRect→bg fillRect→中线 stroke→图元 fillRect/fillText 与 six 图元一一对应（单测 calls 序列断言 + 探针段5 全帧 216 fillRect 零异常）✓；PALETTE 与 tokens 逐值核对：bg/line/p1/p2/bullet/collision/verdict 全等 token（#0e1116/#30363d/#58a6ff/#f85149/#e6edf3/#f85149/#3fb950）✓；**hit #f0883e 无 token 对应 → P2-3**（注释已登记）；s=1：canvas 1024×128 与盒 1:1 ✓；no-ctx → {painted:false, reason:'no-ctx'} 跳过 ✓（单测+mount 分支）。 |
| 5 | **mount 接线：paint 内绘制不破坏 renders 纯函数纪律 + render.frame 日志 + 无 unhandled** | ❌ FAIL→P1-3（已修） | **P1-3 根因定案**：`<canvas id="battle">` 骨架 `display:none` 且无定位（index.html:13 实证）——paintCanvas 画出的内容浏览器不可见；且 `.dl-box` 坐标注入整体缺失（style.css:24 注释「坐标由布局层注入（JS 写 position/left/top/width/height）」从未实现——全仓 grep 实证）→ **F2~F5 所有屏的盒子在浏览器视觉堆叠于 (0,0)**。修复：mount paint 增加 `injectBoxGeom`（data-box-* → 元素几何，唯一注入点）+ `#battle` 显隐/定位（回放屏按画布盒 16,80,1024,128 显示，其余屏隐藏）。绘制在 mount 层（DOM 写入层）→ layout/render 纯函数纪律保持 ✓；render.frame 日志（canvas 分支 + paint 汇总）✓；无 unhandled（fake doc 三场景 + 探针段4 实证）✓；之后 mount/index 分支 100/100/100。 |
| 6 | **架构/铁律：check-arch 43；console/裸导入/server import；项 7 逐文件；效果层 import** | ✅ PASS | check-arch **43 文件 0 违规**（与声明一致；render/planFrame.js、views/replay.js、mount/canvas.js 经 public/ 通配登记 L7）✓；public 全域 console 零命中（仅 app.js 注释提及）✓；无裸导入/无 server import ✓；项 7 逐文件（审查复测）：planFrame.js **100/90.48/100**、replay.js **100/87.18/100**、canvas.js **100/91.67/100**、effects.js **100/86.90/93.10**、mount/index.js **100/96.36/100** —— 三阈全达标 ✓；effects→render/planFrame 同 L7 层内依赖、无环 ✓（check-arch cycle DFS 实证）。 |
| 7 | **覆盖与门禁** | ✅ PASS（经一次 FAIL 修复） | `npm test` **516/516**（515 + 审查新增 1 例「mount 盒注入+画布显隐回归锁」；0 fail/0 skip）；`npm run gate` **9 PASS/0 FAIL/0 PEND**（修复进程中一度 8/1 FAIL——mount/index 分支 73.68%<85 与 replay 分支 83.33%<85；补锤后全绿，**未放宽阈值**）；`npm run cov` 聚合 **97.90/87.94/96.37**；check-arch 43 ✓；golden 18 tick ✓（快照未改写，git status 实证）。 |
| 8 | **死代码/死分支：七测试口径 + playbackTimer 生命周期 + frame null/tick 越界** | ✅ PASS（净化附注） | 测试文件 5→**6 用例**（planFrame golden/clamp、布局 verify 两态、播放状态机假 timers 全链、paintCanvas stub、mount canvas 路径+分支、注入回归锁）全分支锤；playbackTimer：pause 后 null ✓ / 重复 play 新开 ✓ / 末帧自清 ✓；frame null / tick 越界（tick=5 越界帧）→ planFrame(undefined) 安全 ✓；**分支净化登记**：injectBoxGeom 删除结构性死防御（boxes||[]/?:/||0——collectBoxes 恒数组、num() 恒数字、mountApp 入口守卫）；假 timers id 存根加固（清定时器断言此前空转）。 |

## 缺口自查（六项按任务逐条）

① **HUD 基地条缺失——判定 P1 并已修**：§6.6 HUD 含「双方 hp/资源/基地条（y=84..112）」；实装只投影 hp/mp/sp 文本；bases{p1,p2}{hp,def} 每帧有数据（engine.js:427-430 实证）。修复：HUD 文本行补 `base ${hp}` 投影（探针段3 实证真帧 base hp=100 上屏）；「条状视觉 vs 文本行」形式差异登记 P2-4（F7 统一）。
② **speed=0/负值 reducer 防御——判定 P2（P2-1）**：`battle/speed` reducer 无校验；运行时 `(speed || 1)` 短路使 0 → 1000ms 无除零，负值 → max(50,·) 20fps 兜底不崩。UI 域仅 1/2/4 按钮，域外值仅测试面/改装可达。建议 reducer 夹取 [1,4]（F7）。
③ **播放中 goto 其它屏定时器泄漏——判定 P2（P2-2）**：实装路径分析实证——replay 屏无播放中导航按钮（shell 无 goto，结算 Modal 仅播放停止后出现）→ 无现实触发路径；回调自读最新 state + 末帧自清 → 泄漏有界且自愈。防御建议：EFFECTS['goto'] 统一清 playbackTimer（F7/后续批次）。
④ **aiTrace 增量语义——✅ PASS（P1-5 修复附注）**：diff.aiTrace 每帧 = 本 tick 新增（engine step3 清缓冲 + battle.js driver fresh slice + owner 标注实证）——渲染「本 tick 轨迹」尾 8 条语义正确；**但字段映射错**：实装 {tick,owner,seq,path,nodeType,phase,depth,result?}（runtime.js:133-134），原读 t.name||t.type 恒空 → 行文本空转（P1-5 修复 + 测试 fixture 按实装重写）。
⑤ **canvas 板凳/风格统一——登记（P2-4）**：HUD 为 DOM 文本行 vs §7.2 血条/资源条/基地条 canvas 描绘——风格统一归 F7；绘制器「禁止业务判断」纪律已守（paintCanvas 只执行图元）。
⑥ **结算「再来一局」保留 frames vs 清空——✅ PASS**：goto battle 保留旧帧，下一场 battle/loaded 整体覆盖（reducer:98-104 实证）+ playing 复位 ✓，无残留无泄漏。

## 发现的问题

### P0

无。

### P1（必须当批修复——已全部修复并复测）

1. **P1-1 暂停按钮死按钮：播放中按钮 action 为 `replay/pause`（§4.2 契约名），但 EFFECTS 与 reducer 均无此键 → 点击无任何效果（定时器不清、playing 不复位），播放无法暂停**。实证：replayLayout:32 action 翻转 + effects.js 键表无 'replay/pause' + reducer 无 case（default 原样返回）。**修复**：effects.js 增 `'replay/pause'` 别名（与 `battle/pause` 共用新提取的 `stopPlayback(ctx)`：清 playbackTimer + 复位 playing）。**回归锁**：播放状态机测试新增 replay/pause 断言（playing=false + 定时器 cleared）；假 timers 补 id 存根（此前 clearInterval 断言空转）。
2. **P1-2 结算 Modal 时刻错误：`b.result` 存在即渲染全屏遮罩 → battle/loaded 起 tick=0 遮罩即全屏覆盖，控制条（播放/暂停/步进/倍速）全程不可点，回放不可用**；不符 §6.6「到最后一帧自动 pause + 结算」。实证：battle/run → battle/loaded（result 恒有值）→ replay 屏 modal 恒在（mask z90 拦截全部点击；modal 无关闭按钮）。**修复**：replay.js 结算门控改为 `res && (frames.length === 0 || tick >= frames.length - 1)`（末帧弹结算；空帧 fail-safe 仍显示）。**回归锁**：布局测试新增「多帧+result tick0 不弹 / 末帧弹 / 空帧 fail-safe 弹」三断言。
3. **P1-3 画布与全部盒子浏览器不可见：`#battle` 画布骨架 `display:none`（index.html:13）且从未定位；且 `.dl-box` 坐标注入整体缺失**（style.css:24 注释「坐标由布局层注入（JS 写 position/left/top/width/height），CSS 只负责视觉」从未实现——全仓 JS/CSS grep 实证无任何注入代码）→ F2~F5 所有屏视觉堆叠于 (0,0)，F5 画布绘制不可见 = 本批核心交付失效。**修复**：mount/index.js paint 增 `injectBoxGeom(doc, boxes)`（data-box-* → 元素 position/left/top/width/height/zIndex，唯一注入点，无 style 桩元素安全跳过）+ `#battle` 显隐/定位接线（回放屏显示于画布盒 16,80,1024,128，其余屏 display:none）。**回归锁**：新增测试「mount 盒注入+画布显隐（style 感知假 doc）」：16px/80px/1024×128/z=1 + 非回放屏 hidden + 无 style 元素不抛 + 无参 mountApp 兜底。
4. **P1-4 HUD 基地条缺失**：§6.6 HUD 含基地条；bases{p1,p2}.hp 每帧可用但未投影（基地决胜/消耗在回放中不可见）。**修复**：replay.js HUD 文本行补 `base ${hp}`（bases 缺失时 `—` 兜底）。**回归锁**：布局测试补 base 断言 + players 在/bases 缺分支。
5. **P1-5 aiTrace 右栏行文本恒空：渲染读 `t.name || t.type`，实装字段为 {tick,owner,seq,path,nodeType,phase,depth,result?}（runtime.js traceNode）**——行文本退化为仅 owner 前缀。**修复**：replay.js `traceText(t)` 按实装映射（owner + path/nodeType + result 箭头）；测试 fixture 从虚构 `{name:'move_right'}` 重写为实装形状（B22/F4 沿革：渲染层按实装）。**回归锁**：行文本含 'move_right'/无 result 无箭头断言 + 探针段3 真帧行文本非空。

### P2 登记（不阻塞；建议随批落实项以 ★ 标注）

1. **speed reducer 未防 0/负（缺口②）**：`battle/speed` 直存；运行时 `||1` 免除零、max(50,·) 防抖兜底（±均不崩）；UI 仅 1/2/4。建议 reducer 夹取 [1,4]。★
2. **playbackTimer 跨 goto 未清理（缺口③）**：有界且当前无现实触发路径（实证分析）；建议 EFFECTS['goto'] 统一清理（防改装/未来导航入口）。
3. **PALETTE.hit #f0883e 无 token（核查项 4）**：tokens.css 无对应；建议增 `--color-hit`（现文件内注释已登记）。★
4. **HUD 视觉风格统一（缺口⑤）**：DOM 文本行 vs §7.2 血条/资源条/基地条 canvas 描绘 + 「基地条」条状语义——F7 统一（本条含 P1-4 的形式残留）。
5. **tick 滑块未实现**：§6.6 控制条「tick 滑块(0..frames-1)」——实装为 tick 文本盒（replay_tick）；建议 F7 滑块或 spec 改文本口径。★
6. **播放插值未实现（§7.1）**：`x(t)=fromX+(toX-fromX)*t` 的 t 插值——实装静态 toX 投影（本批交付口径「toX 投影」）；F7 打磨。
7. **bullet 视觉固定 64×8 矩形**：len/dir 已投影未用；§7.2「起点→命中点连线」语义（hitAt 火花）属 F7 打磨。
8. **结算文本无 phase/reason（§6.6「胜负 + reason」）**：battle/run 响应 summary 自带 phase（server/battle.js:96），effect 仅取 winner/ticks；建议 result 补带 phase 上屏。★
9. **replay/step payload.delta=0 → `||1` 语义丢失**：delta 0 被当 1 步进（无 UI 触发）；防御登记。
10. **timers 注入缝未透传 app 层**（同 F4 P2-8 randomInt 同型）：ctx.timers 仅测试面可达，浏览器走真定时器缺省——无功能影响。
11. **结算遮罩无「继续查看」按钮**：结算出现即结束回放（自觉终止语义）；若 F7 需可关闭/可继续，mask 上加关闭按钮。

## 实测

| 命令 | 期望 | 实际 | 结果 |
|---|---|---|---|
| `npm test` | 515/515 全绿 | **516 pass / 0 fail / 0 skip**（515 + 审查新增 1：mount 注入回归锁；全量回归） | ✅ |
| `npm run gate` | 9 PASS/0 FAIL/0 PEND | **9 PASS / 0 FAIL / 0 PEND**（修复进程中一度 8/1 FAIL：mount/index 分支 73.68 + replay 分支 83.33 <85 → 补锤后全绿；未放宽阈值） | ✅ |
| `npm run cov` | 聚合与声明一致 | 聚合 **97.90 / 87.94 / 96.37**；逐文件 planFrame 100/90.48/100、replay 100/87.18/100、canvas 100/91.67/100、effects 100/86.90/93.10、mount/index 100/96.36/100 | ✅ |
| `node scripts/check-arch.js` | 43 文件 0 违规 | `[PASS] 43 个文件无依赖违规` | ✅ |
| `node .audit/golden-battle.js` | 18 tick 一致 | 黄金战斗复算通过（18 tick, winner=p2, phase=role）；快照未改写 | ✅ |
| 探针 `.review-f5/probe-replay.js` | 6 段全 PASS | **6 段全 PASS（复跑 2 次稳定）**：①真 /battle 28 帧 planFrame 全帧快照（player=56/collision=17/verdict=1，1px 整数域 0 违例）②播放状态机真实帧推进（27 seeks 达末帧 auto-pause + replay/pause 清定时器）③结算时刻（tick0 无遮罩/末帧弹）+ HUD base hp=100 + aiTrace 实装字段行 + 结算文本 ④mount 注入（#battle 16,80,1024×128 z=1 / 非回放屏隐藏）⑤技能局 62 帧 bullets=30 + paintCanvas 216 fillRect 零异常 ⑥直线局 18 帧 bulletHits=5 命中图元投影 | ✅（P1 定案 + 修复验证） |

## 独立实证（非测试代码路径，可复跑 `.review-f5/probe-replay.js`）

① **P1-1 定案（代码路径逐行）**：replayLayout 按钮 action 翻转存在，但 EFFECTS 键表（effects.js）无 'replay/pause'、reducer（reducer.js）无对应 case → dispatch 后 default 原样返回 + runEffect 无 fn → 死按钮；修复后探针段2 replay/pause 断言两端绿。② **P1-2 定案**：batte/run 链 battle/loaded 恒带 result → 原实装 replay 屏恒弹遮罩（mask 全屏拦截点击，控制条不可达）；修复后探针段3 tick0 断言 `replay_modal_mask === undefined` + 控制条 action 可达。③ **P1-3 定案（DOM 实证）**：index.html:13 `display:none` + 全仓无坐标注入代码（public/js 与 css grep）→ 修复后探针段4 断言 style 注入值（16px/80px/1024px×128px/z=1）+ 非回放屏 display:none。④ **P1-5 定案（真帧字段实拍）**：探针段3 打印真帧 aiTrace[0] = {tick,owner,seq,path,nodeType,phase,depth,result?}——无 name/type；修复后行文本含 path 与 result。⑤ **真帧 1px 域**：段1/5/6 全帧 INT 断言（toX/atX/contactX/bullet.x 全整数，0 违例）。⑥ **frontend 常量 ↔ battle-config 对照**：FIELD_PX=1024=fieldPx、CELL_PX=64=cellPx、ACTOR_HALF=32=actorHalfPx 逐值一致。⑦ **技能行动名实证**：引擎 normalizeAction 的 skill 前缀为 `skill:`（非 skill_）——探针段5 首版用 `skill_skill1` 全局 wait 0 子弹（引擎行为，非 F5 缺陷），改 `skill:skill1` 后 62 帧 30 子弹；行动名形态登记为准绳（AI 程序作者需知）。

## 结论

**CONDITIONAL PASS（P1×5 必须当批修复后转 PASS；本批已全部修复并复测）**。交付物覆盖面与测度声明达成：516/516（515+审查新增回归锁 1 例）、gate 9 PASS/0 FAIL/0 PEND（修复中一次分支覆盖 FAIL 经补锤转绿，阈值未放宽）、cov 聚合 97.90/87.94/96.37 、check-arch 43 文件 0 违规、golden 18 tick 一致、public/js 全域无 console。P1×5 均为「产品可见性/可操作性」断裂：①replay/pause 死按钮（暂停不可用）②结算遮罩时刻错误（进屏即全覆盖 → 回放不可用）③画布与全部盒子坐标注入缺失（**根因级**：style.css 声明的 JS 坐标注入从未实现，F2~F5 各屏浏览器均不可见——本批在 mount 补齐为唯一注入点 + #battle 显隐定位）④HUD 基地条缺失（bases 数据每帧可用）⑤aiTrace 行字段错读（实装 path/nodeType/result vs 虚构 name/type → 恒空行）。P2×11 登记（★3：speed 夹取、--color-hit、结算 phase）。六项缺口自查全部闭环（①判定 P1 已修、②P2、③P2 有界论证、④P1-5 修复实证、⑤P2-4 风格登记、⑥核对 PASS）。复跑基线：`npm test && npm run gate`（串行）＋ `.review-f5/probe-replay.js` 全绿即为回归通过。

## 修复落实（审查侧，2026-09-17；P1×5 当批）

1. **P1-1**：`public/js/store/effects.js` 提取 `stopPlayback(ctx)`（清 playbackTimer + 复位 playing），`battle/pause` 与新增 `replay/pause` 共用（§4.2 契约 action 名）；注释登记沿革。
2. **P1-2**：`public/js/views/replay.js` 结算门控 `res && (frames.length === 0 || tick >= frames.length - 1)`——末帧弹结算，空帧 fail-safe。
3. **P1-3**：`public/js/mount/index.js` paint 增 `injectBoxGeom(doc, boxes)`（.dl-box data-box-* → 元素几何注入，唯一注入点；结构性死防御分支净化——boxes 恒数组/id 恒非空/坐标恒数字/mountApp 入口已守卫，删除 ||[] / ?: / ||0 冗余）+ `#battle` 画布显隐/定位（回放屏显示于画布盒，其余屏隐藏）。
4. **P1-4**：`public/js/views/replay.js` HUD 文本行补 `base ${hp}`（bases 缺失 `—` 兜底）。
5. **P1-5**：`public/js/views/replay.js` 新增 `traceText(t)`（owner + path/nodeType + `→result` 实装字段映射）。
6. **回归锁**：`tests/frontend/replay.test.js` 更新——aiTrace fixture 按实装形状重写；HUD base 断言；结算时刻三断言（tick0 不弹/末帧弹/空帧 fail-safe）；replay/pause 断言；假 timers 补 id 存根（清定时器断言此前空转）；新增第 6 用例「mount 盒注入+画布显隐（style 感知假 doc）」含分支锤（无 style 元素/无 canvas 盒/无参 mountApp/无 records/shell 缺省）。