# B7 审查记录

> 批次：B7（P1：core/bullets.js 弹幕运动学）
> 审查方式：独立上下文 agent（P/Q/C/B 全分支独立复算 + C9 双变体次序反推验证 + 事件矩阵核对 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS（修复 2 个 P1 后）**

## 审查范围

新增 `server/core/bullets.js`（spawnBullets/resolveBullets/solveIntersection/bulletBattle/bulletsOnField + withLogger）、`tests/unit/bullets.test.js`（10 测试）；修改 `docs/interfaces.md` §1 bullets 行、`docs/systems/04-bullets.md` §6 接口名、`docs/examples/04-bullets.md` C9 叙述口径。

## 核对结果

| 项目 | 结果 |
|---|---|
| P1..P7 t* 与命中位置独立复算全过（1.0@736 / 0.3929@425 / 2.75 / 无解 / 0.3438@400 / −0.195 / 1.2857）；C1..C5（578/800/736/512/无解） | ✅ PASS |
| **C9 双变体反推验证**：无 t=0 全局优先则变体得 1 命中（与文档 ⚠ 0 命中矛盾）→ 两阶段次序为必要条件 | ✅ PASS |
| 语义：AOE 位移后判定（Q1..Q4）、falloff 系数、while 终止性（每轮至少移除 1 枚）、hitSet、等级矩阵、同方 none | ✅ PASS |
| 边界：射程含终点（D-30）、不伤己、基地不判命中（D-60）、已死目标抵消照常、tick 末存活 0（T-BT-19）、fullDodge 忽略（D-72） | ✅ PASS |
| 日志矩阵：bullet.spawn(debug)/collide(debug)/hit(debug)/expire(trace) 与 §4.6 冻结矩阵一致 | ✅ PASS |
| 测试：BU-* 过 §3.3（定点复算非镜像、失败路径、T-BT-8 上界断言补齐） | ✅ PASS |
| 铁律：L2 / L9 / L11 / L17 | ✅ PASS |
| 实测：npm test 234/234；cov bullets.js 96.59/89.61/100；gate 8 PASS/0 FAIL/1 PEND | ✅ PASS |

## 已修复的问题（当批）

1. **P1-1 事件名**：`bullet.block` → `bullet.collide`（§4.6 冻结名）；补 `bullet.expire`(trace)（清场阶段）；BU-8 断言同步。
2. **P1-2 接口名与 ICD 不符**：interfaces §1 bullets 行与 systems §6 同步为实际导出名（spawnBullets/resolveBullets/solveIntersection/bulletBattle/bulletsOnField）。
3. **P2-1**：补 T-BT-8（生成数上界 + 每 tick 清零）断言。
4. **P2-2**：examples C9 叙述修订为 D-32 口径（两枚均 t=0@800 拦截；变体叙事与结果不变）。

## 登记取向

- 本批交付运动学；完整伤害数值（×atk×mult×抵消×护甲×背击×暴击）由 B9 伤害链路组合（示例 04-bullets 的 9/12/10/7 含护甲系数 1−def/(def+40)）。
- solveIntersection 闭区间 [tMin,tMax]（命中 t=0 天然不可达；D-32 互撞含 t=0）。
- 互撞两阶段：全局 t=0 立即拦截扫描（循环至无；每次结算重扫存活集合）→ t>0 逐对（生成序 × 存活敌弹）——D-27 递归 + C9 双变体复现的必要次序。

## 遗留

- 项 8（日志冒烟）PEND 待 B11；B9 补伤害数值断言（04-bullets P1 9 / Q1 12 等含护甲）。