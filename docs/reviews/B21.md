# B21 审查记录

> 批次：B21（P3 收尾：属性测试全套 T-PB-10 + 数值校准只改数据表：`server/data/battle-config.json` 增 `defK:40`（D-128）与 `dodgeChanceBonus=0.2` 定稿（D-127）；`server/core/engine.js` 删 `DEF_K` 常量、两处减伤公式改读 `cfg.defK`；`docs/decisions.md` D-127/D-128 + §14 改「已校准」；文档同步 v3-design/progress/tasks/interfaces；`tests/unit/b21.test.js`（9 个 test 块）；`tests/integration/interfaces.test.js` IF-1 D 计数 78→80）
> 审查方式：独立对抗性上下文（8 项核查清单逐条 + 缺口自查 4 项 + 攻击探针（schema 冻结缺口实证 / v8 函数级覆盖死分支定位 / checkNumericHardcode 直跑 / git diff 与快照未重写实证 / defK·dodge 语义逐行核对）+ 实测五件套 + verify-rest 22 项 + golden 18 tick）
> 日期：2026-09-13　结论：FAIL → **PASS**（P1-1 已当批修复 + P2×7 落实要点当批处理；复测 420 用例 / gate 9 PASS/0 FAIL/0 PEND / cov 聚合 97.95/87.67/96.54 / check-arch 18 文件 / golden-battle 18 tick 复算通过）

## 审查范围

交付物（git status 实证，9 文件）：`server/data/battle-config.json`（+`defK:40`，唯一数值增项；dodgeChanceBonus 0.2 为既有值定稿）；`server/core/engine.js`（`-const DEF_K = 40`，两处公式 `def + cfg.defK`：dealDamage 步骤 2-3 普通链路 49 行 / 撞基地链路 357 行；头注释 L9 登记）；`docs/decisions.md`（+D-127/D-128，§14 改写）；`docs/{v3-design,tasks,progress,interfaces}.md` 同步；`tests/unit/b21.test.js`（新，9 个 test）；`tests/integration/interfaces.test.js`（IF-1 计数 80）。**` .audit/golden-battle.json 未被重写**——golden 复算通过即 DEF_K→cfg.defK 迁移零数值漂移的硬证据。
契约依据：tasks §3.6 T-PB-1..10 / §6 B21 行 / §8.1 R9·R17；decisions D-126..D-128；examples/README §1（减伤基准 1−8/48 等）；examples/01-items I-5/I-6、05-effects E-8；B8 遗留（DEF_K=40 入表评估 → 已入表 ✓）、B9 遗留（附加效果候选 → 已定稿 ✓）、B6 遗留（melee 不可增强 → 已冻结 ✓）。

## 核对结果

| # | 核查项 | 结果 | 依据 |
|---|---|---|---|
| 1 | **校准正确性**：dodge 定稿与引擎语义一致；defK 两处读 cfg 无漏网；golden 18 tick 与 verify-rest 22 项零漂移 | ✅ PASS | dealDamage 步骤 1（engine.js:41）`Math.min(1, panel + (defender.dodging ? cfg.dodgeChanceBonus : 0))`，步骤 6（:281）dodge 意图置 `p.dodging=true`（控制复写后置位不错加，B9 时序保持）——与 D-127「本 tick 使用 dodge 行动 +20%、叠加面板、封顶 1」逐字一致；`defK` 仅 49/357 两处消费，全仓无 `DEF_K`/`def+40` 代码残留（grep 仅注释历史表述）；`git diff` 确认引擎改动 = 删常量 + 两行 `cfg.defK`，无其它算路径；golden-battle 18 tick（winner=p2, phase=role）复算通过且**快照文件未改**（git 未列）→ 数值零漂移；verify-rest 22 项全过（M2/M5/N2/N3/基地 3/超时 7/2.25→22 等）。
| 2 | **D-127/D-128 登记**：含义清晰、编号连续、§14 改口无残留、v3-design/progress 开放表述全关闭 | ✅ PASS（P2-1 附注） | D-127（+20% 语义、封顶 1、不再开放）/D-128（defK 入表零字面量、附加效果 stun r1/knockback·pull ±1/dot r3/true_dmg 直扣、melee 冻结、regen 维持）落于 §12 表尾，编号 D-120..128 连续无跳段；interfaces.test IF-1 断言 80 条 = D-01..128 全集（独立复算 8+10+16+7+2+3+3+5+3+5+9+9=80 ✓）且 D-127/D-128 在 interfaces.md engine 行/数据表有整词落点（gate 项5 过）；§14 已改「B21 已统一校准…不再开放」；v3-design §17 开放问题第 8 项（任务所称「§8」实际位于 §17.8）改 ✅ D-127；progress「仍开放的唯一数值项」改「已全部关闭」。**残留（P2-1）**：tasks.md §9 风险表 R17 行仍写「`dodge` 闪避加成数值未定/占位 +20%/B21 校准」、R9 仍写「B21 校准」——校准已完成，两行状态未收口为 ✅ 已决（D-127/D-128）；decisions §14 标题「待补充的数值」措辞与内容「不再开放」张力（建议改「战斗数值定稿清单」）。
| 3 | **T-PB-10 全套**：包裹 T-PB-1..9 口径一致；「往返后」=仅序列化；变异用例在往返态上执行 | ✅ PASS（P2-4/2-5 附注） | b21 九个 test 逐条对应 T-PB-1（mismatch 拒绝且状态不变）/2（1+2+2>4 points_exceeded）/3（装配→拆卸 deepEqual）/4（档位单调数据表机器校验 + 往返态 tier 有效性）/5+6（面板 1.38/16、非声明维不变、减耗 ceil 8）/7（gated tier_locked + mythic 放行）/8+9（双引用/悬挂引用拒绝）——与 B18（I-10 系列）、B20（T-PB-5/6/7/8-9）口径一致；rtWH() 在推入测试插件后**再做一次** `JSON.parse(JSON.stringify())`，变异（mismatch/points/gated/双引用/悬挂）全部作用于往返态；「往返」= 纯序列化不触状态（无副作用操作）。**附注**：T-PB-4 包装为「数据表单调 + 往返态 tier 合法」轻量形态（实例级逐档复算由 B18 T-IT-9 常驻），可接受；**P2-4**：T-PB-5/6/8/9 仅 loadout 侧 JSON 往返，warehouse 侧直用 fixture（`warehouse: LD.warehouse` 未过序列化），与 T-PB-10「warehouse+loadout 往返后」字面不完全闭合；**P2-5**：rtWH() 推入的 `p4`/`q1` 与 wh-ok.json 既有 uid **重复**（同桶重复 uid，lookup 取首个，断言靠两实例 pointCost/kind 相同才不歧义——测试数据卫生隐患，建议改名 px4/qx1）。
| 4 | **校准回归数值机器推导**：16/8/13/1.38·16；附加效果与 B9/B2 E-* 一致 | ✅ PASS | `floor(20×40/48)=16`（b21 用 `Math.floor(20×(1−8/(8+CFG.defK)))` 机器复算而非手敲 16，0.1 行差分即碎）；`ceil(10×0.8)=8`；melee `10+3×1=13`（costBaseOf.rare=3 × tier1，skills.js:75-82 读表）；面板 `1.2×1.15=1.38`、`10+3×2=16`（B20 既有锚）；附加效果 D-128② 与 B9 登记候选逐条核对：stun `{control, displacement:0, remaining:1}`、knockback `dir×v`、pull `−dir×v`、dot `{continuous, stat:hp, delta:−v, remaining:3}`、true_dmg 直扣 `floor(v)`（engine.js:87-104），数据侧插件 sp_stun/sp_knockback/sp_pull/sp_dot/sp_true_dmg 存在且 params v=1/1/1/3/1（D-128「数值 v 由词条档位给出」= 读 affix.params 无引擎常量 ✓）；既有 DM-8/DM-13/E-8 断言保持全绿。
| 5 | **回归完整性**：DEF_K 迁移零影响；checkNumericHardcode 对「40」无误报 | ✅ PASS（P1-1 附注） | 数值未变（cfg.defK=40 == 原常量）→ golden 18 tick 快照未重写且复算一致、verify-rest 22 项、BT 全套（golden.test/battle-end/damage/engine）在 420/420 中全绿；`checkNumericHardcode` 直跑 pass（11 个文件无战斗数值硬编码）——把 40 加入 configValues 后扫描 core/ai 无任何字面量 40 命中（注释/字符串已剥离），DEF_K 删除后清理干净。**但（P1-1）**：本批把 defK 加入冻结清单（tasks §2.5.7/D-128）后，唯一机器执行「冻结」的校验器 `schema.js BATTLE_CONFIG_FROZEN`（头注释「期望值硬编码于此（校验器而非战斗代码）」+ DS-2「§2.5.7 逐值」）**未纳入 defK** —— 探针实证 `defK=45` 漂移与 `defK` 缺失均 `validateStructure → ok:true`，而 dodgeChanceBonus=0.3 漂移被拦截 → 冻结契约对新增头部值失效（间接兜底：golden 伤害锚能拦截 defK 改值，但那是快照网、且 `--write` 可重写；T-DC-1/DS-2 才是批内声明的冻结通道）。
| 6 | **架构/铁律**：check-arch 18 文件；L9/L11/L17；IF-1 计数 80 | ✅ PASS | check-arch `18 个文件无依赖违规`（engine 无新依赖，cfg 走 DEFAULT_CFG 数据层读表合法）；L9：engine 零字面量兜底（`DEFAULT_CFG = require(battle-config)`，dealDamage/base hit 均 `cfg.defK`；`def` 兜底 `?? common` 系 skills.js 读表兜底属既有登记）；L11/L17：b21 全部期望值由公式机器推导（注释内置公式 + 独立复算，无手敲）；IF-1 `decided.size === 80` 断言通过（B20 期 78 + D-127/128 = 80 ✓，interfaces.test.js:30 注释同步）。
| 7 | **覆盖与门禁**：420/420；gate 9 PASS；cov 97.95/87.67/96.54（engine 100/93.26/94.44）；check-arch 18；golden 复算 | ✅ PASS | `npm test` 420 pass / 0 fail / 0 skip（B20 411 + b21 9）；`npm run gate` 9 PASS / 0 FAIL / 0 PEND（项7 含四目录阈值、项8 黄金 18 tick cid 链、项9 接口冒烟）；`npm run cov` 聚合 97.95/87.67/96.54、engine 100.00/93.26/94.44 —— 与交付声明逐值一致；`node scripts/check-arch.js` 18 文件 0 违规；golden-battle 复算通过（18 tick, winner=p2, phase=role）。
| 8 | **死代码/死分支**：defK 两处读表路径（trueDamage 绕过）；b21 断言可达性 | ✅ PASS（P2-7 附注） | `p.trueDamage ? 1 : 1 − def/(def+cfg.defK)` 两分支均有断言（DM-5 真伤 15 / DM-1 普通 9）；撞基地分支（`1 − 1 − base.def/(base.def+cfg.defK)`）由 engine.test O1/O2 基地伤害覆盖；v8 函数级覆盖探针：engine.js 19 个函数仅 **1 个零覆盖** = dealDamage 缺省 `{ chance: () => 0 }` 兜底（:39，所有调用方均注入 critRng/dodge 流，纯防御路径，登记即可）——对应 funcs 94.44%；b21 九断言全部在 420/420 中真实执行（无 skip/todo，node:test 统计实证）；追加 `defK` 相关的新增路径（b21 D-127/128 两 test）正常落入覆盖率。

## 缺口自查（四项按任务要求逐条）

① **附加效果数值定稿冻结核对**（D-128 ② vs engine.addAffixEffect）：stun `remaining=1` ✓、knockback `+dir×v`✓、pull `−dir×v`✓（均 remaining:1）、dot `remaining=3` + `delta:−v`✓、true_dmg `Math.max(0, Math.floor(v))` 直扣✓——与 B9 登记候选（stun control 0 r1 / knockback ±dir×v / pull −dir×v / dot hp −v r3 / true_dmg 直扣）逐条一致，且数据表 affix params v=1/1/1/3/1（数值由词条档位给出、引擎无附加效果常量）✓；DM-8/DM-13 与 b21 存在性断言三重锚定 ✓。
② **defK=40 与其它「40」字面量**：battle-config 仅 defK=40 一处（其余 64/48/0.8 等无 40）；代码中 DEF_K 已删除且无任何裸 40（checkNumericHardcode 直跑 pass + grep 实证；engine.js:8 头注释的 `def+40` 为历史机制描述，已由 :10 新注释说明入表）✓。**但** schema 冻结清单未同步（P1-1）。
③ **dodge 实战语义（0.2 叠加封顶 1）**：引擎 `Math.min(1, panel+bonus)` 语义与 D-127 一致；DM T-EN-8 断言 `0.3+0.2=0.5`（叠加路径 ✓）；**封顶 1 无显式断言**（0.9+dodging→1.0 锚缺失，`min` 截断不被任何用例显式验证）——登记 P2-7；golden 战斗中 dodge 流照常消费且 18 tick 快照一致（cd 语义无回归）。
④ **T-PB-10「往返后」断言作用于往返态**：tests 1/2/3/4/7 全部在 `rtWH()`（含 JSON 往返）上执行 ✓；tests 5/8/9 loadout 侧往返 ✓、warehouse 侧未往返（P2-4）。变异用例（mismatch/points/gated/双引用/悬挂）均在往返态上构造并断言 ✓；无任何断言作用于原始 fixture 直通态（test 3 的 deepEqual 基准也是往返态 wh）。

## 发现的问题

### P0

无。

### P1（必须当批修复）

**P1-1（冻结契约缺口）`defK` 未纳入 schema.js `BATTLE_CONFIG_FROZEN`，T-DC-1/DS-2「§2.5.7 逐值」冻结对新增定稿值失效**

位置：`server/data/schema.js:27-33`（`BATTLE_CONFIG_FROZEN` 无 `defK`；该对象头注释明言「期望值硬编码于此（校验器而非战斗代码）」）；`tests/integration/data-schema.test.js:52`（DS-2 仅验 cellPx）。实证（独立探针）：复制 server/data → 改 `defK=45` → `validateStructure` 返回 **ok:true**；删 `defK` 键 → 仍 **ok:true**；对照组 `dodgeChanceBonus=0.3` → ok:false（「应为 0.2（§2.5.7 冻结）」）。本批同时把 defK=40 写进 tasks §2.5.7 冻结清单与 D-128（「入表冻结」），而机器执行冻结的校验器未同步 → 批内文档/校验器自相矛盾；defK 漂移仅剩 golden-battle 快照间接拦截（该快照 `--write` 可重写，且只锚定少数 atk/def 组合）。修复：`BATTLE_CONFIG_FROZEN` 补 `defK: 40`（一行），DS-2 增 defK 漂移断言（如 `bc.defK = 41` → fail 且 detail 含 `defK`）。属数据层校验器改动，不违背「数值校准只改数据表」的批约束。

### P2 登记（不阻塞，建议当批或随收口落实）

1. **P2-1（tasks.md §9 风险表未收口）**：R17 行仍写「`dodge` 的闪避加成数值未定 / `dodgeChanceBonus` 占位 +20% / B21 校准」、R9 行仍写「B21 校准」——校准已完成（D-127/D-128），两行应改 ✅ 已决（含期限列）；同时 decisions §14 标题「待补充的数值」建议改「战斗数值定稿清单」以消除措辞张力。
2. **P2-2（items-data.md 前瞻标记滞留）**：`docs/items-data.md` §2.1「…蓝 [3,6,9]、紫 [4,8,12]…，具体值 B21 校准」与 §3「特化/专家模板按流派差异化（B21 校准）」仍为未来时态——B20 已定稿 delta 公式（数组值为名义值）、D-128④ 已定 regen 维持数据表现值（实测 11 模板全为 {mp:1,sp:2}），两处应改为校准结论。
3. **P2-3（systems/07-engine.md 公式未引 defK）**：§4.4 第 3 步仍写 `max(1, floor(atk × 倍率 × (1 − def/(def+40))))` 字面 40，未引 `battle-config.defK`（D-128 影响列点名「07-engine §4.4」）；engine.js 头注释 :8 的历史 `def+40` 表述可一并更新为 defK 引用（纯文档，公式数值不变）。
4. **P2-4（T-PB-10 包裹口径部分未闭合）**：b21 tests 5/8/9 对 warehouse 侧未做 JSON 往返（仅 loadout 侧），与 T-PB-10「warehouse+loadout 往返后」字面不一致；补 `JSON.parse(JSON.stringify(LD.warehouse))` 一行即闭合。
5. **P2-5（b21 fixture 重复 uid）**：rtWH() 推入的 `p4`/`q1` 与 wh-ok.json 既有同桶 uid 重复；items 侧按 uid 取首个实例使断言依赖两实例字段巧合（当前 pointCost/kind 相同故通过）。建议改名（px4/qx1）防未来歧义陷阱。
6. **P2-6（用例计数口径）**：批次交付描述「8 用例」，实际 b21.test.js 为 9 个 test 块（T-PB-1..9 包裹 7 块 + 校准 2 块）；总数 420/420 与声明一致，但「8」与「9」需统一（计入 tasks §6 或本批描述）。
7. **P2-7（dodge 封顶 1 无显式断言 + 引擎唯一防御函数零覆盖）**：DM T-EN-8 只验 0.3+0.2=0.5，缺 `0.9 + dodging → dodgeChanceTotal === 1` 的封顶锚（D-127 明言封顶 1）；engine.js:39 缺省 `chance:()=>0` 兜底函数从未被调（v8 函数级实证，19 函数仅此 1 个零覆盖，对应 funcs 94.44%）——防御路径登记接受，若补封顶断言时可顺带消除（显式传 critRng 的既有用例已全覆盖）。

## 实测

| 命令 | 期望 | 实际 | 结果 |
|---|---|---|---|
| `npm test` | 420/420 全绿 | 420 pass / 0 fail / 0 skipped（B20 411 + b21 9） | ✅ |
| `npm run gate` | 9 PASS / 0 FAIL / 0 PEND | 9 PASS / 0 FAIL / 0 PEND（项7 420 用例 + 四目录阈值；项8 黄金 18 tick；项9 接口冒烟） | ✅ |
| `npm run cov` | 聚合 ≥90/85/90 | 聚合 97.95 / 87.67 / 96.54；engine 100.00 / 93.26 / 94.44（与交付声明逐值一致） | ✅ |
| `node scripts/check-arch.js` | 18 文件 0 违规 | `[PASS] check-arch: 18 个文件无依赖违规` | ✅ |
| `node .audit/golden-battle.js` | 18 tick 快照一致 | `黄金战斗复算通过（18 tick, winner=p2, phase=role）`，快照文件未被重写 | ✅ |
| `node .audit/verify-rest.js` | 22 项复算 | 22 项全过（M2/M5/N2/N3/碰撞 7·12/基地 3/超时 7/背爆 22） | ✅ |

## 独立实证（非测试代码路径，可复跑）

① `checkNumericHardcode` 直跑 → pass（11 文件无战斗数值硬编码；defK 入表后无 40 误报）；② schema 冻结缺口探针：defK=45 / defK 缺失 → validateStructure ok:true vs dodgeChanceBonus=0.3 → fail（**P1-1 物证**）；③ v8 函数级覆盖（NODE_V8_COVERAGE + 4 测试文件）：engine.js 19 函数仅 1 零覆盖（:39 缺省 chance 兜底），与 funcs 94.44% 对应；④ `git diff` 实证引擎改动仅删 DEF_K + 两行 cfg.defK（无偷改数值）、golden-battle.json 未在改动清单（零漂移硬证）；⑤ grep 全仓：无代码级 `def+40`/`DEF_K` 残留（engine.js:8 头注释为历史机制描述）；⑥ 全仓 D 编号独立复算 80 = IF-1 断言一致。

## 结论

**FAIL**（无 P0；**P1×1 必须当批修复**，P2×7 登记）。必须当批落实的最小集合：

1. **P1-1**：`server/data/schema.js` `BATTLE_CONFIG_FROZEN` 补 `defK: 40`；`tests/integration/data-schema.test.js` DS-2 增 defK 漂移断言（改 41 → fail 且 detail 含 defK）——使 T-DC-1/DS-2「§2.5.7 逐值」冻结覆盖本批新增的头号校准值（2 行级改动，数据层校验器，不触战斗代码）。

其余 7 项核查全部通过（附 P2 注），缺口自查 4 项均闭合或登记（P2-4 为唯一口径半闭合项）。门禁五件套全绿但**不拦截 P1-1**——冻结契约缺口对 gate 不可见（校验器不报、错值不触发任何测试差——golden 仅因 defK 恰为 40 而通过，与 B19/B20「输入防御形态」盲区同族：必须探针形态补防 + 回归断言）。修复后复跑 `npm test`（420/420）/ `npm run gate`（9 PASS）/ `npm run cov` / `check-arch` / golden-battle 收口，并建议随批落实 P2-1（tasks §9 R17/R9 状态行）、P2-3（07-engine §4.4 引 defK）、P2-4（warehouse 侧往返一行）、P2-5（fixture uid 去重）。

## 修复落实（实现侧，2026-09-13；P1-1 + P2×7 当批）

1. **P1-1**：`schema.js BATTLE_CONFIG_FROZEN` 补 `defK: 40`（注释 D-128）；`data-schema.test.js` DS-2 增两条回归——defK 漂移 40→41 fail（detail 含 defK）与 defK 缺失 fail。
2. **P2-1**：tasks.md §9 R9/R17 状态行改 ✅ 已决（D-128/D-127）。
3. **P2-2**：items-data.md 两处「B21 校准」前瞻标记改定稿（costDeltaByTier 公式口径、regen 维持 D-128④）。
4. **P2-3**：systems/07-engine.md §4.4 基础伤害公式改引 `defK`（标注 battle-config/D-128）。
5. **P2-4**：b21 tests 5/8/9 warehouse 侧改 JSON 往返态执行（T-PB-10 外层闭合）。
6. **P2-5**：b21 rtWH() 去重 uid（移除重复 p4 推入；新增 qx1 避开既有 q1；pa/pb 无冲突）。
7. **P2-6**：交付描述按实际 9 个 test 块核对（420 总数与声明一致）。
8. **P2-7**：D-127 测试补 dodge 封顶 1 显式断言（面板 1.0 + dodging 0.2 → total 1 且必闪）；engine `chance:()=>0` 兜底为纯防御路径登记。

复测：`npm test` 420/420 ✅；`npm run gate` 9 PASS/0 FAIL/0 PEND ✅；`npm run cov` 聚合 97.95/87.67/96.54 ✅；`node scripts/check-arch.js` 18 文件 0 违规 ✅；`node .audit/golden-battle.js` 18 tick 复算通过 ✅（DEF_K→cfg.defK 迁移零漂移）。