# B2 审查记录

> 批次：B2（P1：core/effects.js 效果系统）
> 审查方式：独立上下文 agent（E-1..E-8 数值独立复算 + remaining 边界实测 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS（修复 3 个 P1 后）**

## 审查范围

新增 `server/core/effects.js`、`tests/unit/effects.test.js`（EF-1..13）；修改 `docs/interfaces.md` §1 effects 行（接口清单补全）。

## 核对结果

| 项目 | 结果 |
|---|---|
| E-1..E-8 数值逐条独立复算全对（132→129→126→123 移除；E-2 封顶/钳 0；E-5b/d 536、E-5c 32；E-6 优先级矩阵） | ✅ PASS |
| 语义：addedTick 与引擎管线兼容；resolveControl 全 control 递减；towardEnemy 双侧 + 背离分支覆盖 | ✅ PASS |
| 日志矩阵：effect.add/continuous/expire/control.override 与 §4.6 一致；连续+控制两条移除路径都记 expire | ✅ PASS |
| 测试：EF-1..13 过 §3.3 黑名单（边界 remaining:0、失败路径未知 target、无镜像） | ✅ PASS |
| 铁律：L2 / L11（无 IO/随机/console）/ L17 | ✅ PASS |
| 实测：npm test 167/167；cov effects 98.28/90.00/100；gate 8 PASS/0 FAIL/1 PEND | ✅ PASS |

## 已修复的 P1（当批）

1. **P1-a ICD 落空**：interfaces.md §1 effects 行只列 3 个接口、模块导出 5 个 → 补全（addEffect 语义/resolveControl 返回结构/resolveControlMove/withLogger）。
2. **P1-b remaining=0 边界**：两个 resolver 入口未清 remaining≤0（违反 05-effects §5"立即移除不结算"）→ 入口清理 + 记 expire + EF-12 锁定（连续不结算、控制不采用）。
3. **P1-c 未知 target**：RangeError 路径无测试 → EF-13（含中文错误消息断言）。

## 登记取向（B8 交接）

- resolveControl 输出为**意图**（wait / forced_move{dir,cells}）；落位 + 碰撞伤害（D-35/D-71）由 B8 步骤 7 统一解算（resolveControlMove 仅单方静止假设，T-FD-4 用；相向移动权威解算 B8，含近边界 gap 兜底）。
- 控制效果复写发生于 AI 返回后（D-83）；被复写技能不扣资源（D-84）由引擎步骤序保证。

## 遗留

- resolveControlMove 头注释（cells）与签名（displacement）命名不一致 → 已隐含在注释中，B8 重构时统一。
- 项 8（日志冒烟）PEND 待 B11。