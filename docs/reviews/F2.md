# F2 审查记录

> 批次：F2（P6 前端第三批：外壳 shell + menu 四态 + settings 日志面板 + mount 装配层/事件委托 + 日志动作 effects + app 重排接线）
> 审查方式：独立对抗性上下文（8 项核查清单逐条 + 缺口自查 6 项 + 探针 `.review-f2/probe-contract.js`（DLLog records 数组接缝/levelValue 刻度 off-by-one 实证）+ 手动 verifyLayout 复算（settings 20 条记录 zconflict、menu 按钮栈几何）+ 实测六件套）
> 日期：2026-09-16　结论：**CONDITIONAL PASS→需修（P1×8 必须当批修复；已全部修复并复测绿）**：①menu 按钮栈 y=180+260=440 把 5 按钮中 3 个推出面板（§6.1「y 从 260 起」为绝对坐标；测试断言 [560,440] 把缺陷固化）；②settings 级别 radio 无 payload → log/level 死控件；③settings 记录行 12 行（260..618）与导出/复位按钮（560..592）相交（≥10 条时行覆盖按钮不可点）+ 按钮 z 缺省 0 ≤ 面板 z0 → zconflict 自检每帧误报（verifyLayout 实测 false）；④menu error 态「重试」action='boot' 无 EFFECTS['boot'] → 死按钮；⑤filterLogs 级别刻度索引 0..7 vs DLLog silent:-1..all:99（info 档混入 debug、debug 档混入 trace，探针实证）；⑥app.js 默认 records 源读 DLLog `records` 数组时 typeof==='function' 永假 → 浏览器日志面板恒 0 条；⑦app.js reportBootError 引入 console.error —— 破坏 F1 实证属性「public/js 全域无 console」；⑧F1 审查「落实 6」声称 seed/set 已入 SAVE_ON_ACTIONS 但提交代码缺失（git show HEAD 实证）→ 非开箱流 seed 回带不落盘。P2×18 登记；复测基线 **492/492、gate 9 PASS/0 FAIL/0 PEND、cov 聚合 97.72/87.61/96.43、check-arch 37 文件 0 违规、golden 18 tick 一致**。

## 审查范围

交付物（git status 实证）：`public/js/views/{shell,menu,settings,index}.js`（新增）；`public/js/mount/{render,delegate,index}.js`（新增）；`public/js/store/effects.js`（改：log/level|reset|toggle|export + boot）、`store/index.js`（改：ctx 增 records/doc）、`app.js`（改：bootstrap start 日志/start() 模块启动可注入/mount 接线/records 数据源）、`public/css/style.css`（改：.dl-box 盒模型组件类）、`ui/{layout,verify}.js`（改：button 标注透传/region 豁免）；测试 `views.test.js`（6 用例）+ `mount.test.js`（7 用例）+ `util-log.test.js`（改）。契约依据：frontend-spec.md §0.2/§2/§3.1-3.6/§6.0/§6.1/§6.7/§8；tasks.md §7 F2 行（外壳+menu+settings 含日志面板）；F1 审查沿革（P1×5 修复验证 + P2 承接点：P2-1 toast 自消、P2-2 seed/set 落盘、P2-5 超时、P2-10 modal-mask、P2-13 死分支）。

## 核对结果

| # | 核查项 | 结果 | 依据 |
|---|---|---|---|
| 1 | **shell/menu/settings 布局** | ⚠️ FAIL→P1×2 | **shell**：header y0..64 ✓、tier badge 24×24@1120（§3.2 Badge 尺寸 ✓）、seed@1156 w28、log 按钮 1184 w96 右缘 1280 ✓、main y64 ✓；右簇与 §6.0 散文差异**登记论证**（见 P2-1：散文 1120..1176/1184..1272 与 §3.2 及 log 按钮几何自相矛盾，实现 1120..1144+1156..1184+1184..1280 连续无交——实现可接受，spec 应改）。**menu**：center(560,360)→(360,180) 与 §3.4 函数权威一致、§6.1 散文 y184 笔误（P2-2）；**按钮栈 y=440（=c.y+260）把 btn_gacha/battle/settings 推出面板 180..540（P1-1）**——§6.1「y 从 260 起」为绝对坐标（面板内其余元素均绝对坐标标注）；四态判定 ✓（serverOk null/false/loadout.role null）；error 态 btn_boot z 缺省 0 ≤ 面板 0 → zconflict 误报（P1-3 并）；loading/empty 提示与按钮栈几何冲突面修复。**settings**：左 16,80,560,560 与右 600,80,664,560（spec 散文均 400 高——**偏差登记论证** P2-3）；**记录列表 12 行与按钮行相交（P1-3）**；radio 无 payload（P1-2）；存档行（§6.7 left 内容/16,500,560,80 存档区）未实现——任务范围外登记 P2-4。 |
| 2 | **mount/render 往返与委托安全** | ✅ PASS（P2-8/9 附注） | boxToHtml↔collectBoxes 往返保真：`> `必须转义（escapeAttr 含 &quot;&amp;&lt;&gt;——原始实现漏 `>` 时外层属性正则 `[^>]*` 截断，实现已带注释修复）；文本/detail 实体往返 ✓（"A&B\<tag\>\"q\"" 断言）；payload JSON 往返（含 `\"` 反斜杠路径安全：escapeAttr 全引号转义 → 解析器还原 → JSON.parse 直接可用——**缺口⑥结论**：浏览器 dataset 自动解码实体，无需再处理；collectBoxes 手动解码仅为 node 侧等价）；可见性 hidden 双通道 ✓；collectBoxes 畸形防护全套（残缺无闭合/非数字坐标→0/无 span/空 payload→null/多盒/属性乱序 ATTR_RE 全扫/无 id 跳过）✓；validateBoxIds 重复 id ✓（mount 内 dup → ui.layout.report warn 实测）；delegate：goto 优先 → action 次之、坏 payload → badPayload:true 不抛不 dispatch ✓、closest/dataset 缺省守卫 ✓、payload 注入面（try/catch + reducer default no-op + effect 白名单路由）✓。P2-8：非 payload 属性（id/action/goto/parent/kind/style class 位）未转义——内部受控，防御登记。P2-9：payload JSON 若含合法 `&quot;` 文本 → 双编码往返仍保真（测试未直测，代码路径推演 + 双编码语义正确）。 |
| 3 | **mountApp 装配** | ✅ PASS（P2-5/6/10 附注） | 唯一 DOM 写入点：仅 paint() 写 app.innerHTML（**全量替换 vs §6.0「goto 只替换 main 区」——性能/行为登记 P2-5**；shell 依赖 tier/seed 需随屏重绘，功能等价）；verifyLayout 每次 paint 后自检 + 非空时 ui.layout.report warn ✓（clip 夹具实测）；toast 挂载区（存在复用/缺 createElement → toastsEl:null 不抛 ✓）；subscribe→paint→**paint 不 dispatch 无循环**（代码路径逐行核查：paint 内仅 render/boxesToHtml/collectBoxes/validateBoxIds/verifyLayout/log）✓；unsubscribe 幂等 + removeEventListener 缺失守卫 ✓；handleClick 测试钩子 ✓（goto 路由实测）；no-doc/no-app 双跳过 ✓。P2-6 事件偏差：paint 打 `render.frame`(debug) 描述 DOM 绘制（§2.2 语义为 canvas 每帧汇总）而 §2.3 契约事件为 `ui.layout`(debug,{view,boxes})——事件名/通道偏差登记；view.render 事件（§2.2 必打）全缺。P2-10：F1 P2-1 toast 3s 自消仍无实现（F2 只挂容器），ui/toast 消费 F3 承接。 |
| 4 | **app.js start/boot 两态与落盘链** | ⚠️ FAIL→P1×3（⑥⑦⑧） | 模块级 `start()`：node 无 console 报错路径（P1-7 修复后经注入 logger/模块 log 报 store.boot error，测试实证无未处理拒绝）；浏览器 boot 同路径（动态 import mount）✓；**records 默认源接缝（P1-6）**：DLLog api.records 是**活数组**（shared/log.js 实证），原 `typeof==='function'` 永假 → 浏览器设置屏日志面板恒「0 条」；SAVE_ON_ACTIONS 落盘链：box/open→box/done→onChange savePersist + store/save→ctx.save 双路径（全链路测试 mock localStorage 实证 mem['dl.v3.state'] ✓）；**seed/set 不在列表（P1-8：F1 声称落实未落码）**→ 非开箱流回带不落盘；doc 引用安全：log/export 无 doc → 跳过（node 安全）、有 doc 但 Blob 不可用 → try 吞掉 ✓；`P6 bootstrap start` 日志补回 ✓（§1.3）；skipMount 分支 ✓。P2-7：unlock 拉取（§1.3 step 5）仍未接线。 |
| 5 | **settings 过滤/导出** | ⚠️ FAIL→P1×1（⑤） | filterLogs 方向论证：**lv ≤ 阈值保留，与 DLLog log() 的 `lv > effective → 丢弃` 同向** ✓；通道集合空=全部 ✓；mapLevel 未知名兜底 4 ✓；**刻度对齐（P1-5）**：原 LOG_LEVELS 为索引 0..7 → mapLevel('debug')=5，而 DLLog 真实刻度 debug=4/info=3/trace=5（探针实证）→ info 档混入 debug、debug 档混入 trace；修复后与 shared/log.js LEVELS 逐值一致（silent:-1…all:99），边界复算：levelValue=0 → 仅 fatal、silent(-1) → 空、all(99) → 全量 ✓；通道大小写敏感（与 DLLog 小写规范一致，登记）；导出 shape：filterLogs 结果数组 JSON.stringify(,null,2) ✓；**导出未跟随面板当前过滤**（exportLogs(records,{}) 恒默认 debug 档）登记 P2-11；日志面板数据源 records() 接缝修复后浏览器真源连通（P1-6）。导出文件名 dl-logs-<ts>.json vs §2.4 dl-trace-<ts>.json（dlui.exportTrace 语义）——P2-11 并。 |
| 6 | **架构/铁律** | ⚠️ FAIL→P1×1（⑦） | check-arch **37 文件 0 违规**（22+8+7：F2 新增 public 7 = views×4 + mount×3）✓；L7 无裸导入/无 server import（grep 实证）✓；**console：mount/index.js 无 ✓，但 app.js reportBootError 引入 console.error（P1-7）**——破坏 F1 实证属性「public/js 全域无 console」，当批修复后全域零命中（仅注释提及）✓；L7 文件全部达标项 7（逐文件：最低 persist 分支 85.29/client 89.74/app 89.80 压线过关）✓；无 Math.random 等 ✓；动态 import()（effects→views/settings、app→mount/index）不在 check-arch 依赖图——登记 P2-12。 |
| 7 | **覆盖与门禁** | ✅ PASS | `npm test` **492/492**（491+1：views/mount 新增与修复断言）；`npm run gate` **9 PASS/0 FAIL/0 PEND**（项 7 内 492 用例 + 五目录阈值；项 3 37 文件；项 8 黄金 18 tick）；`npm run cov` 聚合 **97.72/87.61/96.43**（任务声明 97.69/87.62/96.26——修复新增分支后数值迁移，登记 P2-18）；public/js 逐文件全达标（见独立实证）；check-arch 37 ✓；golden 18 tick ✓。 |
| 8 | **死代码/死分支** | ✅ PASS（P2-14 附注） | render 表格驱动 optional 循环全臂（极端形状矩阵 + 双标注盒已锤）✓；delegate 各臂（goto/action/坏 payload/无匹配/无 closest/无 payload 属性）✓；mountApp 全臂（no-doc/no-app/createElement 缺/toasts 复用/挂载/removeEventListener 缺）✓；effects 日志动作双态（doc 有无）✓ + boot 双态 ✓；views/index.js `view` 死赋值已删；reducer 未覆盖行 81/89（loadout/errors、ai/validate/result——F1 P2-13 延续，无派发者）；check-arch/gate 脚本自身非 L7 范围。 |

## 缺口自查（六项按任务逐条）

① **§6.7「最近 200 条」vs 实现 12 行渲染**：**判定——渲染面截断，数据源全量，可接受实现决策，SPEC 措辞应明确**。records() 恒返回 DLLog 环形缓冲全量（util/log ringSize=3000 > spec 200，数值三处不一致：200/3000/渲染行数），渲染 `slice(-10)` 仅取尾部摘要；「200 条」为数据窗口语义、渲染行数为显示面语义，两者不冲突但文档需点明（P2-17）。修复后渲染行数 10（12 行与按钮行几何冲突，见 P1-3 论证）。
② **menu loading 悬挂（serverOk null 且 meta/loaded 永不回）**：**登记（P2-13）**——health fetch 无 AbortController（挂在 F1 P2-5），服务端挂起则 loading 永久；error 态由「fetch 拒绝」驱动（undici 连接拒绝同步 reject → catch → meta/loaded ok:false ✓ 实测。建议 F5 前补健康检查超时（≤8s）→ 转 error 态。
③ **mount paint 全量 innerHTML 替换对 Blockly（F6）的接管冲突**：**登记（P2-5 并）**——§6.2「Blockly 区 DOM 由 Blockly 管理，mount 不触碰 workspace 内部」；现 paint 整树替换 #app。F6 前的容器豁免预案：mountApp 增 protectedIds/子树跳过（或 paint 改只替换 `#dl-main` 容器），F3/F4/F5 各屏纯 DOM 不受影响，F6 前必须落地。
④ **shell seed w28 文本溢出**：**登记（P2-6）**——seed 最长 `seed:2147483647`≈19 字符 ≫ 28px；CSS `.dl-t` ellipsis 兜底（style.css 已备），视觉可接受；如需完整显示可加 title 提示或扩宽至 56。
⑤ **filterLogs 缺省 4 vs logPrefs.level 默认 debug 一致性**：**一致 ✓（修复后）**——settingsLayout 传入 `mapLevel('debug')=4` 与 filterLogs 缺省 4 同为 DLLog 刻度（修复前 mapLevel('debug')=5 ≠ 4，为 P1-5 的一个表现）；测试断言 debug 档含 info 记录（≤4）✓。
⑥ **delegate 对 data-payload 内双引号 JSON 的转义处理**：**结论——浏览器 dataset 自动解码，JSON.parse 直接可用，无需再处理**。boxToHtml 的 escapeAttr 把 payload JSON 的裸 `"` 全转 &quot;（含 `\"` 序列中的引号——反斜杠非属性转义符，解析器原样保留）→ HTML 解析器还原属性值 → dataset.payload 即原始 JSON 文本 → JSON.parse ✓；collectBoxes 的 &quot;/&amp; 解码仅为 node 侧等价物；双编码（payload 文本含字面 `&quot;`）往返仍保真（P2-9）。

## 发现的问题

### P0

无。

### P1（必须当批修复——已全部修复并复测）

1. **P1-1 menu 按钮栈 y 错位：`c.y+260=440`，5 按钮中 3 个（544..688）溢出面板（180..540）**。§6.1「y 从 260 起」为绝对坐标（§6 全部坐标均绝对标注；面板 180+80=260 恰为「标题 40」下的自然位置）；按面板内可用高度核算：栈底必须在 540 内——260 起步栈底 508 ✓，440 起步栈底 688 ✗。**且原测试断言 [560,440] 把缺陷固化**（测试注释自证「y=180+260=440」）。另 empty 态提示原在 260 会与新按钮栈相叠 → 三态提示文本统一移至 236（=c.y+24+16，SPACES 组合）。**修复**：`let y = 260`（绝对）；提示 y = c.y+SPACES.s5+SPACES.s4；测试 → [560,260,160,40] 与 ys [260,312,364,416,468] + 三态 verifyLayout 回归。
2. **P1-2 settings 级别 radio 死控件：无 payload**。`log_level_*` 盒子 action='log/level' 但不带 payload.level；effect `action.payload && action.payload.level` → undefined → **点击 radio 无任何效果**（不 dispatch log/set、不 setLevel）。**修复**：radio 补 `payload:{level:lv}`；测试断言 payload。
3. **P1-3 settings 行/按钮几何相交 + 按钮 zconflict**。记录行 12 行步 30 从 260 → 行 10/11 覆盖导出/复位按钮（560..592）——≥10 条记录时**按钮被行遮挡不可点**（自检器查不到 overlap 只因 z 不同：行 z1 vs 按钮 z0）；且按钮 `button()` 缺省 z=0 ≤ 面板 z0 → **zconflict 自检恒误报**（verifyLayout 实测：`btn_log_export:zconflict | btn_log_reset:zconflict`，与记录数无关）。**修复**：行数上限 10（260..558 与按钮 560..592 无交——「12 行」口径登记 P2-4）；按钮 z:1；menu error 态 btn_boot 同病（缺省 z0）→ 一并 z:1；测试补 verifyLayout 全绿断言。
4. **P1-4 menu error 态「重试」死按钮：无 EFFECTS['boot']**。§6.1 `data-action="boot"` → 重跑启动步骤；runEffect 查无此 effect → **点击仅 dispatch no-op**。**修复**：effects 增 `boot`（ctx.api.get('/health') → meta/loaded ok/version/tableNames + store.boot info/warn 日志）；测试双臂（ok/失败）+ 经 store.dispatch 全链实测。
5. **P1-5 filterLogs 级别刻度与 DLLog 不一致（索引 0..7 vs silent:-1..all:99）**。probe-contract 实证：LOG_LEVELS indexOf('debug')=5 但 DLLog 记录 debug=4、trace=5、info=3 → 面板选 info 档保留 [info,warn,debug]（debug 混入）、选 debug 档保留 [info,warn,debug,trace]（trace 混入）——**每一档都多显示一档**。**修复**：LOG_LEVELS 改为与 shared/log.js LEVELS 逐值一致的冻结映射（silent:-1, fatal:0, error:1, warn:2, info:3, debug:4, trace:5, all:99）；mapLevel 按值取值、未知名兜底 4；测试夹具改真实刻度并补边界复算（lv=0 仅 fatal、silent(-1) 空、通道大小写敏感）。
6. **P1-6 app.js 默认 records 源接缝断裂：浏览器日志面板恒 0 条**。shared/log.js 实证：`createLogger().records` 是**活数组**（api 导出 `records` 引用），非函数；app.js 原 `typeof raw.records === 'function' ? raw.records() : []` 永走空数组分支。**修复**：双臂（函数面给注入 logger / 数组面给 DLLog）+ 畸形兜底 []；测试新增数组面与畸形臂。
7. **P1-7 app.js reportBootError 引入 console.error——F1 审查实证属性「public/js 全域无 console」回归**。**修复**：start() 改经注入 logger（deps.log || 模块 log）报 `store.boot` error（含 message），测试改断言 log.error 被调用（不再 stub console）；顺手达成「启动失败不静默且无未处理拒绝」双语义。全域 grep 0 命中复验。
8. **P1-8 F1 审查「修复落实 6」声称 seed/set 已入 SAVE_ON_ACTIONS——git show HEAD 实证提交代码缺失**。非开箱流（ai/battle 等）seed 回带只进 state 不落盘（dl.v3.seed 键只写不读悬空问题 F1 P2-3 未动）。**修复**：`'seed/set'` 入 SAVE_ON_ACTIONS；全链测试断言 box/open 后 mem['dl.v3.seed']==='9'。

### P2 登记（不阻塞；建议随批落实项以 ★ 标注）

1. **spec §6.0 右簇散文与实现差异——可接受实现决策 + spec 应改**：散文 `tier badge(1120..1176) seed(1184..1272)` 与 §3.2 Badge 24×24 矛盾（w56 不是任何组件宽），且 seed 1184..1272 与「log 按钮(右)」必然重叠；实现 badge 1120..1144 + seed 1156..1184(w28) + log 1184..1280(w96) 连续无交、右缘贴合 1280（自检器复验）。建议 spec 改：`tier badge(1120,w24) seed(1156,w28) log(1184,w96)`。★
2. **spec §6.1 散文 y184 vs §3.4 函数权威 y180——笔误登记**：center(560,360)=(round(720-360)/2)=180；§6.1 散文 184 为笔误，实现取函数值（任务书既定「函数权威」口径），spec 散文待改。★
3. **spec §6.7 右面板 600,80,664,400 vs 实现 664×560——可接受实现决策 + spec 应改**：400 高（80..480）放不下 级别区(108px)+头部(24)+记录行(≥300)+按钮(32)=~464+间距；实现 560（80..640）为最小容纳面；同样左面板 560（spec 400）。建议 spec 改 560 并明确行数口径。★
4. **记录行数任务口径「12 行」→ 实现 10 行**：几何论证——面板 560 内可用 380px（按钮 560..592 以上），行步 30+行高 28：10 行 = 260..558 ✓；12 行需 388px 超出可用区；维持「最近 200 条」为数据窗口、渲染 10 行摘要的两层语义。若要 12 行：面板高 600 或按钮移行（F7 打磨时定）。
5. **mount paint 全量 innerHTML 替换 vs §6.0「goto 只替换 main 区」+ Blockly 接管预案（缺口③）**：功能等价（shell 依赖 tier/seed/全局状态需重绘），性能与 F6 冲突登记——F6 前实现 protectedIds/子树跳过或 `#dl-main` 容器替换；F3 起盒子增多后评估增量 diff。
6. **shell seed 盒 w28 文本溢出（缺口④）**：`.dl-t` ellipsis 兜底；如需完整 seed 显示建议 w56 + title 提示（F7 打磨）。
7. **unlock 拉取未接线（§1.3 step 5）**：boot 只打 health；menu 文案「拉 health/unlock」与实现不符；tierInfo 恒 null——F3/F4 对接（与 F1 P2-5 超时一并）。
8. **render.js 非 payload 属性未转义（data-box-id/action/goto/parent/kind/style-class）**：内含 `"`/空白会断属性——内部受控（id 由代码构造、goto/action 白名单枚举、parent 引用代码侧 id），防御登记：boxToHtml 统一 escapeAttr 或集会校验。★
9. **payload 双编码往返**：payload 文本含字面 `&quot;`/`&amp;` 时 escapeAttr→解析器→JSON.parse 往返保真（双编码正确，`\&quot;` 序列不被误解码——collectBoxes 的 &quot; 替换对 `A&amp;quot;B` 不命中再解 &amp; 最终还原）；建议补一条直测（当前靠代码推演）。
10. **mount paint 日志事件名偏差 + view.render 缺失（§2.2/§2.3）**：DOM 绘制应打 `ui.layout`(debug,{view,boxes})，实现打 `render.frame`(debug,{screen,boxes,issues})；`view.render`(debug,{view,phase}) 全缺；ui.layout.report ✓。建议 F3 前按 §2.2 对齐（若 T-LG-10 前端断言启用会红）。
11. **log/export 未跟随面板当前过滤 + 文件名差异**：effect 恒 `exportLogs(records,{})`（默认 debug 档），用户切 info 档后导出仍含 debug；文件名 dl-logs-<ts>.json vs §2.4 dl-trace-<ts>（dlui.exportTrace 语义未实现——console/全局 dlui 均缺，F7 打磨）。★
12. **log/reset 通道复位无效 + channels 全链路未接线**：reducer log/set 通道为合并语义 → `channels:{}` 不清除已存覆盖；且 util/log 只读 prefs.level、不应用 prefs.channels、app.js 未实现 §2.1「强制 render 通道 trace」——initialState channels{render:'trace'} 为死配置；§6.7 通道勾选（19 通道）F2 未交付（任务范围外）→ F3+ 承接时一并接线。
13. **menu loading 悬挂无超时（缺口②）**：健康检查无 AbortController（F1 P2-5 承接），fetch 挂起 → loading 永久；建议 ≤8s 超时转 error。
14. **死分支延续（F1 P2-13）**：reducer `loadout/errors`（无 effect 直测）、`ai/validate/result`（无派发者，F6 debounce 承接）。
15. **toast 渲染/自消仍未实现（F1 P2-1 延续）**：F2 只完成 #dl-toasts 挂载区；ui/toast → snackbar 渲染 + 3s dismiss 定时器 F3 承接；log/toggle 若已在 settings 屏无变化（panelOpen 浮动面板语义 spec §6.7 字段存在未用）。
16. **check-arch 不扫描动态 import()**：effects→views/settings.js、app→mount/index.js 不在依赖图（相对路径无方向违规，但图不完整）——建议 check-arch 补 `import(` 静态串扫描纳入图。★
17. **「最近 200 条」窗口口径三处不一致**：spec §6.7 200 / util/log ringSize 3000 / 渲染 10 行——建议 spec 明确「DLLog.dump() 全量窗口 + 渲染 10 行摘要」，ringSize 与 §6.7 对齐（2000 或 200）由 F3 定。
18. **cov 声明值迁移**：任务声明 97.69/87.62/96.26，修复后实跑 97.72/87.61/96.43（新增分支/测试致数值移动）——五目录逐文件阈值全达标（最低 persist 分支 85.29），门禁项 7 与声明差异登记。
19. **F1 P2-15 冗余双写延续**：box/open 链 onChange（box/done∈列表）+ store/save→ctx.save 双落盘——同内容二次写，无危害，可去一。
20. **任务书 mount.test「7 用例」兑现**：修复后 7 用例 ✓（新增 records 接缝 + boot 重试臂），views.test 6 ✓；exportLogs 空数组 `[]`、DLLog 缺省 `all` 语义等边界已覆盖。

## 实测

| 命令 | 期望 | 实际 | 结果 |
|---|---|---|---|
| `npm test` | 491/491 全绿 | **492 pass / 0 fail / 0 skip**（491 + F2 修复新增 1：seed 落盘断言等；views 6 + mount 7 + 全量回归） | ✅ |
| `npm run gate` | 9 PASS/0 FAIL/0 PEND | **9 PASS / 0 FAIL / 0 PEND**（项 7 492 用例 + 五目录阈值；项 3 check-arch 37 文件；项 8 黄金 18 tick；串行复跑无 flake） | ✅ |
| `npm run cov` | 聚合与声明一致 | 聚合 **97.72 / 87.61 / 96.43**（声明 97.69/87.62/96.26 为修复前；post-fix 逐文件全达标：app 100/89.80/100、client 100/89.74/100、effects 100/87.76/100、store/index 100/87.80/100、reducer 98.50/85.92/100、persist 100/85.29/100、layout 100/86.49/100、verify 100/97.44/100、views 全 100 行、mount 三文件 100/87.5~90.91/100、util/log 100/87.50/100） | ✅ |
| `node scripts/check-arch.js` | 37 文件 0 违规 | `[PASS] 37 个文件无依赖违规`（=30+F2 新增 7） | ✅ |
| `node .audit/golden-battle.js` | 18 tick 一致 | **黄金战斗复算通过（18 tick, winner=p2, phase=role）** | ✅ |
| 探针 `.review-f2/probe-contract.js` | 接缝/刻度实证 | DLLog records=数组（typeof 'object'）→ app 旧函数面 0 条；日志真值 info=3/debug=4/trace=5 与索引刻度偏差 → info 档保留集 [info,warn,debug]、debug 档 [info,warn,debug,trace]（P1-5/6 定案） | ✅（P1 触发） |
| 手动复算 `node -e` | filterLogs 边界 | levelValue=0 → 仅 fatal；-1(silent) → 空；99(all) → 全量；通道大小写敏感；LOG_LEVELS 刻度与 DLLog 逐值一致 | ✅ |
| 手动 `node -e` verifyLayout | 布局自检 | settings(20 条) 修复前 `btn_log_export:zconflict` 恒报（按钮 z=0 ≤ 面板）+ 行/按钮可视相交；修复后全绿；menu 三态全绿 | ✅（P1-3 定案） |

## 独立实证（非测试代码路径，可复跑 `.review-f2/probe-contract.js` 与上述 node -e）

① **probe-contract（P1-5/6 定案）**：真实 `shared/log.js createLogger` → `api.records` 为**数组**（typeof 'object'）；dump 记录真值 info=3/warn=2/debug=4/trace=5；模拟 app 默认 records 闭包（函数面）→ **0 条**（期望 4）；模拟 settings mapLevel（索引刻度）→ 选 info 档保留集 [info,warn,debug]、选 debug 档 [info,warn,debug,trace]——off-by-one 一档混入实证。② **verifyLayout 复算**：settingsLayout(20 条) → `{ok:false, issues:[btn_log_export:zconflict, btn_log_reset:zconflict]}`（与记录数无关）+ 行 10/11(560..618) 与按钮(560..592) 可视相交（z 不同故自检查不出 overlap——P1-3 双重面）；menu ready 布局按钮 440..688 溢出面板底 540（P1-1）。③ **git show HEAD:public/js/app.js**：SAVE_ON_ACTIONS 无 'seed/set'（F1 落实记录与提交不符，P1-8 坐实）。④ 代码级实证：paint() 内无 dispatch（grep 函数体）；public/js 修复后 console 零命中（仅注释提及）；动态 import 两处相对路径无裸导入。⑤ 缺口⑥结论：escapeAttr 全引号转义 + 浏览器解析器实体解码 → dataset.payload 即原始 JSON（含 `\"` 序列原样保留——反斜杠非属性转义符），JSON.parse 直接可用。

## 结论

**CONDITIONAL PASS（P1×8 必须当批修复后转 PASS；本批已全部修复并复测）**。交付物覆盖面与测度声明达成：492/492、gate 9 PASS/0 FAIL/0 PEND（串行复跑稳定无 flake）、cov 聚合 97.72/87.61/96.43 逐文件过五目录阈值、check-arch 37 文件 0 违规、golden 18 tick 一致、public/js 全域无 console。八项 P1 全部为「契约 vs 实现」的对账断裂且均有探针/复算实证：菜单按钮栈绝对坐标误读（测试断言同谋固化）、radio 无 payload 死控件、行/按钮几何相交 + z 缺省坑（自检器每帧误报 + 按钮不可点）、boot 重试死按钮、级别刻度 off-by-one（真实数据源下每档都偏一档）、records 数组接缝（浏览器面板恒空）、console 铁律回归、F1 声称落实未落码的 seed/set 落盘。三处 spec 散文偏差（§6.0 右簇、§6.1 y184、§6.7 高度/行数）均给出论证并判「实现可接受，spec 应改」；六项缺口自查全部闭环：①200 条/3000/10 行三层口径判定渲染面-数据源分离可接受；②loading 悬挂登记（挂在 F1 P2-5 超时缺失）；③Blockly 接管冲突登记 F6 前容器豁免预案；④seed w28 溢出 ellipsis 兜底登记；⑤缺省阈值与 logPrefs.level 一致（修复后同为 DLLog 刻度 4）；⑥dataset 自动解码无需二次处理（含 `\"` 序列推演 + 双编码保真）。

随批建议落实（★）：P2-1（spec §6.0 右簇改写）、P2-2（§6.1 y184 笔误）、P2-3（§6.7 面板高度）、P2-8（render 属性统一转义）、P2-11（导出跟随过滤）、P2-16（check-arch 补动态 import 扫描）；其余 P2 登记承接（F3/F5/F6/F7 对齐点逐条注明）。复跑基线：`npm test && npm run gate`（串行）＋ `.review-f2/probe-contract.js` 全绿即为回归通过；F2 合同「截图核对清单」需人工浏览器侧执行（本审查无浏览器，见 §9 出口）。

## 修复落实（实现侧，2026-09-16；P1×8 当批）

1. **P1-1 菜单按钮栈**：`menu.js` 按钮 `let y = 260`（绝对；§6.1）；三态提示文本统一 `textY = c.y + SPACES.s5 + SPACES.s4`（236）；btn_boot y=textY+SPACES.s8（300）。回归：views.test 按钮坐标 [560,260,…] + ys [260,312,364,416,468] + 三态 verifyLayout 断言。
2. **P1-2 radio payload**：`settings.js` radio 补 `payload:{level:lv}`；测试断言 payload 往返（含 mount.test 已有 payload 用例）。
3. **P1-3 行/按钮几何 + z**：行数 `slice(-10)`（送 10 行上限登记 P2-4）；btn_log_export/reset、btn_boot 全补 `z:1`；视图测试补 verifyLayout(20 条) 全绿。
4. **P1-4 boot effect**：`effects.js` 增 `'boot'`（/health → meta/loaded + store.boot info/warn 双分支）；mount.test 增 effect 双臂 + dispatch 全链断言。
5. **P1-5 级别刻度**：`settings.js` LOG_LEVELS → 与 shared/log.js LEVELS 逐值一致的冻结映射；mapLevel 按值 + 未知名兜底 4；测试夹具改真实刻度（info3/debug4/trace5）+ 边界复算（0/-1/99/大小写）入用例。
6. **P1-6 records 接缝**：`app.js` 默认 records 源双臂（function→DLLog 数组→[] 兜底）；mount.test 增数组面 + 畸形臂 + 消费断言。
7. **P1-7 无 console**：`app.js` start() 改经注入 logger 报 `store.boot` error（删 reportBootError 的 console.error）；mount.test start 用例改断言 log.error（含 channel/event/message）。
8. **P1-8 seed/set 落盘**：`app.js` SAVE_ON_ACTIONS 增 `'seed/set'`；mount.test 全链断言 mem['dl.v3.seed']==='9'。

复测：`npm test` 492/492 ✅；`npm run gate` 9 PASS/0 FAIL/0 PEND ✅；`npm run cov` 聚合 97.72/87.61/96.43 ✅；`node scripts/check-arch.js` 37 文件 0 违规 ✅；`node .audit/golden-battle.js` 18 tick 一致 ✅；`.review-f2/probe-contract.js` 语义与修复后实现一致（刻度对齐、接缝双臂可达）✅。