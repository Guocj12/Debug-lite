# P0-6 审查记录

> 批次：P0-6（数据表：schema.js + 7 张表 + T-DC-1/2 接线）
> 审查方式：独立上下文 agent（逐表数值核对 + 独立复算 8 项 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS**（无 P0/P1；P2×2、P3×2 全部当批落实）

## 审查范围

新增 `server/data/schema.js`、7 张 JSON 表、`tests/integration/data-schema.test.js`（DS-1..10b）；修改 `scripts/gate.js`（项 4 → validateStructure；项 5 拆 T-DC-8/T-DC-2）、`tests/integration/gate-extra.test.js`（GX-1..5b）、`server/data/README.md`、`scripts/README.md`。

## 核对结果

| 项目 | 结果 |
|---|---|
| 逐表数值核对：battle-config 18 冻结值、10 技能、29 插件、5 品质、unlock 11/14/17/17/19 与文档全符 | ✅ PASS |
| schema.js：T-DC-1 清单齐全（regen/slotWeights/unlockTier/costDeltaByTier/falloff/bulletLevel/无 bulletSpeed）；无实质误伤 | ✅ PASS |
| 测试 DS-1..10b：过 §3.3 黑名单；破坏矩阵 34 例逐一触发真分支（逐条核对过 detail 关键字配对） | ✅ PASS |
| gate 接线：项 4/5 与 scripts/README 契约一致；fail>pend>pass 合并语义正确 | ✅ PASS |
| 铁律：L2（改动面 4 M + 8 ??）/ L9（战斗数值全在表，schema.js 属校验器）/ L17 抽查 | ✅ PASS |
| 独立复算 8 项全过（rare tiers 三等分、重击 1.3、I-6a 0.08×1.20=0.096、dropRates 和 1.0、突击盾 sp10、rare tier1 costDelta=3、epic/mythic 三等分、unlock 累积计数） | ✅ PASS |
| 门禁重跑：npm test 100/100；cov 97.38/86.17/97.08；gate 6 PASS/0 FAIL/3 PEND | ✅ PASS |

## 已落实的 P2/P3（当批）

1. **P2-1 T-DC-2 插件词条基础值未校验** → `PLUGIN_EXPECTED` 增第 4 列期望 v（含 sp_buff duration），DS-7b 破坏用例验证。
2. **P2-2 README"三表"措辞** → 改为"角色/技能两表"（插件当前无 unlockTier，即全体已解锁）。
3. **P2-3 battle-config 通用值误报风险**：`{0,1,-1}` 已在 `checkNumericHardcode` 排除（朝向/计数器与机制数值无关；B11 引擎必然用到 ±1），GX-5b 正反用例锁定。
4. **P3-1/P3-2**：DS-10 破坏矩阵 34 例（含 DS-7b）文案与实际一致；unlock aiNodes 保持固定顺序比较（当前数据顺序正确，Set 化留待顺序出现分歧时）。

## 过程说明

- 审查 agent 首两次运行因环境中断（其一恰逢电脑异常关机），重试后完成；期间工作区文件经 `git status` + 全量重跑确认无损。
- 实现过程中测试先行抓到 1 个真 bug：unlock 段位缺失时 schema.js 抛 TypeError（`byTier[tt].aiNodes` 空引用）→ 已修（缺段位报错而非崩溃），DS-10 的"解锁缺段位"用例锁定。

## 遗留（阶段语义）

- 项 5 的 T-DC-2 已激活（真实仓库 pass）；整体仍 PEND 因 T-DC-8 等待 P0-7 `docs/interfaces.md`。
- 数据占位待 B21 校准：技能 unlockTier 分配、特化/专家 regen、slotWeights、dodgeChanceBonus=0.20。