# B8 审查记录

> 批次：B8（P1：core/engine.js 战斗引擎骨架）
> 审查方式：独立上下文 agent（M/N/O/P 全矩阵独立复算 + 14 步管线逐条核对 + 门禁重跑）
> 日期：2026-09-12　结论：**PASS（修复 1 个 P1 后）**

## 审查范围

新增 `server/core/engine.js`（createBattle/step 14 步/normalizeAction/resolveActorCollision/judge/dealDamage 基础链路/runFull）、`tests/unit/engine.test.js`（21 测试 EN-*）；`docs/interfaces.md` §1 engine 行已冻结（B8~B11）。

## 核对结果

| 项目 | 结果 |
|---|---|
| M/N/O/P 全矩阵独立复算全过（M2 t*=0.5@536/568/12-7、M3 564、M5 536/568/19+12、M8 无伤、M9 停原地+基地3、N2 518/582/550、N3 432/496/464、N5 互换、N6 592/464、O1 544、O2 416、defend 14、超时 7） | ✅ PASS |
| 语义：五步落位与 07 §1 一致；撞基地未 clamp 意图判定（D-61）；恰 64 不碰撞（D-19）；D-33 后推；M5 双结算 | ✅ PASS |
| 14 步管线与 §4.2 完全同序；D-84 复写不扣资源；死亡统一步骤 12（EN-14） | ✅ PASS |
| 日志矩阵：L4 行 11 事件全落地；子系统 withLogger 接线（skills/effects/bullets/field） | ✅ PASS |
| 测试：EN-* 过 §3.3 黑名单（M2 接触点 568 独立推导非镜像、失败路径、矩阵分支） | ✅ PASS |
| 铁律：L2 / L9（数值默认自表，无字面量兜底）/ L11 / L17；engine L4 不依赖 L5（AI 注入） | ✅ PASS |
| 实测：npm test 259/259；cov engine.js 100/92.59/100；gate 8 PASS/0 FAIL/1 PEND | ✅ PASS |

## 已修复的问题（当批）

1. **P1-1 撞基地 owner 硬编码**：`aBase→'p2'/bBase→'p1'` 改为与 field.touchesBase 同规则（`facing>0?'p2':'p1'`）——翻转朝向打自家基地时引擎与 field 语义一致。
2. **P2-1 超时扣血单值**：`p1.maxHp` 单值切双方 → 各按本方 maxHp。
3. **P2-2 field 未接 logger**：engine 内 `field.withLogger(logger)` 接线（field.clamp/base 事件可达）。
4. **P2-3 补 M5 双结算用例**（碰撞 536/接触 568/B 合受 19/A 受 12）——审查指出 M5 当前链路即可测。

## 登记取向

- 本批伤害 = 基础链路（普通/碰撞/基地），**不记 damage.\* 事件**（B9 行交付）；背击/暴击/吸血/真实/附加效果与 damage.* 事件由 B9 扩展。
- 步骤 3 AI 由调用方注入 actions（函数或字符串）；B15 起 AI 链路替换。
- 判定条件"意图落位后中心距 < 64"（恰 64 相邻不碰撞）；撞基地用未 clamp 意图。
- 减伤公式常数 DEF_K=40（examples/README §1），非战斗数值（L9 注释登记）；B21 校准评估是否入表。

## 遗留

- M6/M7（不可穿无伤/可穿无伤位移技）归 B9 T-BT-21；runFull 裸函数 actions 形态由 AI 批（B15）正式化；T-BT-3 的 p1/p2 互换等价正式断言随黄金战斗（B11）；引擎造 64 前必终局（overtime 数学保证）故 runFull 无 draw 兜底。