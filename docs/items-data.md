# 物品数据文档（数值 / 名称 / 描述 / 贴图）

> 所属：Debug-Lite v3　本文档用**自然语言**描述所有物品的标准数值、名称、描述与贴图占位。
> 所有数值为「绿品质标准值」；实际物品按品质系数随机（见 `../docs/v3-design.md` §4.2）。具体命名与词条数值可继续在本文档上迭代。

---

> ## ⚠️ 示例数据声明（必读）
>
> - 本文档列出的**全部名称、数值、词条、贴图形状**都是**示例/占位数据**，与 `server/data/` 下的内容层表（`role-templates.json` / `skill-templates.json` / `plugins.json` / `qualities.json` / `items-config.json` / `unlock.json`）一一对应，**任何数值都不是最终设计**。
> - **正式内容待用户手动完整设计后冻结**：本文档与上述内容层表都是"待替换"的载体，**改内容只需改表**（机制层见 `systems/01-items.md` §2A / `systems/03-skills.md` §2A）。
> - **门禁不再锁内容数量**（2026-09-16 用户拍板 A）：增删角色/技能/插件/品质条目都不会让 `scripts/gate.js` 失败；`server/data/schema.js`（T-DC-1）只校验**结构 + 机制自洽**（类型/词条/AI 权限必须在机制层登记、`typeModifiers` 等机制字段必填）。
> - **逐值比对由 `_sample` 开关控制**：内容表带 `_sample: true` 时，T-DC-2（`validateConsistency`，gate 项 5）才按本文档的示例期望表逐值核对；**去掉该标记（可逐表去）后，该表不再与本文档比对**——正式内容与示例不同不会失败。当前四张内容表都保留 `_sample: true`。
> - 机制层表（`skill-mechanics.json` / `affix-registry.json` / `ai-nodes.json`）是**机制词汇**，不是内容，不随内容重构而变。

---

## 1. 贴图占位规范（像素画）

- 角色模板：16×16 像素方块，主体用模板主色，1 像素黑边；品质用 1 像素描边色（绿/蓝/紫/橙/青）区分。
- 技能模板：8×8 像素图标，按类型区分形状（近战=横条、平射=箭头、垂直=竖条、位移=残影），技能主色填充。
- 角色插件：8×8 像素图标，按功能分类用固定几何形（见下表）。
- 技能插件：8×8 像素图标，按功能分类用固定几何形（见下表）。
- 以上均为占位；将来替换为图片素材（PNG）时只替换 `assets/` 与资源加载器，不改数值与逻辑。

## 2. 品质与区间（摘录）

| 品质 | 色 | 属性系数区间 | 角色插槽数 | 技能插槽数 | 最大插件点数（建议） |
|---|---|---|---|---|---|
| 绿 common | `#2ecc71` | 0.80~1.05 | 1~3 | 0~1 | 3 |
| 蓝 rare | `#3498db` | 1.00~1.25 | 2~4 | 0~2 | 4 |
| 紫 epic | `#9b59b6` | 1.20~1.45 | 3~5 | 1~3 | 5 |
| 橙 legendary | `#e67e22` | 1.40~1.70 | 4~6 | 2~4 | 6 |
| 青 mythic | `#1abc9c` | 1.60~2.00 | 5~7 | 3~4 | 7 |

- 技能插槽类型：**基础**（修改基本属性）、**特殊**（特殊效果）。
- 角色插槽类型：**攻击 / 防御 / 生命 / SP / MP**（五维槽位）+ **特殊**。

### 2.1 插件强度档位（同品质三档）

- 每品质把「属性系数区间 [lo, hi]」划分为三档，档位越高词条数值越强、消耗越高。
- 绿品质示例（用户指定）：一档 0.80~0.88、二档 0.88~0.97、三档 0.97~1.05；其他品质按同样方式在自身区间内三等分（具体边界数值阶段确定）。
- 角色插件：点数消耗 = 档位（1 / 2 / 3）。
- 技能插件：资源消耗增量统一为 **`costDeltaByTier` 逐档数组**（D-113）：绿 `[2,4,6]`，更高品质基础值递增（蓝 `[3,6,9]`、紫 `[4,8,12]`…，**B20/B21 已定稿**：逐档期望值由公式 `delta = costDeltaBase[品质]×tier` 导出、数组值仅维度声明——见 systems/03-skills §4.2.6）。
- **AOE 技能新增 `falloff`**（D-29）：每向外一格减伤百分比，默认 `0` = 不衰减；属模板固有字段，不随品质随机。
- **消耗增量维度（D-113）**：`costDeltaByTier` 逐档数组的**键**才是"该插件往哪个维度加消耗"（数组值仅作维度声明、不参与计算）。现表：多数技能插件声明 `mp`；**`sp_cooldown` 声明 `sp`、`sp_displacement` 声明 `sp`**（下方 §6 表内已按真实维度标注）。

- **`sp_true_dmg` 的真实语义（B21/D-128）**：命中后**额外直扣 `v` 点真实伤害**（`hp ← max(0, hp − v)`），**不是**"把本次伤害改为真实伤害"。若将来要改成 D-40 的 `trueDamage = max(1, floor(atk×倍率))` 公式语义，属**设计变更**（需同时改机制表 `affix-registry.json` 的 `hitEffect` 与引擎，并补测试）。⚠️ 内容层 `plugins.json` 中 `sp_true_dmg` 的 `desc` 仍写"改为真实伤害"（内容层待用户设计时一并订正）。

## 3. 角色模板（示例 11 种；**数量不锁定**）

> 基础五维（均衡、绿品质）：hp 100 / atk 10 / def 8 / sp 60 / mp 40。
> **每个模板都必须给出 `regen{mp,sp}`（必填，D-110）**：每 tick 自动回复量；均衡模板建议 `{mp:1, sp:2}`，特化/专家模板按流派差异化（**B21 定稿维持数据表现值**，D-128④）；插件词条「回复 +1」在其上叠加。
> 命名规则：均衡叫「均衡」；特化与专家都**只按最高属性**分类命名「特化·xx」「专家·xx」，**其余属性随机**。
> 特化：最高属性 +15%；其余 4 属性随机取 1 个 -15%、3 个标准。
> 专家：最高属性 +30%；其余 4 属性随机分配 略高 +10% / 极低 -30% / 略低 -10% / 标准。
> **类型修饰在开箱生成时即生效**（2026-09-16 修正）：`items.generateRoleItem` 与 `roles.instantiateRole` 共用 `items.applyTypeModifier`，同品质下「特化·攻击」的 `atk` 下界 12、「专家·攻击」13、均衡 10（此前三者完全相同）。
> 每个模板还带 **`drop` / `dropWeight`**（是否进掉落池 / 同类池内相对权重，见 §5 末尾的"掉落与解锁配置"）。

**均衡（1 种）**

| 模板 | 名称 | 五维 | 描述 |
|---|---|---|---|
| role_bal | 均衡 | 100/10/8/60/40 | 五维平稳，无明显短板 |

**特化（5 种，按最高属性命名；低属性随机）**

| 模板 | 名称 | 最高属性(+15%) | 描述 |
|---|---|---|---|
| role_spc_hp | 特化·HP | HP | 生命强化，其余属性随机（1 低 -15%） |
| role_spc_atk | 特化·攻击 | 攻击 | 攻击强化，其余属性随机（1 低 -15%） |
| role_spc_def | 特化·防御 | 防御 | 防御强化，其余属性随机（1 低 -15%） |
| role_spc_sp | 特化·SP | SP | 体力强化，其余属性随机（1 低 -15%） |
| role_spc_mp | 特化·MP | MP | 法力强化，其余属性随机（1 低 -15%） |

- 示例：「特化·攻击」可能是「高攻击低生命」或「高攻击低法力」，二者都归类为「特化·攻击」。

**专家（5 种，按最高属性命名；其余修饰随机）**

| 模板 | 名称 | 最高属性(+30%) | 描述 |
|---|---|---|---|
| role_exp_hp | 专家·HP | HP | 生命极高，其余属性随机分配 略高/极低/略低/标准 |
| role_exp_atk | 专家·攻击 | 攻击 | 攻击极高，其余属性随机分配 略高/极低/略低/标准 |
| role_exp_def | 专家·防御 | 防御 | 防御极高，其余属性随机分配 略高/极低/略低/标准 |
| role_exp_sp | 专家·SP | SP | 体力极高，其余属性随机分配 略高/极低/略低/标准 |
| role_exp_mp | 专家·MP | MP | 法力极高，其余属性随机分配 略高/极低/略低/标准 |

- 角色插槽类型：攻击槽位 / 防御槽位 / 生命槽位 / SP槽位 / MP槽位 / 特殊槽位；各类型插槽按模板设定分配（数值阶段确定）。
- 角色模板新增属性「**最大插件点数**」：身上装配的插件点数总和 ≤ 最大点数（§2.1、§5）。

## 4. 技能模板（参考 legacy 设计）

> 参考 `legacy/data/skills.json` 中适用于 v3 四类型的技能；legacy 内置效果（眩晕/冰冻/燃烧/中毒/击退/防御↑）在 v3 中改由**技能插件**实现。
> legacy 的「魔法盾」为护盾弹幕，v3 暂无对应机制，暂不纳入。

### 4.1 近战 melee

| 模板 | 名称 | 参考 legacy | 倍率 | 消耗 | 冷却 | 弹幕等级 | 范围 | 描述 |
|---|---|---|---|---|---|---|---|---|
| skill_melee_whirl | 旋风斩 | warrior_whirlwind | 1.0 | sp 12 | 2 | 2 | [-1,1] | 前后各一格回旋 |
| skill_melee_heavy | 重击 | warrior_heavy | 1.3 | mp 10 + sp 8 | 4 | 2 | [0,2] | 前方两格重击 |

### 4.2 平射 straight

| 模板 | 名称 | 参考 legacy | 倍率 | 消耗 | 冷却 | 弹幕等级 | 射程 | 弹幕数 | 描述 |
|---|---|---|---|---|---|---|---|---|---|
| skill_straight_precise | 精准射击 | archer_precise | 0.9 | sp 6 | 2 | 3 | 8 | 1 | 单发远射 |
| skill_straight_rapid | 连续射击 | archer_rapid | 0.5 | mp 8 | 4 | 4 | 5 | 4 | 四连发 |
| skill_straight_ice | 冰锥 | mage_ice | 1.3 | mp 8 | 3 | 4 | 5 | 1 | 高倍率单发（冰冻由插件实现） |
| skill_straight_poison | 毒瓶 | assassin_poison | 0.5 | mp 6 | 4 | 4 | 5 | 1 | 低倍率单发（中毒由插件实现） |

> **`弹幕速度` 列已删除**（D-21）：弹幕在生成当 tick 飞完全射程（`range × 64px`），速度不再是独立参数。

### 4.3 垂直 vertical

| 模板 | 名称 | 参考 legacy | 倍率 | 消耗 | 冷却 | 弹幕等级 | 射程 | 范围 | 描述 |
|---|---|---|---|---|---|---|---|---|---|
| skill_vert_rain | 箭雨 | archer_rain | 0.9 | mp 12 | 5 | 4 | 8 | [-2,2] | 落点大范围 |
| skill_vert_fireball | 火球术 | mage_fireball | 0.8 | mp 14 | 5 | 3 | 8 | [-1,1] | 落点范围（燃烧由插件实现） |

### 4.4 位移 displacement

| 模板 | 名称 | 参考 legacy | 倍率 | 消耗 | 冷却 | **弹幕等级** | 距离 | 穿敌 | 造成伤害 | 全程闪避 | 描述 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| skill_dash_bash | 突击盾 | warrior_shieldbash | 0.8 | sp 10 | 3 | **2** | 4 | 否 | 是 | 否 | 冲锋造成伤害（击退由插件实现） |
| skill_dash_shadow | 暗影步 | assassin_shadowstep | 0 | sp 8 | 3 | **3** | 3 | 是 | 否 | 是 | 穿敌位移、位移中完全闪避 |

> **`弹幕等级` 列（D-118）**：位移技 `dealDamage=true` 时沿路径每格放一枚 0 速弹幕，其等级取自本列（参与与其它弹幕相同的抵消规则）。
> 倍率/距离/冷却随品质系数随机；**弹幕等级、消耗结构、穿敌/伤害/闪避开关不随品质随机**。

## 5. 角色插件（按插槽分类）

> 插件名称简洁直白；五维类插件分「百分比 / 数值」两种，都装在对应五维槽位。
> 档位越高词条数值越强、点数消耗越高（§2.1）。
> **D-114：百分比/数值变体拆成独立插件 id**（掉落池与装配逻辑无需特判）。

| 插槽 | 插件 id | 插件名称 | 词条 | 点数消耗（档位 1/2/3） | 描述 |
|---|---|---|---|---|---|
| 攻击槽位 | `rp_atk_pct` | 攻击提升·百分比 | atk +8% | 1/2/3 | 攻击提升 |
| 攻击槽位 | `rp_atk_flat` | 攻击提升·数值 | atk +4 | 1/2/3 | 攻击提升 |
| 防御槽位 | `rp_def_pct` | 防御强化·百分比 | def +8% | 1/2/3 | 防御强化 |
| 防御槽位 | `rp_def_flat` | 防御强化·数值 | def +3 | 1/2/3 | 防御强化 |
| 生命槽位 | `rp_hp_pct` | 生命强化·百分比 | hp +8% | 1/2/3 | 生命强化 |
| 生命槽位 | `rp_hp_flat` | 生命强化·数值 | hp +20 | 1/2/3 | 生命强化 |
| SP 槽位 | `rp_sp_opt` | SP 优化·上限 | sp 上限 +15% | 1/2/3 | 体力优化 |
| SP 槽位 | `rp_sp_regen` | SP 优化·回复 | sp 回复 +1 | 1/2/3 | 体力优化 |
| MP 槽位 | `rp_mp_opt` | MP 优化·上限 | mp 上限 +15% | 1/2/3 | 法力优化 |
| MP 槽位 | `rp_mp_regen` | MP 优化·回复 | mp 回复 +1 | 1/2/3 | 法力优化 |
| 特殊槽位 | `rp_dodge` | 闪避 | 闪避率 +5% | 1/2/3 | 概率闪避 |
| 特殊槽位 | `rp_lifesteal` | 吸血 | 普攻吸血 10% | 1/2/3 | 普攻吸血 |
| 特殊槽位 | `rp_crit` | 暴击 | 暴击率 +8% | 1/2/3 | 概率暴击 |
| 特殊槽位 | `rp_regen` | 回复 | 每 tick hp +1 | 1/2/3 | 持续回复 |

> `rp_sp_regen` / `rp_mp_regen` 的"回复 +1"叠加在**角色模板的 `regen{mp,sp}`** 之上（D-110）；`rp_regen` 的 `hp_regen` 叠加出 `regen.hp`，由引擎**步骤 10 逐 tick 回复 hp**（`hp≤0` 时不复活）。
> **叠加位置（2026-09-16 更新）**：regen 词条的叠加由**单一聚合实现** `items.buildRolePanel` 完成（`roles.getFinalStats` 与 `loadout.buildPanel` 共用），**只加一次**——旧实现里 `roles.equipPlugins` 与 `loadout.buildPanel` 各叠一次会**双计**；经 `/api/v1/panel` 与 `/api/v1/battle` 的战斗中 `hp_regen` 正常生效。
> 表中的"`+1` / `+8%` / `+20`"等均为**基础值 v**，实例化时按档位系数滚动（`int` 取整 / `stat` 保留 2 位），实际数值随档位变化（§2.1）。
> 每个插件条目还带 **`drop` / `dropWeight`**（是否进掉落池 / 同类池内相对权重，见 §5 末尾的"掉落与解锁配置"）。

## 6. 技能插件（按插槽分类）

> 除「消耗优化」外，其余插件均增加技能消耗；**消耗增量统一为 `costDeltaByTier` 逐档数组**（D-113）：绿 `[2,4,6]`，更高品质基础值递增（**数组键 = 加消耗的维度，数组值仅作维度声明**）。
> 插槽类型：**基础**（修改基本属性）、**特殊**（特殊效果）。每行对应一个稳定插件 id（`sp_*`）。

| 插槽 | 插件名称 | 词条 | costDelta（档位 1/2/3） | 描述 |
|---|---|---|---|---|
| 基础 | 倍率提升 | 倍率 +15% | mp +2/+4/+6 | 修改基本属性：倍率 |
| 基础 | 消耗优化 | 消耗 −20%（**基础值，实际按档位滚动，见下注**） | 无（减耗类不增消耗） | 修改基本属性：消耗（`ceil(cost×(1−v))`） |
| 基础 | 冷却缩减 | 冷却 -1 | **sp** +2/+4/+6 | 修改基本属性：冷却 |
| 基础 | 射程增强 | 射程 +2 | mp +2/+4/+6 | 修改基本属性：射程/范围 |
| 基础 | 弹幕增强 | 弹幕数量 +1 | mp +2/+4/+6 | 修改基本属性：弹幕数量 |
| 基础 | 等级凝练 | 弹幕等级 -1（更高） | mp +2/+4/+6 | 修改基本属性：弹幕等级 |
| 基础 | 位移增强 | 位移距离 +1 | **sp** +2/+4/+6 | 修改基本属性：位移距离 |
| 特殊 | 眩晕 | 命中眩晕 1 tick | mp +2/+4/+6 | 特殊效果：眩晕 |
| 特殊 | 击退 | 命中击退 1 格 | mp +2/+4/+6 | 特殊效果：击退 |
| 特殊 | 拉近 | 命中拉近 1 格 | mp +2/+4/+6 | 特殊效果：拉近 |
| 特殊 | 持续伤害 | 命中持续伤害 3 tick | mp +2/+4/+6 | 特殊效果：持续伤害 |
| 特殊 | 真实伤害 | **命中额外附加 `v` 点真实伤害（直扣，B21/D-128）** | mp +2/+4/+6 | 特殊效果：附加真实伤害（**不是**"改为真实伤害"） |
| 特殊 | 暴击 | 暴击率 +10% | mp +2/+4/+6 | 特殊效果：暴击（叠加角色面板值，封顶 1） |
| 特殊 | 吸血 | 命中吸 hp 20% | mp +2/+4/+6 | 特殊效果：吸血（叠加角色面板值，封顶 1） |
| 特殊 | 释放增益 | 释放后 atk +2 持续 2 tick | mp +2/+4/+6 | 特殊效果：释放增益（**释放即入队、下一 tick 起效**，D-70） |

> **列含义**：表中数值均为**基础值**（实例化时按档位系数滚动）；`costDelta` 列的**维度（mp/sp）**取自 `plugins.json` 的 `costDeltaByTier` 键，**逐档数值**由 `costDeltaBase[品质]×tier` 导出（D-113，例如 rare tier1 = 3）。
> **实现语义（以代码为准）**：① 减耗类按 `ceil(cost×(1−v))` 结算，而 `v` 是**滚动值**（品质×档位系数后保留 2 位），所以实际减耗率随品质/档位浮动，**恒定 −20% 只是基础值口径**；② `cost` 的各维基础值**不随品质缩放**（模板 `baseCost` 直入，见 `systems/01-items.md` §4.4）。
> **两个"叠加"语义**：暴击/吸血词条**叠加在角色面板值之上**并按 `caps.probability=1` 封顶；释放增益生成 `continuous/stat=atk` 效果，持续 `params.duration`（缺省取注册表 `fallbackDuration=2`）。
> **投影（2026-09-16 更新）**：`loadout.buildPanel` 的技能参数白名单**已包含** `specials` / `castEffects` / `affixes`，因此 `crit_chance` / `lifesteal` / `cast_buff` / `stun` 等词条会随 `/api/v1/battle` 的面板投影进入技能实例并真实生效（`tests/unit/mechanics.test.js` 覆盖）；此前"尚未投影"的说法是旧实现的失真陈述。

### 6.1 掉落与解锁配置（全部是数据字段，2026-09-16 用户拍板 A）

**"某个模板/插件是否掉落、掉落权重多少、在哪个段位解锁"都不写代码，直接改 JSON：**

| 字段 | 位置 | 语义 | 缺省 |
|---|---|---|---|
| `drop` | `role-templates.json` / `skill-templates.json` / `plugins.json` 的**每一条** | `false` = 不进掉落池（开箱永不产出；条目本身仍可用于装配/文档） | `true`（缺省可掉落，兼容旧表） |
| `dropWeight` | 同上 | **同类池内**相对权重（同类 = 角色 / 技能 / 角色插件 / 技能插件） | `1`（非正数/非数值也按 1） |
| `unlockTier` | 同上（可选） | 从该段位起进掉落池、可装配（D-112） | 已解锁 |
| `kindWeights` / `dropRates` | `items-config.json` | **类别**权重与**品质**概率（全局，与 `dropWeight` 相乘生效） | — |
| `unlocks[].roleTemplates` / `skills` / `aiNodes` | `unlock.json` | 按段位的模板/技能清单与该段位新增 AI 权限名（与各表 `unlockTier` 交叉校验，防双源漂移） | — |

- 开箱流程：品质（`dropRates`，段位截断）→ 类别（`kindWeights`）→ **掉落池 = `drop !== false` 且 `unlockTier ≤ 玩家段位`** → 池内抽取（权重全 1 时均匀，存在权重时按 `dropWeight` 加权）。
- **当前四张内容表都是示例数据（`_sample: true`），待用户设计**；示例内容里所有条目都是 `drop: true` / `dropWeight: 1`（等价于旧行为）。
- 数量不锁定：增删条目由 `schema.js` 的结构校验兜底（至少 1 项），只有 "条目引用的类型/词条/AI 权限未在机制表登记" 才会 FAIL。

## 7. 备注

- **本文档全部内容为示例数据（见文首声明）**：名称均为**占位专有名称**，数值为标准值；最终命名与词条数值由用户独立文档确定，本文档固定「种类 + 标准数值 + 描述 + 贴图占位」结构。
- **机制层 vs 内容层**：本文档（及 `server/data/` 的内容层表）描述**内容**；"某个词条怎么滚、打到哪个字段、命中做什么"属**机制**，写在 `server/data/affix-registry.json` 与 `server/data/skill-mechanics.json`（说明见 `systems/01-items.md` §2A / `systems/03-skills.md` §2A）。
- 「最大插件点数」「各品质基础 costDelta」为建议值，数值平衡阶段可调。
- ⏳ **计划（未实现）**：本文档的"贴图占位"（§1）目前只有 `assets/sprites.json` / `assets/animations.json` 占位表（`shape` 为自由字符串，schema **不再锁形状枚举与条数**：可增形状、可缺占位）；真实素材（PNG）替换与加载器未实现。
- 数值平衡可继续沿用 `legacy/docs/技能点数换算系统.md` 的点数框架校验。
